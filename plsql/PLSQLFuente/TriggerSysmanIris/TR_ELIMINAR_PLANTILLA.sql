create or replace TRIGGER "TR_ELIMINAR_PLANTILLA"
BEFORE DELETE ON MODELO_PLANTILLA
FOR EACH ROW
BEGIN
 DELETE FROM MODELO_VARIABLES WHERE PLANTILLA = :OLD.CODIGO AND FECHA =  :OLD.FECHA;
 DELETE FROM MODELO_TABLA WHERE PLANTILLA = :OLD.CODIGO AND FECHA = :OLD.FECHA;
END;