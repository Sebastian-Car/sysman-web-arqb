MERGE INTO CONSULTAS FIN USING (SELECT '002090EstadoFlujoEfectivo' INFORME ,TO_CLOB(q'[WITH FLUJOEFECTIVO AS ( SELECT
     ROW_NUMBER() OVER (PARTITION BY PLAN_CONTABLE.ID order by PLAN_CONTABLE.ID) ROOW,
     PLAN_CONTABLE.ID,
     PLAN_CONTABLE.CODIGO,
     DETALLE_COMPROBANTE_CNT.TIPO_CPTE,
     DETALLE_COMPROBANTE_CNT.COMPROBANTE,
     PLAN_FLUJO_EFECTIVO.ACTIVIDAD,
     UPPER(PLAN_FLUJO_EFECTIVO.NOMBRE) NOMBRE,
     PLAN_FLUJO_EFECTIVO.NATURALEZA                      IND,
     DETALLE_COMPROBANTE_CNT.FECHA,
     PLAN_CONTABLE.COMPANIA,
     DETALLE_COMPROBANTE_CNT.VALOR_DEBITO    DANTERIOR,
     DETALLE_COMPROBANTE_CNT.VALOR_CREDITO   CANTERIOR,
     (CASE WHEN 0  = s$mesTrabajo$s -1  THEN PLAN_CONTABLE.SALDO0  ELSE 0 END
            + CASE WHEN 1  = s$mesTrabajo$s -1  THEN PLAN_CONTABLE.SALDO1  ELSE 0 END
            + CASE WHEN 2  = s$mesTrabajo$s -1  THEN PLAN_CONTABLE.SALDO2  ELSE 0 END
            + CASE WHEN 3  = s$mesTrabajo$s -1  THEN PLAN_CONTABLE.SALDO3  ELSE 0 END
            + CASE WHEN 4  = s$mesTrabajo$s -1  THEN PLAN_CONTABLE.SALDO4  ELSE 0 END
            + CASE WHEN 5  = s$mesTrabajo$s -1  THEN PLAN_CONTABLE.SALDO5  ELSE 0 END
            + CASE WHEN 6  = s$mesTrabajo$s -1  THEN PLAN_CONTABLE.SALDO6  ELSE 0 END
            + CASE WHEN 7  = s$mesTrabajo$s -1  THEN PLAN_CONTABLE.SALDO7  ELSE 0 END
            + CASE WHEN 8  = s$mesTrabajo$s -1  THEN PLAN_CONTABLE.SALDO8  ELSE 0 END
            + CASE WHEN 9  = s$mesTrabajo$s -1  THEN PLAN_CONTABLE.SALDO9  ELSE 0 END
            + CASE WHEN 10 = s$mesTrabajo$s -1  THEN PLAN_CONTABLE.SALDO10 ELSE 0 END
            + CASE WHEN 11 = s$mesTrabajo$s -1  THEN PLAN_CONTABLE.SALDO11 ELSE 0 END
            + CASE WHEN 12 = s$mesTrabajo$s -1  THEN PLAN_CONTABLE.SALDO12 ELSE 0 END
            + CASE WHEN 13 = s$mesTrabajo$s -1  THEN PLAN_CONTABLE.SALDO13 ELSE 0 END
            )    DANTERIORN,
     0 DNUEVO,
     0 CNUEVO,
     0 DNUEVON,
     ]') || TO_CLOB(q'[
     CASE
         WHEN PLAN_FLUJO_EFECTIVO.NATURALEZA IN ('D') THEN DETALLE_COMPROBANTE_CNT.VALOR_DEBITO
         ELSE DETALLE_COMPROBANTE_CNT.VALOR_CREDITO
     END SANTERIOR,
     0 NUEVO,
     SUBSTR(PLAN_CONTABLE.CODIGO,1,1) CTABAL,
     CASE
         WHEN LENGTH(s$mesTrabajo$s) = 1 THEN '0' ||s$mesTrabajo$s
         ELSE 's$mesTrabajo$s'
     END MES
 FROM
     V_PLAN_CONTABLE PLAN_CONTABLE
     LEFT JOIN DETALLE_COMPROBANTE_CNT ON PLAN_CONTABLE.COMPANIA = DETALLE_COMPROBANTE_CNT.COMPANIA
                                          AND PLAN_CONTABLE.ANO = DETALLE_COMPROBANTE_CNT.ANO
                                          AND PLAN_CONTABLE.CODIGO = DETALLE_COMPROBANTE_CNT.CUENTA
     INNER JOIN PLAN_FLUJO_EFECTIVO ON DETALLE_COMPROBANTE_CNT.COMPANIA = PLAN_FLUJO_EFECTIVO.COMPANIA
                                       AND DETALLE_COMPROBANTE_CNT.CODIGO_FLUJO_EFECTIVO = PLAN_FLUJO_EFECTIVO.CODIGO
                                       AND DETALLE_COMPROBANTE_CNT.ANO = PLAN_FLUJO_EFECTIVO.ANO
 WHERE PLAN_CONTABLE.COMPANIA ='s$compania$s'
     AND PLAN_CONTABLE.ANO =s$anioTrabajo$s
     AND PLAN_CONTABLE.CODIGO BETWEEN 's$codigoInicial$s' AND 's$codigoFinal$s'
     AND PLAN_CONTABLE.CLASECUENTA IN ('B','J')
     AND EXTRACT(MONTH FROM DETALLE_COMPROBANTE_CNT.FECHA) =s$mesTrabajo$s
     ]') || TO_CLOB(q'[
     UNION 
     
     SELECT
     ROW_NUMBER() OVER (PARTITION BY PLAN_CONTABLE.ID order by PLAN_CONTABLE.ID) ROOW,
     PLAN_CONTABLE.ID,
     PLAN_CONTABLE.CODIGO,
     DETALLE_COMPROBANTE_CNT.TIPO_CPTE,
     DETALLE_COMPROBANTE_CNT.COMPROBANTE,
     PLAN_FLUJO_EFECTIVO.ACTIVIDAD,
     UPPER(PLAN_FLUJO_EFECTIVO.NOMBRE)  NOMBRE,
     PLAN_FLUJO_EFECTIVO.NATURALEZA                 IND,
     DETALLE_COMPROBANTE_CNT.FECHA,
     PLAN_CONTABLE.COMPANIA,
     0 DANTERIOR,
     0 CANTERIOR,
     0 CANTERIORN,
     DETALLE_COMPROBANTE_CNT.VALOR_DEBITO    DNUEVO,
     DETALLE_COMPROBANTE_CNT.VALOR_CREDITO   CNUEVO,
     (CASE WHEN 0  = s$mesComparar$s -1  THEN PLAN_CONTABLE.SALDO0  ELSE 0 END
            + CASE WHEN s$mesComparar$s  = 1 -1  THEN PLAN_CONTABLE.SALDO1  ELSE 0 END
            + CASE WHEN s$mesComparar$s  = 1 -1  THEN PLAN_CONTABLE.SALDO2  ELSE 0 END
            + CASE WHEN 3  = s$mesComparar$s -1  THEN PLAN_CONTABLE.SALDO3  ELSE 0 END
            + CASE WHEN 4  = s$mesComparar$s -1  THEN PLAN_CONTABLE.SALDO4  ELSE 0 END
            + CASE WHEN 5  = s$mesComparar$s -1  THEN PLAN_CONTABLE.SALDO5  ELSE 0 END
            + CASE WHEN 6  = s$mesComparar$s -1  THEN PLAN_CONTABLE.SALDO6  ELSE 0 END
            + CASE WHEN 7  = s$mesComparar$s -1  THEN PLAN_CONTABLE.SALDO7  ELSE 0 END
            + CASE WHEN 8  = s$mesComparar$s -1  THEN PLAN_CONTABLE.SALDO8  ELSE 0 END
            + CASE WHEN 9  = s$mesComparar$s -1  THEN PLAN_CONTABLE.SALDO9  ELSE 0 END
            + CASE WHEN 10 = s$mesComparar$s -1  THEN PLAN_CONTABLE.SALDO10 ELSE 0 END
            + CASE WHEN 11 = s$mesComparar$s -1  THEN PLAN_CONTABLE.SALDO11 ELSE 0 END
            + CASE WHEN 12 = s$mesComparar$s -1  THEN PLAN_CONTABLE.SALDO12 ELSE 0 END
            + CASE WHEN 13 = s$mesComparar$s -1  THEN PLAN_CONTABLE.SALDO13 ELSE 0 END
            )    DNUEVON,
     0 SANTERIOR,
     CASE
         WHEN PLAN_FLUJO_EFECTIVO.NATURALEZA = 'D' THEN DETALLE_COMPROBANTE_CNT.VALOR_DEBITO
         ELSE DETALLE_COMPROBANTE_CNT.VALOR_CREDITO
     END NUEVO,
     SUBSTR (PLAN_CONTABLE.CODIGO,1,1)  CTABAL,
     CASE
         WHEN LENGTH(s$mesComparar$s) = 1 THEN '0' ||s$mesComparar$s
         ELSE 's$mesComparar$s'
     END MES
 FROM
     V_PLAN_CONTABLE PLAN_CONTABLE
     LEFT JOIN DETALLE_COMPROBANTE_CNT ON PLAN_CONTABLE.COMPANIA = DETALLE_COMPROBANTE_CNT.COMPANIA
                                          AND PLAN_CONTABLE.ANO = DETALLE_COMPROBANTE_CNT.ANO
               ]') || TO_CLOB(q'[                           AND PLAN_CONTABLE.CODIGO = DETALLE_COMPROBANTE_CNT.CUENTA
     INNER JOIN PLAN_FLUJO_EFECTIVO ON DETALLE_COMPROBANTE_CNT.COMPANIA = PLAN_FLUJO_EFECTIVO.COMPANIA
                                      AND DETALLE_COMPROBANTE_CNT.CODIGO_FLUJO_EFECTIVO = PLAN_FLUJO_EFECTIVO.CODIGO
                                      AND DETALLE_COMPROBANTE_CNT.ANO = PLAN_FLUJO_EFECTIVO.ANO
 WHERE
     PLAN_CONTABLE.COMPANIA ='s$compania$s'
     AND PLAN_CONTABLE.ANO =s$anioComparar$s
     AND PLAN_CONTABLE.CODIGO BETWEEN 's$codigoInicial$s' AND 's$codigoFinal$s'
     AND PLAN_CONTABLE.CLASECUENTA IN ('B','J')
     AND EXTRACT(MONTH FROM DETALLE_COMPROBANTE_CNT.FECHA) =s$mesComparar$s)
     ]') || TO_CLOB(q'[
SELECT FLUJOEFECTIVO.COMPANIA, CASE WHEN FLUJOEFECTIVO.IND = 'D' THEN 'ENTRADA ' ELSE 'SALIDA' END  IND,
 CASE WHEN FLUJOEFECTIVO.ACTIVIDAD='O' THEN 'OPERACIÓN'  WHEN FLUJOEFECTIVO.ACTIVIDAD='I' THEN 'INVERSIÓN' WHEN FLUJOEFECTIVO.ACTIVIDAD='F' THEN 'FINANCIACIÓN' END ACTIVIDAD, 
 FLUJOEFECTIVO.NOMBRE, 
 SUM(FLUJOEFECTIVO.SANTERIOR)  SANTERIOR, 
 SUM(FLUJOEFECTIVO.DANTERIOR)  DANTERIOR,
 SUM(CASE WHEN ROOW = 1 THEN FLUJOEFECTIVO.DANTERIORN ELSE 0 END)  DANTERIORN, 
 SUM(FLUJOEFECTIVO.CANTERIOR)  CANTERIOR, 
 SUM(CASE WHEN FLUJOEFECTIVO.IND = 'D' THEN FLUJOEFECTIVO.DANTERIOR ELSE 0 END) ENTANTERIOR, 
 SUM(CASE WHEN FLUJOEFECTIVO.IND = 'D' THEN 0 ELSE FLUJOEFECTIVO.CANTERIOR END)  SALANTERIOR, 
 SUM(FLUJOEFECTIVO.NUEVO) AS NUEVO, 
 SUM(FLUJOEFECTIVO.DNUEVO) AS DNUEVO,
 SUM(CASE WHEN ROOW = 1 THEN FLUJOEFECTIVO.DNUEVON ELSE 0 END) AS DNUEVON, 
 SUM(FLUJOEFECTIVO.CNUEVO) AS CNUEVO, 
 SUM(CASE WHEN FLUJOEFECTIVO.IND = 'D' THEN FLUJOEFECTIVO.DNUEVO ELSE 0 END)  ENTNUEVO, 
 SUM(CASE WHEN FLUJOEFECTIVO.IND = 'D' THEN 0 ELSE FLUJOEFECTIVO.CNUEVO END)  SALNUEVO, 
 FLUJOEFECTIVO.CTABAL, 
 FLUJOEFECTIVO.MES 
 FROM FLUJOEFECTIVO 
  WHERE FLUJOEFECTIVO.ACTIVIDAD IS NOT NULL 
   AND FLUJOEFECTIVO.IND IS NOT NULL
  GROUP BY 
  FLUJOEFECTIVO.COMPANIA, CASE WHEN FLUJOEFECTIVO.IND = 'D' THEN 'ENTRADA ' ELSE 'SALIDA' END  ,
 CASE WHEN FLUJOEFECTIVO.ACTIVIDAD='O' THEN 'OPERACIÓN'  WHEN FLUJOEFECTIVO.ACTIVIDAD='I' THEN 'INVERSIÓN' WHEN FLUJOEFECTIVO.ACTIVIDAD='F' THEN 'FINANCIACIÓN' END , 
 FLUJOEFECTIVO.NOMBRE, FLUJOEFECTIVO.CTABAL, 
  FLUJOEFECTIVO.MES
ORDER BY  FLUJOEFECTIVO.COMPANIA, CASE WHEN FLUJOEFECTIVO.IND = 'D' THEN 'ENTRADA ' ELSE 'SALIDA' END,   CASE WHEN FLUJOEFECTIVO.ACTIVIDAD='O' THEN 'OPERACIÓN'  WHEN FLUJOEFECTIVO.ACTIVIDAD='I' THEN 'INVERSIÓN' WHEN FLUJOEFECTIVO.ACTIVIDAD='F' THEN 'FINANCIACIÓN' END , FLUJOEFECTIVO.NOMBRE 
  ]') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);