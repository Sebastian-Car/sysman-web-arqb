MERGE INTO CONSULTAS FIN USING (SELECT '002018LisCompContable' INFORME ,TO_CLOB(q'[SELECT    COMPROBANTE_CNT.COMPANIA, 
          COMPROBANTE_CNT.ANO, 
          COMPROBANTE_CNT.TIPO, 
          COMPROBANTE_CNT.NUMERO, 
          TO_CHAR(COMPROBANTE_CNT.FECHA,'DD/MM/YYYY') FECHA, 
          COMPROBANTE_CNT.DESCRIPCION, 
          COMPROBANTE_CNT.VLR_BASE, 
          COMPROBANTE_CNT.DESCRIPCION 
          || ' ' || COMPROBANTE_CNT.TEXTO TEXTO, 
          COMPROBANTE_CNT.NRO_DOCUMENTO, 
          COMPROBANTE_CNT.FECHA_VCN_DOC, 
          COMPROBANTE_CNT.VLR_DOCUMENTO, 
          COMPROBANTE_CNT.TERCERO, 
          COMPROBANTE_CNT.SUCURSAL, 
          COMPROBANTE_CNT.DEBITO, 
          COMPROBANTE_CNT.CREDITO, 
          COMPROBANTE_CNT.ABONADO,  
          COMPROBANTE_CNT.CUENTA, 
          COMPROBANTE_CNT.ENTREGADO, 
          COMPROBANTE_CNT.PAGADOBANCO, 
          COMPROBANTE_CNT.IMPRESO, 
          COMPROBANTE_CNT.ANULADO, 
          COMPROBANTE_CNT.VLR_BASEIVA, 
          TIPO_COMPROBANTE.NOMBRE, 
          TIPO_COMPROBANTE.DISPOSICION_FINAL,
          TIPO_COMPROBANTE.ARCHIVO_GESTION,
          TIPO_COMPROBANTE.ARCHIVO_CENTRAL,
          TIPO_COMPROBANTE.NUMERO_DOCUMENTO,
          CASE WHEN COMPROBANTE_CNT.TERCERO  = LPAD('9',18,'9')     
               THEN ' ' 
               ELSE COMPROBANTE_CNT.TERCERO 
               END CODTERCERO,
          CASE WHEN COMPROBANTE_CNT.SUCURSAL = LPAD('9',3,'9')
               THEN ' ' 
               ELSE COMPROBANTE_CNT.SUCURSAL 
               END CODSUCURSAL,
          COMPROBANTE_CNT.VLRAGIRAR, 
          TERCERO.NOMBRE AS TERCERONOMBRE,
            (SELECT    DISTINCT
                       RTRIM(XMLAGG(XMLELEMENT("CUENTA", 
                       COMPROBANTE_CNTAFECTADOS.ANO_AFECT        || ' ' || 
                       COMPROBANTE_CNTAFECTADOS.TIPO_CPTE_AFECT  || ' ' ||
                       COMPROBANTE_CNTAFECTADOS.COMPROBANTE_AFECT||',')).EXTRACT('//text()'),',')
              FROM     COMPROBANTE_CNTAFECTADOS
              WHERE    COMPROBANTE_CNTAFECTADOS.COMPANIA = COMPROBANTE_CNT.COMPANIA
               AND     COMPROBANTE_CNTAFECTADOS.ANO = COMPROBANTE_CNT.ANO
               AND     COMPROBANTE_CNTAFECTADOS.TIPO_CPTE = COMPROBANTE_CNT.TIPO
               AND     COMPROBANTE_CNTAFECTADOS.COMPROBANTE = COMPROBANTE_CNT.NUMERO
              GROUP BY COMPROBANTE_CNTAFECTADOS.COMPROBANTE) DOCAFECTADOFINAL
,
              ROUND(MONTHS_BETWEEN(TRUNC(SYSDATE) , TRUNC(COMPROBANTE_CNT.FECHA)),2) EDAD_COMPROBANTE
  FROM        COMPROBANTE_CNT 
  INNER JOIN  TIPO_COMPROBANTE ON(COMPROBANTE_CNT.COMPANIA = TIPO_COMPROBANTE.COMPANIA
                                  AND COMPROBANTE_CNT.TIPO = TIPO_COMPROBANTE.CODIGO)
   LEFT JOIN  TERCERO ON (COMPROBANTE_CNT.COMPANIA = TERCERO.COMPANIA 
                            AND COMPROBANTE_CNT.TERCERO = TERCERO.NIT
                            AND COMPROBANTE_CNT.SUCURSAL = TERCERO.SUCURSAL) 
       WHERE COMPROBANTE_CNT.COMPANIA = 's$compania$s'
        AND  COMPROBANTE_CNT.TIPO    BETWE]') || TO_CLOB(q'[EN 's$tipoInicial$s' AND 's$tipoFinal$s'
        AND  COMPROBANTE_CNT.NUMERO  BETWEEN 's$comprobanteInicial$s' AND 's$comprobanteFinal$s'
  AND  TRUNC(COMPROBANTE_CNT.FECHA)   BETWEEN TO_DATE(s$fechaInicial$s,'DD/MM/YYYY') AND TO_DATE(s$fechaFinal$s,'DD/MM/YYYY')]') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);