MERGE INTO CONSULTAS FIN USING (SELECT '002323CDCIDI' INFORME ,TO_CLOB(q'[SELECT TIPO_COMPROBANTE_NOM,
  COMPROBANTE,
  CENTRO_COSTO,
  FECHA_COMPROBANTE,
  TERCERO_COM_NOM TERCERO_NOM,
  TERCERO_COM TERCERO,
  NRO_DOCUMENTO,
  FECHA_VCN_DOC,
  NVL(VLR_DOCUMENTO,0) VLR_DOCUMENTO,
  DESCRIPCION,
  TEXTO,
  NRO_DOCUMENTO_DET,
  CUENTAPPTAL,
  CODIGO_CUENTA_DET,
  CUENTANOMBRE,
  NVL(VALOR_DEBITO,0) VALOR_DEBITO,
  NVL(VALOR_CREDITO,0) VALOR_CREDITO
FROM V_DETALLE_AUXILIAR_CNT
WHERE COMPANIA   = s$compania$s
  AND ANO             = s$ano$s 
  AND TIPO_CPTE  = 's$tipoCpte$s'  
  AND COMPROBANTE BETWEEN s$numeroPptoInicial$s AND s$numeroPptoFinal$s
ORDER BY COMPROBANTE]') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'calarcon' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
