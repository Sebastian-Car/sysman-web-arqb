MERGE INTO CONSULTAS FIN USING (SELECT '800594FEREPORTEBUSQUEDANOTASAJUSTE' INFORME ,TO_CLOB(q'[SELECT 
ESTADO_NOTAS.CLASE,        
ESTADO_NOTAS.ESTADO,        
ESTADO_NOTAS.FECHA,        
ESTADO_NOTAS.NUMFACTURA,        
ESTADO_NOTAS.NUMFORMATO,
COMPROBANTE.VLR_DOCUMENTO VALOR,
COMPROBANTE.TERCERO,
ESTADO_NOTAS.OBSERVACION
 CUDE
FROM ESTADO_NOTAS INNER JOIN (SELECT TIPO, NUMERO, TERCERO, VLR_DOCUMENTO FROM TIPO_COMPROBANTE INNER JOIN
COMPROBANTE_CNT ON (COMPROBANTE_CNT.TIPO = TIPO_COMPROBANTE.CODIGO) WHERE NOTAAJUSTE_CUDS NOT IN (0)) COMPROBANTE ON (COMPROBANTE.NUMERO = ESTADO_NOTAS.NUMFORMATO)]') CONSULTA, 69 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
COMMIT;