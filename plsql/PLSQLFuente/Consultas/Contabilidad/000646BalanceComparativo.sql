MERGE INTO CONSULTAS FIN USING (SELECT '000646BalanceComparativo' INFORME ,TO_CLOB(q'[SELECT COMPANIA
      , CODIGO
      , CENTRO_COSTO
      , TERCERO
      , TERCERO_NOMBRE
      , AUXILIAR
      , REFERENCIA
      , FUENTE_RECURSOS
      , NOMBRE
      , SUM(SANTERIOR) SANTERIOR
      , SUM(NUEVO) NUEVO
      , CLASE
      , ORDEN 
FROM (
SELECT  COMPANIA
      , CODIGO
      , CENTRO_COSTO
      , TERCERO_NOMBRE
      , TERCERO
      , AUXILIAR
      , REFERENCIA
      , FUENTE_RECURSOS
      , NOMBRE
      , SALDOs$mestrabajo$s SANTERIOR
      , 0 NUEVO
      , SUBSTR(CODIGO,1,1) CLASE
      , CASE
          WHEN SUBSTR(CODIGO,1,1)='0'
            THEN 'z'
              ||CODIGO
            ELSE 
              CODIGO
        END ORDEN
 
FROM (
s$baseBalance$s) 
BASE
UNION ALL
SELECT  COMPANIA
      , CODIGO
      , CENTRO_COSTO
      , TERCERO_NOMBRE
      , TERCERO
      , AUXILIAR
      , REFERENCIA
      , FUENTE_RECURSOS
      , NOMBRE
      , 0 NUEVO
      , SALDOs$mesComparar$s SANTERIOR
      , SUBSTR(CODIGO,1,1) CLASE
      , CASE
          WHEN SUBSTR(CODIGO,1,1)='0'
            THEN 'z'
              ||CODIGO
            ELSE 
              CODIGO
        END ORDEN
 
FROM (
s$baseBalanceUnion$s ) 
)
WHERE LENGTH(CODIGO)<=s$digitos$s
 s$saldoCero$s
GROUP BY COMPANIA
      , CODIGO
      , CENTRO_COSTO
      , TERCERO_NOMBRE
      , TERCERO
      , AUXILIAR
      , REFERENCIA
      , FUENTE_RECURSOS
      , NOMBRE
      , CLASE
      , ORDEN]') CONSULTA, 1 APLICACION ,TO_CLOB(q'[SELECT PLAN_CONTABLE.CODIGO, 
PLAN_CONTABLE.CENTRO_COSTO_INFORME CENTRO_COSTO, 
PLAN_CONTABLE.AUXILIAR_INFORME AUXILIAR,
PLAN_CONTABLE.TERCERO_INFORME TERCERO, 
PLAN_CONTABLE.SUCURSAL_INFORME SUCURSAL, 
PLAN_CONTABLE.NOMBRE, 
PLAN_CONTABLE.CODIGO || PLAN_CONTABLE.CENTRO_COSTO || PLAN_CONTABLE.NOMBRE ORDENCUENTAS,
PLAN_CONTABLE.COMPANIA, 
PLAN_CONTABLE.SALDOs$mesTrabajo$s SANTERIOR, 
PLAN_CONTABLE_1.SALDOs$mesComparar$s NUEVO, 
CASE WHEN SUBSTR(PLAN_CONTABLE.CODIGO,1,1)='0' THEN 'Z'||PLAN_CONTABLE.CODIGO ELSE PLAN_CONTABLE.CODIGO END ORDENINF,
SUBSTR(PLAN_CONTABLE.CODIGO,1,1) CLASE,
CASE WHEN SUBSTR(PLAN_CONTABLE.CODIGO,1,1)='0' THEN 'Z'||PLAN_CONTABLE.CODIGO ELSE PLAN_CONTABLE.CODIGO END ORDEN,
SUBSTR(CASE WHEN SUBSTR(PLAN_CONTABLE.CODIGO,1,1)='0' THEN 'Z'||PLAN_CONTABLE.CODIGO ELSE PLAN_CONTABLE.CODIGO END,1,1) CLASEORDEN
FROM V_PLAN_CONTABLE PLAN_CONTABLE 
  INNER JOIN V_PLAN_CONTABLE PLAN_CONTABLE_1 
    ON PLAN_CONTABLE.COMPANIA = PLAN_CONTABLE_1.COMPANIA
    AND PLAN_CONTABLE.CODIGO = PLAN_CONTABLE_1.CODIGO
WHERE PLAN_CONTABLE.COMPANIA=s$compania$s
AND PLAN_CONTABLE.ANO= s$anioTrabajo$s
AND PLAN_CONTABLE_1.ANO= s$anioComparar$s
AND PLAN_CONTABLE.CODIGO BETWEEN 's$codigoInicial$s' AND 's$codigoFinal$s'
AND LENGTH(PLAN_CONTABLE.CODIGO) <= s$digitos$s
  s$auxiliar$s 
  s$tercero$s 
  s$saldoCero$s]') CONSULTA_OPCIONAL, NULL CREATED_BY, 'jdsalcedo' MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);