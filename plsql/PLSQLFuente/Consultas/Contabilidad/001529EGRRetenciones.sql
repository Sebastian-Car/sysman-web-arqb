MERGE INTO CONSULTAS FIN USING (SELECT '001529EGRRetenciones' INFORME ,TO_CLOB(q'[SELECT 
COMPROBANTE_CNTRETENCION.COMPANIA,
COMPROBANTE_CNTRETENCION.ANO,
COMPROBANTE_CNTRETENCION.TIPORETENCION,
TIPORETENCION.NOMBRE,
RETENCIONES.PCT_APLICAR,
  CASE
    WHEN LEY1450 <> 0
    THEN VALORBASELEY1607
    ELSE COMPROBANTE_CNTRETENCION.VALORBASE
  END VALORBASE,
COMPROBANTE_CNTRETENCION.VALOR,
COMPROBANTE_CNTRETENCION.TIPO,
COMPROBANTE_CNTRETENCION.NUMERO
FROM COMPROBANTE_CNTRETENCION
LEFT JOIN DETALLE_COMPROBANTE_CNT
ON COMPROBANTE_CNTRETENCION.COMPANIA = DETALLE_COMPROBANTE_CNT.COMPANIA
AND COMPROBANTE_CNTRETENCION.ANO     = DETALLE_COMPROBANTE_CNT.ANO
AND COMPROBANTE_CNTRETENCION.TIPO    = DETALLE_COMPROBANTE_CNT.TIPO_CPTE_AFECT
AND COMPROBANTE_CNTRETENCION.NUMERO  = DETALLE_COMPROBANTE_CNT.CMPTE_AFECTADO
INNER JOIN COMPROBANTE_CNT
ON COMPROBANTE_CNT.COMPANIA = DETALLE_COMPROBANTE_CNT.COMPANIA
AND COMPROBANTE_CNT.ANO = DETALLE_COMPROBANTE_CNT.ANO
AND COMPROBANTE_CNT.TIPO = DETALLE_COMPROBANTE_CNT.TIPO_CPTE
AND COMPROBANTE_CNT.NUMERO = DETALLE_COMPROBANTE_CNT.COMPROBANTE
LEFT JOIN RETENCIONES
ON COMPROBANTE_CNTRETENCION.COMPANIA         = RETENCIONES.COMPANIA
AND COMPROBANTE_CNTRETENCION.ANO             = RETENCIONES.ANO
AND COMPROBANTE_CNTRETENCION.TIPORETENCION   = RETENCIONES.TIPO
AND COMPROBANTE_CNTRETENCION.CODIGORETENCION = RETENCIONES.CODIGO
LEFT JOIN TIPORETENCION
ON COMPROBANTE_CNTRETENCION.TIPORETENCION = TIPORETENCION.CODIGO
WHERE DETALLE_COMPROBANTE_CNT.COMPANIA = $P{PR_COMPANIA}
AND DETALLE_COMPROBANTE_CNT.ANO          = $P{PR_ANO}
AND DETALLE_COMPROBANTE_CNT.TIPO_CPTE = $P{PR_TIPO}
AND DETALLE_COMPROBANTE_CNT.COMPROBANTE = $P{PR_COMPROBANTE}]') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);

MERGE INTO CONSULTAS_SUB FIN USING (SELECT '002287EGRDCCNT_REDV' INFORME_PADRE, '001529EGRRetenciones' INFORME_HIJO, 'PR_STRSQL_SECUNDARIO1529' PARAMETRO ,NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (FIN.INFORME_PADRE = INI.INFORME_PADRE AND INI.INFORME_HIJO = FIN.INFORME_HIJO)  WHEN MATCHED THEN  UPDATE SET FIN.PARAMETRO = INI.PARAMETRO , FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME_PADRE,INFORME_HIJO, PARAMETRO,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME_PADRE,INI.INFORME_HIJO, INI.PARAMETRO,INI.CREATED_BY,SYSDATE);

--7701127
MERGE INTO CONSULTAS_SUB FIN USING (SELECT '002287EGRDCCNT_REDV' INFORME_PADRE, '001529EGRRetenciones' INFORME_HIJO, 'PR_STRSQL_SECUNDARIO1529' PARAMETRO ,NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (FIN.INFORME_PADRE = INI.INFORME_PADRE AND INI.INFORME_HIJO = FIN.INFORME_HIJO)  WHEN MATCHED THEN  UPDATE SET FIN.PARAMETRO = INI.PARAMETRO , FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME_PADRE,INFORME_HIJO, PARAMETRO,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME_PADRE,INI.INFORME_HIJO, INI.PARAMETRO,INI.CREATED_BY,SYSDATE);