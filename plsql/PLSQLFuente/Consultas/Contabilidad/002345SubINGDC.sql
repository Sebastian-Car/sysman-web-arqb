MERGE INTO CONSULTAS FIN USING (SELECT '002345SubINGDC' INFORME ,TO_CLOB(q'[SELECT DISTINCT DETALLE_COMPROBANTE_PPTAL.ANO ANOCOMP,
	DETALLE_COMPROBANTE_PPTAL.CONSECUTIVO,
	DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE TIPOCOMP,
	DETALLE_COMPROBANTE_PPTAL.COMPROBANTE NUMCOMP,
	NVL(DETALLE_COMPROBANTE_PPTAL_1.CMPTE_AFECTADO,0) NUMDIS,
	DETALLE_COMPROBANTE_PPTAL.CMPTE_AFECTADO NUMRESERVA,
	DETALLE_COMPROBANTE_PPTAL.CUENTA,
	DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO,
	PLAN_PRESUPUESTAL.NOMBRE
	|| ' /  '
	|| AUXILIAR.NOMBRE NOM_CUENTA,
	CASE
	WHEN PLAN_PRESUPUESTAL.NATURALEZA = 'C'
	THEN SUM(DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)-SUM(DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO)
	ELSE SUM(DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO) -SUM(DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)
	END VLR
	FROM DETALLE_COMPROBANTE_PPTAL
	LEFT JOIN DETALLE_COMPROBANTE_PPTAL DETALLE_COMPROBANTE_PPTAL_1
	ON DETALLE_COMPROBANTE_PPTAL.CONSECUTIVOPPTO  = DETALLE_COMPROBANTE_PPTAL_1.CONSECUTIVO
	AND DETALLE_COMPROBANTE_PPTAL.CMPTE_AFECTADO  = DETALLE_COMPROBANTE_PPTAL_1.COMPROBANTE
	AND DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE_AFECT = DETALLE_COMPROBANTE_PPTAL_1.TIPO_CPTE
	AND DETALLE_COMPROBANTE_PPTAL.ANO             = DETALLE_COMPROBANTE_PPTAL_1.ANO
	AND DETALLE_COMPROBANTE_PPTAL.COMPANIA        = DETALLE_COMPROBANTE_PPTAL_1.COMPANIA
	INNER JOIN PLAN_PRESUPUESTAL
	ON DETALLE_COMPROBANTE_PPTAL.COMPANIA = PLAN_PRESUPUESTAL.COMPANIA
	AND DETALLE_COMPROBANTE_PPTAL.CUENTA  = PLAN_PRESUPUESTAL.CODIGO
	AND DETALLE_COMPROBANTE_PPTAL.ANO     = PLAN_PRESUPUESTAL.ANO
	LEFT JOIN AUXILIAR
	ON DETALLE_COMPROBANTE_PPTAL.COMPANIA  = AUXILIAR.COMPANIA
	AND DETALLE_COMPROBANTE_PPTAL.AUXILIAR = AUXILIAR.CODIGO
	AND DETALLE_COMPROBANTE_PPTAL.ANO = AUXILIAR.ANO
	GROUP BY DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE,
	DETALLE_COMPROBANTE_PPTAL.CONSECUTIVO,
	DETALLE_COMPROBANTE_PPTAL.COMPROBANTE,
	DETALLE_COMPROBANTE_PPTAL_1.CMPTE_AFECTADO,
	DETALLE_COMPROBANTE_PPTAL.CMPTE_AFECTADO,
	DETALLE_COMPROBANTE_PPTAL.CUENTA,
	DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO,
	PLAN_PRESUPUESTAL.NOMBRE,
	AUXILIAR.NOMBRE,
	PLAN_PRESUPUESTAL.NATURALEZA,
	DETALLE_COMPROBANTE_PPTAL.ANO
	HAVING DETALLE_COMPROBANTE_PPTAL.ANO    = s$ano$s
	AND DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE = $P{PR_TIPOCOMP}
	AND DETALLE_COMPROBANTE_PPTAL.COMPROBANTE BETWEEN $P{PR_NUMCOMP} AND $P{PR_NUMCOMP}]') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'calarcon' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);



    MERGE INTO CONSULTAS_SUB FIN USING (SELECT '002344INGDC' INFORME_PADRE, '002345SubINGDC' INFORME_HIJO, 'PR_STRSQL_INFORMESUBINGDC' PARAMETRO ,'calarcon' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (FIN.INFORME_PADRE = INI.INFORME_PADRE AND INI.INFORME_HIJO = FIN.INFORME_HIJO)  WHEN MATCHED THEN  UPDATE SET FIN.PARAMETRO = INI.PARAMETRO , FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME_PADRE,INFORME_HIJO, PARAMETRO,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME_PADRE,INI.INFORME_HIJO, INI.PARAMETRO,INI.CREATED_BY,SYSDATE);