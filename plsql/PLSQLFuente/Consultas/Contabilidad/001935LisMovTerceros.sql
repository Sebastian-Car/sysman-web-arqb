MERGE INTO CONSULTAS FIN USING (SELECT '001935LisMovTerceros' INFORME ,TO_CLOB(q'[SELECT
    PLAN_CONTABLE.CODIGO CODIGO_CUENTA,
    TERCERO.NOMBRE NOMBRECOMPLETO,
    PLAN_CONTABLE.COMPANIA,
    DETALLE_COMPROBANTE_CNT.DESCRIPCION,
    DETALLE_COMPROBANTE_CNT.COMPROBANTE,
    DETALLE_COMPROBANTE_CNT.FECHA,
    DETALLE_COMPROBANTE_CNT.SUCURSAL,
    DETALLE_COMPROBANTE_CNT.TERCERO,
    DETALLE_COMPROBANTE_CNT.TIPO_CPTE,
    DETALLE_COMPROBANTE_CNT.VALOR_CREDITO,
    DETALLE_COMPROBANTE_CNT.VALOR_DEBITO, 
    PLAN_CONTABLE.NOMBRE NOMBRECUENTA,
    DETALLE_COMPROBANTE_CNT.CODIGO_PPTAL,
    DETALLE_COMPROBANTE_CNT.BASE_GRAVABLE,
    CASE 
        WHEN DETALLE_COMPROBANTE_CNT.AUXILIAR = PCK_DATOS.FC_CONS_AUXILIAR 
        THEN ' '
        ELSE DETALLE_COMPROBANTE_CNT.AUXILIAR
        END CODAUXILIAR,
    CASE
        WHEN DETALLE_COMPROBANTE_CNT.CENTRO_COSTO = PCK_DATOS.FC_CONS_CENTRO
        THEN ' '
        ELSE DETALLE_COMPROBANTE_CNT.CENTRO_COSTO
        END CODCENTROCOSTO  
FROM PLAN_CONTABLE
    INNER JOIN DETALLE_COMPROBANTE_CNT 
        ON PLAN_CONTABLE.COMPANIA = DETALLE_COMPROBANTE_CNT.COMPANIA
        AND PLAN_CONTABLE.ANO = DETALLE_COMPROBANTE_CNT.ANO
        AND PLAN_CONTABLE.CODIGO = DETALLE_COMPROBANTE_CNT.CUENTA
    INNER JOIN COMPROBANTE_CNT
        ON COMPROBANTE_CNT.COMPANIA = DETALLE_COMPROBANTE_CNT.COMPANIA
        AND COMPROBANTE_CNT.ANO = DETALLE_COMPROBANTE_CNT.ANO
        AND COMPROBANTE_CNT.TIPO = DETALLE_COMPROBANTE_CNT.TIPO_CPTE
        AND COMPROBANTE_CNT.NUMERO = DETALLE_COMPROBANTE_CNT.COMPROBANTE 
    INNER JOIN TERCERO
        ON TERCERO.COMPANIA = DETALLE_COMPROBANTE_CNT.COMPANIA
        AND TERCERO.NIT = DETALLE_COMPROBANTE_CNT.TERCERO
        AND TERCERO.SUCURSAL = DETALLE_COMPROBANTE_CNT.SUCURSAL
WHERE DETALLE_COMPROBANTE_CNT.COMPANIA = s$compania$s
    AND   DETALLE_COMPROBANTE_CNT.ANO = s$ano$s
    AND   PCK_SYSMAN_UTL.FC_MES(DETALLE_COMPROBANTE_CNT.FECHA) BETWEEN s$fechaInicial$s AND s$fechaFinal$s
    AND    DETALLE_COMPROBANTE_CNT.TERCERO = 's$terceroNit$s'
    AND  DETALLE_COMPROBANTE_CNT.SUCURSAL = 's$terceroSucursal$s'
ORDER BY
    DETALLE_COMPROBANTE_CNT.TIPO_CPTE,
    DETALLE_COMPROBANTE_CNT.COMPROBANTE,
    DETALLE_COMPROBANTE_CNT.FECHA]') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);