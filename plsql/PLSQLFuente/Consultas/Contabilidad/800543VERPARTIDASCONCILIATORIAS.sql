MERGE INTO CONSULTAS FIN USING (SELECT '800543VERPARTIDASCONCILIATORIAS' INFORME ,TO_CLOB(q'[SELECT
    D.NRO_DOCUMENTO,
    TO_CHAR(D.FECHA, 'DD/MM/YYYY HH24:MI:SS') FECHA,
    D.VALOR_DEBITO,
    D.VALOR_CREDITO,
    D.PAGADOBANCO,
    TO_CHAR(D.FECHA_CONCILIA, 'DD/MM/YYYY HH24:MI:SS') FECHA_CONCILIACION,
    D.TIPO_CPTE,
    D.COMPROBANTE,
    D.CONSECUTIVO, 
    D.DESCRIPCION,
    D.TERCERO,
    D.TIPO_CPTE_AFECT,
    D.CMPTE_AFECTADO,
    C.IMPRESO,
    C.ENTREGADO,
    D.SINIDENTIFICAR,
    TO_CHAR(D.FECHA_IDENTIFICACION, 'DD/MM/YYYY HH24:MI:SS') FECHA_IDENTIFICACION,
    D.CONCILIADOR
FROM
    DETALLE_COMPROBANTE_CNT   D
    INNER JOIN COMPROBANTE_CNT           C ON D.COMPANIA = C.COMPANIA
                                    AND D.ANO = C.ANO
                                    AND D.TIPO_CPTE = C.TIPO
                                    AND D.COMPROBANTE = C.NUMERO
    INNER JOIN TIPO_COMPROBANTE          T ON T.COMPANIA = D.COMPANIA
                                     AND T.CODIGO = D.TIPO_CPTE
WHERE
    D.COMPANIA = s$compania$s
    AND D.CUENTA = s$cuenta$s
	AND T.CLASE_CONTABLE IN(s$clasescontables$s)
 AND ( TO_NUMBER(TO_CHAR(D.FECHA_CONCILIA, 'MM')) = s$mes$s
               AND TO_NUMBER(TO_CHAR(D.FECHA_CONCILIA, 'YYYY')) = s$ano$s ) 
ORDER BY
    D.FECHA,
    COMPROBANTE]') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'MROSERO' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);