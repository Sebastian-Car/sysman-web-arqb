MERGE INTO CONSULTAS FIN USING (SELECT '001956DepuracionRenta1819' INFORME ,TO_CLOB(q'[WITH CONSULTA AS
  (SELECT RESUMEN_RENTAS.COMPANIA,
    RESUMEN_RENTAS.ANO,
    RESUMEN_RENTAS.TIPO,
    RESUMEN_RENTAS.NUMERO,
    RESUMEN_RENTAS.TOTALINGRESO,
    PCK_SYSMAN_UTL.FC_ROUND(RESUMEN_RENTAS.DEDUCCION) DEDUCCION,
    PCK_SYSMAN_UTL.FC_ROUND(RESUMEN_RENTAS.PAGOS_SALUD) PAGOS_SALUD,
    PCK_SYSMAN_UTL.FC_ROUND(RESUMEN_RENTAS.APORTES_PENSION) APORTES_PENSION,
    PCK_SYSMAN_UTL.FC_ROUND(RESUMEN_RENTAS.DEDUCCION_DEPENDIENTE) DEDUCCION_DEPENDIENTE,
    RESUMEN_RENTAS.ARL,
    PCK_SYSMAN_UTL.FC_ROUND(RESUMEN_RENTAS.APORTE_VOLUNTARIO) APORTE_VOLUNTARIO,
    PCK_SYSMAN_UTL.FC_ROUND(RESUMEN_RENTAS.DEDUCCION_PREPAGADA) DEDUCCION_PREPAGADA,
    PCK_SYSMAN_UTL.FC_ROUND(RESUMEN_RENTAS.EXENCION) AS EXENCION,
    RESUMEN_RENTAS.VALORUVT,
    COMPROBANTE_CNT.VLR_BASE,
    COMPROBANTE_CNT.VLR_BASE  - DEDUCCION - PAGOS_SALUD - APORTES_PENSION - DEDUCCION_DEPENDIENTE - ARL - APORTE_VOLUNTARIO - DEDUCCION_PREPAGADA SUBTOTAL,
    COMPROBANTE_CNT.VLR_BASE  - PAGOS_SALUD-APORTES_PENSION SUBTOTAL384,
    PCK_SYSMAN_UTL.FC_ROUND(VALORRETENIDO / VALORUVT,2) RETENCION_UVT,
    RESUMEN_RENTAS.VALORRETENIDO,
    TERCERO.NOMBRE,
    RESUMEN_RENTAS.TERCERO,
    RESUMEN_RENTAS.FECHA,
    RESUMEN_RENTAS.APORTE_AFC,
    RESUMEN_RENTAS.FSP
  FROM RESUMEN_RENTAS
  INNER JOIN TERCERO
  ON RESUMEN_RENTAS.COMPANIA  = TERCERO.COMPANIA
  AND RESUMEN_RENTAS.TERCERO  = TERCERO.NIT
  AND RESUMEN_RENTAS.SUCURSAL = TERCERO.SUCURSAL
  INNER JOIN COMPROBANTE_CNT
   ON RESUMEN_RENTAS.COMPANIA    = COMPROBANTE_CNT.COMPANIA
  AND RESUMEN_RENTAS.ANO        = COMPROBANTE_CNT.ANO
  AND RESUMEN_RENTAS.TIPO       = COMPROBANTE_CNT.TIPO
  AND RESUMEN_RENTAS.NUMERO     = COMPROBANTE_CNT.NUMERO
WHERE RESUMEN_RENTAS.COMPANIA   = s$compania$s
  AND RESUMEN_RENTAS.ANO        = s$ano$s
  AND RESUMEN_RENTAS.TIPO       = 's$tipoCpte$s'
  AND RESUMEN_RENTAS.NUMERO     = s$numeroCpte$s
  )
SELECT CONSULTA.NOMBRE,
  CONSULTA.TERCERO,
  UPPER(PCK_SYSMAN_UTL.FC_NOMBRE_MES(EXTRACT(MONTH FROM CONSULTA.FECHA)))
  ||' DE '
  || EXTRACT(YEAR FROM CONSULTA.FECHA) FECHA,
  CONSULTA.VLR_BASE,
  CONSULTA.PAGOS_SALUD,
  CONSULTA.APORTES_PENSION,
  CONSULTA.FSP,
  CONSULTA.TOTALINGRESO,
  CONSULTA.APORTE_VOLUNTARIO,
  CONSULTA.APORTE_AFC,
  CONSULTA.DEDUCCION,
  CONSULTA.DEDUCCION_DEPENDIENTE,
  CONSULTA.DEDUCCION_PREPAGADA,
  CONSULTA.ARL,
  CONSULTA.SUBTOTAL,
  CONSULTA.EXENCION,
  CONSULTA.SUBTOTAL  - CONSULTA.EXENCION INGRESO_GRAVABLE,
  PCK_SYSMAN_UTL.FC_ROUND((CONSULTA.SUBTOTAL - CONSULTA.EXENCION) / CONSULTA.VALORUVT,2) INGRESO_UVT,
  CONSULTA.RETENCION_UVT,
  CONSULTA.VALORRETENIDO
FROM CONSULTA]') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);