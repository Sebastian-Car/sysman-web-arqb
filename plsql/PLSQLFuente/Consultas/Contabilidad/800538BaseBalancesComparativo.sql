MERGE INTO CONSULTAS FIN USING (SELECT '800538BaseBalancesComparativo' INFORME ,TO_CLOB(q'[SELECT  PLAN.COMPANIA, 
        PLAN.ANO, 
        PLAN.CODIGO,
        PLAN.NATURALEZA,
        PLAN.CORRIENTE,
        PLAN.NOMBRE NOMBRE_1,
        SUM(SALDO.SALDO0)    SALDO0, 
        SUM(SALDO.SALDO1)    SALDO1, 
        SUM(SALDO.SALDO2)    SALDO2, 
        SUM(SALDO.SALDO3)    SALDO3, 
        SUM(SALDO.SALDO4)    SALDO4, 
        SUM(SALDO.SALDO5)    SALDO5, 
        SUM(SALDO.SALDO6)    SALDO6, 
        SUM(SALDO.SALDO7)    SALDO7, 
        SUM(SALDO.SALDO8)    SALDO8, 
        SUM(SALDO.SALDO9)    SALDO9, 
        SUM(SALDO.SALDO10)   SALDO10, 
        SUM(SALDO.SALDO11)   SALDO11, 
        SUM(SALDO.SALDO12)   SALDO12, 
        SUM(SALDO.SALDO13)   SALDO13,  
        SUM(SALDO.AJUSTE0)   AJUSTE0 , 
        SUM(SALDO.AJUSTE1)   AJUSTE1, 
        SUM(SALDO.AJUSTE2)   AJUSTE2, 
        SUM(SALDO.AJUSTE3)   AJUSTE3, 
        SUM(SALDO.AJUSTE4)   AJUSTE4, 
        SUM(SALDO.AJUSTE5)   AJUSTE5, 
        SUM(SALDO.AJUSTE6)   AJUSTE6, 
        SUM(SALDO.AJUSTE7)   AJUSTE7, 
        SUM(SALDO.AJUSTE8)   AJUSTE8, 
        SUM(SALDO.AJUSTE9)   AJUSTE9, 
        SUM(SALDO.AJUSTE10)  AJUSTE10, 
        SUM(SALDO.AJUSTE11)  AJUSTE11, 
        SUM(SALDO.AJUSTE12)  AJUSTE12, 
        SUM(SALDO.AJUSTE13)  AJUSTE13,
        SUM(SALDO.CREDITO0)  CREDITO0 , 
        SUM(SALDO.CREDITO1)  CREDITO1, 
        SUM(SALDO.CREDITO2)  CREDITO2, 
        SUM(SALDO.CREDITO3)  CREDITO3, 
        SUM(SALDO.CREDITO4)  CREDITO4, 
        SUM(SALDO.CREDITO5)  CREDITO5, 
        SUM(SALDO.CREDITO6)  CREDITO6, 
        SUM(SALDO.CREDITO7)  CREDITO7, 
        SUM(SALDO.CREDITO8)  CREDITO8, 
        SUM(SALDO.CREDITO9)  CREDITO9, 
        SUM(SALDO.CREDITO10) CREDITO10, 
        SUM(SALDO.CREDITO11) CREDITO11, 
        SUM(SALDO.CREDITO12) CREDITO12, 
        SUM(SALDO.CREDITO13) CREDITO13,
        SUM(SALDO.DEBITO0)   DEBITO0,
        SUM(SALDO.DEBITO1)   DEBITO1, 
        SUM(SALDO.DEBITO2)   DEBITO2, 
        SUM(SALDO.DEBITO3)   DEBITO3, 
        SUM(SALDO.DEBITO4)   DEBITO4, 
        SUM(SALDO.DEBITO5)   DEBITO5, 
        SUM(SALDO.DEBITO6)   DEBITO6, 
        SUM(SALDO.DEBITO7)   DEBITO7, 
        SUM(SALDO.DEBITO8)   DEBITO8, 
        SUM(SALDO.DEBITO9)   DEBITO9, 
        SUM(SALDO.DEBITO10)  DEBITO10, 
        SUM(SALDO.DEBITO11)  DEBITO11, 
        SUM(SALDO.DEBITO12)  DEBITO12, 
        SUM(SALDO.DEBITO13)  DEBITO13,
        SUM(SALDO.NETO0)     NETO0,
        SUM(SALDO.NETO1)     NETO1, 
        SUM(SALDO.NETO2)     NETO2, 
        SUM(SALDO.NETO3)     NETO3, 
        SUM(SALDO.NETO4)     NETO4, 
        SUM(SALDO.NETO5)     NETO5, 
        SUM(SALDO.NETO6)     NETO6, 
        SUM(SALDO.NETO7)     NETO7, 
        SUM(SALDO.NETO8)     NETO8, 
        SUM(SALDO.NETO9)     NETO9, 
        SUM(SALDO.NETO10)    NETO10, 
        SUM(SALDO.NETO11)    NETO11, 
        SUM(SALDO.NETO12)    NETO12, 
        SUM(SALDO.NETO13)    NETO13, 
        ' ' CLASE,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.TERCERO ELSE NULL END TERCERO,
        CASE WHEN PLAN.CODIGO =]') || TO_CLOB(q'[ SALDO.CODIGO THEN SALDO.AUXILIAR ELSE NULL END AUXILIAR,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.CENTRO_COSTO ELSE NULL END CENTRO_COSTO,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.REFERENCIA ELSE NULL END REFERENCIA,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.FUENTE_RECURSOS ELSE NULL END FUENTE_RECURSOS,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.TERCERO_NOMBRE ELSE NULL END TERCERO_NOMBRE,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.AUXILIAR_NOMBRE ELSE NULL END AUXILIAR_NOMBRE,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.CENTRO_COSTO_NOMBRE ELSE NULL END CENTRO_COSTO_NOMBRE,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.REFERENCIA_NOMBRE ELSE NULL END REFERENCIA_NOMBRE,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.FUENTE_RECURSOS_NOMBRE ELSE NULL END FUENTE_RECURSOS_NOMBRE,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.NOMBRE ELSE PLAN.NOMBRE END NOMBRE,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.ORDEN ELSE PLAN.CODIGO END ORDEN
FROM PLAN_CONTABLE PLAN INNER JOIN 
(
SELECT 
        PLAN_CONTABLE.COMPANIA, 
        PLAN_CONTABLE.ANO, 
        PLAN_CONTABLE.CODIGO,
        SUM(SALDO_AUX_CONTABLE.SALDO0)    SALDO0, 
        SUM(SALDO_AUX_CONTABLE.SALDO1)    SALDO1, 
        SUM(SALDO_AUX_CONTABLE.SALDO2)    SALDO2, 
        SUM(SALDO_AUX_CONTABLE.SALDO3)    SALDO3, 
        SUM(SALDO_AUX_CONTABLE.SALDO4)    SALDO4, 
        SUM(SALDO_AUX_CONTABLE.SALDO5)    SALDO5, 
        SUM(SALDO_AUX_CONTABLE.SALDO6)    SALDO6, 
        SUM(SALDO_AUX_CONTABLE.SALDO7)    SALDO7, 
        SUM(SALDO_AUX_CONTABLE.SALDO8)    SALDO8, 
        SUM(SALDO_AUX_CONTABLE.SALDO9)    SALDO9, 
        SUM(SALDO_AUX_CONTABLE.SALDO10)   SALDO10, 
        SUM(SALDO_AUX_CONTABLE.SALDO11)   SALDO11, 
        SUM(SALDO_AUX_CONTABLE.SALDO12)   SALDO12, 
        SUM(SALDO_AUX_CONTABLE.SALDO13)   SALDO13,  
        SUM(SALDO_AUX_CONTABLE.AJUSTE0)   AJUSTE0 , 
        SUM(SALDO_AUX_CONTABLE.AJUSTE1)   AJUSTE1, 
        SUM(SALDO_AUX_CONTABLE.AJUSTE2)   AJUSTE2, 
        SUM(SALDO_AUX_CONTABLE.AJUSTE3)   AJUSTE3, 
        SUM(SALDO_AUX_CONTABLE.AJUSTE4)   AJUSTE4, 
        SUM(SALDO_AUX_CONTABLE.AJUSTE5)   AJUSTE5, 
        SUM(SALDO_AUX_CONTABLE.AJUSTE6)   AJUSTE6, 
        SUM(SALDO_AUX_CONTABLE.AJUSTE7)   AJUSTE7, 
        SUM(SALDO_AUX_CONTABLE.AJUSTE8)   AJUSTE8, 
        SUM(SALDO_AUX_CONTABLE.AJUSTE9)   AJUSTE9, 
        SUM(SALDO_AUX_CONTABLE.AJUSTE10)  AJUSTE10, 
        SUM(SALDO_AUX_CONTABLE.AJUSTE11)  AJUSTE11, 
        SUM(SALDO_AUX_CONTABLE.AJUSTE12)  AJUSTE12, 
        SUM(SALDO_AUX_CONTABLE.AJUSTE13)  AJUSTE13,
        SUM(SALDO_AUX_CONTABLE.CREDITO0)  CREDITO0 , 
        SUM(SALDO_AUX_CONTABLE.CREDITO1)  CREDITO1, 
        SUM(SALDO_AUX_CONTABLE.CREDITO2)  CREDITO2, 
        SUM(SALDO_AUX_CONTABLE.CREDITO3)  CREDITO3, 
        SUM(SALDO_AUX_CONTABLE.CREDITO4)  CREDITO4, 
        SUM(SALDO_AUX_CONTABLE.CREDITO5)  CREDITO5, 
        SUM(SALDO_AUX_]') || TO_CLOB(q'[CONTABLE.CREDITO6)  CREDITO6, 
        SUM(SALDO_AUX_CONTABLE.CREDITO7)  CREDITO7, 
        SUM(SALDO_AUX_CONTABLE.CREDITO8)  CREDITO8, 
        SUM(SALDO_AUX_CONTABLE.CREDITO9)  CREDITO9, 
        SUM(SALDO_AUX_CONTABLE.CREDITO10) CREDITO10, 
        SUM(SALDO_AUX_CONTABLE.CREDITO11) CREDITO11, 
        SUM(SALDO_AUX_CONTABLE.CREDITO12) CREDITO12, 
        SUM(SALDO_AUX_CONTABLE.CREDITO13) CREDITO13,
        SUM(SALDO_AUX_CONTABLE.DEBITO0)   DEBITO0,
        SUM(SALDO_AUX_CONTABLE.DEBITO1)   DEBITO1, 
        SUM(SALDO_AUX_CONTABLE.DEBITO2)   DEBITO2, 
        SUM(SALDO_AUX_CONTABLE.DEBITO3)   DEBITO3, 
        SUM(SALDO_AUX_CONTABLE.DEBITO4)   DEBITO4, 
        SUM(SALDO_AUX_CONTABLE.DEBITO5)   DEBITO5, 
        SUM(SALDO_AUX_CONTABLE.DEBITO6)   DEBITO6, 
        SUM(SALDO_AUX_CONTABLE.DEBITO7)   DEBITO7, 
        SUM(SALDO_AUX_CONTABLE.DEBITO8)   DEBITO8, 
        SUM(SALDO_AUX_CONTABLE.DEBITO9)   DEBITO9, 
        SUM(SALDO_AUX_CONTABLE.DEBITO10)  DEBITO10, 
        SUM(SALDO_AUX_CONTABLE.DEBITO11)  DEBITO11, 
        SUM(SALDO_AUX_CONTABLE.DEBITO12)  DEBITO12, 
        SUM(SALDO_AUX_CONTABLE.DEBITO13)  DEBITO13,
        SUM(SALDO_AUX_CONTABLE.NETO0)     NETO0,
        SUM(SALDO_AUX_CONTABLE.NETO1)     NETO1, 
        SUM(SALDO_AUX_CONTABLE.NETO2)     NETO2, 
        SUM(SALDO_AUX_CONTABLE.NETO3)     NETO3, 
        SUM(SALDO_AUX_CONTABLE.NETO4)     NETO4, 
        SUM(SALDO_AUX_CONTABLE.NETO5)     NETO5, 
        SUM(SALDO_AUX_CONTABLE.NETO6)     NETO6, 
        SUM(SALDO_AUX_CONTABLE.NETO7)     NETO7, 
        SUM(SALDO_AUX_CONTABLE.NETO8)     NETO8, 
        SUM(SALDO_AUX_CONTABLE.NETO9)     NETO9, 
        SUM(SALDO_AUX_CONTABLE.NETO10)    NETO10, 
        SUM(SALDO_AUX_CONTABLE.NETO11)    NETO11, 
        SUM(SALDO_AUX_CONTABLE.NETO12)    NETO12, 
        SUM(SALDO_AUX_CONTABLE.NETO13)    NETO13, 
        CASE WHEN 1=s$manTer$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN SALDO_AUX_CONTABLE.TERCERO ELSE NULL END TERCERO,
        CASE WHEN 1=s$manAux$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN SALDO_AUX_CONTABLE.AUXILIAR ELSE NULL END AUXILIAR, 
        CASE WHEN 1=s$manCen$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN SALDO_AUX_CONTABLE.CENTRO_COSTO ELSE NULL END CENTRO_COSTO,
        CASE WHEN 1=s$manRef$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN SALDO_AUX_CONTABLE.REFERENCIA ELSE NULL END REFERENCIA,
        CASE WHEN 1=s$manFue$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN SALDO_AUX_CONTABLE.FUENTE_RECURSO ELSE NULL END FUENTE_RECURSOS,
        CASE WHEN 1=s$manTer$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN TERCERO.NOMBRE ELSE NULL END TERCERO_NOMBRE,
        CASE WHEN 1=s$manAux$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN AUXILIAR.NOMBRE ELSE NULL END AUXILIAR_NOMBRE, 
        CASE WHEN 1=s$manCen$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN CENTRO_COSTO.NOMBRE ELSE NULL END CENTRO_COSTO_NOMBRE,
        CA]') || TO_CLOB(q'[SE WHEN 1=s$manRef$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN REFERENCIA.NOMBRE ELSE NULL END REFERENCIA_NOMBRE,
        CASE WHEN 1=s$manFue$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN FUENTE_RECURSOS.NOMBRE ELSE NULL END FUENTE_RECURSOS_NOMBRE,
        NVL(CASE WHEN PLAN_CONTABLE.CODIGO <> SALDO_AUX_CONTABLE.CODIGO OR PLAN_CONTABLE.MOVIMIENTO<>0  THEN PLAN_CONTABLE.NOMBRE ELSE
        PLAN_CONTABLE.NOMBRE ||
        CASE WHEN 1=s$manTer$s AND PLAN_CONTABLE.MAN_AUX_TER <>0 THEN '/' || TERCERO.NOMBRE ELSE NULL END  ||
        CASE WHEN 1=s$manAux$s AND PLAN_CONTABLE.MAN_AUX_GEN <>0 THEN '/' || AUXILIAR.NOMBRE ELSE NULL END ||
        CASE WHEN 1=s$manCen$s AND PLAN_CONTABLE.MAN_CEN_CTO <>0 THEN '/' || CENTRO_COSTO.NOMBRE ELSE NULL END  ||
        CASE WHEN 1=s$manRef$s AND PLAN_CONTABLE.MAN_AUX_REF <>0 THEN '/' || REFERENCIA.NOMBRE ELSE NULL END  ||
        CASE WHEN 1=s$manFue$s AND PLAN_CONTABLE.MAN_AUX_FUE <>0 THEN '/' || FUENTE_RECURSOS.NOMBRE ELSE NULL END  
        END, PLAN_CONTABLE.NOMBRE) NOMBRE,
        PLAN_CONTABLE.CODIGO ||
            CASE WHEN 1=s$manTer$s AND PLAN_CONTABLE.MAN_AUX_TER <>0 THEN '/' || SALDO_AUX_CONTABLE.TERCERO ELSE NULL END  ||
            CASE WHEN 1=s$manAux$s AND PLAN_CONTABLE.MAN_AUX_GEN <>0 THEN '/' || SALDO_AUX_CONTABLE.AUXILIAR ELSE NULL END ||
            CASE WHEN 1=s$manCen$s AND PLAN_CONTABLE.MAN_CEN_CTO <>0 THEN '/' || SALDO_AUX_CONTABLE.CENTRO_COSTO ELSE NULL END  ||
            CASE WHEN 1=s$manRef$s AND PLAN_CONTABLE.MAN_AUX_REF <>0 THEN '/' || SALDO_AUX_CONTABLE.REFERENCIA ELSE NULL END  ||
            CASE WHEN 1=s$manFue$s AND PLAN_CONTABLE.MAN_AUX_FUE <>0 THEN '/' || SALDO_AUX_CONTABLE.FUENTE_RECURSO ELSE NULL END ORDEN
  FROM  PLAN_CONTABLE INNER JOIN SALDO_AUX_CONTABLE 
        ON PLAN_CONTABLE.COMPANIA = SALDO_AUX_CONTABLE.COMPANIA
      AND PLAN_CONTABLE.ANO       = SALDO_AUX_CONTABLE.ANO     
      AND PLAN_CONTABLE.CODIGO     = SALDO_AUX_CONTABLE.CODIGO
    INNER JOIN TERCERO 
      ON SALDO_AUX_CONTABLE.COMPANIA = TERCERO.COMPANIA 
     AND SALDO_AUX_CONTABLE.TERCERO = TERCERO.NIT 
     AND SALDO_AUX_CONTABLE.SUCURSAL = TERCERO.SUCURSAL
    INNER JOIN CENTRO_COSTO 
      ON SALDO_AUX_CONTABLE.COMPANIA = CENTRO_COSTO.COMPANIA 
     AND SALDO_AUX_CONTABLE.ANO = CENTRO_COSTO.ANO 
     AND SALDO_AUX_CONTABLE.CENTRO_COSTO = CENTRO_COSTO.CODIGO
    INNER JOIN AUXILIAR 
      ON SALDO_AUX_CONTABLE.COMPANIA = AUXILIAR.COMPANIA 
     AND SALDO_AUX_CONTABLE.ANO = AUXILIAR.ANO 
     AND SALDO_AUX_CONTABLE.AUXILIAR = AUXILIAR.CODIGO 
    INNER JOIN FUENTE_RECURSOS 
      ON SALDO_AUX_CONTABLE.COMPANIA = FUENTE_RECURSOS.COMPANIA 
     AND SALDO_AUX_CONTABLE.ANO = FUENTE_RECURSOS.ANO 
     AND SALDO_AUX_CONTABLE.FUENTE_RECURSO = FUENTE_RECURSOS.CODIGO 
    INNER JOIN REFERENCIA 
      ON SALDO_AUX_CONTABLE.COMPANIA = REFERENCIA.COMPANIA 
     AND SALDO_AUX_CONTABLE.ANO = REFERENCIA.ANO 
     AND SALDO_AUX_CONTABLE.REFERENCIA = REFERENCIA.CODIGO 
WHERE PLAN_CONTABLE.CO]') || TO_CLOB(q'[MPANIA = s$compania$s
  AND PLAN_CONTABLE.ANO      = s$anio$s
  AND PLAN_CONTABLE.CODIGO  BETWEEN s$codigoInicial$s AND s$codigoFinal$s


  AND ( PLAN_CONTABLE.MAN_AUX_TER = CASE WHEN 1=s$manTer$s THEN -1 ELSE PLAN_CONTABLE.MAN_AUX_TER END  AND
        PLAN_CONTABLE.MAN_AUX_GEN = CASE WHEN 1=s$manAux$s THEN -1 ELSE PLAN_CONTABLE.MAN_AUX_GEN END  AND
        PLAN_CONTABLE.MAN_CEN_CTO = CASE WHEN 1=s$manCen$s THEN -1 ELSE PLAN_CONTABLE.MAN_CEN_CTO END  AND
        PLAN_CONTABLE.MAN_AUX_REF = CASE WHEN 1=s$manRef$s THEN -1 ELSE PLAN_CONTABLE.MAN_AUX_REF END  AND
        PLAN_CONTABLE.MAN_AUX_FUE = CASE WHEN 1=s$manFue$s THEN -1 ELSE PLAN_CONTABLE.MAN_AUX_FUE END  
       )
GROUP BY  PLAN_CONTABLE.COMPANIA, 
          PLAN_CONTABLE.ANO, 
          PLAN_CONTABLE.CODIGO,
          CASE WHEN 1=s$manTer$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN SALDO_AUX_CONTABLE.TERCERO ELSE NULL END,
          CASE WHEN 1=s$manAux$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN SALDO_AUX_CONTABLE.AUXILIAR ELSE NULL END, 
          CASE WHEN 1=s$manCen$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN SALDO_AUX_CONTABLE.CENTRO_COSTO ELSE NULL END,
          CASE WHEN 1=s$manRef$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN SALDO_AUX_CONTABLE.REFERENCIA ELSE NULL END,
          CASE WHEN 1=s$manFue$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN SALDO_AUX_CONTABLE.FUENTE_RECURSO ELSE NULL END,
          CASE WHEN 1=s$manTer$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN TERCERO.NOMBRE ELSE NULL END,
          CASE WHEN 1=s$manAux$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN AUXILIAR.NOMBRE ELSE NULL END, 
          CASE WHEN 1=s$manCen$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN CENTRO_COSTO.NOMBRE ELSE NULL END,
          CASE WHEN 1=s$manRef$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN REFERENCIA.NOMBRE ELSE NULL END,
          CASE WHEN 1=s$manFue$s AND PLAN_CONTABLE.CODIGO = SALDO_AUX_CONTABLE.CODIGO  THEN FUENTE_RECURSOS.NOMBRE ELSE NULL END,
          NVL(CASE WHEN PLAN_CONTABLE.CODIGO <> SALDO_AUX_CONTABLE.CODIGO OR PLAN_CONTABLE.MOVIMIENTO<>0  THEN PLAN_CONTABLE.NOMBRE ELSE
            PLAN_CONTABLE.NOMBRE ||
            CASE WHEN 1=s$manTer$s AND PLAN_CONTABLE.MAN_AUX_TER <>0 THEN '/' || TERCERO.NOMBRE ELSE NULL END  ||
            CASE WHEN 1=s$manAux$s AND PLAN_CONTABLE.MAN_AUX_GEN <>0 THEN '/' || AUXILIAR.NOMBRE ELSE NULL END ||
            CASE WHEN 1=s$manCen$s AND PLAN_CONTABLE.MAN_CEN_CTO <>0 THEN '/' || CENTRO_COSTO.NOMBRE ELSE NULL END  ||
            CASE WHEN 1=s$manRef$s AND PLAN_CONTABLE.MAN_AUX_REF <>0 THEN '/' || REFERENCIA.NOMBRE ELSE NULL END  ||
            CASE WHEN 1=s$manFue$s AND PLAN_CONTABLE.MAN_AUX_FUE <>0 THEN '/' || FUENTE_RECURSOS.NOMBRE ELSE NULL END  
          END, PLAN_CONTABLE.NOMBRE),
          PLAN_CONTABLE.CODIGO ||
            CASE WHEN 1=s$manTer$s AND PLAN_CONTABLE.MAN_AUX_TER <>0 THEN '/' ||]') || TO_CLOB(q'[ SALDO_AUX_CONTABLE.TERCERO ELSE NULL END  ||
            CASE WHEN 1=s$manAux$s AND PLAN_CONTABLE.MAN_AUX_GEN <>0 THEN '/' || SALDO_AUX_CONTABLE.AUXILIAR ELSE NULL END ||
            CASE WHEN 1=s$manCen$s AND PLAN_CONTABLE.MAN_CEN_CTO <>0 THEN '/' || SALDO_AUX_CONTABLE.CENTRO_COSTO ELSE NULL END  ||
            CASE WHEN 1=s$manRef$s AND PLAN_CONTABLE.MAN_AUX_REF <>0 THEN '/' || SALDO_AUX_CONTABLE.REFERENCIA ELSE NULL END  ||
            CASE WHEN 1=s$manFue$s AND PLAN_CONTABLE.MAN_AUX_FUE <>0 THEN '/' || SALDO_AUX_CONTABLE.FUENTE_RECURSO ELSE NULL END 
) SALDO
ON PLAN.COMPANIA = SALDO.COMPANIA
AND PLAN.ANO       = SALDO.ANO     
AND PLAN.CODIGO     = SUBSTR(SALDO.CODIGO, 1, LENGTH(PLAN.CODIGO))

GROUP BY PLAN.COMPANIA, 
        PLAN.ANO, 
        PLAN.CODIGO,
        PLAN.NATURALEZA,
        PLAN.CORRIENTE,
        PLAN.NOMBRE,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.TERCERO ELSE NULL END ,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.AUXILIAR ELSE NULL END ,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.CENTRO_COSTO ELSE NULL END ,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.REFERENCIA ELSE NULL END ,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.FUENTE_RECURSOS ELSE NULL END ,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.TERCERO_NOMBRE ELSE NULL END ,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.AUXILIAR_NOMBRE ELSE NULL END ,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.CENTRO_COSTO_NOMBRE ELSE NULL END ,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.REFERENCIA_NOMBRE ELSE NULL END ,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.FUENTE_RECURSOS_NOMBRE ELSE NULL END ,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.NOMBRE ELSE PLAN.NOMBRE END ,
        CASE WHEN PLAN.CODIGO = SALDO.CODIGO THEN SALDO.ORDEN ELSE PLAN.CODIGO END]') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'mrosero' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);