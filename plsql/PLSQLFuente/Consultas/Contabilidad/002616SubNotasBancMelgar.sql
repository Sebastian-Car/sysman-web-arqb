MERGE INTO CONSULTAS FIN USING (SELECT '002616SubNotasBancMelgar' INFORME ,TO_CLOB(q'[SELECT  DCN.NRO_DOCUMENTO,DCN.VALOR_DEBITO,DCN.VALOR_CREDITO,DCN.FECHA,DCN.TIPO_CPTE,DCN.COMPROBANTE 
FROM
    DETALLE_COMPROBANTE_CNT  DCN 
    INNER JOIN   TIPO_COMPROBANTE TC
    ON DCN.compania =  TC.compania
    AND DCN.TIPO_CPTE  =  TC.CODIGO
WHERE   DCN.COMPANIA = s$compania$s  
AND CLASE_CONTABLE IN(s$strTipos$s)
AND     DCN.CUENTA = 's$codCuenta$s'  
s$condicion$s
ORDER BY  
        DCN.TIPO_CPTE, DCN.COMPROBANTE, DCN.CONSECUTIVO]') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);