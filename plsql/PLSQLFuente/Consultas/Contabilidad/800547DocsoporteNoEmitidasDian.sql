MERGE INTO CONSULTAS FIN USING (SELECT '800547DocsoporteNoEmitidasDian' INFORME ,TO_CLOB(q'[SELECT
    COMPROBANTE_CNT.CONSECUTIVODIAN ,
    COMPROBANTE_CNT.FECHA ,
    COMPROBANTE_CNT.TERCERO,
    COMPROBANTE_CNT.VLR_DOCUMENTO VALOR
FROM
    COMPROBANTE_CNT
    INNER JOIN TIPO_COMPROBANTE ON TIPO_COMPROBANTE.CODIGO = COMPROBANTE_CNT.TIPO AND  TIPO_COMPROBANTE.DOCSOPORTE NOT IN (0)
WHERE
    COMPROBANTE_CNT.COMPANIA = s$compania$s
    AND COMPROBANTE_CNT.FECHA BETWEEN 's$fechaI$s' AND 's$fechaF$s'
    AND 
    COMPROBANTE_CNT.CONSECUTIVODIAN NOT IN (
    SELECT
        ESTADOFAC.NUMERO
    FROM
        ESTADOFAC) AND COMPROBANTE_CNT.CONSECUTIVODIAN IS NULL
ORDER BY
    COMPROBANTE_CNT.CONSECUTIVODIAN DESC]') CONSULTA, 5 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);