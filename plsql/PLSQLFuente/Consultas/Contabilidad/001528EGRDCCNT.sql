MERGE INTO CONSULTAS FIN USING (SELECT '001528EGRDCCNT' INFORME ,TO_CLOB(q'[SELECT
    V_DETALLE_AUXILIAR_CNT.COMPANIA COMP,
    V_DETALLE_AUXILIAR_CNT.ANO ANOCOMP,
    CONCAT(CONCAT(CONCAT(V_DETALLE_AUXILIAR_CNT.TIPO_COMPROBANTE_NOM,' ('), V_DETALLE_AUXILIAR_CNT.TIPO_CPTE),') ') AS TITULO,
    V_DETALLE_AUXILIAR_CNT.TIPO_CPTE TIPOCOMP,
    V_DETALLE_AUXILIAR_CNT.COMPROBANTE NUMCOMP,
    V_DETALLE_AUXILIAR_CNT.COMPROBANTE NUMERO,
    V_DETALLE_AUXILIAR_CNT.FECHA_COMPROBANTE FECHA,
    V_DETALLE_AUXILIAR_CNT.TERCERO_COM_NOM NOMBRETERCERO1,
    CONCAT(CONCAT(V_DETALLE_AUXILIAR_CNT.TERCERO_COM,'-'), V_DETALLE_AUXILIAR_CNT.DIGITOVERIFICACION) AS TERCERO,
    V_DETALLE_AUXILIAR_CNT.TERCERO_COM_DIR TERCERODIRE,
    V_DETALLE_AUXILIAR_CNT.TERCERO_COM_TEL TELEFONOS,
    V_DETALLE_AUXILIAR_CNT.TERCERO_COM_POS CODIGOPOSTAL,
    PLAN_CONTABLE.NOMBRE,
    BANCOS.NOMBREBANCO,
    V_DETALLE_AUXILIAR_CNT.NRO_DOCUMENTO,
    V_DETALLE_AUXILIAR_CNT.VLRAGIRAR,
    V_DETALLE_AUXILIAR_CNT.DESCRIPCION,
    V_DETALLE_AUXILIAR_CNT.TEXTO,
    V_DETALLE_AUXILIAR_CNT.CMPTE_AFECTADO_DET CMPTE_AFECTADO,
    CODIGO_CUENTA_DET CODIGO_CUENTA,
    CUENTANOMBRE TOTALNOMBRE,
    V_DETALLE_AUXILIAR_CNT.VALOR_DEBITO,
    V_DETALLE_AUXILIAR_CNT.VALOR_CREDITO,
    COMPROBANTE_CNT.CREATED_BY CREADOR
FROM V_DETALLE_AUXILIAR_CNT
    INNER JOIN COMPROBANTE_CNT
        ON COMPROBANTE_CNT.COMPANIA = V_DETALLE_AUXILIAR_CNT.COMPANIA
        AND COMPROBANTE_CNT.ANO    = V_DETALLE_AUXILIAR_CNT.ANO
        AND COMPROBANTE_CNT.TIPO   = V_DETALLE_AUXILIAR_CNT.TIPO_CPTE
        AND COMPROBANTE_CNT.NUMERO = V_DETALLE_AUXILIAR_CNT.COMPROBANTE
    INNER JOIN TIPO_COMPROBANTE
        ON COMPROBANTE_CNT.COMPANIA = TIPO_COMPROBANTE.COMPANIA
        AND COMPROBANTE_CNT.TIPO   = TIPO_COMPROBANTE.CODIGO
    LEFT JOIN PLAN_CONTABLE
        ON V_DETALLE_AUXILIAR_CNT.COMPANIA = PLAN_CONTABLE.COMPANIA
        AND V_DETALLE_AUXILIAR_CNT.ANO    = PLAN_CONTABLE.ANO
        AND V_DETALLE_AUXILIAR_CNT.CODIGO_CUENTA = PLAN_CONTABLE.CODIGO
    LEFT JOIN BANCO BANCOS
             ON PLAN_CONTABLE.COMPANIA = BANCOS.COMPANIA
            AND PLAN_CONTABLE.BANCO = BANCOS.BANCO
 
WHERE 
  V_DETALLE_AUXILIAR_CNT.COMPANIA    = s$compania$s
AND V_DETALLE_AUXILIAR_CNT.ANO       = s$ano$s
AND V_DETALLE_AUXILIAR_CNT.TIPO_CPTE = 's$tipoCpte$s'
AND V_DETALLE_AUXILIAR_CNT.COMPROBANTE BETWEEN 's$numeroPptoInicial$s' AND 's$numeroPptoFinal$s']') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);