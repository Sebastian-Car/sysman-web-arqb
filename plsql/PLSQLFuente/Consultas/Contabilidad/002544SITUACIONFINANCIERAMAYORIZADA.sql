MERGE INTO CONSULTAS FIN USING (SELECT '002544SITUACIONFINANCIERAMAYORIZADA' INFORME ,TO_CLOB(q'[SELECT COMPANIA
      , CODIGO
      , NOMBRE
      , SUM(SANTERIOR) SANTERIOR
      , SUM(NUEVO) NUEVO
      , CASE WHEN CLASE = '1' THEN 'ACTIVO'
         ELSE CASE WHEN CLASE = '2' THEN 'PASIVOS'
             ELSE CASE WHEN CLASE = '3' THEN 'PATRIMONIO'
                 ELSE CASE WHEN CLASE = '8' THEN 'CUENTAS DE ORDEN DEUDORAS'
                     ELSE 'CUENTAS DE ORDEN ACREEDORAS'
                 END
             END
         END
     END CLASE,
     CORRIENTE,
     CASE WHEN CORRIENTE = '0'  AND (SUBSTR(CODIGO,1,1) = '1' OR SUBSTR(CODIGO,1,1)='2') THEN 'NO CORRIENTE'
         ELSE  CASE WHEN CORRIENTE = '-1'  AND (SUBSTR(CODIGO,1,1) = '1' OR SUBSTR(CODIGO,1,1)='2') THEN 'CORRIENTE' 
         ELSE ''
     END END IND,
     CASE WHEN CORRIENTE = '0'  AND (SUBSTR(CODIGO,1,1) = '1' OR SUBSTR(CODIGO,1,1)='2') THEN 'NO CORRIENTE'
         ELSE  CASE WHEN CORRIENTE = '-1'  AND (SUBSTR(CODIGO,1,1) = '1' OR SUBSTR(CODIGO,1,1)='2') THEN 'CORRIENTE' 
         ELSE ''
     END END IND2,
     ORDEN
FROM (
SELECT  COMPANIA
      , CODIGO
      , NOMBRE
      , SALDOs$mesTrabajo$s SANTERIOR
      , 0 NUEVO
      , SUBSTR(CODIGO,1,1) CLASE
      , CORRIENTE
      , CASE
          WHEN SUBSTR(CODIGO,1,1)='0'
            THEN 'z'
              ||CODIGO
            ELSE 
              CODIGO
        END ORDEN 
FROM (
-- CORRIENTE ACTIVOS Y PASIVOS
SELECT  PLAN.COMPANIA, 
        PLAN.ANO, 
        PLAN.CODIGO,
        PLAN.NATURALEZA,
        SALDO.CORRIENTE,
        PLAN.NOMBRE,
        SUM(SALDO.SALDO0)    SALDO0, 
        SUM(SALDO.SALDO1)    SALDO1, 
        SUM(SALDO.SALDO2)    SALDO2, 
        SUM(SALDO.SALDO3)    SALDO3, 
        SUM(SALDO.SALDO4)    SALDO4, 
        SUM(SALDO.SALDO5)    SALDO5, 
        SUM(SALDO.SALDO6)    SALDO6, 
        SUM(SALDO.SALDO7)    SALDO7, 
        SUM(SALDO.SALDO8)    SALDO8, 
        SUM(SALDO.SALDO9)    SALDO9, 
        SUM(SALDO.SALDO10)   SALDO10, 
        SUM(SALDO.SALDO11)   SALDO11, 
        SUM(SALDO.SALDO12)   SALDO12, 
        SUM(SALDO.SALDO13)   SALDO13
FROM PLAN_CONTABLE PLAN INNER JOIN 
(
SELECT 
        PLAN_CONTABLE.COMPANIA, 
        PLAN_CONTABLE.ANO, 
        PLAN_CONTABLE.CODIGO,
        PLAN_CONTABLE.NOMBRE,
        PLAN_CONTABLE.CORRIENTE,
        SUM(SALDO_AUX_CONTABLE.SALDO0)    SALDO0, 
        SUM(SALDO_AUX_CONTABLE.SALDO1)    SALDO1, 
        SUM(SALDO_AUX_CONTABLE.SALDO2)    SALDO2, 
        SUM(SALDO_AUX_CONTABLE.SALDO3)    SALDO3, 
        SUM(SALDO_AUX_CONTABLE.SALDO4)    SALDO4, 
        SUM(SALDO_AUX_CONTABLE.SALDO5)    SALDO5, 
        SUM(SALDO_AUX_CONTABLE.SALDO6)    SALDO6, 
        SUM(SALDO_AUX_CONTABLE.SALDO7)    SALDO7, 
        SUM(SALDO_AUX_CONTABLE.SALDO8)    SALDO8, 
        SUM(SALDO_AUX_CONTABLE.SALDO9)    SALDO9, 
        SUM(SALDO_AUX_CONTABLE.SALDO10)   SALDO10, 
        SUM(SALDO_AUX_CONTABLE.SALDO11)   SALDO11, 
        SUM(SALDO_AUX_CONTABLE.SALDO12)   SALDO12, 
        SUM(SALDO_AUX_CONTABLE.SALDO13)   SALDO13
  FROM  PLAN_CONTABLE INNER JOIN SALDO_AUX_CONTABLE 
   ]') || TO_CLOB(q'[     ON PLAN_CONTABLE.COMPANIA = SALDO_AUX_CONTABLE.COMPANIA
      AND PLAN_CONTABLE.ANO       = SALDO_AUX_CONTABLE.ANO     
      AND PLAN_CONTABLE.CODIGO    = SALDO_AUX_CONTABLE.CODIGO
WHERE PLAN_CONTABLE.COMPANIA = s$compania$s
  AND PLAN_CONTABLE.ANO      = s$anioTrabajo$s
  AND PLAN_CONTABLE.CODIGO  BETWEEN 's$codigoInicial$s' AND 's$codigoFinal$s'
  AND PLAN_CONTABLE.CORRIENTE =0
  AND (SUBSTR(PLAN_CONTABLE.CODIGO,1,1) = '1' OR SUBSTR(PLAN_CONTABLE.CODIGO,1,1)='2')
GROUP BY  PLAN_CONTABLE.COMPANIA, 
          PLAN_CONTABLE.ANO, 
          PLAN_CONTABLE.CODIGO,
          PLAN_CONTABLE.NOMBRE,
          PLAN_CONTABLE.CORRIENTE
) SALDO
ON PLAN.COMPANIA = SALDO.COMPANIA
AND PLAN.ANO     = SALDO.ANO     
AND PLAN.CODIGO  = SUBSTR(SALDO.CODIGO, 1, LENGTH(PLAN.CODIGO))
GROUP BY PLAN.COMPANIA, 
        PLAN.ANO, 
        PLAN.CODIGO,
        PLAN.NATURALEZA,
        SALDO.CORRIENTE,
        PLAN.NOMBRE
UNION ALL 
-- NO CORRIENTE ACTIVOS Y PASIVOS
SELECT  PLAN.COMPANIA, 
        PLAN.ANO, 
        PLAN.CODIGO,
        PLAN.NATURALEZA,
        SALDO.CORRIENTE,
        PLAN.NOMBRE,
        SUM(SALDO.SALDO0)    SALDO0, 
        SUM(SALDO.SALDO1)    SALDO1, 
        SUM(SALDO.SALDO2)    SALDO2, 
        SUM(SALDO.SALDO3)    SALDO3, 
        SUM(SALDO.SALDO4)    SALDO4, 
        SUM(SALDO.SALDO5)    SALDO5, 
        SUM(SALDO.SALDO6)    SALDO6, 
        SUM(SALDO.SALDO7)    SALDO7, 
        SUM(SALDO.SALDO8)    SALDO8, 
        SUM(SALDO.SALDO9)    SALDO9, 
        SUM(SALDO.SALDO10)   SALDO10, 
        SUM(SALDO.SALDO11)   SALDO11, 
        SUM(SALDO.SALDO12)   SALDO12, 
        SUM(SALDO.SALDO13)   SALDO13
FROM PLAN_CONTABLE PLAN INNER JOIN 
(
SELECT 
        PLAN_CONTABLE.COMPANIA, 
        PLAN_CONTABLE.ANO, 
        PLAN_CONTABLE.CODIGO,
        PLAN_CONTABLE.NOMBRE,
        PLAN_CONTABLE.CORRIENTE,
        SUM(SALDO_AUX_CONTABLE.SALDO0)    SALDO0, 
        SUM(SALDO_AUX_CONTABLE.SALDO1)    SALDO1, 
        SUM(SALDO_AUX_CONTABLE.SALDO2)    SALDO2, 
        SUM(SALDO_AUX_CONTABLE.SALDO3)    SALDO3, 
        SUM(SALDO_AUX_CONTABLE.SALDO4)    SALDO4, 
        SUM(SALDO_AUX_CONTABLE.SALDO5)    SALDO5, 
        SUM(SALDO_AUX_CONTABLE.SALDO6)    SALDO6, 
        SUM(SALDO_AUX_CONTABLE.SALDO7)    SALDO7, 
        SUM(SALDO_AUX_CONTABLE.SALDO8)    SALDO8, 
        SUM(SALDO_AUX_CONTABLE.SALDO9)    SALDO9, 
        SUM(SALDO_AUX_CONTABLE.SALDO10)   SALDO10, 
        SUM(SALDO_AUX_CONTABLE.SALDO11)   SALDO11, 
        SUM(SALDO_AUX_CONTABLE.SALDO12)   SALDO12, 
        SUM(SALDO_AUX_CONTABLE.SALDO13)   SALDO13
  FROM  PLAN_CONTABLE INNER JOIN SALDO_AUX_CONTABLE 
        ON PLAN_CONTABLE.COMPANIA = SALDO_AUX_CONTABLE.COMPANIA
      AND PLAN_CONTABLE.ANO       = SALDO_AUX_CONTABLE.ANO     
      AND PLAN_CONTABLE.CODIGO    = SALDO_AUX_CONTABLE.CODIGO
WHERE PLAN_CONTABLE.COMPANIA = s$compania$s
  AND PLAN_CONTABLE.ANO      = s$anioTrabajo$s
  AND PLAN_CONTABLE.CODIGO  BETWEEN 's$codigoInicial$s' AND 's$codigoFinal$s'
  AND PLAN_CONTABLE.CORRIENTE =-1
  AND (SUBSTR(PLAN]') || TO_CLOB(q'[_CONTABLE.CODIGO,1,1) = '1' OR SUBSTR(PLAN_CONTABLE.CODIGO,1,1)='2')
GROUP BY  PLAN_CONTABLE.COMPANIA, 
          PLAN_CONTABLE.ANO, 
          PLAN_CONTABLE.CODIGO,
          PLAN_CONTABLE.NOMBRE,
          PLAN_CONTABLE.CORRIENTE
) SALDO
ON PLAN.COMPANIA = SALDO.COMPANIA
AND PLAN.ANO     = SALDO.ANO     
AND PLAN.CODIGO  = SUBSTR(SALDO.CODIGO, 1, LENGTH(PLAN.CODIGO))
GROUP BY PLAN.COMPANIA, 
        PLAN.ANO, 
        PLAN.CODIGO,
        PLAN.NATURALEZA,
        SALDO.CORRIENTE,
        PLAN.NOMBRE
UNION ALL
-- OTRAS CUENTAS <> 1 Y 2
SELECT  PLAN.COMPANIA, 
        PLAN.ANO, 
        PLAN.CODIGO,
        PLAN.NATURALEZA,
        SALDO.CORRIENTE,
        PLAN.NOMBRE,
        SUM(SALDO.SALDO0)    SALDO0, 
        SUM(SALDO.SALDO1)    SALDO1, 
        SUM(SALDO.SALDO2)    SALDO2, 
        SUM(SALDO.SALDO3)    SALDO3, 
        SUM(SALDO.SALDO4)    SALDO4, 
        SUM(SALDO.SALDO5)    SALDO5, 
        SUM(SALDO.SALDO6)    SALDO6, 
        SUM(SALDO.SALDO7)    SALDO7, 
        SUM(SALDO.SALDO8)    SALDO8, 
        SUM(SALDO.SALDO9)    SALDO9, 
        SUM(SALDO.SALDO10)   SALDO10, 
        SUM(SALDO.SALDO11)   SALDO11, 
        SUM(SALDO.SALDO12)   SALDO12, 
        SUM(SALDO.SALDO13)   SALDO13
FROM PLAN_CONTABLE PLAN INNER JOIN 
(
SELECT 
        PLAN_CONTABLE.COMPANIA, 
        PLAN_CONTABLE.ANO, 
        PLAN_CONTABLE.CODIGO,
        PLAN_CONTABLE.NOMBRE,
        PLAN_CONTABLE.CORRIENTE,
        SUM(SALDO_AUX_CONTABLE.SALDO0)    SALDO0, 
        SUM(SALDO_AUX_CONTABLE.SALDO1)    SALDO1, 
        SUM(SALDO_AUX_CONTABLE.SALDO2)    SALDO2, 
        SUM(SALDO_AUX_CONTABLE.SALDO3)    SALDO3, 
        SUM(SALDO_AUX_CONTABLE.SALDO4)    SALDO4, 
        SUM(SALDO_AUX_CONTABLE.SALDO5)    SALDO5, 
        SUM(SALDO_AUX_CONTABLE.SALDO6)    SALDO6, 
        SUM(SALDO_AUX_CONTABLE.SALDO7)    SALDO7, 
        SUM(SALDO_AUX_CONTABLE.SALDO8)    SALDO8, 
        SUM(SALDO_AUX_CONTABLE.SALDO9)    SALDO9, 
        SUM(SALDO_AUX_CONTABLE.SALDO10)   SALDO10, 
        SUM(SALDO_AUX_CONTABLE.SALDO11)   SALDO11, 
        SUM(SALDO_AUX_CONTABLE.SALDO12)   SALDO12, 
        SUM(SALDO_AUX_CONTABLE.SALDO13)   SALDO13
  FROM  PLAN_CONTABLE INNER JOIN SALDO_AUX_CONTABLE 
        ON PLAN_CONTABLE.COMPANIA = SALDO_AUX_CONTABLE.COMPANIA
      AND PLAN_CONTABLE.ANO       = SALDO_AUX_CONTABLE.ANO     
      AND PLAN_CONTABLE.CODIGO    = SALDO_AUX_CONTABLE.CODIGO
WHERE PLAN_CONTABLE.COMPANIA = s$compania$s
  AND PLAN_CONTABLE.ANO      = s$anioTrabajo$s
  AND PLAN_CONTABLE.CODIGO  BETWEEN 's$codigoInicial$s' AND 's$codigoFinal$s'
  AND (SUBSTR(PLAN_CONTABLE.CODIGO,1,1) <> '1' AND SUBSTR(PLAN_CONTABLE.CODIGO,1,1)<>'2')
GROUP BY  PLAN_CONTABLE.COMPANIA, 
          PLAN_CONTABLE.ANO, 
          PLAN_CONTABLE.CODIGO,
          PLAN_CONTABLE.NOMBRE,
          PLAN_CONTABLE.CORRIENTE
) SALDO
ON PLAN.COMPANIA = SALDO.COMPANIA
AND PLAN.ANO     = SALDO.ANO     
AND PLAN.CODIGO  = SUBSTR(SALDO.CODIGO, 1, LENGTH(PLAN.CODIGO))
GROUP BY PLAN.COMPANIA, 
        PLAN.ANO, 
        PLAN.CODIGO,
      ]') || TO_CLOB(q'[  PLAN.NATURALEZA,
        SALDO.CORRIENTE,
        PLAN.NOMBRE) 
BASE
UNION ALL
SELECT  COMPANIA
      , CODIGO
      , NOMBRE
      , 0 NUEVO
      , SALDOs$mesComparar$s SANTERIOR 
      , SUBSTR(CODIGO,1,1) CLASE
      , CORRIENTE
      , CASE
          WHEN SUBSTR(CODIGO,1,1)='0'
            THEN 'z'
              ||CODIGO
            ELSE 
              CODIGO
        END ORDEN
 
FROM (
-- CORRIENTE ACTIVOS Y PASIVOS
SELECT  PLAN.COMPANIA, 
        PLAN.ANO, 
        PLAN.CODIGO,
        PLAN.NATURALEZA,
        SALDO.CORRIENTE,
        PLAN.NOMBRE,
        SUM(SALDO.SALDO0)    SALDO0, 
        SUM(SALDO.SALDO1)    SALDO1, 
        SUM(SALDO.SALDO2)    SALDO2, 
        SUM(SALDO.SALDO3)    SALDO3, 
        SUM(SALDO.SALDO4)    SALDO4, 
        SUM(SALDO.SALDO5)    SALDO5, 
        SUM(SALDO.SALDO6)    SALDO6, 
        SUM(SALDO.SALDO7)    SALDO7, 
        SUM(SALDO.SALDO8)    SALDO8, 
        SUM(SALDO.SALDO9)    SALDO9, 
        SUM(SALDO.SALDO10)   SALDO10, 
        SUM(SALDO.SALDO11)   SALDO11, 
        SUM(SALDO.SALDO12)   SALDO12, 
        SUM(SALDO.SALDO13)   SALDO13
FROM PLAN_CONTABLE PLAN INNER JOIN 
(
SELECT 
        PLAN_CONTABLE.COMPANIA, 
        PLAN_CONTABLE.ANO, 
        PLAN_CONTABLE.CODIGO,
        PLAN_CONTABLE.NOMBRE,
        PLAN_CONTABLE.CORRIENTE,
        SUM(SALDO_AUX_CONTABLE.SALDO0)    SALDO0, 
        SUM(SALDO_AUX_CONTABLE.SALDO1)    SALDO1, 
        SUM(SALDO_AUX_CONTABLE.SALDO2)    SALDO2, 
        SUM(SALDO_AUX_CONTABLE.SALDO3)    SALDO3, 
        SUM(SALDO_AUX_CONTABLE.SALDO4)    SALDO4, 
        SUM(SALDO_AUX_CONTABLE.SALDO5)    SALDO5, 
        SUM(SALDO_AUX_CONTABLE.SALDO6)    SALDO6, 
        SUM(SALDO_AUX_CONTABLE.SALDO7)    SALDO7, 
        SUM(SALDO_AUX_CONTABLE.SALDO8)    SALDO8, 
        SUM(SALDO_AUX_CONTABLE.SALDO9)    SALDO9, 
        SUM(SALDO_AUX_CONTABLE.SALDO10)   SALDO10, 
        SUM(SALDO_AUX_CONTABLE.SALDO11)   SALDO11, 
        SUM(SALDO_AUX_CONTABLE.SALDO12)   SALDO12, 
        SUM(SALDO_AUX_CONTABLE.SALDO13)   SALDO13
  FROM  PLAN_CONTABLE INNER JOIN SALDO_AUX_CONTABLE 
        ON PLAN_CONTABLE.COMPANIA = SALDO_AUX_CONTABLE.COMPANIA
      AND PLAN_CONTABLE.ANO       = SALDO_AUX_CONTABLE.ANO     
      AND PLAN_CONTABLE.CODIGO    = SALDO_AUX_CONTABLE.CODIGO
WHERE PLAN_CONTABLE.COMPANIA = s$compania$s
  AND PLAN_CONTABLE.ANO      = s$anioComparar$s
  AND PLAN_CONTABLE.CODIGO  BETWEEN 's$codigoInicial$s' AND 's$codigoFinal$s'
  AND PLAN_CONTABLE.CORRIENTE =0
  AND (SUBSTR(PLAN_CONTABLE.CODIGO,1,1) = '1' OR SUBSTR(PLAN_CONTABLE.CODIGO,1,1)='2')
GROUP BY  PLAN_CONTABLE.COMPANIA, 
          PLAN_CONTABLE.ANO, 
          PLAN_CONTABLE.CODIGO,
          PLAN_CONTABLE.NOMBRE,
          PLAN_CONTABLE.CORRIENTE
) SALDO
ON PLAN.COMPANIA = SALDO.COMPANIA
AND PLAN.ANO     = SALDO.ANO     
AND PLAN.CODIGO  = SUBSTR(SALDO.CODIGO, 1, LENGTH(PLAN.CODIGO))
GROUP BY PLAN.COMPANIA, 
        PLAN.ANO, 
        PLAN.CODIGO,
        PLAN.NATURALEZA,
        SALDO.CORRIENTE,
        PLAN.NOMBRE
UNION ALL 
-- NO CORR]') || TO_CLOB(q'[IENTE ACTIVOS Y PASIVOS
SELECT  PLAN.COMPANIA, 
        PLAN.ANO, 
        PLAN.CODIGO,
        PLAN.NATURALEZA,
        SALDO.CORRIENTE,
        PLAN.NOMBRE,
        SUM(SALDO.SALDO0)    SALDO0, 
        SUM(SALDO.SALDO1)    SALDO1, 
        SUM(SALDO.SALDO2)    SALDO2, 
        SUM(SALDO.SALDO3)    SALDO3, 
        SUM(SALDO.SALDO4)    SALDO4, 
        SUM(SALDO.SALDO5)    SALDO5, 
        SUM(SALDO.SALDO6)    SALDO6, 
        SUM(SALDO.SALDO7)    SALDO7, 
        SUM(SALDO.SALDO8)    SALDO8, 
        SUM(SALDO.SALDO9)    SALDO9, 
        SUM(SALDO.SALDO10)   SALDO10, 
        SUM(SALDO.SALDO11)   SALDO11, 
        SUM(SALDO.SALDO12)   SALDO12, 
        SUM(SALDO.SALDO13)   SALDO13
FROM PLAN_CONTABLE PLAN INNER JOIN 
(
SELECT 
        PLAN_CONTABLE.COMPANIA, 
        PLAN_CONTABLE.ANO, 
        PLAN_CONTABLE.CODIGO,
        PLAN_CONTABLE.NOMBRE,
        PLAN_CONTABLE.CORRIENTE,
        SUM(SALDO_AUX_CONTABLE.SALDO0)    SALDO0, 
        SUM(SALDO_AUX_CONTABLE.SALDO1)    SALDO1, 
        SUM(SALDO_AUX_CONTABLE.SALDO2)    SALDO2, 
        SUM(SALDO_AUX_CONTABLE.SALDO3)    SALDO3, 
        SUM(SALDO_AUX_CONTABLE.SALDO4)    SALDO4, 
        SUM(SALDO_AUX_CONTABLE.SALDO5)    SALDO5, 
        SUM(SALDO_AUX_CONTABLE.SALDO6)    SALDO6, 
        SUM(SALDO_AUX_CONTABLE.SALDO7)    SALDO7, 
        SUM(SALDO_AUX_CONTABLE.SALDO8)    SALDO8, 
        SUM(SALDO_AUX_CONTABLE.SALDO9)    SALDO9, 
        SUM(SALDO_AUX_CONTABLE.SALDO10)   SALDO10, 
        SUM(SALDO_AUX_CONTABLE.SALDO11)   SALDO11, 
        SUM(SALDO_AUX_CONTABLE.SALDO12)   SALDO12, 
        SUM(SALDO_AUX_CONTABLE.SALDO13)   SALDO13
  FROM  PLAN_CONTABLE INNER JOIN SALDO_AUX_CONTABLE 
        ON PLAN_CONTABLE.COMPANIA = SALDO_AUX_CONTABLE.COMPANIA
      AND PLAN_CONTABLE.ANO       = SALDO_AUX_CONTABLE.ANO     
      AND PLAN_CONTABLE.CODIGO    = SALDO_AUX_CONTABLE.CODIGO
WHERE PLAN_CONTABLE.COMPANIA = s$compania$s
  AND PLAN_CONTABLE.ANO      = s$anioComparar$s
  AND PLAN_CONTABLE.CODIGO  BETWEEN 's$codigoInicial$s' AND 's$codigoFinal$s'
  AND PLAN_CONTABLE.CORRIENTE =-1
  AND (SUBSTR(PLAN_CONTABLE.CODIGO,1,1) = '1' OR SUBSTR(PLAN_CONTABLE.CODIGO,1,1)='2')
GROUP BY  PLAN_CONTABLE.COMPANIA, 
          PLAN_CONTABLE.ANO, 
          PLAN_CONTABLE.CODIGO,
          PLAN_CONTABLE.NOMBRE,
          PLAN_CONTABLE.CORRIENTE
) SALDO
ON PLAN.COMPANIA = SALDO.COMPANIA
AND PLAN.ANO     = SALDO.ANO     
AND PLAN.CODIGO  = SUBSTR(SALDO.CODIGO, 1, LENGTH(PLAN.CODIGO))
GROUP BY PLAN.COMPANIA, 
        PLAN.ANO, 
        PLAN.CODIGO,
        PLAN.NATURALEZA,
        SALDO.CORRIENTE,
        PLAN.NOMBRE
UNION ALL
-- OTRAS CUENTAS <> 1 Y 2
SELECT  PLAN.COMPANIA, 
        PLAN.ANO, 
        PLAN.CODIGO,
        PLAN.NATURALEZA,
        SALDO.CORRIENTE,
        PLAN.NOMBRE,
        SUM(SALDO.SALDO0)    SALDO0, 
        SUM(SALDO.SALDO1)    SALDO1, 
        SUM(SALDO.SALDO2)    SALDO2, 
        SUM(SALDO.SALDO3)    SALDO3, 
        SUM(SALDO.SALDO4)    SALDO4, 
        SUM(SALDO.SALDO5)    SALDO5, 
        SUM(SALDO.SALDO6]') || TO_CLOB(q'[)    SALDO6, 
        SUM(SALDO.SALDO7)    SALDO7, 
        SUM(SALDO.SALDO8)    SALDO8, 
        SUM(SALDO.SALDO9)    SALDO9, 
        SUM(SALDO.SALDO10)   SALDO10, 
        SUM(SALDO.SALDO11)   SALDO11, 
        SUM(SALDO.SALDO12)   SALDO12, 
        SUM(SALDO.SALDO13)   SALDO13
FROM PLAN_CONTABLE PLAN INNER JOIN 
(
SELECT 
        PLAN_CONTABLE.COMPANIA, 
        PLAN_CONTABLE.ANO, 
        PLAN_CONTABLE.CODIGO,
        PLAN_CONTABLE.NOMBRE,
        PLAN_CONTABLE.CORRIENTE,
        SUM(SALDO_AUX_CONTABLE.SALDO0)    SALDO0, 
        SUM(SALDO_AUX_CONTABLE.SALDO1)    SALDO1, 
        SUM(SALDO_AUX_CONTABLE.SALDO2)    SALDO2, 
        SUM(SALDO_AUX_CONTABLE.SALDO3)    SALDO3, 
        SUM(SALDO_AUX_CONTABLE.SALDO4)    SALDO4, 
        SUM(SALDO_AUX_CONTABLE.SALDO5)    SALDO5, 
        SUM(SALDO_AUX_CONTABLE.SALDO6)    SALDO6, 
        SUM(SALDO_AUX_CONTABLE.SALDO7)    SALDO7, 
        SUM(SALDO_AUX_CONTABLE.SALDO8)    SALDO8, 
        SUM(SALDO_AUX_CONTABLE.SALDO9)    SALDO9, 
        SUM(SALDO_AUX_CONTABLE.SALDO10)   SALDO10, 
        SUM(SALDO_AUX_CONTABLE.SALDO11)   SALDO11, 
        SUM(SALDO_AUX_CONTABLE.SALDO12)   SALDO12, 
        SUM(SALDO_AUX_CONTABLE.SALDO13)   SALDO13
  FROM  PLAN_CONTABLE INNER JOIN SALDO_AUX_CONTABLE 
        ON PLAN_CONTABLE.COMPANIA = SALDO_AUX_CONTABLE.COMPANIA
      AND PLAN_CONTABLE.ANO       = SALDO_AUX_CONTABLE.ANO     
      AND PLAN_CONTABLE.CODIGO    = SALDO_AUX_CONTABLE.CODIGO
WHERE PLAN_CONTABLE.COMPANIA = s$compania$s
  AND PLAN_CONTABLE.ANO      = s$anioComparar$s
  AND PLAN_CONTABLE.CODIGO  BETWEEN 's$codigoInicial$s' AND 's$codigoFinal$s'
  AND (SUBSTR(PLAN_CONTABLE.CODIGO,1,1) <> '1' AND SUBSTR(PLAN_CONTABLE.CODIGO,1,1)<>'2')
GROUP BY  PLAN_CONTABLE.COMPANIA, 
          PLAN_CONTABLE.ANO, 
          PLAN_CONTABLE.CODIGO,
          PLAN_CONTABLE.NOMBRE,
          PLAN_CONTABLE.CORRIENTE
) SALDO
ON PLAN.COMPANIA = SALDO.COMPANIA
AND PLAN.ANO     = SALDO.ANO     
AND PLAN.CODIGO  = SUBSTR(SALDO.CODIGO, 1, LENGTH(PLAN.CODIGO))
GROUP BY PLAN.COMPANIA, 
        PLAN.ANO, 
        PLAN.CODIGO,
        PLAN.NATURALEZA,
        SALDO.CORRIENTE,
        PLAN.NOMBRE) 
)
WHERE LENGTH(CODIGO)<=s$digitos$s
HAVING (SUM(SANTERIOR)<>0 OR SUM(NUEVO)<>0)
GROUP BY COMPANIA
      , CODIGO
      , NOMBRE
      , CASE WHEN CLASE = '1' THEN 'ACTIVO'
         ELSE CASE WHEN CLASE = '2' THEN 'PASIVOS'
             ELSE CASE WHEN CLASE = '3' THEN 'PATRIMONIO'
                 ELSE CASE WHEN CLASE = '8' THEN 'CUENTAS DE ORDEN DEUDORAS'
                     ELSE 'CUENTAS DE ORDEN ACREEDORAS'
                 END
             END
         END
     END
      ,
     CORRIENTE,
     CASE WHEN CORRIENTE = '0'  AND (SUBSTR(CODIGO,1,1) = '1' OR SUBSTR(CODIGO,1,1)='2') THEN 'NO CORRIENTE'
         ELSE  CASE WHEN CORRIENTE = '-1'  AND (SUBSTR(CODIGO,1,1) = '1' OR SUBSTR(CODIGO,1,1)='2') THEN 'CORRIENTE' 
         ELSE ''
     END END,
     ORDEN
ORDER BY CORRIENTE,CODIGO]') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'ADMIN' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);