MERGE INTO CONSULTAS FIN USING (SELECT '800508AfectacionesContablesWebService' INFORME ,TO_CLOB(q'[SELECT
    D.TIPO_CPTE,
    TO_CHAR(D.FECHA,'DD/MM/YYYY') FECHA,
    D.COMPROBANTE,
    D.CUENTAPPTAL,
    D.CUENTA,
    D.VALOR_DEBITO,
    D.VALOR_CREDITO,
    D.CENTRO_COSTO,
    D.REFERENCIA,
    D.FUENTE_RECURSO,
    D.AUXILIAR,
    D.DESCRIPCION,
    C.TIPOCOBRO,
    T.NOMBRE AS NOMBRE_COBRO,
    C.CODIGO,
    C.NOMBRE AS NOMBRE_CONCEPTO,
    C.CUENTADEBITOBASE,
    C.CUENTACREDITOBASE,
    C.CUENTADEBITOBASE_AV,
    C.CUENTACREDITOBASE_AV,
    C.CUENTA_RECAUDO,
    C.CLASE_CONCEPTO,
    C.REFERENCIA       AS REFERENCIAN_CONCEPTO_CONCEPTO,
    C.FUENTE_RECURSO   AS FUENTE_RECURSO_CONCEPTO,
    C.CENTRO_COSTO     AS CENTRO_COSTO_CONCEPTO,
    C.AUXILIAR         AS AUXILIAR_CONCEPTO
,
    D.CNTOPPTAL
FROM
    DETALLE_CPTE_AFECT_WS   D
    JOIN SF_CONCEPTOS            C ON ( C.COMPANIA = D.COMPANIA
                             AND C.ANO = D.ANO
                             AND D.CODIGO_CONCEPTO = C.CODIGO )
    INNER JOIN SF_TIPO_COBRO           T ON ( C.COMPANIA = T.COMPANIA
                                    AND C.TIPOCOBRO = T.CODIGO
                                    AND C.ANO = T.ANO )
WHERE
    C.COMPANIA = s$compania$s
    AND C.ANO = s$ano$s
    AND D.CREATED_BY = 's$usuario$s'
    AND TO_CHAR(D.DATE_CREATED, 'yyyy/MM/dd') BETWEEN 's$fechaini$s' AND 's$fechafin$s']') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);