MERGE INTO CONSULTAS FIN USING (SELECT '002932ReporteDianInvoway' INFORME ,TO_CLOB(q'[SELECT
NVL(RET.BASE_RETEFUENTE, 0)  AS BASE_RETEFUENTE,
NVL(RET.BASE_RETEICA, 0)     AS BASE_RETEICA,
NVL(RET.BASE_RETEIVA, 0)     AS BASE_RETEIVA,
NVL(RET.VALOR_RETEFUENTE, 0) AS VALOR_RETEFUENTE,
NVL(RET.VALOR_RETEICA, 0)    AS VALOR_RETEICA,
NVL(RET.VALOR_RETEIVA, 0)    AS VALOR_RETEIVA,
    RESOLUCIONDIAN_CT.NUMERO NUMRES,
    TO_CHAR(RESOLUCIONDIAN_CT.FECHAINICIAL,'YYYY-MM-DD') FECHAINI_RES,
    TO_CHAR(RESOLUCIONDIAN_CT.FECHAFINAL,'YYYY-MM-DD') FECHAFIN_RES,
    RESOLUCIONDIAN_CT.CONSECUTIVOINI,
    RESOLUCIONDIAN_CT.CONSECUTIVOFIN,
    T2.DIRECCION DIRECCIONR,
    T2.TELEFONOS TELEFONOR,
    T2.DIRECCIONEMAIL,
    ACT.COD_CIIU,
    COMPROBANTE_CNT.COMPANIA,
    COMPROBANTE_CNT.ANO,
    COMPROBANTE_CNT.TIPO,
    TO_CHAR(COMPROBANTE_CNT.NUMERO) NUMERO,
    COMPROBANTE_CNT.FECHA,
    EXTRACT(DAY   FROM COMPROBANTE_CNT.FECHA) AS FECHA_FACTURA_DIA,
    EXTRACT(MONTH FROM COMPROBANTE_CNT.FECHA) AS FECHA_FACTURA_MES,
    EXTRACT(YEAR  FROM COMPROBANTE_CNT.FECHA) AS FECHA_FACTURA_ANO,
    EXTRACT(DAY   FROM COMPROBANTE_CNT.FECHA_VCN_DOC) AS FECHA_VENC_DIA,
    EXTRACT(MONTH FROM COMPROBANTE_CNT.FECHA_VCN_DOC) AS FECHA_VENC_MES,
    EXTRACT(YEAR  FROM COMPROBANTE_CNT.FECHA_VCN_DOC) AS FECHA_VENC_ANO,
    COMPROBANTE_CNT.MES,
    COMPROBANTE_CNT.DIA,
    COMPROBANTE_CNT.DESCRIPCION,
    COMPROBANTE_CNT.FECHA_VCN_DOC,
    COMPROBANTE_CNT.CREATED_BY,
    COMPROBANTE_CNT.DATE_CREATED,
    TERCERO.NOMBRE               AS CLIENTE,
    TERCERO.NIT                  AS T_NIT,
    CIUDAD.NOMBRE                AS T_CIUDAD,
    TERCERO.TELEFONOS            AS T_TELEFONO,
    TERCERO.DIRECCION            AS T_DIRECCION,
    DEPARTAMENTO.NOMBRE          AS T_DEPARTAMENTO,
    COMPROBANTE_CNT.PREFIJODIAN,
    COMPROBANTE_CNT.CONSECUTIVODIAN,
    COMPROBANTE_CNT.RESOLUCIONDIAN,
    ROWNUM                                         AS ITEM_NUM,
    DETALLE_COMPROBANTE_CNT.CUENTA                 AS ITEM_CODIGO,
    PLAN_CONTABLE.NOMBRE                           AS ITEM_DESCRIPCION,
    'UNIDAD'                                       AS ITEM_UND_MEDIDA,
    '1'                                            AS ITEM_CANT,
    COMPROBANTE_CNT.PORCIVA                        AS ITEM_IVA,
    COMPROBANTE_CNT.VLR_BASEIVA                    AS ITEM_DESCUENTO,
    DETALLE_COMPROBANTE_CNT.VALOR_DEBITO           AS ITEM_V_UNITARIO,
    COMPROBANTE_CNT.VLR_DOCUMENTO                  AS ITEM_V_TOTAL,
    CASE
    WHEN COMPROBANTE_CNT.TIPO = 'NCR' THEN 'NC' || TO_CHAR(COMPROBANTE_CNT.NUMERO)
    WHEN COMPROBANTE_CNT.TIPO = 'NDB' THEN 'NB' || TO_CHAR(COMPROBANTE_CNT.NUMERO)
    ELSE COMPROBANTE_CNT.PREFIJODIAN || '-' || COMPROBANTE_CNT.CONSECUTIVODIAN
    END AS TIPONUMERO,
 TO_CHAR(
        PCK_CODIGODEBARRAS.FC_IMPRIMIRCODIGODEBARRAS(
            CHR(204)
            || CHR(102)
            || '415'
            || COMPANIA.NITCOMPANIA
            || '8020'
            || LPAD(COMPROBANTE_CNT.CONSECUTIVODIAN, 19, '0')
            || CHR(102)
            || '3900'
            || LPAD(SUM(]') || TO_CLOB(q'[DETALLE_COMPROBANTE_CNT.VALOR_DEBITO)
                    OVER (PARTITION BY COMPROBANTE_CNT.NUMERO), 14, '0')

            || CHR(102)
            || '96'
            || TO_CHAR(COMPROBANTE_CNT.FECHA_VCN_DOC, 'YYYYMMDD')
        )
    ) AS CODIGODEBARRAS_BIN
FROM
    COMPROBANTE_CNT
    INNER JOIN TERCERO
        ON  COMPROBANTE_CNT.COMPANIA = TERCERO.COMPANIA
        AND COMPROBANTE_CNT.TERCERO  = TERCERO.NIT
        AND COMPROBANTE_CNT.SUCURSAL = TERCERO.SUCURSAL
    LEFT JOIN DEPARTAMENTO
        ON  TERCERO.PAIS         = DEPARTAMENTO.PAIS
        AND TERCERO.DEPARTAMENTO = DEPARTAMENTO.CODIGO
    LEFT JOIN CIUDAD
        ON  TERCERO.PAIS         = CIUDAD.PAIS
        AND TERCERO.DEPARTAMENTO = CIUDAD.DEPARTAMENTO
        AND TERCERO.CIUDAD       = CIUDAD.CODIGO
    INNER JOIN DETALLE_COMPROBANTE_CNT
        ON  COMPROBANTE_CNT.COMPANIA = DETALLE_COMPROBANTE_CNT.COMPANIA
        AND COMPROBANTE_CNT.ANO      = DETALLE_COMPROBANTE_CNT.ANO
        AND COMPROBANTE_CNT.TIPO     = DETALLE_COMPROBANTE_CNT.TIPO_CPTE
        AND COMPROBANTE_CNT.NUMERO   = DETALLE_COMPROBANTE_CNT.COMPROBANTE
    LEFT JOIN PLAN_CONTABLE
        ON  DETALLE_COMPROBANTE_CNT.CUENTA   = PLAN_CONTABLE.CODIGO
        AND DETALLE_COMPROBANTE_CNT.ANO      = PLAN_CONTABLE.ANO
        AND DETALLE_COMPROBANTE_CNT.COMPANIA = PLAN_CONTABLE.COMPANIA
    LEFT JOIN COMPANIA
        ON  COMPROBANTE_CNT.COMPANIA = COMPANIA.CODIGO
    LEFT JOIN TERCERO T2
        ON COMPANIA.CODIGO = T2.COMPANIA
        AND  COMPANIA.NITCOMPANIA = T2.NIT
    LEFT JOIN RESOLUCIONDIAN_CT
        ON  RESOLUCIONDIAN_CT.COMPANIA         = COMPROBANTE_CNT.COMPANIA
        AND RESOLUCIONDIAN_CT.TIPOCOMPROBANTE  = COMPROBANTE_CNT.TIPO
    LEFT JOIN ACT_ECONOMICA_TERCERO ACT
        ON T2.COMPANIA = ACT.COMPANIA
        AND T2.NIT = ACT.TERCERO
        AND T2.SUCURSAL = ACT.SUCURSAL
   LEFT JOIN (
    SELECT
        COMPANIA,
        ANO,
        TIPO,
        NUMERO,
        SUM(CASE WHEN TIPORETENCION = 'FUE' THEN VALORBASE ELSE 0 END) AS BASE_RETEFUENTE,
        SUM(CASE WHEN TIPORETENCION = 'ICA' THEN VALORBASE ELSE 0 END) AS BASE_RETEICA,
        SUM(CASE WHEN TIPORETENCION = 'IVA' THEN VALORBASE ELSE 0 END) AS BASE_RETEIVA,
        SUM(CASE WHEN TIPORETENCION = 'FUE' THEN VALOR ELSE 0 END)      AS VALOR_RETEFUENTE,
        SUM(CASE WHEN TIPORETENCION = 'ICA' THEN VALOR ELSE 0 END)      AS VALOR_RETEICA,
        SUM(CASE WHEN TIPORETENCION = 'IVA' THEN VALOR ELSE 0 END)      AS VALOR_RETEIVA
    FROM COMPROBANTE_CNTRETENCION
    GROUP BY COMPANIA, ANO, TIPO, NUMERO
) RET
ON RET.COMPANIA = COMPROBANTE_CNT.COMPANIA
AND RET.ANO      = COMPROBANTE_CNT.ANO
AND RET.TIPO     = COMPROBANTE_CNT.TIPO
AND RET.NUMERO   = COMPROBANTE_CNT.NUMERO
WHERE
        COMPROBANTE_CNT.COMPANIA = 's$compania$s'
        AND COMPROBANTE_CNT.NUMERO = 's$numero$s'
        AND COMPROBANTE_CNT.TIPO   = 's$tipo$s'
        AND DETALLE_COMPROBANTE_CNT.NATURALEZA = 'D']') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);