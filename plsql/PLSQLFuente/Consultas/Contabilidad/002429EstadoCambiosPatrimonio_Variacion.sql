MERGE INTO CONSULTAS FIN USING (SELECT '002429EstadoCambiosPatrimonio_Variacion' INFORME ,TO_CLOB(q'[SELECT CODIGO, CENTRO_COSTO, AUXILIAR, TERCERO, SUCURSAL, NOMBRE, ORDENCUENTAS,COMPANIA,SANTERIOR, NUEVO, CTABAL,ORDEN,CLASEORDEN,IND,MES,
SUM(NUEVO-SANTERIOR)AS VAR_NETA

FROM(WITH CAMBIOS_PATRIMONIO AS (SELECT
     PLAN_CONTABLE.CODIGO,
     PLAN_CONTABLE.CENTRO_COSTO,
     PLAN_CONTABLE.AUXILIAR,
     PLAN_CONTABLE.TERCERO,
     PLAN_CONTABLE.SUCURSAL,
     PLAN_CONTABLE.NOMBRE,
     PLAN_CONTABLE.CODIGO
     || NVL(PLAN_CONTABLE.CENTRO_COSTO,' ')
     || PLAN_CONTABLE.NOMBRE ORDENCUENTAS,
     PLAN_CONTABLE.COMPANIA,
     CASE WHEN s$mesTrabajo$s = 0 THEN SALDO0
          WHEN s$mesTrabajo$s = 1 THEN SALDO1
          WHEN s$mesTrabajo$s = 2 THEN SALDO2
          WHEN s$mesTrabajo$s = 3 THEN SALDO3
          WHEN s$mesTrabajo$s = 4 THEN SALDO4
          WHEN s$mesTrabajo$s = 5 THEN SALDO5
          WHEN s$mesTrabajo$s = 6 THEN SALDO6
          WHEN s$mesTrabajo$s = 7 THEN SALDO7
          WHEN s$mesTrabajo$s = 8 THEN SALDO8
          WHEN s$mesTrabajo$s = 9 THEN SALDO9
          WHEN s$mesTrabajo$s = 10 THEN SALDO10
          WHEN s$mesTrabajo$s = 11 THEN SALDO11
          WHEN s$mesTrabajo$s = 12 THEN SALDO12
          WHEN s$mesTrabajo$s = 13 THEN SALDO13
     END  SANTERIOR,
    0 NUEVO,
     SUBSTR(PLAN_CONTABLE.CODIGO,1,1) CTABAL,
     CASE WHEN SUBSTR(PLAN_CONTABLE.CODIGO,1,1) = '0' THEN 'z' ELSE SUBSTR(PLAN_CONTABLE.CODIGO,1,1) || PLAN_CONTABLE.CODIGO END  ORDEN, 
    SUBSTR(CASE WHEN SUBSTR(PLAN_CONTABLE.CODIGO,1,1) = '0' THEN 'z' ELSE SUBSTR(PLAN_CONTABLE.CODIGO,1,1) || PLAN_CONTABLE.CODIGO END ,1,1)  CLASEORDEN, 
     CASE WHEN LENGTH(s$mesComparar$s) = 1 THEN '0' ||s$mesComparar$s
         ELSE 's$mesComparar$s'
     END MES
 FROM
     V_PLAN_CONTABLE PLAN_CONTABLE
 WHERE
     PLAN_CONTABLE.COMPANIA = 's$compania$s'
     AND PLAN_CONTABLE.ANO = s$anioTrabajo$s
     AND PLAN_CONTABLE.CODIGO BETWEEN 's$codigoInicial$s' AND 's$codigoFinal$s'
     AND LENGTH(PLAN_CONTABLE.CODIGO) <= s$digitos$s
     AND PLAN_CONTABLE.CODIGO LIKE ( '3%' )

UNION 
SELECT
     PLAN_CONTABLE.CODIGO,
     PLAN_CONTABLE.CENTRO_COSTO,
     PLAN_CONTABLE.AUXILIAR,
     PLAN_CONTABLE.TERCERO,
     PLAN_CONTABLE.SUCURSAL,
     PLAN_CONTABLE.NOMBRE,
     PLAN_CONTABLE.CODIGO
     || NVL(PLAN_CONTABLE.CENTRO_COSTO,' ')
     || PLAN_CONTABLE.NOMBRE ORDENCUENTAS,
     PLAN_CONTABLE.COMPANIA,
     0 SANTERIOR,
     CASE WHEN s$mesComparar$s = 0 THEN SALDO0
          WHEN s$mesComparar$s = 1 THEN SALDO1
          WHEN s$mesComparar$s = 2 THEN SALDO2
          WHEN s$mesComparar$s = 3 THEN SALDO3
          WHEN s$mesComparar$s = 4 THEN SALDO4
          WHEN s$mesComparar$s = 5 THEN SALDO5
          WHEN s$mesComparar$s = 6 THEN SALDO6
          WHEN s$mesComparar$s = 7 THEN SALDO7
          WHEN s$mesComparar$s = 8 THEN SALDO8
          WHEN s$mesComparar$s = 9 THEN SALDO9
          WHEN s$mesComparar$s = 10 THEN SALDO10
          WHEN s$mesComparar$s = 11 THEN SALDO11
          WHEN s$mesComparar$s =]') || TO_CLOB(q'[ 12 THEN SALDO12
          WHEN s$mesComparar$s = 13 THEN SALDO13
     END  NUEVO,
     SUBSTR(PLAN_CONTABLE.CODIGO,1,1) CTABAL,
     CASE WHEN SUBSTR(PLAN_CONTABLE.CODIGO,1,1) = '0' THEN 'z' ELSE SUBSTR(PLAN_CONTABLE.CODIGO,1,1) || PLAN_CONTABLE.CODIGO END  ORDEN, 
    SUBSTR(CASE WHEN SUBSTR(PLAN_CONTABLE.CODIGO,1,1) = '0' THEN 'z' ELSE SUBSTR(PLAN_CONTABLE.CODIGO,1,1) || PLAN_CONTABLE.CODIGO END ,1,1)  CLASEORDEN, 
     CASE WHEN LENGTH(s$mesComparar$s) = 1 THEN '0' ||s$mesComparar$s
         ELSE 's$mesComparar$s'
     END MES
 FROM
     V_PLAN_CONTABLE PLAN_CONTABLE
 WHERE
     PLAN_CONTABLE.COMPANIA = 's$compania$s'
     AND PLAN_CONTABLE.ANO = s$anioComparar$s
     AND PLAN_CONTABLE.CODIGO BETWEEN 's$codigoInicial$s' AND 's$codigoFinal$s'
     AND LENGTH(PLAN_CONTABLE.CODIGO) <= s$digitos$s
     AND PLAN_CONTABLE.CODIGO LIKE ( '3%' ))
SELECT CODIGO,
     CENTRO_COSTO,
     AUXILIAR,
     TERCERO,
     SUCURSAL,
     NOMBRE,
     ORDENCUENTAS,
     COMPANIA,
     CASE WHEN s$digitos$s = 2 THEN 
        SUM(SANTERIOR) + 
        ((SELECT SUM(SALDOs$mesTrabajo$s) FROM SALDO_AUX_CONTABLE WHERE ANO = s$anioTrabajo$s AND COMPANIA = 's$compania$s' AND SUBSTR(CODIGO,1,1) = 1) - 
        ((SELECT SUM(SALDOs$mesTrabajo$s) FROM SALDO_AUX_CONTABLE WHERE ANO = s$anioTrabajo$s AND COMPANIA = 's$compania$s' AND SUBSTR(CODIGO,1,1) = 2)
        + SUM(SANTERIOR)))
      ELSE SUM(SANTERIOR) END SANTERIOR,
      CASE WHEN s$digitos$s = 2 THEN 
        SUM(NUEVO) + 
        ((SELECT SUM(SALDOs$mesComparar$s) FROM SALDO_AUX_CONTABLE WHERE ANO = s$anioComparar$s AND COMPANIA = 's$compania$s' AND SUBSTR(CODIGO,1,1) = 1) - 
        ((SELECT SUM(SALDOs$mesComparar$s) FROM SALDO_AUX_CONTABLE WHERE ANO = s$anioComparar$s AND COMPANIA = 's$compania$s' AND SUBSTR(CODIGO,1,1) = 2)
        + SUM(NUEVO)))
      ELSE SUM(NUEVO) END NUEVO,
     CTABAL,
     ORDEN,
     CLASEORDEN,
    CASE WHEN SUM(NUEVO) > SUM(SANTERIOR) 
         THEN 'INCREMENTOS' 
         ELSE CASE WHEN SUM(NUEVO) = SUM(SANTERIOR) 
                   THEN ' ' 
                   ELSE 'DISMINUCIONES' 
              END 
    END IND,
    MES
FROM CAMBIOS_PATRIMONIO
WHERE LENGTH(CODIGO) = s$digitos$s
GROUP BY CODIGO,
     CENTRO_COSTO,
     AUXILIAR,
     TERCERO,
     SUCURSAL,
     NOMBRE,
     CODIGO,
     ORDENCUENTAS,
     COMPANIA,
     CTABAL,
     ORDEN,
     CLASEORDEN,
     MES
HAVING (((TRUNC(SUM(SANTERIOR) * 100 + 0.501)) <> 0)) OR (((TRUNC(SUM(NUEVO) * 100 + 0.501)) <> 0))
AND SUM(NUEVO) <> SUM(SANTERIOR)ORDER BY CODIGO
) GROUP BY CODIGO, CENTRO_COSTO, AUXILIAR, TERCERO, SUCURSAL, 
NOMBRE, ORDENCUENTAS, COMPANIA, SANTERIOR, NUEVO, 
CTABAL, ORDEN, CLASEORDEN, IND, MES
ORDER BY CODIGO]') CONSULTA, 1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);