MERGE INTO CONSULTAS FIN USING (SELECT '800364RecienNacidosRIPS' INFORME ,TO_CLOB(q'[SELECT 
DISTINCT 
	AF.CONSECUTIVO_RIPS CUENTA,
	AF.COD_PREST_SERV_SALUD,
	AF.NOMBRE_RAZON_SOCIAL,
	AF.TIPO_IDENTI_PREST_SERV_SALUD,
	AF.NUM_IDENTIF_PRESTADOR,
	AF.NUM_FACTURA,
	TO_CHAR(AF.FECHA_EXP_FACTURA,'DD/MM/YYYY') FECHA_EXP_FACTURA,
	TO_CHAR(AF.FECHA_INICIAL,'DD/MM/YYYY') FECHA_INICIAL,
	TO_CHAR(AF.FECHA_FINAL,'DD/MM/YYYY') FECHA_FINAL,
	AF.COD_ENTIDAD_ADMINISTRADORA,
	AF.NOMBRE_ENTIDAD_ADMINISTRADORA,
	AF.NUMERO_CONTRATO,
	AF.PLAN_BENEFICIOS,
	AF.NUMERO_POLIZA,
	AF.VALOR_TOT_PAGO_COMPARTIDO,
	AF.VALOR_COMISION,
	AF.VALOR_TOTAL_DESCUENTOS,
	AF.VALOR_NETO_A_PAGAR_ENTI_CONTR,
	CASE  WHEN AF.VALIDADO=0 THEN 'NO' WHEN AF.VALIDADO<>0 THEN 'SI' END VALIDADO_AF,
	AF.OBSERVACION_VALIDACION OBSERVACION_AF,
	AN.TIPO_IDENTI_MADRE,
	AN.NUMERO_IDENTI_MADRE,
	TO_CHAR(AN.FECHA_RECIEN_NACIDO,'DD/MM/YYYY') FECHA_RECIEN_NACIDO,
	TO_CHAR(AN.HORA_NACIMIENTO,'HH:MI') HORA_NACIMIENTO,
	AN.EDAD_GESTACIONAL,
	AN.CONTROL_PRENATAL,
	AN.SEXO,
	AN.PESO,
	AN.DIAGNOSTICO_RECIEN_NACIDO,
	AN.CAUSA_BASICA_MUERTE,
	TO_CHAR(AN.FECHA_MUERTE_NACIDO,'DD/MM/YYYY') FECHA_MUERTE_NACIDO,
	TO_CHAR(AN.HORA_MUERTE_NACIDO,'HH:MI') HORA_MUERTE_NACIDO,
	CASE  WHEN AN.VALIDADO=0 THEN 'NO' WHEN AN.VALIDADO<>0 THEN 'SI' END VALIDADO_AN,
	AN.OBSERVACION_VALIDACION OBSERVACION_AN
FROM
	CM_ARCHIVO_TRANSACCIONES AF
INNER JOIN CM_ARCHIVO_RECIEN_NACIDOS AN 
ON AN.COMPANIA=AF.COMPANIA 
AND AN.NUM_FACTURA=AF.NUM_FACTURA 
/*AND AF.COD_PREST_SERV_SALUD=AN.COD_PREST_SERV_SALUD */
AND AF.CONSECUTIVO_RIPS = AN.CONSECUTIVO_RIPS
WHERE
	AF.COMPANIA ='s$compania$s' 
	AND TO_CHAR(AF.CONSECUTIVO_RIPS) BETWEEN 's$conseucutivoInicial$s' AND 's$conseucutivoFinal$s' 
	AND AF.NUM_IDENTIF_PRESTADOR BETWEEN 's$terceroInicial$s' AND 's$terceroFinal$s'
	AND AF.NUM_FACTURA BETWEEN 's$facturaInicial$s' AND 's$facturaFinal$s'
	AND TO_CHAR(AF.FECHA_EXP_FACTURA,'DD/MM/YYYY') BETWEEN TO_DATE('s$fechaInicial$s', 'DD/MM/YYYY') AND TO_DATE('s$fechaFinal$s', 'DD/MM/YYYY')
	AND AN.NUMERO_IDENTI_MADRE BETWEEN 's$numIdentInicial$s' AND 's$numIdentFinal$s'
ORDER BY CUENTA]') CONSULTA, 84 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
COMMIT;