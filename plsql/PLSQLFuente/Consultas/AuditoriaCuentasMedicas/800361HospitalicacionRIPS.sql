MERGE INTO CONSULTAS FIN USING (SELECT '800361HospitalicacionRIPS' INFORME ,TO_CLOB(q'[SELECT 
 AF.CONSECUTIVO_RIPS CUENTA,
 AF.COD_PREST_SERV_SALUD,
 AF.NOMBRE_RAZON_SOCIAL,
 AF.TIPO_IDENTI_PREST_SERV_SALUD,
 AF.NUM_IDENTIF_PRESTADOR,
 AF.NUM_FACTURA,
 TO_CHAR(AF.FECHA_EXP_FACTURA,'DD/MM/YYYY') FECHA_EXP_FACTURA,
 TO_CHAR(AF.FECHA_INICIAL,'DD/MM/YYYY') FECHA_INICIAL,
 TO_CHAR(AF.FECHA_FINAL,'DD/MM/YYYY') FECHA_FINAL,
 AF.COD_ENTIDAD_ADMINISTRADORA,
 AF.NOMBRE_ENTIDAD_ADMINISTRADORA,
 AF.NUMERO_CONTRATO,
 AF.PLAN_BENEFICIOS,
 AF.NUMERO_POLIZA,
 AF.VALOR_TOT_PAGO_COMPARTIDO,
 AF.VALOR_COMISION,
 AF.VALOR_TOTAL_DESCUENTOS,
 AF.VALOR_NETO_A_PAGAR_ENTI_CONTR,
 CASE  WHEN AF.VALIDADO=0 THEN 'NO' WHEN AF.VALIDADO<>0 THEN 'SI' END VALIDADO_AF,
 AF.OBSERVACION_VALIDACION OBSERVACION_AF,
 AH.TIPO_IDENTIFICACION_USUARIO,
 AH.NUM_IDENTI_USUARIO_SISTEMA,
 AH.VIA_INGRESO_INSTITUCION,
 TO_CHAR(AH.FECHA_INGRESO_USUARIO_SISTEMA,'DD/MM/YYYY') FECHA_INGRESO_USUARIO_SISTEMA,
 TO_CHAR(AH.HORA_INGRESO_USUARIO,'HH:MI') HORA_INGRESO_USUARIO,
 AH.NUMERO_AUTORIZACION,
 AH.CAUSA_EXTERNA,
 AH.DIAGNOSTICO_PRINCIPAL_INGRESO,
 AH.DIAGNOSTICO_PRINCIPAL_EGRESO,
 AH.DIAGNOSTICO_RELACIONADO_1,
 AH.DIAGNOSTICO_RELACIONADO_2,
 AH.DIAGNOSTICO_RELACIONADO_3,
 AH.DIAGNOSTICO_COMPILACION,
 AH.ESTADO_SALIDA,
 AH.DIAG_CAUSA_BASIC_MUERTE,
 TO_CHAR(AH.FECHA_EGRESO_USUARIO,'DD/MM/YYYY') FECHA_EGRESO_USUARIO, 
 TO_CHAR(AH.HORA_EGRESO_USUARIO,'HH:MI') HORA_EGRESO_USUARIO,
 CASE  WHEN AH.VALIDADO=0 THEN 'NO' WHEN AH.VALIDADO<>0 THEN 'SI' END VALIDADO_AH,
 AH.OBSERVACION_VALIDACION OBSERVACION_AH
FROM
 CM_ARCHIVO_TRANSACCIONES AF
INNER JOIN CM_ARCHIVO_HOSPITALIZACION AH 
ON AH.COMPANIA=AF.COMPANIA 
AND AH.NUM_FACTURA=AF.NUM_FACTURA 
/*AND AF.COD_PREST_SERV_SALUD=AH.COD_PREST_SERV_SALUD */
AND AF.CONSECUTIVO_RIPS = AH.CONSECUTIVO_RIPS
WHERE
 AF.COMPANIA ='s$compania$s'
 AND TO_CHAR(AF.CONSECUTIVO_RIPS) BETWEEN 's$conseucutivoInicial$s' AND 's$conseucutivoFinal$s' 
 AND AF.NUM_FACTURA BETWEEN 's$facturaInicial$s' AND 's$facturaFinal$s'
 AND AF.NUM_IDENTIF_PRESTADOR BETWEEN 's$terceroInicial$s'  AND 's$terceroFinal$s' 
 AND TO_CHAR(AF.FECHA_EXP_FACTURA,'DD/MM/YYYY') BETWEEN TO_DATE('s$fechaInicial$s', 'DD/MM/YYYY') AND TO_DATE('s$fechaFinal$s', 'DD/MM/YYYY')
 ]') CONSULTA, 84 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
 COMMIT;