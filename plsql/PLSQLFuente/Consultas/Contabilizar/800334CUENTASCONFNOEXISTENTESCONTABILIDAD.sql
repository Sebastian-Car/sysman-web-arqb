MERGE INTO CONSULTAS FIN USING (SELECT '800334CUENTASCONFNOEXISTENTESCONTABILIDAD' INFORME ,TO_CLOB(q'[SELECT ID_DE_CONCEPTO,
      NOMBRE_CONCEPTO,
      CUENTA_DEBITO,
      '' CUENTA_CREDITO,
      'La cuenta contable esta configurada al concepto de nomina y no existe en el plan_contable' INCONSISTENCIA

FROM (
SELECT DISTINCT 's$companiaDs$s' COMPANIA,
        HISTORICOS.ANO,
        CONCEPTOS.ID_DE_CONCEPTO,
        CONCEPTOS.NOMBRE_CONCEPTO,

       CASE WHEN PERSONAL.GRUPOCONTABLE='A'
          THEN CTA_DBT_ADMINISTRACION
          ELSE
         CASE WHEN PERSONAL.GRUPOCONTABLE='P'     
           THEN CTA_DBT_PRODUCCION
         ELSE 
         CASE WHEN PERSONAL.GRUPOCONTABLE='V' 
            THEN CTA_DBT_VENTAS
          ELSE 
           CASE WHEN PERSONAL.GRUPOCONTABLE='O' 
            THEN CTA_DBT_OTRO
            ELSE
          CASE WHEN PERSONAL.GRUPOCONTABLE='S' 
             THEN CTA_DBT_SUPERNUM
         END END END END END CUENTA_DEBITO,
    CASE WHEN PERSONAL.GRUPOCONTABLE='A'
          THEN CTA_CRD_ADMINISTRACION
          ELSE
         CASE WHEN PERSONAL.GRUPOCONTABLE='P'     
           THEN CTA_CRD_PRODUCCION
         ELSE 
         CASE WHEN PERSONAL.GRUPOCONTABLE='V' 
            THEN CTA_CRD_VENTAS
          ELSE 
           CASE WHEN PERSONAL.GRUPOCONTABLE='O' 
            THEN CTA_CRD_OTRO
            ELSE
          CASE WHEN PERSONAL.GRUPOCONTABLE='S' 
             THEN CTA_CRD_SUPERNUM
         END END END END END CUENTA_CREDITO

     FROM CONCEPTOS INNER JOIN HISTORICOS
     ON  CONCEPTOS.COMPANIA         =HISTORICOS.COMPANIA
     AND CONCEPTOS.ID_DE_CONCEPTO   =HISTORICOS.ID_DE_CONCEPTO
     INNER JOIN  PERSONAL  
     ON  PERSONAL.COMPANIA         =PERSONAL.COMPANIA
     AND PERSONAL.ID_DE_EMPLEADO  =PERSONAL.ID_DE_EMPLEADO


  WHERE CONCEPTOS.COMPANIA='s$compania$s'
   AND CONCEPTOS.CLASE IN(3,5,7,8)
   AND HISTORICOS.ANO=s$ano$s
   AND HISTORICOS.MES=s$mesFinal$s
   AND HISTORICOS.PERIODO=s$periodo$s
   AND HISTORICOS.ID_DE_PROCESO=s$procesos$s


) CUENTASCONFIGURADAS
LEFT JOIN PLAN_CONTABLE
 ON CUENTASCONFIGURADAS.COMPANIA=PLAN_CONTABLE.COMPANIA
 AND CUENTASCONFIGURADAS.ANO=PLAN_CONTABLE.ANO
 AND CUENTASCONFIGURADAS.CUENTA_DEBITO =PLAN_CONTABLE.CODIGO

 WHERE CUENTASCONFIGURADAS.COMPANIA='s$companiaDs$s'
 AND PLAN_CONTABLE.COMPANIA IS NULL
 AND (CUENTA_DEBITO IS NOT NULL)

 UNION ALL

 SELECT ID_DE_CONCEPTO,
      NOMBRE_CONCEPTO,
      '' CUENTA_DEBITO,
      CUENTA_CREDITO,
      'La cuenta contable esta configurada al concepto de nomina y no existe en el plan_contable' INCONSISTENCIA

FROM (
SELECT DISTINCT 's$companiaDs$s' COMPANIA,
        HISTORICOS.ANO,
        CONCEPTOS.ID_DE_CONCEPTO,
        CONCEPTOS.NOMBRE_CONCEPTO,

       CASE WHEN PERSONAL.GRUPOCONTABLE='A'
          THEN CTA_DBT_ADMINISTRACION
          ELSE
         CASE WHEN PERSONAL.GRUPOCONTABLE='P'     
           THEN CTA_DBT_PRODUCCION
         ELSE 
         CASE WHEN PERSONAL.GRUPOCONTABLE='V' 
            THEN CTA_DBT_VENTAS
          ELSE 
       ]') || TO_CLOB(q'[    CASE WHEN PERSONAL.GRUPOCONTABLE='O' 
            THEN CTA_DBT_OTRO
            ELSE
          CASE WHEN PERSONAL.GRUPOCONTABLE='S' 
             THEN CTA_DBT_SUPERNUM
         END END END END END CUENTA_DEBITO,
    CASE WHEN PERSONAL.GRUPOCONTABLE='A'
          THEN CTA_CRD_ADMINISTRACION
          ELSE
         CASE WHEN PERSONAL.GRUPOCONTABLE='P'     
           THEN CTA_CRD_PRODUCCION
         ELSE 
         CASE WHEN PERSONAL.GRUPOCONTABLE='V' 
            THEN CTA_CRD_VENTAS
          ELSE 
           CASE WHEN PERSONAL.GRUPOCONTABLE='O' 
            THEN CTA_CRD_OTRO
            ELSE
          CASE WHEN PERSONAL.GRUPOCONTABLE='S' 
             THEN CTA_CRD_SUPERNUM
         END END END END END CUENTA_CREDITO

     FROM CONCEPTOS INNER JOIN HISTORICOS
     ON  CONCEPTOS.COMPANIA         =HISTORICOS.COMPANIA
     AND CONCEPTOS.ID_DE_CONCEPTO   =HISTORICOS.ID_DE_CONCEPTO
     INNER JOIN  PERSONAL  
     ON  PERSONAL.COMPANIA         =PERSONAL.COMPANIA
     AND PERSONAL.ID_DE_EMPLEADO  =PERSONAL.ID_DE_EMPLEADO


   WHERE CONCEPTOS.COMPANIA='s$compania$s'
   AND CONCEPTOS.CLASE IN(3,5,7,8)
   AND HISTORICOS.ANO=s$ano$s
   AND HISTORICOS.MES=s$mesFinal$s
   AND HISTORICOS.PERIODO=s$periodo$s
   AND HISTORICOS.ID_DE_PROCESO=s$procesos$s

) CUENTASCONFIGURADAS
LEFT JOIN PLAN_CONTABLE
 ON CUENTASCONFIGURADAS.COMPANIA=PLAN_CONTABLE.COMPANIA
 AND CUENTASCONFIGURADAS.ANO=PLAN_CONTABLE.ANO
 AND  CUENTASCONFIGURADAS.CUENTA_CREDITO =PLAN_CONTABLE.CODIGO

  WHERE CUENTASCONFIGURADAS.COMPANIA='s$companiaDs$s'
 AND PLAN_CONTABLE.COMPANIA IS NULL
 AND CUENTA_CREDITO IS NOT NULL]') CONSULTA, 96 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);