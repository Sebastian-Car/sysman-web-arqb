SELECT COMPANIA,
  CICLO,
  CODIGOINTERNO,
  CODIGORUTA,
  TRIM(PRIMERAPELLIDO
  || ' '
  || SEGUNDOAPELLIDO
  || ' '
  || NOMBRES) AS NOMBRES
FROM SP_USUARIO
WHERE COMPANIA            = s$compania$s
AND CICLO                 = s$ciclo$s
AND LENGTH(CODIGOCATASTRAL) <> 15
