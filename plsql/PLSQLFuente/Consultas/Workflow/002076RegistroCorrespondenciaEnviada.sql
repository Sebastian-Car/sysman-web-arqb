MERGE INTO CONSULTAS FIN USING (SELECT '002076RegistroCorrespondenciaEnviada' INFORME ,TO_CLOB(q'[SELECT
TRAMITES.NUMERO,
TO_CHAR(TRAMITES.FECHA,'DD/MM/YYYY') AS FECHA,
TRAMITES.PROCEDENCIA AS REMITENTE,
PROCEDENCIA_TRAMITE.NOMBRE AS RESPONSABLE_REMITENTE,
TRAMITES.DESTINO AS NOMBRE_DESTINATARIO,
TRAMITES.DIRECCION_DESTINO,
TIPO_MEDIO.NOMBRE MEDIO_ENVIO,
TRAMITES.DESCRIPCION AS ASUNTO
FROM TRAMITES
INNER JOIN NODOS 
ON TRAMITES.COMPANIA = NODOS.COMPANIA
AND TRAMITES.PROCESOS = NODOS.CODIGO_PROCESO
AND TRAMITES.NODO_ACTUAL = NODOS.CODIGO
LEFT JOIN PROCEDENCIA_TRAMITE
 ON TRAMITES.COMPANIA = PROCEDENCIA_TRAMITE.COMPANIA
AND TRAMITES.PROCEDENCIA = PROCEDENCIA_TRAMITE.CODIGO
LEFT JOIN TIPO_MEDIO 
ON TRAMITES.COMPANIA = TIPO_MEDIO.COMPANIA
AND TRAMITES.TIPO_MEDIO = TIPO_MEDIO.CODIGO
WHERE TRAMITES.COMPANIA = s$compania$s
AND TO_CHAR(TRAMITES.FECHA,'DD/MM/YYYY') BETWEEN 's$fechaInicial$s' AND 's$fechaFinal$s'
--AND TRAMITES.TIPO_MEDIO = 's$tipoMedio$s'
AND NODOS.NODO_FINAL NOT IN (0)]') CONSULTA, 35 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);