MERGE INTO CONSULTAS FIN USING (SELECT '002298ResgistroDeProcesos' INFORME ,TO_CLOB(q'[SELECT *
FROM (SELECT
TRAMITES.COMPANIA,
TRAMITES.NUMERO,
TRAMITES.TIPO_TRAMITE,
TP.NOMBRE NOMBRE_TIPO,
TRAMITES.PROCESOS AS PROCESO,
P.NOMBRE NOMBRE_PROCESO,
TRAMITES.FECHA FECHA_RADICADO,
TRAMITES.NODO_ACTUAL AS ETAPA_ACTUAL,
N.NOMBRE NOMBRE_ETAPA,
TRAMITES.USUARIO_INTERNO AS RESPONSABLE_ACTUAL,
TO_CHAR(pt.estimado_fecha,'DD/MM/YYYY') fecha_limite,
TRAMITES.DEPENDENCIA,
D.NOMBRE NOMBRE_DEPENDENCIA,
TRAMITES.ESTADO,
TV1.VARIABLE VAR,
TV1.ADJUNTO,
TV.VARIABLE,
CASE TV.TIPO_DATO WHEN 8 THEN TV.VALOR_TEXTO
WHEN 6 THEN TO_CHAR(TV.VALOR)
WHEN 7 THEN TO_CHAR(TV.VALOR_FECHA, 'DD/MM/YYYY')END VALOR,
PRT.NOMBRE AS NOMBRE_SOLICITANTE,
PRT.DOCUMENTO AS NUMERO_CEDULA,
PRT.DIRECCION AS DIRECCION_SOLICITANTE,
PRT.DIRECCIONE_MAIL AS EMAIL,
PRT.TELEFONO AS TELEFONO
FROM TRAMITES
INNER JOIN TIPOTRAMITES TP
 ON TRAMITES.COMPANIA = TP.COMPANIA
AND TRAMITES.PROCESOS = TP.PROCESOS
AND TRAMITES.TIPO_TRAMITE = TP.TIPOTRAMITE
INNER JOIN PROCESOS P
 ON TRAMITES.COMPANIA = P.COMPANIA
AND TRAMITES.PROCESOS = P.CODIGO
INNER JOIN NODOS N
 ON TRAMITES.COMPANIA = N.COMPANIA
AND TRAMITES.PROCESOS = N.CODIGO_PROCESO
AND TRAMITES.NODO_ACTUAL = N.CODIGO
INNER JOIN DEPENDENCIA D
 ON TRAMITES.COMPANIA = D.COMPANIA
AND TRAMITES.DEPENDENCIA = D.CODIGO
INNER JOIN TRAMITE_VARIABLE TV
ON TRAMITES.COMPANIA = TV.COMPANIA
AND TRAMITES.PROCESOS = TV.PROCESO
AND TRAMITES.TIPO_TRAMITE = TV.TIPO_TRAMITE
AND TRAMITES.NUMERO = TV.TRAMITE
INNER JOIN TRAMITE_VARIABLE TV1
ON TRAMITES.COMPANIA = TV1.COMPANIA
AND TRAMITES.PROCESOS = TV1.PROCESO
AND TRAMITES.TIPO_TRAMITE = TV1.TIPO_TRAMITE
AND TRAMITES.NUMERO = TV1.TRAMITE
LEFT JOIN proyecciones_tramite   pt 
ON tramites.compania = pt.compania
AND tramites.procesos = pt.proceso
AND tramites.nodo_actual = pt.nodo
AND tramites.numero = pt.tramite
AND tramites.tipo_tramite = pt.tipo_tramite
INNER JOIN PROCEDENCIA_TRAMITE PRT
ON TRAMITES.PROCEDENCIA = PRT.CODIGO ) 
TRAMITES PIVOT (MAX(VALOR)
FOR VARIABLE
IN (
'genero' AS GENERO,
'rangoedad' AS RANGO_EDAD,
'tipopersona' AS TIPO_PERSONA,
'tipopoblacion' AS TIPO_POBLACION,
'vulnerabilidad' AS VULNERABILIDAD,
'ocupacion' AS OCUPACION,
'escolaridad' AS ESCOLARIDAD
))
PIVOT (MAX(ADJUNTO)
FOR VAR
IN ( 
'anexouno' AS anexouno,
'anexodos' AS anexodos,
'anexotres' AS anexotres
)
)
WHERE COMPANIA = s$compania$s
AND FECHA_RADICADO BETWEEN TO_DATE('s$fechaInicial$s','DD/MM/YYYY') AND TO_DATE('s$fechaFinal$s','DD/MM/YYYY')
AND NUMERO BETWEEN 's$tramiteIni$s' AND 's$tramiteFin$s'
AND DEPENDENCIA BETWEEN 's$dependenciaIni$s' AND 's$dependenciaFin$s'
AND RESPONSABLE_ACTUAL BETWEEN 's$responsableIni$s' AND 's$responsableFin$s'
AND ESTADO IN (CASE WHEN 's$estado$s' = 'true' THEN 4 ELSE 5 END)]') CONSULTA, 35 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);