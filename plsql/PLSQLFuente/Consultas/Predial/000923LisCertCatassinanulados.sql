SELECT NUMCER,
  CODIGOPREDIO,
  NOMBRE,
  VALOR,
  (VALOR) AS VLR
FROM IP_CERTIFICADOCATAS
WHERE COMPANIA = s$compania$s
  AND FECHA_EXP
  BETWEEN s$fechaInicial$s AND s$fechaFinal$s
  AND ANULADO=0
