MERGE INTO CONSULTAS FIN USING (SELECT '002409REG_COT' INFORME ,TO_CLOB(q'[SELECT
    V_DETALLE_AUXILIAR_PPTAL.CUENTA AS CODIGO_CUENTA,
    V_DETALLE_AUXILIAR_PPTAL.FECHA,
    V_DETALLE_AUXILIAR_PPTAL.DESCRIPCION,
    V_DETALLE_AUXILIAR_PPTAL.TERCERO_COM_DIR AS DIRECCION,
    V_DETALLE_AUXILIAR_PPTAL.COMPROBANTE,
    V_DETALLE_AUXILIAR_PPTAL.NRO_DOCUMENTO,
    V_DETALLE_AUXILIAR_PPTAL.TIPO,
    V_DETALLE_AUXILIAR_PPTAL.CUENTA || ' ' || V_DETALLE_AUXILIAR_PPTAL.FUENTE_RECURSOS AS RUBRO,
    V_DETALLE_AUXILIAR_PPTAL.SUCURSAL_COM AS SUCURSAL,
    V_DETALLE_AUXILIAR_PPTAL.TERCERO_COM AS NOMBRETERCERO1,
    V_DETALLE_AUXILIAR_PPTAL.TERCERO_DET_NOM AS NOMBRET,
    V_DETALLE_AUXILIAR_PPTAL.VALOR_DEBITO,
    V_DETALLE_AUXILIAR_PPTAL.VALOR_CREDITO AS CONTRACREDITO,
    V_DETALLE_AUXILIAR_PPTAL.CUENTANOMBRE AS TOTALNOMBRE,
    V_DETALLE_AUXILIAR_PPTAL.TEXTO AS TXT,
    V_DETALLE_AUXILIAR_PPTAL.FECHA_VCN_DOC,
    V_DETALLE_AUXILIAR_PPTAL.CREATED_BY AS CREADO,
    V_DETALLE_AUXILIAR_PPTAL.FUENTE_RECURSOS AS FUENTE,
    V_DETALLE_AUXILIAR_PPTAL.CODIGO_CPC,
    V_DETALLE_AUXILIAR_PPTAL.TIPO_CPTE_AFECT || ' ' || V_DETALLE_AUXILIAR_PPTAL.CMPTE_AFECTADO AS AFECTADO,
    V_DETALLE_AUXILIAR_PPTAL.VLR_DOCUMENTO,
    SD.IMPUESTO,
    SD.VALOR_BASE
FROM V_DETALLE_AUXILIAR_PPTAL
LEFT JOIN (
    SELECT DISTINCT
        V_DETALLE_AUXILIAR_PPTAL.COMPROBANTE,
        V_DETALLE_AUXILIAR_PPTAL.CUENTA,
        SOLICITUDDISPONIBILIDAD.ANO,
        SOLICITUDDISPONIBILIDAD.IMPUESTO,
        SOLICITUDDISPONIBILIDAD.VALOR_BASE,
        SOLICITUDDISPONIBILIDAD.NUMERO
    FROM V_DETALLE_AUXILIAR_PPTAL
    LEFT JOIN SOLICITUDDISPONIBILIDAD ON V_DETALLE_AUXILIAR_PPTAL.ANO = SOLICITUDDISPONIBILIDAD.ANO
        AND V_DETALLE_AUXILIAR_PPTAL.NRO_DOCUMENTO = TO_CHAR(SOLICITUDDISPONIBILIDAD.NUMERO)  
) SD ON V_DETALLE_AUXILIAR_PPTAL.ANO = SD.ANO
AND V_DETALLE_AUXILIAR_PPTAL.CMPTE_AFECTADO = SD.COMPROBANTE
AND V_DETALLE_AUXILIAR_PPTAL.CUENTA =SD.CUENTA
WHERE V_DETALLE_AUXILIAR_PPTAL.COMPANIA          = s$compania$s
AND V_DETALLE_AUXILIAR_PPTAL.ANO                 = s$ano$s
AND V_DETALLE_AUXILIAR_PPTAL.TIPO                = 's$tipoCpte$s'
AND V_DETALLE_AUXILIAR_PPTAL.COMPROBANTE BETWEEN s$numeroPptoInicial$s AND s$numeroPptoFinal$s
]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);