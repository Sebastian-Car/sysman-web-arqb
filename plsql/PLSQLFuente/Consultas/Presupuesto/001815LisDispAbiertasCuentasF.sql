MERGE INTO CONSULTAS FIN USING (SELECT '001815LisDispAbiertasCuentasF' INFORME ,TO_CLOB(q'[SELECT DETALLE_COMPROBANTE_PPTAL.COMPANIA, 
       DETALLE_COMPROBANTE_PPTAL.ANO, 
       DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE, 
       TO_CHAR(DETALLE_COMPROBANTE_PPTAL.COMPROBANTE)COMPROBANTE, 
       DETALLE_COMPROBANTE_PPTAL.DESCRIPCION, 
       DETALLE_COMPROBANTE_PPTAL.FECHA  FECHA,
       DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO,
       DETALLE_COMPROBANTE_PPTAL.AUXILIAR,
  	   DETALLE_COMPROBANTE_PPTAL.CENTRO_COSTO,
  	   DETALLE_COMPROBANTE_PPTAL.REFERENCIA, 
       TIPO_COMPROBPP.CLASE, 
       DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO, 
       DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO, 
       DETALLE_COMPROBANTE_PPTAL.DEBITO_AFECTADO, 
       DETALLE_COMPROBANTE_PPTAL.CREDITO_AFECTADO, 
       DETALLE_COMPROBANTE_PPTAL.MODIFICACION_DEBITO, 
       DETALLE_COMPROBANTE_PPTAL.MODIFICACION_CREDITO, 
       (VALOR_DEBITO - VALOR_CREDITO) -(DEBITO_AFECTADO - CREDITO_AFECTADO) +(MODIFICACION_DEBITO - MODIFICACION_CREDITO)  SALDO, 
       DETALLE_COMPROBANTE_PPTAL.TERCERO  CODTERCERO, 
       DETALLE_COMPROBANTE_PPTAL.SUCURSAL  CODSUCURSAL, 
       TERCERO.NOMBRE  NOMBRETERCERO, 
       TIPO_COMPROBPP.NOMBRE NOMBRETIPO, 
       (VALOR_DEBITO - VALOR_CREDITO)  MOVIMIENTO, 
       (DEBITO_AFECTADO - CREDITO_AFECTADO)  AFECTACION, 
       (MODIFICACION_DEBITO - MODIFICACION_CREDITO)  MODIFICACION, 
       V_PLAN_PRESUPUESTAL.NOMBRE NOMBREPLAN, 
       DETALLE_COMPROBANTE_PPTAL.CUENTA, 
      CASE WHEN s$tipoCuenta$s = 1
            THEN DETALLE_COMPROBANTE_PPTAL.CUENTA
            ELSE LPAD(NVL(V_PLAN_PRESUPUESTAL.NIVEL1, ''), 3) || '-' || LPAD(NVL(V_PLAN_PRESUPUESTAL.NIVEL2, ''), 3) || '-' || LPAD(NVL(V_PLAN_PRESUPUESTAL.NIVEL3, ''), 3) || '-' || LPAD(NVL(V_PLAN_PRESUPUESTAL.NIVEL4, ''), 3) || '-' || LPAD(NVL(V_PLAN_PRESUPUESTAL.NIVEL5, ''), 3) || '-' || LPAD(NVL(V_PLAN_PRESUPUESTAL.NIVEL6, ''), 3) || '-' || LPAD(NVL(V_PLAN_PRESUPUESTAL.NIVEL7, ''), 3)
            END  RUBRO, 
       CASE WHEN s$tipoCuenta$s = 1
            THEN 'Codigo CUENTA'
            ELSE 'FID-UNE-PROG-SPR-PROY-DIS-REC'
            END  TITULOCUENTA, 
       DETALLE_COMPROBANTE_PPTAL.NRO_DOCUMENTO 
FROM DETALLE_COMPROBANTE_PPTAL 
INNER JOIN TERCERO 
ON DETALLE_COMPROBANTE_PPTAL.COMPANIA       = TERCERO.COMPANIA
AND DETALLE_COMPROBANTE_PPTAL.SUCURSAL      = TERCERO.SUCURSAL
AND DETALLE_COMPROBANTE_PPTAL.TERCERO       = TERCERO.NIT
INNER JOIN V_PLAN_PRESUPUESTAL 
ON DETALLE_COMPROBANTE_PPTAL.COMPANIA       = V_PLAN_PRESUPUESTAL.COMPANIA
AND DETALLE_COMPROBANTE_PPTAL.ANO           = V_PLAN_PRESUPUESTAL.ANO
AND DETALLE_COMPROBANTE_PPTAL.ID        = V_PLAN_PRESUPUESTAL.ID
INNER JOIN TIPO_COMPROBPP 
ON DETALLE_COMPROBANTE_PPTAL.COMPANIA       = TIPO_COMPROBPP.COMPANIA
AND DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE     = TIPO_COMPROBPP.CODIGO
WHERE DETALLE_COMPROBANTE_PPTAL.COMPANIA = s$compania$s
AND DETALLE_COMPROBANTE_PPTAL.CUENTA BETWEEN 's$cuentaInicial$s' AND 's$cuentaFinal$s' 
AND DETALLE_COMPROBANTE_PPTAL.FECHA BETWEEN TO_DATE('s$fechaInicial$s','DD/MM/YYYY') AND TO_DATE('s$fechaFina]') || TO_CLOB(q'[l$s','DD/MM/YYYY') 
AND TIPO_COMPROBPP.CLASE = 'DIS' 
AND ((ROUND((VALOR_DEBITO - VALOR_CREDITO) -(DEBITO_AFECTADO - CREDITO_AFECTADO) +(MODIFICACION_DEBITO - MODIFICACION_CREDITO), 2)) NOT IN  (0)) 
ORDER BY 
    DETALLE_COMPROBANTE_PPTAL.COMPANIA, 
    DETALLE_COMPROBANTE_PPTAL.ANO, 
    DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE, 
    DETALLE_COMPROBANTE_PPTAL.COMPROBANTE]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);