MERGE INTO CONSULTAS FIN USING (SELECT '800697FCrptEjePptalGtosAGRExcel' INFORME ,TO_CLOB(q'[SELECT ID RUBRO, 
          FUENTE, 
          FUENTECUIPO,
          NOMBRE DESCRIPCION, 
          APROPIACIONANTERIOR APROPIACION_INICIAL, 
          TDEBITO CREDITOS, 
          TCREDITO CONTRA_CREDITO,
          APLAZAMIENTO APLAZAMIENTOS,
          REDUCCION REDUCCIONES,
          ADICION ADICIONES, 
          DEFINITIVO APROPIACION_DEFINITIVA,
          DISPONIBILIDAD CDP_EXPEDIDOS,
          ABIERTAS CDP_ABIERTOS,
          DISPONIBILIDAD_MES,
          COMPROMISO COMPROMISOS,
          COMPROMISOS_MES,
          OBLIGACIONES,
          OBLIGACIONES_MES
,
          PAGOS,
          PAGOS_MES
,
          SALDOCOMP SALDO_POR_COMPROMETER,
          DEFINITIVO - DISPONIBILIDAD SALDO_DISPONIBLE          
FROM (
  s$consultaBase$s
    )]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
COMMIT;