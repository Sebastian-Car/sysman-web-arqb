MERGE INTO CONSULTAS FIN USING (SELECT '002286SOLCERTIFDISPPTALIDIPRON' INFORME ,TO_CLOB(q'[SELECT 
    BPTIPONOVEDAD.CODIGODOC                             AS CODIGODOC,
    BPTIPONOVEDAD.VERSIONDOC                             AS VERSIONDOC,
    TO_CHAR(SOLICITUDDISPONIBILIDAD.FECHA, 'DD/MM/YYYY') AS FECHAVERSIONDOC,
    TO_CHAR(SOLICITUDDISPONIBILIDAD.FECHA, 'DD')         AS DIA,
    TO_CHAR(SOLICITUDDISPONIBILIDAD.FECHA, 'MM')        AS MES,
    TO_CHAR(SOLICITUDDISPONIBILIDAD.FECHA, 'YYYY')      AS ANO,
    SOLICITUDDISPONIBILIDAD.NUMERO                      AS CONSECUTIVO,
    SOLICITUDDISPONIBILIDAD.OBJETO                      AS OBJETO,
    RESPONSABLE.CARGO                                   AS CARGORESPONSABLE,
    TERCERO.NOMBRE                                      AS NOMBRERESPONSABLE,
    BPTIPONOVEDAD.TITULOFORMATO                         AS TITULOFORMATO,
    SOLICITUDDISPONIBILIDAD.COMPANIA                    AS COMPANIA,
    BPTIPONOVEDAD.TIPOT                                 AS TIPOT,
    BPTIPONOVEDAD.CLASET                                AS CLASET,
    SOLICITUDDISPONIBILIDAD.DEPENDENCIA                 AS DEPENDENCIA
FROM SOLICITUDDISPONIBILIDAD
INNER JOIN BPTIPONOVEDAD
        ON BPTIPONOVEDAD.COMPANIA = SOLICITUDDISPONIBILIDAD.COMPANIA 
        AND BPTIPONOVEDAD.TIPOT   = SOLICITUDDISPONIBILIDAD.TIPO_CPTE_AFECT
INNER JOIN TERCERO
        ON SOLICITUDDISPONIBILIDAD.COMPANIA       = TERCERO.COMPANIA
        AND SOLICITUDDISPONIBILIDAD.TERCERO    = TERCERO.NIT
INNER JOIN RESPONSABLE
        ON TERCERO.COMPANIA       = RESPONSABLE.COMPANIA
        AND TERCERO.NIT    = RESPONSABLE.CEDULA  
WHERE 
        SOLICITUDDISPONIBILIDAD.COMPANIA='s$compania$s'
    AND SOLICITUDDISPONIBILIDAD.NUMERO=s$consecutivo$s
    AND SOLICITUDDISPONIBILIDAD.TIPO_CPTE_AFECT='s$tipot$s'
    AND BPTIPONOVEDAD.CLASET='B'
    ]') CONSULTA, 52 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);