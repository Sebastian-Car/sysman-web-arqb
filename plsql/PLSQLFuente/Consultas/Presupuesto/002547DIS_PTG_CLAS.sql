MERGE INTO CONSULTAS FIN USING (SELECT '002547DIS_PTG_CLAS' INFORME ,TO_CLOB(q'[SELECT 
COMPROBANTE_PPTAL.TIPO,
    DETALLE_COMPROBANTE_PPTAL.CUENTA,
CASE  WHEN PLAN_PRESUPUESTAL.MAN_AUX_GEN <> 0 THEN    
    PLAN_PRESUPUESTAL.NOMBRE ||  
    CASE WHEN COMPROBANTE_PPTAL.CENTRO_COSTO = '' THEN '' ELSE ' / ' || CENTRO_COSTO.NOMBRE END ||
    CASE WHEN COMPROBANTE_PPTAL.TERCERO = '' THEN '' ELSE ' / ' || TERCERO.NOMBRE END ||
    CASE WHEN COMPROBANTE_PPTAL.AUXILIAR = '' THEN '' ELSE ' / ' || AUXILIAR.NOMBRE END
ELSE PLAN_PRESUPUESTAL.NOMBRE END AS NOMBREFINAL,
REPLACE(DETALLE_COMPROBANTE_PPTAL.ID, ' ', '') AS CUENTAID,
DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO AUXILIAR,
DETALLE_COMPROBANTE_PPTAL.SECTOR SECTOR_CCP,
DETALLE_COMPROBANTE_PPTAL.PROGRAMA, 
DETALLE_COMPROBANTE_PPTAL.COD_PROD_CUIPO COD_PRODUCTO,
DETALLE_COMPROBANTE_PPTAL.CODIGO_CPC CPC_COD_DANE,
DETALLE_COMPROBANTE_PPTAL.CODIGOUNIDADEJE UNIDAD_EJECUTORA,
DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO VALORDIS, 
DETALLE_COMPROBANTE_PPTAL.REFERENCIA,
DETALLE_COMPROBANTE_PPTAL.CENTRO_COSTO, 
COMPROBANTE_PPTAL.COD_PROYECTO_PPTAL COMPROBANTE_PROYECTO, 
COMPROBANTE_PPTAL.CMPTE_AFECTADO,
TIPO_COMPROBPP.NOMBRE,
COMPROBANTE_PPTAL.NUMERO AS COMPROBANTE,
COMPROBANTE_PPTAL.FECHA,
COMPROBANTE_PPTAL.FECHA_VCN_DOC,
COMPROBANTE_PPTAL.DESCRIPCION,
COMPROBANTE_PPTAL.ANO,
SYSDATE, 
    TO_CHAR(COMPROBANTE_PPTAL.FECHA, 'DD') AS DIA,
    TO_CHAR(COMPROBANTE_PPTAL.FECHA, 'MM') AS MES,
    UPPER(PCK_SYSMAN_UTL.FC_NOMBRE_MES(TO_CHAR(COMPROBANTE_PPTAL.FECHA, 'MM'))) AS MESN,
    TO_CHAR(COMPROBANTE_PPTAL.FECHA, 'YYYY') AS ANIO

FROM COMPROBANTE_PPTAL 
LEFT JOIN DETALLE_COMPROBANTE_PPTAL
       ON COMPROBANTE_PPTAL.COMPANIA = DETALLE_COMPROBANTE_PPTAL.COMPANIA
       AND COMPROBANTE_PPTAL.ANO = DETALLE_COMPROBANTE_PPTAL.ANO
       AND COMPROBANTE_PPTAL.TIPO = DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE
       AND COMPROBANTE_PPTAL.NUMERO = DETALLE_COMPROBANTE_PPTAL.COMPROBANTE   
LEFT JOIN PLAN_PRESUPUESTAL
    ON DETALLE_COMPROBANTE_PPTAL.COMPANIA       = PLAN_PRESUPUESTAL.COMPANIA
    AND DETALLE_COMPROBANTE_PPTAL.ANO           = PLAN_PRESUPUESTAL.ANO	
    AND DETALLE_COMPROBANTE_PPTAL.CUENTA        = PLAN_PRESUPUESTAL.CODIGO

LEFT JOIN TIPO_COMPROBPP
    ON COMPROBANTE_PPTAL.COMPANIA = TIPO_COMPROBPP.COMPANIA
    AND COMPROBANTE_PPTAL.TIPO    = TIPO_COMPROBPP.CODIGO
 LEFT JOIN CENTRO_COSTO 
       ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = CENTRO_COSTO.COMPANIA
       AND DETALLE_COMPROBANTE_PPTAL.ANO = CENTRO_COSTO.ANO
       AND DETALLE_COMPROBANTE_PPTAL.CENTRO_COSTO = CENTRO_COSTO.CODIGO
    LEFT JOIN TERCERO 
       ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = TERCERO.COMPANIA
       AND DETALLE_COMPROBANTE_PPTAL.TERCERO = TERCERO.NIT
       AND DETALLE_COMPROBANTE_PPTAL.SUCURSAL = TERCERO.SUCURSAL
    LEFT JOIN AUXILIAR 
       ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = AUXILIAR.COMPANIA
       AND DETALLE_COMPROBANTE_PPTAL.ANO = AUXILIAR.ANO
       AND DETALLE_COMPROBANTE_PPTAL.AUXILIAR = AUXILIAR.CODIGO 
   WHERE COMPROBANTE_PPTAL.COMPANIA = s$compania$s 
   AND COMPROBANTE_PPTAL.ANO = s$ano$s
   AND COMPROB]') || TO_CLOB(q'[ANTE_PPTAL.TIPO = 's$tipoCpte$s' 
   AND COMPROBANTE_PPTAL.NUMERO BETWEEN s$numeroPptoInicial$s AND s$numeroPptoFinal$s]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'jdsalcedo' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);