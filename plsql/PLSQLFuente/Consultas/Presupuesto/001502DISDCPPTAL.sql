MERGE INTO CONSULTAS FIN USING (SELECT '001502DISDCPPTAL' INFORME ,TO_CLOB(q'[SELECT
NVL(PCK_SYSMAN_UTL.FC_PAR(V_DETALLE_AUXILIAR_PPTAL.COMPANIA,'OCULTAR FECHA VENCIMIENTO EN FORMATO DIS_DC',69,SYSDATE,-1),'NO') PARAMETRO,
TEXTO|| ' '|| NRO_DOCUMENTO  PARARESPALDAR,
CARGO,
COMPROBANTE,
FECHA,
FECHA_COM,
CUENTA,
CUENTANOMBRE,
TERCERO_NOM,
NVL(V_DETALLE_AUXILIAR_PPTAL.DEPENDENCIA ,' ')||' - '||NVL(DEPENDENCIA.NOMBRE,' ') DEPENDENCIA_NOM,
NVL(V_DETALLE_AUXILIAR_PPTAL.DEPENDENCIA,' ') DEPENDENCIA, 
FECHA_VCN_DOC,
NVL(VALOR_DEBITO,0) VALOR_DEBITO,
NVL(SALDO+ CASE WHEN VALOR_DEBITO=0 THEN VALOR_CREDITO ELSE VALOR_DEBITO END,0) SALDOO,
NVL(SALDO+ CASE WHEN VALOR_DEBITO=0 THEN VALOR_CREDITO ELSE VALOR_DEBITO END -  CASE WHEN VALOR_DEBITO=0 THEN VALOR_CREDITO ELSE VALOR_DEBITO END,0)   DISPONIBILIDADNETA1,
TIPO_COMPROBANTE_NOM,
CUENTA ||','||CONSECUTIVO AGRUPAMIENTO,
NVL(DISACUMULADAS,0) DISACUMULADAS
FROM V_DETALLE_AUXILIAR_PPTAL
LEFT JOIN DEPENDENCIA
ON  V_DETALLE_AUXILIAR_PPTAL.COMPANIA = DEPENDENCIA.COMPANIA 
AND V_DETALLE_AUXILIAR_PPTAL.DEPENDENCIA = DEPENDENCIA.CODIGO
WHERE V_DETALLE_AUXILIAR_PPTAL.COMPANIA = s$compania$s
AND V_DETALLE_AUXILIAR_PPTAL.ANO = s$ano$s
AND V_DETALLE_AUXILIAR_PPTAL.TIPO = 's$tipoCpte$s'
AND COMPROBANTE BETWEEN s$numeroPptoInicial$s AND s$numeroPptoFinal$s]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);