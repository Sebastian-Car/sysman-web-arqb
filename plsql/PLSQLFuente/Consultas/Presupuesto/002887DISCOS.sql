MERGE INTO CONSULTAS FIN USING (SELECT '002887DISCOS' INFORME ,TO_CLOB(q'[SELECT 
       DETALLE_COMPROBANTE_PPTAL.CONSECUTIVO,
       COMPROBANTE_PPTAL.FECHA_VCN_DOC,
       COMPROBANTE_PPTAL.DESCRIPCION,
       COMPROBANTE_PPTAL.TEXTO,
       DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO,
       V_PLAN_PRESUPUESTAL.NOMBRE
       || (CASE WHEN MAN_CEN_CTO = 0 THEN '' ELSE ' / ' || CENTRO_COSTO.NOMBRE END)
       || (CASE WHEN MAN_AUX_TER = 0 THEN '' ELSE ' / ' || TERCERODETALLE.NOMBRE END)
       || (CASE WHEN MAN_AUX_GEN = 0 THEN '' ELSE ' / ' || AUXILIAR.NOMBRE END)
       || (CASE WHEN MAN_AUX_REF = 0 THEN '' ELSE ' / ' || REFERENCIA.NOMBRE END)
       || (CASE WHEN MAN_AUX_FUE = 0 THEN '' ELSE ' / ' || FUENTE_RECURSOS.NOMBRE END) AS NOMBRE,
       DETALLE_COMPROBANTE_PPTAL.CUENTA CODIGO_CUENTA,
       TERCERO.NOMBRE NOMBRE_TERCERO,
       COMPROBANTE_PPTAL.NUMERO COMPROBANTE,
       TO_CHAR(COMPROBANTE_PPTAL.FECHA, 'DD/MM/YYYY') FECHA,
       COMPROBANTE_PPTAL.TERCERO,
       COMPROBANTE_PPTAL.SUCURSAL,
       TIPO_COMPROBPP.NOMBRE AS NOMBRE_TIPO,
       COMPROBANTE_PPTAL.CREATED_BY CREADOR
  FROM COMPROBANTE_PPTAL
     INNER JOIN TERCERO
     ON  COMPROBANTE_PPTAL.COMPANIA  = TERCERO.COMPANIA
       AND COMPROBANTE_PPTAL.TERCERO  = TERCERO.NIT
       AND COMPROBANTE_PPTAL.SUCURSAL = TERCERO.SUCURSAL
       INNER JOIN DETALLE_COMPROBANTE_PPTAL
       ON  COMPROBANTE_PPTAL.COMPANIA = DETALLE_COMPROBANTE_PPTAL.COMPANIA
       AND COMPROBANTE_PPTAL.ANO    = DETALLE_COMPROBANTE_PPTAL.ANO
       AND COMPROBANTE_PPTAL.TIPO   = DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE
       AND COMPROBANTE_PPTAL.NUMERO = DETALLE_COMPROBANTE_PPTAL.COMPROBANTE
       INNER JOIN V_PLAN_PRESUPUESTAL
       ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = V_PLAN_PRESUPUESTAL.COMPANIA
       AND DETALLE_COMPROBANTE_PPTAL.ANO = V_PLAN_PRESUPUESTAL.ANO
       AND DETALLE_COMPROBANTE_PPTAL.CUENTA = V_PLAN_PRESUPUESTAL.ID
       LEFT JOIN COMPROBANTE_PPTALAFECTADOS CPA
       ON  COMPROBANTE_PPTAL.COMPANIA = CPA.COMPANIA
       AND COMPROBANTE_PPTAL.ANO    = CPA.ANO
       AND COMPROBANTE_PPTAL.TIPO   = CPA.TIPO_CPTE
       AND COMPROBANTE_PPTAL.NUMERO = CPA.COMPROBANTE
       LEFT JOIN CENTRO_COSTO
       ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = CENTRO_COSTO.COMPANIA
       AND DETALLE_COMPROBANTE_PPTAL.ANO = CENTRO_COSTO.ANO
       AND DETALLE_COMPROBANTE_PPTAL.CENTRO_COSTO = CENTRO_COSTO.CODIGO
       LEFT JOIN AUXILIAR
       ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = AUXILIAR.COMPANIA
       AND DETALLE_COMPROBANTE_PPTAL.ANO = AUXILIAR.ANO
       AND DETALLE_COMPROBANTE_PPTAL.AUXILIAR = AUXILIAR.CODIGO
       LEFT JOIN FUENTE_RECURSOS
       ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = FUENTE_RECURSOS.COMPANIA
       AND DETALLE_COMPROBANTE_PPTAL.ANO = FUENTE_RECURSOS.ANO
       AND DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO = FUENTE_RECURSOS.CODIGO
       LEFT JOIN REFERENCIA
       ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = REFERENCIA.COMPANIA
       AND DETALLE_COMPROBANTE_PPTAL.ANO = REFERENCIA.ANO
       AND DETALLE_COMPROBANTE_PPTAL.REFERENCIA = REFERENCIA.CODIGO
       LEFT JOIN TERCERO TERCE]') || TO_CLOB(q'[RODETALLE
       ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = TERCERODETALLE.COMPANIA
       AND DETALLE_COMPROBANTE_PPTAL.TERCERO = TERCERODETALLE.NIT
       AND DETALLE_COMPROBANTE_PPTAL.SUCURSAL = TERCERODETALLE.SUCURSAL
       INNER JOIN TIPO_COMPROBPP
       ON COMPROBANTE_PPTAL.COMPANIA = TIPO_COMPROBPP.COMPANIA
       AND COMPROBANTE_PPTAL.TIPO = TIPO_COMPROBPP.CODIGO  
WHERE COMPROBANTE_PPTAL.COMPANIA = s$compania$s
   AND COMPROBANTE_PPTAL.ANO = s$ano$s
   AND COMPROBANTE_PPTAL.TIPO = 's$tipoCpte$s'
   AND COMPROBANTE_PPTAL.NUMERO BETWEEN s$numeroPptoInicial$s AND s$numeroPptoFinal$s
  ORDER BY COMPROBANTE_PPTAL.NUMERO ASC]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'calarcon' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);