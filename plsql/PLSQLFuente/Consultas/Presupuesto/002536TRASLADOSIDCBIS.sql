MERGE INTO CONSULTAS FIN USING (SELECT '002536TRASLADOSIDCBIS' INFORME ,TO_CLOB(q'[SELECT COMPANIA,
  ANO,
  TIPO,
  COMPROBANTE NUMERO,
  FECHA_COM FECHA,
  TERCERO_COM TERCERO,
  SUCURSAL_COM SUCURSAL,
  TERCERO_COM_NOM NOMBRE,
  TIPO_COMPROBANTE_NOM  NOMBRETIPO,
  CUENTANOMBRE AS NOMBRECUENTA,
  DESCRIPCION,
  CUENTA,
  VALOR_CREDITO AS CONTRACREDITO,
  VALOR_DEBITO  AS CREDITO  
FROM V_DETALLE_AUXILIAR_PPTAL
WHERE COMPANIA = s$compania$s
    AND ANO    = s$ano$s
    AND TIPO   = 's$tipoCpte$s'
    AND COMPROBANTE BETWEEN s$numeroPptoInicial$s AND s$numeroPptoFinal$s
]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'mrosero' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);