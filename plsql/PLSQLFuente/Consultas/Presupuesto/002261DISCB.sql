MERGE INTO CONSULTAS FIN USING (SELECT '002261DISCB' INFORME ,TO_CLOB(q'[SELECT
TIPO_COMPROBPP.NOMBRE NOMBRECOMPROBANTE,
DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE  AS TIPO,
TO_CHAR(COMPROBANTE_PPTAL.FECHA,'DD/MM/YYYY') AS FECHA,
 TERCERO.NIT AS TERCERO,
  TERCERO.NOMBRE NOMBRE_TERCERO,
COMPROBANTE_PPTAL.SUCURSAL,
DEPENDENCIA.CODIGO ,
DEPENDENCIA.NOMBRE DEPENDENCIA,
COMPROBANTE_PPTAL.CARGO,
DETALLE_COMPROBANTE_PPTAL.CUENTA RUBRO,
COMPROBANTE_PPTAL.TIPO,
 TRIM(PLAN_PRESUPUESTAL.NOMBRE)
  || CASE NVL(FUENTE_RECURSOS.CODIGO,'')
   WHEN ''
   THEN ''
   ELSE' / ' || NVL(FUENTE_RECURSOS.NOMBRE,'')
   END
   || CASE NVL(REFERENCIA.CODIGO,'')
   WHEN ''
   THEN ''
   ELSE' / ' || NVL(REFERENCIA.NOMBRE,'')
   END  AS TOTALNOMBRE,
       TRIM(PLAN_PRESUPUESTAL.NOMBRE)
  || CASE NVL(FUENTE_RECURSOS.CODIGO,'')
   WHEN ''
   THEN ''
   ELSE' / ' || NVL(FUENTE_RECURSOS.NOMBRE,'')
   END
   || CASE NVL(REFERENCIA.CODIGO,'')
   WHEN ''
   THEN ''
   ELSE' / ' || NVL(REFERENCIA.NOMBRE,'')
   END  
   || CASE NVL(AUXILIAR.CODIGO,'')
   WHEN ''
   THEN ''
   ELSE' / ' || NVL(AUXILIAR.NOMBRE,'')
   END AS TOTALNOMBREAUX,
  DETALLE_COMPROBANTE_PPTAL.CUENTA CODIGOCUENTA,
  DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO,
 DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO,
  DETALLE_COMPROBANTE_PPTAL.AUXILIAR,
 DETALLE_COMPROBANTE_PPTAL.REFERENCIA,
 COMPROBANTE_PPTAL.DESCRIPCION,
 COMPROBANTE_PPTAL.TEXTO,
 TO_CHAR(COMPROBANTE_PPTAL.FECHA_VCN_DOC,'DD/MM/YYYY') AS FECHA_VCN_DOC,
COMPROBANTE_PPTAL.NUMERO COMPROBANTE,
DETALLE_COMPROBANTE_PPTAL.CODIGO_CPC,
COMPROBANTE_PPTAL.FICHA
FROM TIPO_COMPROBPP
    INNER JOIN COMPROBANTE_PPTAL
   ON TIPO_COMPROBPP.CODIGO    = COMPROBANTE_PPTAL.TIPO
    AND TIPO_COMPROBPP.COMPANIA = COMPROBANTE_PPTAL.COMPANIA
INNER JOIN DETALLE_COMPROBANTE_PPTAL
    ON COMPROBANTE_PPTAL.NUMERO    = DETALLE_COMPROBANTE_PPTAL.COMPROBANTE
       AND COMPROBANTE_PPTAL.TIPO     = DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE
       AND COMPROBANTE_PPTAL.ANO      = DETALLE_COMPROBANTE_PPTAL.ANO
       AND COMPROBANTE_PPTAL.COMPANIA = DETALLE_COMPROBANTE_PPTAL.COMPANIA
    INNER JOIN TERCERO
    ON COMPROBANTE_PPTAL.SUCURSAL  = TERCERO.SUCURSAL
        AND COMPROBANTE_PPTAL.TERCERO  = TERCERO.NIT
        AND COMPROBANTE_PPTAL.COMPANIA = TERCERO.COMPANIA
    INNER JOIN CENTRO_COSTO
    ON DETALLE_COMPROBANTE_PPTAL.CENTRO_COSTO = CENTRO_COSTO.CODIGO
     AND DETALLE_COMPROBANTE_PPTAL.ANO = CENTRO_COSTO.ANO
    AND DETALLE_COMPROBANTE_PPTAL.COMPANIA    = CENTRO_COSTO.COMPANIA
    INNER JOIN PLAN_PRESUPUESTAL
    ON DETALLE_COMPROBANTE_PPTAL.ANO            = PLAN_PRESUPUESTAL.ANO
        AND DETALLE_COMPROBANTE_PPTAL.CUENTA = PLAN_PRESUPUESTAL.CODIGO
     AND DETALLE_COMPROBANTE_PPTAL.COMPANIA      = PLAN_PRESUPUESTAL.COMPANIA
    INNER JOIN AUXILIAR
    ON DETALLE_COMPROBANTE_PPTAL.AUXILIAR  = AUXILIAR.CODIGO
    AND  DETALLE_COMPROBANTE_PPTAL.ANO = AUXILIAR.ANO
    AND DETALLE_COMPROBANTE_PPTAL.COMPANIA = AUXILIAR.COMPANIA
    LEFT JOIN DEPENDENCIA
ON COMPROBANTE_PPTAL.DEPENDENCIA = DEPENDENCIA.CODIGO
AND COMPROBANTE_PPTAL.COMPANIA = DEPENDENCIA.COMPANIA
INNER JOIN F]') || TO_CLOB(q'[UENTE_RECURSOS
     ON DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO  = FUENTE_RECURSOS.CODIGO
    AND DETALLE_COMPROBANTE_PPTAL.ANO = FUENTE_RECURSOS.ANO
    AND DETALLE_COMPROBANTE_PPTAL.COMPANIA = FUENTE_RECURSOS.COMPANIA
     INNER JOIN REFERENCIA
     ON DETALLE_COMPROBANTE_PPTAL.REFERENCIA  = REFERENCIA.CODIGO
    AND DETALLE_COMPROBANTE_PPTAL.ANO = REFERENCIA.ANO
    AND DETALLE_COMPROBANTE_PPTAL.COMPANIA = REFERENCIA.COMPANIA
WHERE COMPROBANTE_PPTAL.COMPANIA = s$compania$s
AND COMPROBANTE_PPTAL.ANO = s$ano$s
AND COMPROBANTE_PPTAL.TIPO = 's$tipoCpte$s'
AND COMPROBANTE_PPTAL.NUMERO BETWEEN s$numeroPptoInicial$s AND s$numeroPptoFinal$s]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);