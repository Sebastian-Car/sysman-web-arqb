MERGE INTO CONSULTAS FIN USING (SELECT '001818LisReoAbiertosCuentasF' INFORME ,TO_CLOB(q'[SELECT 
    DETALLE_COMPROBANTE_PPTAL.REFERENCIA,
    DETALLE_COMPROBANTE_PPTAL.SECTOR,
    DETALLE_COMPROBANTE_PPTAL.CODIGO_CPC,
    DETALLE_COMPROBANTE_PPTAL.PROGRAMA,
    COMPROBANTE_PPTAL.DEPENDENCIA,
    DEPENDENCIA.NOMBRE AS NOMBRE_DEPENDENCIA,
    DETALLE_COMPROBANTE_PPTAL.COD_PROD_CUIPO AS PRODUCTO,
    DETALLE_COMPROBANTE_PPTAL.FECHA FECHA,
    DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO,
    DETALLE_COMPROBANTE_PPTAL.AUXILIAR,
    DETALLE_COMPROBANTE_PPTAL.CENTRO_COSTO,
    DETALLE_COMPROBANTE_PPTAL.TERCERO CODTERCERO, 
    DETALLE_COMPROBANTE_PPTAL.COMPROBANTE,    
    SUM((DETALLE_COMPROBANTE_PPTAL.DEBITO_AFECTADO-DETALLE_COMPROBANTE_PPTAL.CREDITO_AFECTADO)) AFECTACION, 
    SUM((DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO-DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)) MOVIMIENTO,
    SUM((DETALLE_COMPROBANTE_PPTAL.MODIFICACION_DEBITO-DETALLE_COMPROBANTE_PPTAL.MODIFICACION_CREDITO)) MODIFICACION, 
    TERCERO.NOMBRE NOMBRETERCERO, 
    DETALLE_COMPROBANTE_PPTAL.SUCURSAL CODSUCURSAL, 
    CASE 
      WHEN 's$tipoCuenta$s'='1'
      THEN DETALLE_COMPROBANTE_PPTAL.ID
      ELSE SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL1,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL2,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL3,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL4,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL5,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL6,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL7,''),0,3)
    END RUBRO,
    CASE
      WHEN 's$tipoCuenta$s'='1'
      THEN 'Codigo cuenta'
      ELSE 'Fid-Une-Prog-Spr-Proy-Dis-Rec'
    END TITULOCUENTA,

  DETALLE_COMPROBANTE_PPTAL.DESCRIPCION, 
  PLAN_PRESUPUESTAL.NOMBRE PLAN_PPTAL_CUENTA_NOM, 
  TIPO_COMPROBPP.NOMBRE TIPO_COMPROBPP_NOM, 
  DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE, 
  SUM((DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO-DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)-(DETALLE_COMPROBANTE_PPTAL.DEBITO_AFECTADO-DETALLE_COMPROBANTE_PPTAL.CREDITO_AFECTADO)+(DETALLE_COMPROBANTE_PPTAL.MODIFICACION_DEBITO-DETALLE_COMPROBANTE_PPTAL.MODIFICACION_CREDITO)) SALDO,
  DETALLE_COMPROBANTE_PPTAL.COMPANIA 
FROM DETALLE_COMPROBANTE_PPTAL
 INNER JOIN  PLAN_PRESUPUESTAL
  ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = PLAN_PRESUPUESTAL.COMPANIA
  AND DETALLE_COMPROBANTE_PPTAL.ANO      = PLAN_PRESUPUESTAL.ANO
  AND DETALLE_COMPROBANTE_PPTAL.CUENTA   = PLAN_PRESUPUESTAL.CODIGO
 INNER JOIN TERCERO
  ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = TERCERO.COMPANIA 
  AND DETALLE_COMPROBANTE_PPTAL.TERCERO  = TERCERO.NIT 
  AND DETALLE_COMPROBANTE_PPTAL.SUCURSAL = TERCERO.SUCURSAL 
 INNER JOIN TIPO_COMPROBPP
  ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA   = TIPO_COMPROBPP.COMPANIA
  AND DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE = TIPO_COMPROBPP.CODIGO
  LEFT JOIN COMPROBANTE_PPTAL
  ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA   = COMPROBANTE_PPTAL.COMPANIA 
  AND DETALLE_COMPROBANTE_PPTAL.ANO        = COMPROBANTE_PPTAL.ANO
  AND DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE  = COMPROBANTE_PPTAL.TIPO
  AND DETALLE_COMPROBANTE_PPT]') || TO_CLOB(q'[AL.COMPROBANTE = COMPROBANTE_PPTAL.NUMERO
 LEFT JOIN DEPENDENCIA
  ON COMPROBANTE_PPTAL.COMPANIA   = DEPENDENCIA.COMPANIA 
   AND  COMPROBANTE_PPTAL.DEPENDENCIA   = DEPENDENCIA.CODIGO  
WHERE DETALLE_COMPROBANTE_PPTAL.COMPANIA = s$compania$s 
  AND TIPO_COMPROBPP.CLASE               = 'REO'
  AND DETALLE_COMPROBANTE_PPTAL.CUENTA    BETWEEN 's$cuentaInicial$s' AND 's$cuentaFinal$s'
  AND DETALLE_COMPROBANTE_PPTAL.FECHA     BETWEEN TO_DATE('s$fechaInicial$s', 'DD/MM/YYYY') AND TO_DATE('s$fechaFinal$s', 'DD/MM/YYYY')
  AND DETALLE_COMPROBANTE_PPTAL.TERCERO   BETWEEN 's$terceroInicial$s' AND 's$terceroFinal$s' 
GROUP BY 
DETALLE_COMPROBANTE_PPTAL.REFERENCIA,
DETALLE_COMPROBANTE_PPTAL.SECTOR,
DETALLE_COMPROBANTE_PPTAL.CODIGO_CPC,
DETALLE_COMPROBANTE_PPTAL.PROGRAMA,
COMPROBANTE_PPTAL.DEPENDENCIA,
DEPENDENCIA.NOMBRE,
DETALLE_COMPROBANTE_PPTAL.COD_PROD_CUIPO,
  DETALLE_COMPROBANTE_PPTAL.COMPANIA,
  DETALLE_COMPROBANTE_PPTAL.ANO, 
  DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE, 
  DETALLE_COMPROBANTE_PPTAL.COMPROBANTE, 
  DETALLE_COMPROBANTE_PPTAL.DESCRIPCION, 
  DETALLE_COMPROBANTE_PPTAL.FECHA,
  DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO,
  DETALLE_COMPROBANTE_PPTAL.AUXILIAR,
  DETALLE_COMPROBANTE_PPTAL.CENTRO_COSTO,
  TIPO_COMPROBPP.CLASE, 
  DETALLE_COMPROBANTE_PPTAL.TERCERO, 
  DETALLE_COMPROBANTE_PPTAL.SUCURSAL, 
  TERCERO.NOMBRE, 
  TIPO_COMPROBPP.NOMBRE, 
  PLAN_PRESUPUESTAL.NOMBRE, 
  DETALLE_COMPROBANTE_PPTAL.ID, 
  DETALLE_COMPROBANTE_PPTAL.CUENTA,  
  CASE 
      WHEN 's$tipoCuenta$s'='1'
      THEN DETALLE_COMPROBANTE_PPTAL.ID
      ELSE SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL1,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL2,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL3,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL4,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL5,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL6,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL7,''),0,3)
    END,
    CASE
      WHEN 's$tipoCuenta$s'='1'
      THEN 'Codigo cuenta'
      ELSE 'Fid-Une-Prog-Spr-Proy-Dis-Rec'
    END
HAVING SUM((DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO-DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)-(DETALLE_COMPROBANTE_PPTAL.DEBITO_AFECTADO-DETALLE_COMPROBANTE_PPTAL.CREDITO_AFECTADO)+(DETALLE_COMPROBANTE_PPTAL.MODIFICACION_DEBITO-DETALLE_COMPROBANTE_PPTAL.MODIFICACION_CREDITO)) NOT IN (0)

UNION ALL
(
SELECT DISTINCT 
    REG_OBLIGACION_ABIERTOS.REFERENCIA,
    REG_OBLIGACION_ABIERTOS.SECTOR,
    REG_OBLIGACION_ABIERTOS.CODIGO_CPC,
    REG_OBLIGACION_ABIERTOS.PROGRAMA,
    REG_OBLIGACION_ABIERTOS.DEPENDENCIA,
    REG_OBLIGACION_ABIERTOS.NOMBRE AS NOMBRE_DEPENDENCIA,
    REG_OBLIGACION_ABIERTOS.PRODUCTO,
    REG_OBLIGACION_ABIERTOS.FECHA FECHA,
    REG_OBLIGACION_ABIERTOS.FUENTE_RECURSO,
    REG_OBLIGACION_ABIERTOS.AUXILIAR,
    REG_OBLIGACION_ABIERTOS.CENTRO_COSTO,
    REG_OBLIGACION_ABIERTOS.CODTERCERO,
    REG_OBLIGACION_ABIERTOS.COMPROBANTE,
    (REG_OBLIGACION_ABIERTOS.SUMADEDEBITO_AFECTADO-REG_EGRESOS_ABIERTOS.SUMADEVALOR_DEBITO)-(REG_OBLIGACION_ABIERTOS.SUMADECREDITO_AFECTADO-REG_EGRESOS_ABIERTOS.SU]') || TO_CLOB(q'[MADEVALOR_CREDITO) AFECTACION,
    REG_OBLIGACION_ABIERTOS.MOVIMIENTO,
    REG_OBLIGACION_ABIERTOS.MODIFICACION,
    REG_OBLIGACION_ABIERTOS.NOMBRETERCERO,
    REG_OBLIGACION_ABIERTOS.CODSUCURSAL,
    REG_OBLIGACION_ABIERTOS.RUBRO,
    REG_OBLIGACION_ABIERTOS.TITULOCUENTA,
    REG_OBLIGACION_ABIERTOS.DESCRIPCION,
    REG_OBLIGACION_ABIERTOS.NOMBRE PLAN_PPTAL_CUENTA_NOM,
    REG_OBLIGACION_ABIERTOS.TIPO_COMPROBPP_NOM,
    REG_OBLIGACION_ABIERTOS.TIPO_CPTE,
    (NVL(REG_EGRESOS_ABIERTOS.SUMADEVALOR_DEBITO,0)-NVL(REG_EGRESOS_ABIERTOS.SUMADEVALOR_CREDITO,0))-+(NVL(REG_EGRESOS_ABIERTOS.SUMADEMODIFICACION_DEBITO,0)-NVL(REG_EGRESOS_ABIERTOS.SUMADEMODIFICACION_CREDITO,0))+REG_OBLIGACION_ABIERTOS.SALDO SALDO, 
    REG_OBLIGACION_ABIERTOS.COMPANIA
FROM (
SELECT DETALLE_COMPROBANTE_PPTAL.COMPANIA,
DETALLE_COMPROBANTE_PPTAL.AUXILIAR,
DETALLE_COMPROBANTE_PPTAL.CENTRO_COSTO,
DETALLE_COMPROBANTE_PPTAL.REFERENCIA,
    DETALLE_COMPROBANTE_PPTAL.ANO,
    DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE,
    DETALLE_COMPROBANTE_PPTAL.COMPROBANTE,
    DETALLE_COMPROBANTE_PPTAL.DESCRIPCION,
    DETALLE_COMPROBANTE_PPTAL.FECHA,
    DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO,
    TIPO_COMPROBPP.CLASE,
    SUM(DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO) SUMADEVALOR_DEBITO,
    SUM(DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO) SUMADEVALOR_CREDITO, 
    SUM(DETALLE_COMPROBANTE_PPTAL.DEBITO_AFECTADO) SUMADEDEBITO_AFECTADO, 
    SUM(DETALLE_COMPROBANTE_PPTAL.CREDITO_AFECTADO) SUMADECREDITO_AFECTADO, 
    SUM(DETALLE_COMPROBANTE_PPTAL.MODIFICACION_DEBITO) SUMADEMODIFICACION_DEBITO, 
    SUM(DETALLE_COMPROBANTE_PPTAL.MODIFICACION_CREDITO) SUMADEMODIFICACION_CREDITO, 
    SUM((DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO-DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)-(DETALLE_COMPROBANTE_PPTAL.DEBITO_AFECTADO-DETALLE_COMPROBANTE_PPTAL.CREDITO_AFECTADO)+(DETALLE_COMPROBANTE_PPTAL.MODIFICACION_DEBITO-DETALLE_COMPROBANTE_PPTAL.MODIFICACION_CREDITO)) SALDO, 
    DETALLE_COMPROBANTE_PPTAL.TERCERO CODTERCERO, 
    DETALLE_COMPROBANTE_PPTAL.SUCURSAL CODSUCURSAL, 
    TERCERO.NOMBRE NOMBRETERCERO, 
    TIPO_COMPROBPP.NOMBRE TIPO_COMPROBPP_NOM, 
    SUM(DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO-DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO) MOVIMIENTO, 
    SUM(DETALLE_COMPROBANTE_PPTAL.DEBITO_AFECTADO-DETALLE_COMPROBANTE_PPTAL.CREDITO_AFECTADO) AFECTACION, 
    SUM(DETALLE_COMPROBANTE_PPTAL.MODIFICACION_DEBITO-DETALLE_COMPROBANTE_PPTAL.MODIFICACION_CREDITO) MODIFICACION, 
    PLAN_PRESUPUESTAL_CUENTA.NOMBRE PLAN_PPTAL_CUENTA_NOM, 
    DETALLE_COMPROBANTE_PPTAL.ID CODIGO_CUENTA, 
    DETALLE_COMPROBANTE_PPTAL.CUENTA,
    CASE
      WHEN 's$tipoCuenta$s'='1'
      THEN DETALLE_COMPROBANTE_PPTAL.ID
      ELSE SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL1,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL2,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL3,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL4,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL5,''),0,3)
       ]') || TO_CLOB(q'[ ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL6,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL7,''),0,3)
    END RUBRO,
    CASE
      WHEN 's$tipoCuenta$s'='1'
      THEN 'Codigo cuenta'
      ELSE 'Fid-Une-Prog-Spr-Proy-Dis-Rec'
    END TITULOCUENTA,
    DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE_AFECT,
    DETALLE_COMPROBANTE_PPTAL.CMPTE_AFECTADO,
    DETALLE_COMPROBANTE_PPTAL.CONSECUTIVOPPTO
  FROM DETALLE_COMPROBANTE_PPTAL
  LEFT JOIN TERCERO
  ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA  = TERCERO.COMPANIA
  AND DETALLE_COMPROBANTE_PPTAL.TERCERO  = TERCERO.NIT 
  AND DETALLE_COMPROBANTE_PPTAL.SUCURSAL = TERCERO.SUCURSAL 
  INNER JOIN TIPO_COMPROBPP 
  ON DETALLE_COMPROBANTE_PPTAL.COMPANIA   = TIPO_COMPROBPP.COMPANIA
  AND DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE = TIPO_COMPROBPP.CODIGO
  LEFT JOIN PLAN_PRESUPUESTAL
  ON DETALLE_COMPROBANTE_PPTAL.COMPANIA = PLAN_PRESUPUESTAL.COMPANIA 
  AND DETALLE_COMPROBANTE_PPTAL.ANO     = PLAN_PRESUPUESTAL.ANO 
  AND DETALLE_COMPROBANTE_PPTAL.CUENTA      = PLAN_PRESUPUESTAL.CODIGO 
  LEFT JOIN PLAN_PRESUPUESTAL PLAN_PRESUPUESTAL_CUENTA 
  ON DETALLE_COMPROBANTE_PPTAL.COMPANIA = PLAN_PRESUPUESTAL_CUENTA.COMPANIA 
  AND DETALLE_COMPROBANTE_PPTAL.ANO     = PLAN_PRESUPUESTAL_CUENTA.ANO 
  AND DETALLE_COMPROBANTE_PPTAL.CUENTA      = PLAN_PRESUPUESTAL_CUENTA.CODIGO 
  WHERE DETALLE_COMPROBANTE_PPTAL.COMPANIA    = s$compania$s 
    AND TIPO_COMPROBPP.CLASE                  = 'EGR'
    AND DETALLE_COMPROBANTE_PPTAL.FECHA  > TO_DATE('s$fechaFinal$s', 'DD/MM/YYYY')
  GROUP BY DETALLE_COMPROBANTE_PPTAL.COMPANIA, 
DETALLE_COMPROBANTE_PPTAL.AUXILIAR,
DETALLE_COMPROBANTE_PPTAL.CENTRO_COSTO,
DETALLE_COMPROBANTE_PPTAL.REFERENCIA,  
    DETALLE_COMPROBANTE_PPTAL.ANO,
    DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE,
    DETALLE_COMPROBANTE_PPTAL.COMPROBANTE,
    DETALLE_COMPROBANTE_PPTAL.DESCRIPCION,
    DETALLE_COMPROBANTE_PPTAL.FECHA,
    DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO,
    TIPO_COMPROBPP.CLASE,
    DETALLE_COMPROBANTE_PPTAL.TERCERO,
    DETALLE_COMPROBANTE_PPTAL.SUCURSAL,
    TERCERO.NOMBRE,
    TIPO_COMPROBPP.NOMBRE,
    PLAN_PRESUPUESTAL_CUENTA.NOMBRE,
    DETALLE_COMPROBANTE_PPTAL.ID,
    DETALLE_COMPROBANTE_PPTAL.CUENTA,
    CASE
      WHEN 's$tipoCuenta$s'='1'
      THEN DETALLE_COMPROBANTE_PPTAL.ID
      ELSE SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL1,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL2,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL3,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL4,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL5,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL6,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL7,''),0,3)
    END,
    CASE
      WHEN 's$tipoCuenta$s'='1'
      THEN 'Codigo cuenta'
      ELSE 'Fid-Une-Prog-Spr-Proy-Dis-Rec'
    END,
    DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE_AFECT,
    DETALLE_COMPROBANTE_PPTAL.CMPTE_AFECTADO,
    DETALLE_COMPROBANTE_P]') || TO_CLOB(q'[PTAL.CONSECUTIVOPPTO
)REG_EGRESOS_ABIERTOS 
LEFT JOIN 
(
SELECT DETALLE_COMPROBANTE_PPTAL.COMPANIA, 
    DETALLE_COMPROBANTE_PPTAL.AUXILIAR,
    DETALLE_COMPROBANTE_PPTAL.CENTRO_COSTO,
    DETALLE_COMPROBANTE_PPTAL.REFERENCIA,
    DETALLE_COMPROBANTE_PPTAL.SECTOR,
    DETALLE_COMPROBANTE_PPTAL.CODIGO_CPC,
    DETALLE_COMPROBANTE_PPTAL.PROGRAMA,
    COMPROBANTE_PPTAL.DEPENDENCIA,
    DEPENDENCIA.NOMBRE AS NOMBRE_DEPENDENCIA,
    DETALLE_COMPROBANTE_PPTAL.COD_PROD_CUIPO AS PRODUCTO,
    DETALLE_COMPROBANTE_PPTAL.ANO,
    DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE,
    DETALLE_COMPROBANTE_PPTAL.COMPROBANTE,
    DETALLE_COMPROBANTE_PPTAL.DESCRIPCION,
    DETALLE_COMPROBANTE_PPTAL.CONSECUTIVO,
    DETALLE_COMPROBANTE_PPTAL.FECHA,
    DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO,
    TIPO_COMPROBPP.CLASE,
    SUM(DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO) SUMADEVALOR_DEBITO,
    SUM(DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO) SUMADEVALOR_CREDITO,
    SUM(DETALLE_COMPROBANTE_PPTAL.DEBITO_AFECTADO) SUMADEDEBITO_AFECTADO,
    SUM(DETALLE_COMPROBANTE_PPTAL.CREDITO_AFECTADO) SUMADECREDITO_AFECTADO,
    SUM(DETALLE_COMPROBANTE_PPTAL.MODIFICACION_DEBITO) SUMADEMODIFICACION_DEBITO,
    SUM(DETALLE_COMPROBANTE_PPTAL.MODIFICACION_CREDITO) SUMADEMODIFICACION_CREDITO,
    SUM((DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO-DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)-(DETALLE_COMPROBANTE_PPTAL.DEBITO_AFECTADO-DETALLE_COMPROBANTE_PPTAL.CREDITO_AFECTADO)+(DETALLE_COMPROBANTE_PPTAL.MODIFICACION_DEBITO-DETALLE_COMPROBANTE_PPTAL.MODIFICACION_CREDITO)) SALDO,
    DETALLE_COMPROBANTE_PPTAL.TERCERO CODTERCERO,  
    DETALLE_COMPROBANTE_PPTAL.SUCURSAL CODSUCURSAL,
    TERCERO.NOMBRE NOMBRETERCERO,
    TIPO_COMPROBPP.NOMBRE TIPO_COMPROBPP_NOM,
    SUM(DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO-DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO) MOVIMIENTO,
    SUM(DETALLE_COMPROBANTE_PPTAL.DEBITO_AFECTADO-DETALLE_COMPROBANTE_PPTAL.CREDITO_AFECTADO) AFECTACION, 
    SUM(DETALLE_COMPROBANTE_PPTAL.MODIFICACION_DEBITO-DETALLE_COMPROBANTE_PPTAL.MODIFICACION_CREDITO) MODIFICACION, 
    PLAN_PRESUPUESTAL_CUENTA.NOMBRE ,
    DETALLE_COMPROBANTE_PPTAL.ID CODIGO_CUENTA,
    DETALLE_COMPROBANTE_PPTAL.CUENTA,
    CASE
      WHEN 's$tipoCuenta$s'='1'
      THEN DETALLE_COMPROBANTE_PPTAL.ID
      ELSE SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL1,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL2,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL3,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL4,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL5,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL6,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL7,''),0,3)
    END RUBRO,
    CASE
      WHEN 's$tipoCuenta$s'='1'
      THEN 'Codigo Cuenta'
      ELSE 'Fid-Une-Prog-Spr-Proy-Dis-Rec'
    END TITULOCUENTA
  FROM DETALLE_COMPROBANTE_PPTAL
  LEFT JOIN TERCERO
  ON DETALLE_COMPROBANTE_PPTAL.COMPANIA  = TERCERO.COMPANIA
  AND DETALLE_COMPROBANT]') || TO_CLOB(q'[E_PPTAL.SUCURSAL = TERCERO.SUCURSAL
  AND DETALLE_COMPROBANTE_PPTAL.TERCERO  = TERCERO.NIT
  INNER JOIN TIPO_COMPROBPP
  ON DETALLE_COMPROBANTE_PPTAL.COMPANIA   = TIPO_COMPROBPP.COMPANIA
  AND DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE = TIPO_COMPROBPP.CODIGO
  LEFT JOIN PLAN_PRESUPUESTAL
  ON DETALLE_COMPROBANTE_PPTAL.COMPANIA = PLAN_PRESUPUESTAL.COMPANIA
  AND DETALLE_COMPROBANTE_PPTAL.ANO     = PLAN_PRESUPUESTAL.ANO
  AND DETALLE_COMPROBANTE_PPTAL.CUENTA  = PLAN_PRESUPUESTAL.CODIGO
  LEFT JOIN PLAN_PRESUPUESTAL PLAN_PRESUPUESTAL_CUENTA
  ON DETALLE_COMPROBANTE_PPTAL.COMPANIA = PLAN_PRESUPUESTAL_CUENTA.COMPANIA
  AND DETALLE_COMPROBANTE_PPTAL.ANO     = PLAN_PRESUPUESTAL_CUENTA.ANO
  AND DETALLE_COMPROBANTE_PPTAL.CUENTA      = PLAN_PRESUPUESTAL_CUENTA.CODIGO
    LEFT JOIN COMPROBANTE_PPTAL
  ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA   = COMPROBANTE_PPTAL.COMPANIA 
  AND DETALLE_COMPROBANTE_PPTAL.ANO        = COMPROBANTE_PPTAL.ANO
  AND DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE  = COMPROBANTE_PPTAL.TIPO
  AND DETALLE_COMPROBANTE_PPTAL.COMPROBANTE = COMPROBANTE_PPTAL.NUMERO
 LEFT JOIN DEPENDENCIA
  ON COMPROBANTE_PPTAL.COMPANIA   = DEPENDENCIA.COMPANIA 
   AND  COMPROBANTE_PPTAL.DEPENDENCIA   = DEPENDENCIA.CODIGO 
  WHERE DETALLE_COMPROBANTE_PPTAL.COMPANIA    = s$compania$s
  AND DETALLE_COMPROBANTE_PPTAL.FECHA BETWEEN TO_DATE('s$fechaInicial$s', 'DD/MM/YYYY') AND TO_DATE('s$fechaFinal$s', 'DD/MM/YYYY')
  AND DETALLE_COMPROBANTE_PPTAL.CUENTA    BETWEEN 's$cuentaInicial$s' AND 's$cuentaFinal$s'
  AND DETALLE_COMPROBANTE_PPTAL.FECHA     BETWEEN TO_DATE('s$fechaInicial$s', 'DD/MM/YYYY') AND TO_DATE('s$fechaFinal$s', 'DD/MM/YYYY')
  AND DETALLE_COMPROBANTE_PPTAL.TERCERO   BETWEEN 's$terceroInicial$s' AND 's$terceroFinal$s' 
  AND TIPO_COMPROBPP.CLASE='REO' 
  GROUP BY DETALLE_COMPROBANTE_PPTAL.COMPANIA, 
    DETALLE_COMPROBANTE_PPTAL.AUXILIAR,
    DETALLE_COMPROBANTE_PPTAL.CENTRO_COSTO,
    DETALLE_COMPROBANTE_PPTAL.REFERENCIA,
    DETALLE_COMPROBANTE_PPTAL.SECTOR,
    DETALLE_COMPROBANTE_PPTAL.CODIGO_CPC,
    DETALLE_COMPROBANTE_PPTAL.PROGRAMA,
    COMPROBANTE_PPTAL.DEPENDENCIA,
    DEPENDENCIA.NOMBRE,
    DETALLE_COMPROBANTE_PPTAL.COD_PROD_CUIPO,
    DETALLE_COMPROBANTE_PPTAL.ANO,
    DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE,
    DETALLE_COMPROBANTE_PPTAL.COMPROBANTE,
    DETALLE_COMPROBANTE_PPTAL.CONSECUTIVO,
    DETALLE_COMPROBANTE_PPTAL.DESCRIPCION,
    DETALLE_COMPROBANTE_PPTAL.FECHA,
    DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO,
    TIPO_COMPROBPP.CLASE,
    DETALLE_COMPROBANTE_PPTAL.TERCERO,
    DETALLE_COMPROBANTE_PPTAL.SUCURSAL,
    TERCERO.NOMBRE,
    TIPO_COMPROBPP.NOMBRE, 
    PLAN_PRESUPUESTAL_CUENTA.NOMBRE,
    DETALLE_COMPROBANTE_PPTAL.ID, 
    DETALLE_COMPROBANTE_PPTAL.CUENTA,
    CASE
      WHEN 's$tipoCuenta$s'='1'
      THEN DETALLE_COMPROBANTE_PPTAL.ID
      ELSE SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL1,''),0,3)
        ||'-'||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL2,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL3,''),0,3)
        ||'-'
   ]') || TO_CLOB(q'[     ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL4,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL5,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL6,''),0,3)
        ||'-'
        ||SUBSTR(NVL(PLAN_PRESUPUESTAL.NIVEL7,''),0,3)
    END,
    CASE
      WHEN 's$tipoCuenta$s'='1'
      THEN 'Codigo cuenta'
      ELSE 'Fid-Une-Prog-Spr-Proy-Dis-Rec'
    END
 )REG_OBLIGACION_ABIERTOS
  ON  REG_EGRESOS_ABIERTOS.COMPANIA           = REG_OBLIGACION_ABIERTOS.COMPANIA
  AND REG_EGRESOS_ABIERTOS.ANO                = REG_OBLIGACION_ABIERTOS.ANO
  AND REG_EGRESOS_ABIERTOS.TIPO_CPTE_AFECT    = REG_OBLIGACION_ABIERTOS.TIPO_CPTE
  AND REG_EGRESOS_ABIERTOS.CUENTA             = REG_OBLIGACION_ABIERTOS.CUENTA
  AND REG_EGRESOS_ABIERTOS.CMPTE_AFECTADO     = REG_OBLIGACION_ABIERTOS.COMPROBANTE
  AND REG_EGRESOS_ABIERTOS.CONSECUTIVOPPTO    = REG_OBLIGACION_ABIERTOS.CONSECUTIVO 
WHERE REG_OBLIGACION_ABIERTOS.TIPO_CPTE IS NOT NULL)]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);