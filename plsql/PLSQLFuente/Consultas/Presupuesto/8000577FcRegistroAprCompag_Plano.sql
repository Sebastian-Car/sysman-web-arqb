MERGE INTO CONSULTAS FIN USING (SELECT '8000577FcRegistroAprCompag_Plano' INFORME ,TO_CLOB(q'[SELECT
         DETALLE_COMPROBANTE_PPTAL.CUENTA AS CODIGO_INTERNO
       , PLAN_PRESUPUESTAL.ID
       , DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO
       , DETALLE_COMPROBANTE_PPTAL.MES
       , DETALLE_COMPROBANTE_PPTAL.FECHA
       , DETALLE_COMPROBANTE_PPTAL.DIA 
       , DETALLE_COMPROBANTE_PPTAL.ANO
       , DETALLE_COMPROBANTE_PPTAL.COMPROBANTE
       , DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE
       , TIPO_COMPROBPP.CLASE
       , (DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO
           -DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO) VALORTOTAL
       , CASE WHEN (DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO
           -DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)<0
              THEN '-'
              ELSE '+'
         END SIGNO
       , ABS((DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO
           -DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)) VALOR
       ,TO_CHAR(DETALLE_COMPROBANTE_PPTAL.FECHA,'DD/MM/YYYY')
        ||CASE WHEN CLASE IN ('TRA','ADC','RED','APL','APR') THEN 'APNC'
                WHEN CLASE IN ('DIS','DMD','ADD') THEN 'CDP'
                WHEN CLASE IN ('RES','DMR','ADR') THEN 'COM'
                WHEN CLASE IN ('REO','MRO','ARO','DRO') THEN 'OB'
                WHEN CLASE IN ('EGR','DEG','AEG') THEN 'P'
         END||CASE WHEN CLASE IN ('DIS','RES','REO','EGR') THEN '1'
                   WHEN CLASE IN ('DMD','DMR','DRO','DEG') THEN '2'
                   WHEN CLASE IN ('ADD','ADR','ARO','AEG') THEN '3'
                   ELSE '4'
              END||SUBSTR(DETALLE_COMPROBANTE_PPTAL.COMPROBANTE,LENGTH(DETALLE_COMPROBANTE_PPTAL.COMPROBANTE)-3,3)  ORDENCOMP
       , CASE WHEN CLASE IN ('DIS','DMD','ADD') THEN '11'
              WHEN CLASE IN ('RES','DMR','ADR') THEN '13'
              WHEN CLASE IN ('REO','MRO','ARO','DRO') THEN '15'
              WHEN CLASE IN ('EGR','DEG','AEG') THEN '17'
              WHEN CLASE IN ('TRA','ADC','RED','APL','APR') THEN '10'
              ELSE ''
         END CLASECOMP
       , CASE WHEN CLASE IN ('TRA','ADC','RED','APL','APR') THEN 'APNC'
                WHEN CLASE IN ('DIS','DMD','ADD') THEN 'CDP'
                WHEN CLASE IN ('RES','DMR','ADR') THEN 'COM'
                WHEN CLASE IN ('REO','MRO','ARO','DRO') THEN 'OB'
                WHEN CLASE IN ('EGR','DEG','AEG') THEN 'P'
         END CLASELETRAS
       , APRO.APROPIACIONVIGENTE NOCONDICIONADO
       , CASE WHEN CLASE IN ('DIS','DMD','ADD') 
              THEN ABS((DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO
           -DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)) 
              ELSE 0
         END TOTALCDP
       , CASE WHEN CLASE IN ('RES','DMR','ADR') 
              THEN ABS((DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO
                    -DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO))
              ELSE 0
         END TOTALCOMPROMISOS
       , CASE WHEN CLASE IN ('EGR','DEG','AEG') 
           THEN ABS((DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO
           -DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)) 
           ELSE 0
       END  TOTALPAGOS
       , CASE WHEN CLASE IN ('REO','MRO','ARO','DRO')
           THEN ABS((DETALLE_COMPROBANTE_PPTAL.]') || TO_CLOB(q'[VALOR_DEBITO
           -DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO))
           ELSE 0
       END  OBLIGACIONES
       , DETALLE_COMPROBANTE_PPTAL.DESCRIPCION
       , PLAN_PRESUPUESTAL.NOMBRE
       , DECODE(PLAN_PRESUPUESTAL.DESTINO,'F','Funcionamiento',
                                          'I','Inversión',
                                          'S','Servicio de la Deuda') DESTINO
       , CASE WHEN DETALLE_COMPROBANTE_PPTAL.NRO_DOCUMENTO IS NULL
             THEN ''
             ELSE DETALLE_COMPROBANTE_PPTAL.NRO_DOCUMENTO
        END NRO_DOCUMENTO
       , CASE SUBSTR(DETALLE_COMPROBANTE_PPTAL.NRO_DOCUMENTO,2) WHEN 'Le' THEN 'L'
                                                                WHEN 'De' THEN 'D'
                                                                WHEN 'Ac' THEN 'A'
                                                                WHEN 'Au' THEN 'AUT'
                                                                WHEN 'Co' THEN 'CON'
                                                                WHEN 'Re' THEN 'R'
                                                                ELSE ''
         END TIPODOCUMENTO
       , PLAN_PRESUPUESTAL.AUXILIAR
       , PLAN_PRESUPUESTAL.CENTRO_COSTO 
       , CASE WHEN PLAN_PRESUPUESTAL.DESTINO = 'I'
            THEN 'PROGRAMA'
            ELSE 'CUENTA'
       END NOMBRENIVEL1
      , CASE WHEN PLAN_PRESUPUESTAL.DESTINO = 'I'
            THEN 'SUBPROGRAMA'
            ELSE 'SUBCUENTA'
       END NOMBRENIVEL2
      , CASE WHEN PLAN_PRESUPUESTAL.DESTINO = 'I'
            THEN 'PROYECTO'
            ELSE 'OBJETO DEL GASTO'
       END NOMBRENIVEL3
      , CASE WHEN PLAN_PRESUPUESTAL.DESTINO = 'I'
            THEN 'SUBPROYECTO'
            ELSE 'ORDINAL'
       END NOMBRENIVEL4
      , CASE WHEN PLAN_PRESUPUESTAL.DESTINO = 'I'
            THEN 'RECURSO'
            ELSE 'SUBORDINAL'
       END NOMBRENIVEL5
      , CASE WHEN PLAN_PRESUPUESTAL.DESTINO = 'I'
            THEN '---'
            ELSE 'RECURSO'
       END NOMBRENIVEL6
      , PLAN_PRESUPUESTAL.NIVEL1
      , PLAN_PRESUPUESTAL.NIVEL2 
      , PLAN_PRESUPUESTAL.NIVEL3
      , PLAN_PRESUPUESTAL.NIVEL4
      , PLAN_PRESUPUESTAL.NIVEL5
      , PLAN_PRESUPUESTAL.NIVEL6
      , 0 CONDICIONADO 
FROM DETALLE_COMPROBANTE_PPTAL 
    INNER JOIN COMPROBANTE_PPTAL 
        ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA    = COMPROBANTE_PPTAL.COMPANIA 
        AND DETALLE_COMPROBANTE_PPTAL.ANO         = COMPROBANTE_PPTAL.ANO 
        AND DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE   = COMPROBANTE_PPTAL.TIPO 
        AND DETALLE_COMPROBANTE_PPTAL.COMPROBANTE = COMPROBANTE_PPTAL.NUMERO 
    INNER JOIN TIPO_COMPROBPP 
        ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA  = TIPO_COMPROBPP.COMPANIA 
        AND DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE = TIPO_COMPROBPP.CODIGO 
    INNER JOIN V_PLAN_PRESUPUESTAL PLAN_PRESUPUESTAL 
        ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = PLAN_PRESUPUESTAL.COMPANIA 
        AND DETALLE_COMPROBANTE_PPTAL.ANO      = PLAN_PRESUPUESTAL.ANO 
        AND DETALLE_COMPROBANTE_PPTAL.ID       = PLAN_PRESUPUESTAL.ID 
    LEFT JOIN 
        (SELECT S]') || TO_CLOB(q'[UM(APROPIACIONVIGENTE) OVER (PARTITION BY COMPANIA,ANO,ID ORDER BY MES) APROPIACIONVIGENTE,
                       COMPANIA,
                       ANO,
                       ID,
                       MES
                  FROM V_RESUMENPPTO_BASE
                 WHERE COMPANIA = s$compania$s  
                   AND ANO      = s$ano$s
                 ) APRO
         ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = APRO.COMPANIA 
        AND DETALLE_COMPROBANTE_PPTAL.ANO      = APRO.ANO 
        AND DETALLE_COMPROBANTE_PPTAL.ID       = APRO.ID      
        AND DETALLE_COMPROBANTE_PPTAL.MES      = APRO.MES    
WHERE DETALLE_COMPROBANTE_PPTAL.COMPANIA       =  s$compania$s
  AND DETALLE_COMPROBANTE_PPTAL.ANO            =  s$ano$s
  AND DETALLE_COMPROBANTE_PPTAL.MES            BETWEEN s$mesInicial$s AND s$mesFinal$s
  AND PLAN_PRESUPUESTAL.NATURALEZA             =  'D' 
  AND LENGTH(DETALLE_COMPROBANTE_PPTAL.CUENTA) <= s$nivel$s     
  AND PLAN_PRESUPUESTAL.ID                      BETWEEN 's$cuentaInicial$s' AND 's$cuentaFinal$s'
  s$condicionCentroCos$s
  s$condicionFuenteRec$s]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);