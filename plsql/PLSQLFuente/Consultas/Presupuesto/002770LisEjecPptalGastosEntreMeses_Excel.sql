MERGE INTO CONSULTAS FIN USING (SELECT '002770LisEjecPptalGastosEntreMeses_Excel' INFORME ,TO_CLOB(q'~SELECT CUENTA IMPUTACION,
	NOMBRE DETALLE,
	APROPIADOF PTO_INICIAL,
	CONTRACREDITOF CONTRA_CREDITOS,
	CREDITOF CREDITOS,
	REDUCCIONF REDUCCIONES,
	ADICIONF ADICIONES,
	APROPIACIONVIGENTEF PTO_DEFINITIVO,
	SALDOCOMPROMISOS SALDO_COMPROMISO,
	PAGOSA ANTERIORES,
	PAGOSP MES,
	TOTALP ACUMULADO,
	TOTALCOMPROMISO TOTAL_COMPROMISO,
	SALDOAPROPIACION SALDO_APROPIACION
FROM (s$consultaBase$s)
ORDER BY CASE WHEN REGEXP_LIKE(CLASE,'^[0-9]') THEN 1 ELSE 2 END,CUENTA~') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);