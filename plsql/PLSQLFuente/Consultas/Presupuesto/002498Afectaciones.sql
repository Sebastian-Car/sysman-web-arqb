MERGE INTO CONSULTAS FIN USING (SELECT '002498Afectaciones' INFORME ,TO_CLOB(q'[SELECT 
 SYSDATE FECHA,
 's$tipoComprobante$s' TIPOAFECT,
 DCP.COMPANIA,
 DCP.ANO,
 DCP.TIPO_CPTE,
 DCP.COMPROBANTE,
 case when DCP.ANO_AFECT is null THEN DCP.ANO else DCP.ANO_AFECT end ANO_AFECT,
 case when DCP.TIPO_CPTE_AFECT is null THEN 's$tipoComprobante$s' else DCP.TIPO_CPTE_AFECT end TIPO_CPTE_AFECT,
 case when DCP.CMPTE_AFECTADO is null THEN DCP.COMPROBANTE  else DCP.CMPTE_AFECTADO end CMPTE_AFECTADO,
 case when DCP.CMPTE_AFECTADO is null THEN DCP.COMPROBANTE  else DCP.CMPTE_AFECTADO end COMPAFECT,
 case when TCPP.CLASE is null THEN 's$tipoComprobante$s' ELSE TCPP.CLASE END CLASE,
 case when TCPP.CLASE is null THEN 's$tipoComprobante$s'ELSE TCPP.CLASE END CLASE_AFECTADA 
 FROM DETALLE_COMPROBANTE_PPTAL DCP 
 INNER JOIN TIPO_COMPROBPP TCPP 
 ON(DCP.COMPANIA= TCPP.COMPANIA AND DCP.TIPO_CPTE=TCPP.CODIGO) 
 INNER JOIN TIPO_COMPROBPP TAPP 
 ON(DCP.COMPANIA= TAPP.COMPANIA AND DCP.TIPO_CPTE_AFECT=TAPP.CODIGO) or (DCP.COMPANIA= TAPP.COMPANIA AND DCP.TIPO_CPTE =TAPP.CODIGO) 
 WHERE DCP.COMPANIA = s$compania$s 
 AND ((DCP.ANO_AFECT = s$ano$s AND DCP.TIPO_CPTE_AFECT = 's$tipoComprobante$s' and DCP.CMPTE_AFECTADO = s$numeroComprobante$s ) 
 or ( DCP.ANO = s$ano$s AND  DCP.TIPO_CPTE = 's$tipoComprobante$s' and DCP.COMPROBANTE = s$numeroComprobante$s))
 group by SYSDATE,
  's$tipoComprobante$s' ,
 DCP.COMPANIA,
 DCP.ANO,
 DCP.TIPO_CPTE,
 DCP.COMPROBANTE,
 case when DCP.ANO_AFECT is null THEN DCP.ANO else DCP.ANO_AFECT end ,
 case when DCP.TIPO_CPTE_AFECT is null THEN 's$tipoComprobante$s' else DCP.TIPO_CPTE_AFECT end ,
 case when DCP.CMPTE_AFECTADO is null THEN DCP.COMPROBANTE  else DCP.CMPTE_AFECTADO end ,
 case when DCP.CMPTE_AFECTADO is null THEN DCP.COMPROBANTE  else DCP.CMPTE_AFECTADO end ,
 case when TCPP.CLASE is null THEN 's$tipoComprobante$s'  ELSE TCPP.CLASE END ,
 case when TCPP.CLASE is null THEN 's$tipoComprobante$s' ELSE TCPP.CLASE END ]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);