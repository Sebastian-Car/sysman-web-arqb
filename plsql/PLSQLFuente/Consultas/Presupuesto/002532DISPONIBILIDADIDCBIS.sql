MERGE INTO CONSULTAS FIN USING (SELECT '002532DISPONIBILIDADIDCBIS' INFORME ,TO_CLOB(q'[SELECT COMPROBANTE_PPTAL.DESCRIPCION,
       COMPROBANTE_PPTAL.FECHA_VCN_DOC,
       DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO,
       DETALLE_COMPROBANTE_PPTAL.NUMERO_SOLICITUD NECESIDAD,
       DETALLE_COMPROBANTE_PPTAL.SECTOR || '-'  || TIPO.NOMBRE PROCESO,
       DETALLE_COMPROBANTE_PPTAL.SUBPROGRAMA || '-'  || SUBPROGRAMA.NOMBRE META,
       DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO   || '-'  || FT.NOMBRE FUENTE_RECURSO,
       V_PLAN_PRESUPUESTAL.NOMBRE,
       PLAN.CODIGO CODIGO_CUENTA,
       DETALLE_COMPROBANTE_PPTAL.CUENTA SUBRUBRO,
       COMPROBANTE_PPTAL.NUMERO COMPROBANTE,
       COMPROBANTE_PPTAL.FECHA,
       '' AS NECESIDAD
  FROM COMPROBANTE_PPTAL 
       INNER JOIN DETALLE_COMPROBANTE_PPTAL 
       ON  COMPROBANTE_PPTAL.COMPANIA = DETALLE_COMPROBANTE_PPTAL.COMPANIA
       AND COMPROBANTE_PPTAL.ANO    = DETALLE_COMPROBANTE_PPTAL.ANO
       AND COMPROBANTE_PPTAL.TIPO   = DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE
       AND COMPROBANTE_PPTAL.NUMERO = DETALLE_COMPROBANTE_PPTAL.COMPROBANTE
       LEFT JOIN TIPOCLASIFICADOR TIPO
                   ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = TIPO.COMPANIA
                   AND DETALLE_COMPROBANTE_PPTAL.ANO = TIPO.ANO
                   AND '001' = TIPO.CLASECLASIFICADOR
                   AND DETALLE_COMPROBANTE_PPTAL.SECTOR   =  TIPO.CODIGO 
       LEFT JOIN TIPOCLASIFICADOR SUBPROGRAMA
                   ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = SUBPROGRAMA.COMPANIA
                   AND DETALLE_COMPROBANTE_PPTAL.ANO = SUBPROGRAMA.ANO
                   AND '003'= SUBPROGRAMA.CLASECLASIFICADOR
                   AND DETALLE_COMPROBANTE_PPTAL.SUBPROGRAMA=  SUBPROGRAMA.CODIGO  
       INNER JOIN FUENTE_RECURSOS    FT ON DETALLE_COMPROBANTE_PPTAL.COMPANIA = FT.COMPANIA
                                     AND DETALLE_COMPROBANTE_PPTAL.ANO = FT.ANO
                                     AND DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO = FT.CODIGO
       INNER JOIN V_PLAN_PRESUPUESTAL 
       ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = V_PLAN_PRESUPUESTAL.COMPANIA
       AND DETALLE_COMPROBANTE_PPTAL.ANO = V_PLAN_PRESUPUESTAL.ANO
       AND DETALLE_COMPROBANTE_PPTAL.CUENTA = V_PLAN_PRESUPUESTAL.ID
       INNER JOIN PLAN_PRESUPUESTAL PLAN ON PLAN.COMPANIA= DETALLE_COMPROBANTE_PPTAL.COMPANIA
                                         AND PLAN.ANO= DETALLE_COMPROBANTE_PPTAL.ANO
                                         AND PLAN.CODIGO       = SUBSTR(DETALLE_COMPROBANTE_PPTAL.CUENTA, 1, LENGTH(PLAN.CODIGO))
 WHERE COMPROBANTE_PPTAL.COMPANIA = s$compania$s 
   AND COMPROBANTE_PPTAL.ANO = s$ano$s 
   AND COMPROBANTE_PPTAL.TIPO = 's$tipoCpte$s' 
   AND COMPROBANTE_PPTAL.NUMERO BETWEEN s$numeroPptoInicial$s AND s$numeroPptoFinal$s
   AND PLAN.CODIGO = SUBSTR(DETALLE_COMPROBANTE_PPTAL.CUENTA, 1, LENGTH(DETALLE_COMPROBANTE_PPTAL.CUENTA) - INSTR(REVERSE(DETALLE_COMPROBANTE_PPTAL.CUENTA), '.'))]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);