MERGE INTO CONSULTAS FIN USING (SELECT '002335TRAPPTALSOGA' INFORME ,TO_CLOB(q'[SELECT 
    V_DET_AUX.COMPANIA,
    V_DET_AUX.ANO,
    V_DET_AUX.TIPO,
    V_DET_AUX.COMPROBANTE NUMERO,
    V_DET_AUX.FECHA_COM FECHA,
    V_DET_AUX.TERCERO_COM TERCERO,
    V_DET_AUX.SUCURSAL_COM SUCURSAL,
    V_DET_AUX.TERCERO_COM_NOM NOMBRE,
    V_DET_AUX.TIPO_COMPROBANTE_NOM  NOMBRETIPO,
    V_DET_AUX.CUENTANOMBRE AS NOMBRECUENTA,
    V_DET_AUX.DESCRIPCION,
    V_DET_AUX.CUENTA CUENTA_COD,
    DETALLE_COMPROBANTE_PPTAL.ID CUENTA,
    V_DET_AUX.VALOR_CREDITO AS CONTRACREDITO,
    V_DET_AUX.VALOR_DEBITO  AS CREDITO

FROM V_DETALLE_AUXILIAR_PPTAL V_DET_AUX

LEFT JOIN DETALLE_COMPROBANTE_PPTAL
    ON DETALLE_COMPROBANTE_PPTAL.COMPANIA   =V_DET_AUX.COMPANIA
    AND DETALLE_COMPROBANTE_PPTAL.ANO   =V_DET_AUX.ANO
    AND DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE   =V_DET_AUX.TIPO
    AND DETALLE_COMPROBANTE_PPTAL.COMPROBANTE   =V_DET_AUX.COMPROBANTE
    AND DETALLE_COMPROBANTE_PPTAL.CONSECUTIVO  = V_DET_AUX.CONSECUTIVO

WHERE V_DET_AUX.COMPANIA = s$compania$s
    AND V_DET_AUX.ANO    = s$ano$s
    AND V_DET_AUX.TIPO   = 's$tipoCpte$s'
    AND V_DET_AUX.COMPROBANTE BETWEEN s$numeroPptoInicial$s AND s$numeroPptoFinal$s]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'calarcon' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);