MERGE INTO CONSULTAS FIN USING (SELECT '002259CBREG' INFORME ,TO_CLOB(q'[SELECT
COMPROBANTE_PPTAL.COMPANIA,
   COMPROBANTE_PPTAL.TIPO,
   COMPROBANTE_PPTAL.ANO ANIO,
    COMPROBANTE_PPTAL.FECHA,
    COMPROBANTE_PPTAL.TERCERO,
    COMPROBANTE_PPTAL.SUCURSAL,
    s$companiaSigla$s SIGLA,
    NVL(TERCERO.NOMBRE, ' ') NOMBRE_TERCERO,
    DETALLE_COMPROBANTE_PPTAL.CUENTA       RUBRO,
    DETALLE_COMPROBANTE_PPTAL.CUENTA       CODIGO_CUENTA,
     TRIM(PLAN_PRESUPUESTAL.NOMBRE)
  || CASE NVL(FUENTE_RECURSOS.CODIGO,'')
   WHEN ''
   THEN ''
   ELSE' / ' || NVL(FUENTE_RECURSOS.NOMBRE,'')
   END
   || CASE NVL(REFERENCIA.CODIGO,'')
   WHEN ''
   THEN ''
   ELSE' / ' || NVL(REFERENCIA.NOMBRE,'')
   END  AS NOMBRE_RUBRO,
    DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO,
    DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO,
    COMPROBANTE_PPTAL.NUMERO               COMPROBANTE,
    TIPO_COMPROBPP.CODIGO                  TIPO_CPTE,
    TIPO_COMPROBPP.NOMBRE                  NOMBRECPTE,
    COMPROBANTE_PPTAL.DEPENDENCIA,
    COMPROBANTE_PPTAL.DESCRIPCION,
    COMPROBANTE_PPTAL.TEXTO,
    COMPROBANTE_PPTAL.CREATED_BY   CREADOR,
    DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE_AFECT,
    DETALLE_COMPROBANTE_PPTAL.CMPTE_AFECTADO,
    TIPOORDENDECOMPRA.NOMBRE NOMBRECONTRATO,
    DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO FUENTE,
    DETALLE_COMPROBANTE_PPTAL.REFERENCIA COD_PROYECTO,
    DEPENDENCIA.NOMBRE NOMBRE_DEPENDENCIA,
    COMPROBANTE_PPTAL.NUMEROCONTRATO,
    DETALLE_COMPROBANTE_PPTAL.CODIGO_CPC,
    COMPROBANTE_PPTAL.FICHA
FROM COMPROBANTE_PPTAL
  LEFT JOIN DETALLE_COMPROBANTE_PPTAL
     ON DETALLE_COMPROBANTE_PPTAL.COMPANIA    = COMPROBANTE_PPTAL.COMPANIA
    AND DETALLE_COMPROBANTE_PPTAL.ANO         = COMPROBANTE_PPTAL.ANO
    AND DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE   = COMPROBANTE_PPTAL.TIPO
    AND DETALLE_COMPROBANTE_PPTAL.COMPROBANTE = COMPROBANTE_PPTAL.NUMERO
  LEFT JOIN  PLAN_PRESUPUESTAL
     ON DETALLE_COMPROBANTE_PPTAL.COMPANIA = PLAN_PRESUPUESTAL.COMPANIA
    AND DETALLE_COMPROBANTE_PPTAL.ANO      = PLAN_PRESUPUESTAL.ANO
    AND DETALLE_COMPROBANTE_PPTAL.CUENTA   = PLAN_PRESUPUESTAL.CODIGO
  LEFT JOIN TERCERO
     ON COMPROBANTE_PPTAL.COMPANIA = TERCERO.COMPANIA
    AND COMPROBANTE_PPTAL.TERCERO  = TERCERO.NIT
    AND COMPROBANTE_PPTAL.SUCURSAL = TERCERO.SUCURSAL
  LEFT JOIN TIPO_COMPROBPP
     ON COMPROBANTE_PPTAL.COMPANIA = TIPO_COMPROBPP.COMPANIA
    AND COMPROBANTE_PPTAL.TIPO     = TIPO_COMPROBPP.CODIGO
  LEFT JOIN TIPOORDENDECOMPRA
    ON COMPROBANTE_PPTAL.COMPANIA = TIPOORDENDECOMPRA.COMPANIA
    AND COMPROBANTE_PPTAL.TIPOCONTRATO = TIPOORDENDECOMPRA.CODIGO
INNER JOIN FUENTE_RECURSOS
     ON DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO  = FUENTE_RECURSOS.CODIGO
    AND DETALLE_COMPROBANTE_PPTAL.ANO = FUENTE_RECURSOS.ANO
    AND DETALLE_COMPROBANTE_PPTAL.COMPANIA = FUENTE_RECURSOS.COMPANIA
     INNER JOIN REFERENCIA
     ON DETALLE_COMPROBANTE_PPTAL.REFERENCIA  = REFERENCIA.CODIGO
    AND DETALLE_COMPROBANTE_PPTAL.ANO = REFERENCIA.ANO
    AND DETALLE_COMPROBANTE_PPTAL.COMPANIA = REFERENCIA.COMPANIA
 LEFT JOIN DEPENDENCIA
ON COMP]') || TO_CLOB(q'[ROBANTE_PPTAL.DEPENDENCIA = DEPENDENCIA.CODIGO
AND COMPROBANTE_PPTAL.COMPANIA = DEPENDENCIA.COMPANIA
WHERE COMPROBANTE_PPTAL.COMPANIA = s$compania$s
  AND COMPROBANTE_PPTAL.ANO      = s$ano$s
  AND COMPROBANTE_PPTAL.TIPO     = 's$tipoCpte$s'
  AND COMPROBANTE_PPTAL.NUMERO BETWEEN s$numeroPptoInicial$s AND s$numeroPptoFinal$s
ORDER BY COMPROBANTE_PPTAL.NUMERO]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
COMMIT;