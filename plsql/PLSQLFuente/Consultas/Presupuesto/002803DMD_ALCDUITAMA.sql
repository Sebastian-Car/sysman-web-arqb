MERGE INTO CONSULTAS FIN USING (SELECT '002803DMD_ALCDUITAMA' INFORME ,TO_CLOB(q'[SELECT
	COMPROBANTE_PPTAL.NUMERO,
	COMPROBANTE_PPTAL.FECHA,
	COMPROBANTE_PPTAL.SUCURSAL,
	TO_CHAR(COMPROBANTE_PPTAL.FECHA_VCN_DOC,'DD-MM-YYYY') FECHA_VCN_DOC,
	DETALLE_COMPROBANTE_PPTAL.COMPROBANTE AS NUMERO_CPTE,
	DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO AS VALOR_DEBITO,
	DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO AS VALOR_CREDITO,
	DETALLE_COMPROBANTE_PPTAL.CUENTA AS CODIGO_CUENTA,
	DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE_AFECT || '-' || DETALLE_COMPROBANTE_PPTAL.CMPTE_AFECTADO || ' ' || DETALLE_COMPROBANTE_PPTAL.DESCRIPCION AS TOTAL_DESCRIPCION,
	DETALLE_COMPROBANTE_PPTAL.DESCRIPCION,
	CASE WHEN DETALLE_COMPROBANTE_PPTAL.AUXILIAR='9999999999999999' THEN '' ELSE DETALLE_COMPROBANTE_PPTAL.AUXILIAR END AS COD_AUXILIA,
    	CASE WHEN DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO='9999999999999999' THEN '' ELSE DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO END AS COD_AUXILIAR,
	PLAN_PRESUPUESTAL.NOMBRE ||
		CASE WHEN MAN_CEN_CTO = 0 THEN '' ELSE ' / ' || CENTRO_COSTO.NOMBRE END ||
		CASE WHEN MAN_AUX_TER = 0 THEN '' ELSE ' / ' || TERCERO.NOMBRE END ||
		CASE WHEN MAN_AUX_GEN = 0 THEN '' ELSE ' / ' || AUXILIAR.NOMBRE END AS NOMBRE_TOTAL,
	UPPER(TIPO_COMPROBPP.NOMBRE) AS NOMBRE_TIPO_CPTE,
	TERCERO.NIT AS CEDULA_TERCERO,
	TERCERO.NOMBRE AS NOMBRE_TERCERO,
    COMPROBANTE_PPTAL.CREATED_BY ELABORO
FROM
	COMPROBANTE_PPTAL
	INNER JOIN DETALLE_COMPROBANTE_PPTAL
		ON COMPROBANTE_PPTAL.TIPO = DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE
        AND COMPROBANTE_PPTAL.NUMERO = DETALLE_COMPROBANTE_PPTAL.COMPROBANTE
        AND COMPROBANTE_PPTAL.ANO = DETALLE_COMPROBANTE_PPTAL.ANO
        AND COMPROBANTE_PPTAL.COMPANIA = DETALLE_COMPROBANTE_PPTAL.COMPANIA
	INNER JOIN TERCERO
		ON COMPROBANTE_PPTAL.SUCURSAL = TERCERO.SUCURSAL
        AND COMPROBANTE_PPTAL.TERCERO = TERCERO.NIT
        AND COMPROBANTE_PPTAL.COMPANIA = TERCERO.COMPANIA
	INNER JOIN TIPO_COMPROBPP
		ON COMPROBANTE_PPTAL.TIPO = TIPO_COMPROBPP.CODIGO
		AND COMPROBANTE_PPTAL.COMPANIA = TIPO_COMPROBPP.COMPANIA
	INNER JOIN AUXILIAR
		ON DETALLE_COMPROBANTE_PPTAL.COMPANIA = AUXILIAR.COMPANIA
         AND DETALLE_COMPROBANTE_PPTAL.ANO = AUXILIAR.ANO
        AND DETALLE_COMPROBANTE_PPTAL.AUXILIAR = AUXILIAR.CODIGO
	LEFT JOIN PLAN_PRESUPUESTAL
		ON DETALLE_COMPROBANTE_PPTAL.CUENTA = PLAN_PRESUPUESTAL.CODIGO
        AND DETALLE_COMPROBANTE_PPTAL.ANO = PLAN_PRESUPUESTAL.ANO
        AND DETALLE_COMPROBANTE_PPTAL.COMPANIA = PLAN_PRESUPUESTAL.COMPANIA
	LEFT JOIN CENTRO_COSTO
		ON DETALLE_COMPROBANTE_PPTAL.COMPANIA = CENTRO_COSTO.COMPANIA
        AND DETALLE_COMPROBANTE_PPTAL.ANO = CENTRO_COSTO.ANO
        AND DETALLE_COMPROBANTE_PPTAL.CENTRO_COSTO = CENTRO_COSTO.CODIGO
WHERE
	COMPROBANTE_PPTAL.COMPANIA = s$compania$s
    AND COMPROBANTE_PPTAL.ANO = s$ano$s
    AND COMPROBANTE_PPTAL.TIPO = 's$tipoCpte$s'
    AND COMPROBANTE_PPTAL.NUMERO BETWEEN s$numeroPptoInicial$s AND s$numeroPptoFinal$s]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'mrosero' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);