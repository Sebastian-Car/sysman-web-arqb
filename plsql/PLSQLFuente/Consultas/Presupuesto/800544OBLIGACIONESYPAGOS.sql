MERGE INTO CONSULTAS FIN USING (SELECT '800544OBLIGACIONESYPAGOS' INFORME ,TO_CLOB(q'[SELECT REFERENCIA.CODIGO PROYECTO,
      REFERENCIA.NOMBRE  NOMBRE_PROYECTO,
      DETALLE_COMPROBANTE_PPTAL.CUENTA CODIGO_PRESUPUESTAL,
       COMPROBANTE_PPTAL.TIPOCONTRATO||COMPROBANTE_PPTAL.NUMEROCONTRATO   CONTRATO,
      COMPROBANTE_PPTAL.TIPO TIPO_COMPROBANTE,
      COMPROBANTE_PPTAL.NUMERO NUMERO_COMPROBANTE,
      COMPROBANTE_PPTAL.FECHA FECHA_COMPROBANTE,
      COMPROBANTE_PPTAL.DESCRIPCION,
      CASE WHEN TIPO_COMPROBPP.CLASE IN('RES','ADR')
       THEN VALOR_DEBITO
       ELSE 0 END  COMPROMISO,
       CASE WHEN TIPO_COMPROBPP.CLASE IN('DMR')
       THEN -VALOR_CREDITO
       ELSE 0 END  DISMINUCION_REGISTRO,
       CASE WHEN TIPO_COMPROBPP.CLASE IN('REO','ARO')
       THEN  VALOR_DEBITO
       ELSE CASE WHEN TIPO_COMPROBPP.CLASE IN('DRO')
       THEN VALOR_CREDITO * -1
       ELSE 0 END END  OBLIGACIONES,

     CASE WHEN TIPO_COMPROBPP.CLASE IN('EGR','AEG')
       THEN  VALOR_DEBITO
       ELSE  CASE WHEN TIPO_COMPROBPP.CLASE IN('DEG')
       THEN  VALOR_CREDITO * -1
       ELSE 0 END END   PAGOS,         
       TERCERO.NIT NIT_TERCERO,
       TERCERO.NOMBRE NOMBRE_TERCERO,
       FUENTE_RECURSOS.CODIGO FUENTE_FINANCIACION,
       FUENTE_RECURSOS.NOMBRE NOMBRE_FUENTE
FROM COMPROBANTE_PPTAL INNER JOIN  DETALLE_COMPROBANTE_PPTAL
ON COMPROBANTE_PPTAL.COMPANIA=DETALLE_COMPROBANTE_PPTAL.COMPANIA
AND COMPROBANTE_PPTAL.ANO=DETALLE_COMPROBANTE_PPTAL.ANO
AND  COMPROBANTE_PPTAL.TIPO=DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE
AND  COMPROBANTE_PPTAL.NUMERO=DETALLE_COMPROBANTE_PPTAL.COMPROBANTE
INNER JOIN REFERENCIA
ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = REFERENCIA.COMPANIA
AND DETALLE_COMPROBANTE_PPTAL.ANO = REFERENCIA.ANO
AND DETALLE_COMPROBANTE_PPTAL.REFERENCIA = REFERENCIA.CODIGO
INNER JOIN FUENTE_RECURSOS
ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = FUENTE_RECURSOS.COMPANIA
AND DETALLE_COMPROBANTE_PPTAL.ANO = FUENTE_RECURSOS.ANO
AND DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO = FUENTE_RECURSOS.CODIGO
INNER JOIN TERCERO
ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA=TERCERO.COMPANIA
AND DETALLE_COMPROBANTE_PPTAL.TERCERO=TERCERO.NIT
AND DETALLE_COMPROBANTE_PPTAL.SUCURSAL=TERCERO.SUCURSAL
INNER JOIN TIPO_COMPROBPP 
ON COMPROBANTE_PPTAL.COMPANIA = TIPO_COMPROBPP.COMPANIA
AND COMPROBANTE_PPTAL.TIPO = TIPO_COMPROBPP.CODIGO

WHERE COMPROBANTE_PPTAL.COMPANIA=s$compania$s 
AND COMPROBANTE_PPTAL.ANO=s$ano$s 
AND COMPROBANTE_PPTAL.MES <= s$mesFinal$s 
AND DETALLE_COMPROBANTE_PPTAL.REFERENCIA BETWEEN 's$proyectoInicial$s' AND 's$proyectoFinal$s' 
AND TIPO_COMPROBPP.CLASE IN('RES','ADR','DMR','REO','ARO','DRO','EGR','AEG','DEG')]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'MROSERO' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);