MERGE INTO CONSULTAS FIN USING (SELECT '002717FEjecucionPptalIG' INFORME ,TO_CLOB(q'[SELECT
    CASE WHEN  NATURALEZA='D' THEN eje_ppt_debito-eje_ppt_credito ELSE eje_ppt_credito-eje_ppt_debito END AS IngresosMes,
    Saldo_plan_pptal.Mes,
    EJECUCIONPPTALIG.COMPANIA,
    EJECUCIONPPTALIG.ANO,
    EJECUCIONPPTALIG.CODIGO,
    EJECUCIONPPTALIG.NOMBRE,
    EJECUCIONPPTALIG.APROPIACIONINICIAL,
    EJECUCIONPPTALIG.ADIC,
    EJECUCIONPPTALIG.RED,
    EJECUCIONPPTALIG.APLAZAMIENTOS,
    EJECUCIONPPTALIG.TRASLADOS,
    EJECUCIONPPTALIG.MODIFICACIONES,
    EJECUCIONPPTALIG.APROPIACIONACUMULADA,
    EJECUCIONPPTALIG.INGRESADO,
    EJECUCIONPPTALIG.PORINGRESAR,
    EJECUCIONPPTALIG.PORCENTAJE,
    EJECUCIONPPTALIG.NATURALEZA,
    EJECUCIONPPTALIG.MOVIMIENTO,
    EJECUCIONPPTALIG.FUENTE,
    EJECUCIONPPTALIG.FUENTECUIPO || ' - ' || TC.NOMBRE AS FUENTECUIPO
FROM (SELECT SALDO_PLAN_PPTAL.COMPANIA,
 SALDO_PLAN_PPTAL.ANO,
 SALDO_PLAN_PPTAL.CODIGO,
 PLAN_PRESUPUESTAL.NOMBRE,

 SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN 
    APROPIACION_DEBITO-APROPIACION_CREDITO ELSE 
    APROPIACION_CREDITO-APROPIACION_DEBITO END) APROPIACIONINICIAL,

 SUM(SALDO_PLAN_PPTAL.ADICION) ADIC,

 SUM(SALDO_PLAN_PPTAL.REDUCCION) RED,

 SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN
    APLAZAM_DEBITO-APLAZAM_CREDITO ELSE
    APLAZAM_CREDITO-APLAZAM_DEBITO END) APLAZAMIENTOS,

 SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN
    TRASLADO_DEBITO-TRASLADO_CREDITO ELSE
    TRASLADO_CREDITO-TRASLADO_DEBITO END) TRASLADOS,

 SUM(SALDO_PLAN_PPTAL.ADICION)+SUM(SALDO_PLAN_PPTAL.REDUCCION)+SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN
    TRASLADO_DEBITO-TRASLADO_CREDITO ELSE
    TRASLADO_CREDITO-TRASLADO_DEBITO END)+SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN
    APLAZAM_DEBITO-APLAZAM_CREDITO ELSE
    APLAZAM_CREDITO-APLAZAM_DEBITO END) MODIFICACIONES,

 SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN 
    APROPIACION_DEBITO-APROPIACION_CREDITO ELSE 
    APROPIACION_CREDITO-APROPIACION_DEBITO END)+(SUM(SALDO_PLAN_PPTAL.ADICION)+SUM(SALDO_PLAN_PPTAL.REDUCCION)+SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN
    TRASLADO_DEBITO-TRASLADO_CREDITO ELSE
    TRASLADO_CREDITO-TRASLADO_DEBITO END)+SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN
    APLAZAM_DEBITO-APLAZAM_CREDITO ELSE
    APLAZAM_CREDITO-APLAZAM_DEBITO END)) APROPIACIONACUMULADA,

 SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN
    (EJE_PPT_DEBITO-EJE_PPT_CREDITO)+MODIF_INGRESOS ELSE
    (EJE_PPT_CREDITO-EJE_PPT_DEBITO)+MODIF_INGRESOS END) INGRESADO,

 (SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN 
    APROPIACION_DEBITO-APROPIACION_CREDITO ELSE 
    APROPIACION_CREDITO-APROPIACION_DEBITO END)+(SUM(SALDO_PLAN_PPTAL.ADICION)+SUM(SALDO_PLAN_PPTAL.REDUCCION)+SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN
    TRASLADO_DEBITO-TRASLADO_CREDITO ELSE
    TRASLADO_CREDITO-TRASLADO_DEBITO END)+SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN
    APLAZAM_DEBITO-APLAZAM_CREDITO ELSE
    APLAZAM_CREDITO-APLAZAM_DEBITO END)))-(SUM(CASE WHEN PLAN_PRES]') || TO_CLOB(q'[UPUESTAL.NATURALEZA='D' THEN
    (EJE_PPT_DEBITO-EJE_PPT_CREDITO)+MODIF_INGRESOS ELSE
    (EJE_PPT_CREDITO-EJE_PPT_DEBITO)+MODIF_INGRESOS END)) PORINGRESAR,

 ROUND(CASE WHEN (SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN 
    APROPIACION_DEBITO-APROPIACION_CREDITO ELSE 
    APROPIACION_CREDITO-APROPIACION_DEBITO END)+(SUM(SALDO_PLAN_PPTAL.ADICION)+SUM(SALDO_PLAN_PPTAL.REDUCCION)+SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN
    TRASLADO_DEBITO-TRASLADO_CREDITO ELSE
    TRASLADO_CREDITO-TRASLADO_DEBITO END)+SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN
    APLAZAM_DEBITO-APLAZAM_CREDITO ELSE
    APLAZAM_CREDITO-APLAZAM_DEBITO END)))<>0 THEN
    (SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN
    (EJE_PPT_DEBITO-EJE_PPT_CREDITO)+MODIF_INGRESOS ELSE
    (EJE_PPT_CREDITO-EJE_PPT_DEBITO)+MODIF_INGRESOS END))/(SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN 
    APROPIACION_DEBITO-APROPIACION_CREDITO ELSE 
    APROPIACION_CREDITO-APROPIACION_DEBITO END)+(SUM(SALDO_PLAN_PPTAL.ADICION)+SUM(SALDO_PLAN_PPTAL.REDUCCION)+SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN
    TRASLADO_DEBITO-TRASLADO_CREDITO ELSE
    TRASLADO_CREDITO-TRASLADO_DEBITO END)+SUM(CASE WHEN PLAN_PRESUPUESTAL.NATURALEZA='D' THEN
    APLAZAM_DEBITO-APLAZAM_CREDITO ELSE
    APLAZAM_CREDITO-APLAZAM_DEBITO END)))*100 ELSE 100 END,2) PORCENTAJE,

PLAN_PRESUPUESTAL.NATURALEZA,
 PLAN_PRESUPUESTAL.MOVIMIENTO,
 PLAN_PPTAL_CONFIG.FUENTE_RECURSO ||' - '|| FUENTE_RECURSOS.NOMBRE  FUENTE,
    CASE
        WHEN PLAN_PRESUPUESTAL.TIPOVIGENCIA = 'RA' THEN 
            FUENTE_RECURSOS.EQUIVALENTEAPROPIACION 
        WHEN PLAN_PRESUPUESTAL.TIPOVIGENCIA = 'RC' THEN
            FUENTE_RECURSOS.EQUIVALENTECAJA 
        ELSE
            FUENTE_RECURSOS.EQUIVALENTECUIPO
    END  FUENTECUIPO
FROM PLAN_PRESUPUESTAL 
LEFT JOIN SALDO_PLAN_PPTAL 
    ON (PLAN_PRESUPUESTAL.COMPANIA = SALDO_PLAN_PPTAL.COMPANIA) 
    AND (PLAN_PRESUPUESTAL.ANO = SALDO_PLAN_PPTAL.ANO) 
    AND (PLAN_PRESUPUESTAL.CODIGO = SALDO_PLAN_PPTAL.CODIGO)
LEFT JOIN PLAN_PPTAL_CONFIG
        ON PLAN_PRESUPUESTAL.COMPANIA = PLAN_PPTAL_CONFIG.COMPANIA
        AND PLAN_PRESUPUESTAL.ANO = PLAN_PPTAL_CONFIG.ANO
        AND PLAN_PRESUPUESTAL.CODIGO = PLAN_PPTAL_CONFIG.CODIGO 
LEFT JOIN FUENTE_RECURSOS
        ON PLAN_PPTAL_CONFIG.COMPANIA = FUENTE_RECURSOS.COMPANIA
        AND PLAN_PPTAL_CONFIG.ANO = FUENTE_RECURSOS.ANO
        AND PLAN_PPTAL_CONFIG.FUENTE_RECURSO = FUENTE_RECURSOS.CODIGO
WHERE SALDO_PLAN_PPTAL.COMPANIA = s$compania$s
AND SALDO_PLAN_PPTAL.MES BETWEEN 0 AND s$mes$s
AND SALDO_PLAN_PPTAL.ANO = s$anio$s 
AND SALDO_PLAN_PPTAL.CODIGO BETWEEN 's$cuentaInicial$s' AND 's$cuentaFinal$s'  
AND PLAN_PRESUPUESTAL.NATURALEZA = 'C'
GROUP BY SALDO_PLAN_PPTAL.COMPANIA,
 SALDO_PLAN_PPTAL.ANO,
 SALDO_PLAN_PPTAL.CODIGO,
 PLAN_PRESUPUESTAL.NOMBRE,
 PLAN_PRESUPUESTAL.NATURALEZA,
 PLAN_PRESUPUESTAL.MOVIMIENTO,
 PLAN_PPTAL_CONFIG.FUENTE_RECURSO ||' - '|| FUENTE_RECURSOS.NOMBRE,
      CASE
        WHEN PLAN_PRES]') || TO_CLOB(q'[UPUESTAL.TIPOVIGENCIA = 'RA' THEN 
            FUENTE_RECURSOS.EQUIVALENTEAPROPIACION 
        WHEN PLAN_PRESUPUESTAL.TIPOVIGENCIA = 'RC' THEN
            FUENTE_RECURSOS.EQUIVALENTECAJA 
        ELSE
            FUENTE_RECURSOS.EQUIVALENTECUIPO
    END) EJECUCIONPPTALIG 
LEFT JOIN Saldo_plan_pptal 
    ON EJECUCIONPPTALIG.codigo = Saldo_plan_pptal.codigo 
    AND EJECUCIONPPTALIG.Ano = Saldo_plan_pptal.Ano 
    AND EJECUCIONPPTALIG.Compania = Saldo_plan_pptal.Compania
LEFT JOIN TIPOCLASIFICADOR TC ON EJECUCIONPPTALIG.COMPANIA = TC.COMPANIA
        AND EJECUCIONPPTALIG.ANO = TC.ANO AND EJECUCIONPPTALIG.FUENTECUIPO = TC.CODIGO 
WHERE saldo_plan_pptal.mes = s$mes$s 
s$condicionaux$s]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
COMMIT;