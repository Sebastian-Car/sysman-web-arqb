MERGE INTO CONSULTAS FIN USING (SELECT '001526DisponibilidadesSaldoPorRubro' INFORME ,TO_CLOB(q'[SELECT  V_RESUMENPPTO_BASE.CODIGO,
        V_RESUMENPPTO_BASE.ID,
        V_RESUMENPPTO_BASE.NOMBRE,
        SUM(V_RESUMENPPTO_BASE.APROPIACIONVIGENTE) TOTALAPROPIACION,
        SUM(( V_RESUMENPPTO_BASE.APROPIACIONVIGENTE) - V_RESUMENPPTO_BASE.COMPROMISOSACUM)SALDOREGISTRO,
        SUM((V_RESUMENPPTO_BASE.APROPIACIONVIGENTE) - V_RESUMENPPTO_BASE.DISPONIBILIDAD)SALDODISPONIBILIDAD,
		(V_RESUMENPPTO_BASE.CODIGO || '  ' || V_RESUMENPPTO_BASE.AUXILIAR || '  ' || V_RESUMENPPTO_BASE.CENTRO_COSTO || '  ' || V_RESUMENPPTO_BASE.REFERENCIA) COD_CONCAT
FROM  V_RESUMENPPTO_BASE
WHERE V_RESUMENPPTO_BASE.COMPANIA = s$compania$s
  AND V_RESUMENPPTO_BASE.ANO = s$ano$s
  AND V_RESUMENPPTO_BASE.MES BETWEEN 0 AND s$mes$s
  AND V_RESUMENPPTO_BASE.ID BETWEEN 's$cuentaInicial$s' AND 's$cuentaFinal$s'
  AND V_RESUMENPPTO_BASE.NATURALEZA IN ('D')
GROUP BY V_RESUMENPPTO_BASE.CODIGO,
        V_RESUMENPPTO_BASE.ID,
        V_RESUMENPPTO_BASE.NOMBRE,
		(V_RESUMENPPTO_BASE.CODIGO || '  ' || V_RESUMENPPTO_BASE.AUXILIAR || '  ' || V_RESUMENPPTO_BASE.CENTRO_COSTO || '  ' || V_RESUMENPPTO_BASE.REFERENCIA)
ORDER BY V_RESUMENPPTO_BASE.ID,
        V_RESUMENPPTO_BASE.NOMBRE]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);