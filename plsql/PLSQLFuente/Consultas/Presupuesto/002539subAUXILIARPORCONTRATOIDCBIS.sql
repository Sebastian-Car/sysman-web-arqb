MERGE INTO CONSULTAS FIN USING (SELECT '002539subAUXILIARPORCONTRATOIDCBIS' INFORME ,TO_CLOB(q'[SELECT TO_CHAR(TO_DATE(DETALLE_COMPROBANTE_PPTAL.MES || '-' || DETALLE_COMPROBANTE_PPTAL.ANO, 'MM YYYY'), 'Month YYYY','nls_date_language=spanish') AS MES,
    COMPROBANTE_PPTAL.TERCERO,
    COMPROBANTE_PPTAL.TIPOCONTRATO TIPO_CONTRATO,
    SUM(VALOR_DEBITO) VALOR_DEBITO,
    DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE_AFECT TIPOORDENPAGO,
    DETALLE_COMPROBANTE_PPTAL.CMPTE_AFECTADO NROORDENPAGO,
    DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE TIPOPAGO,
    DETALLE_COMPROBANTE_PPTAL.COMPROBANTE NROPAGO,
    COMPROBANTE_PPTAL.NUMEROCONTRATO NUMEROCONTRATO
    FROM COMPROBANTE_PPTAL
    INNER JOIN DETALLE_COMPROBANTE_PPTAL
    ON COMPROBANTE_PPTAL.COMPANIA = DETALLE_COMPROBANTE_PPTAL.COMPANIA
    AND COMPROBANTE_PPTAL.ANO = DETALLE_COMPROBANTE_PPTAL.ANO
    AND COMPROBANTE_PPTAL.TIPO = DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE
    AND COMPROBANTE_PPTAL.NUMERO = DETALLE_COMPROBANTE_PPTAL.COMPROBANTE
    LEFT JOIN TIPO_COMPROBPP
    ON COMPROBANTE_PPTAL.COMPANIA = TIPO_COMPROBPP.COMPANIA
    AND COMPROBANTE_PPTAL.TIPO = TIPO_COMPROBPP.CODIGO 
    WHERE TIPO_COMPROBPP.CLASE = 'EGR'
    AND COMPROBANTE_PPTAL.TIPOCONTRATO = $P{PR_NO_DOCUMENTO}
    AND COMPROBANTE_PPTAL.NUMEROCONTRATO = $P{PR_NO_CONTRATO}
    GROUP BY DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE, DETALLE_COMPROBANTE_PPTAL.COMPROBANTE,
    COMPROBANTE_PPTAL.TIPOCONTRATO, COMPROBANTE_PPTAL.NUMEROCONTRATO, 
    DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE_AFECT, DETALLE_COMPROBANTE_PPTAL.CMPTE_AFECTADO,
    COMPROBANTE_PPTAL.TERCERO, DETALLE_COMPROBANTE_PPTAL.MES, DETALLE_COMPROBANTE_PPTAL.ANO]') CONSULTA, 3 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'MROSERO' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);