MERGE INTO CONSULTAS FIN USING (SELECT '002448CertificadoContratoCORTOLIMA' INFORME ,TO_CLOB(q'[SELECT ORDENDECOMPRA.COMPANIA AS COMPANIA, 
  ORDENDECOMPRA.NUMERO as NUMERO,
  TIPOORDENDECOMPRA.NOMBRE AS NOMBRETIPO,
  ORDENDECOMPRA.CLASEORDEN AS CLASEORDEN,
  ORDENDECOMPRA.OBJETOCONTRATO AS OBJETO,
  ORDENDECOMPRA.OBLIGACIONC AS ACTIVIDAD,
  NVL2(ORDENDECOMPRA.PLAZODEENTREGA,ORDENDECOMPRA.PLAZODEENTREGA || ' DIAS','-') AS PLAZO,
  NVL(TRIM(UPPER(ORDENDECOMPRA.DURACION)),NVL2(ORDENDECOMPRA.PLAZODEENTREGA,ORDENDECOMPRA.PLAZODEENTREGA || ' ' || DECODE(ORDENDECOMPRA.UNIDAD_PLAZO,1,'DIAS',2,'MESES',3,'AÑOS','DIAS'),'-')) AS DURACION,
  ORDENDECOMPRA.VALORTOTAL AS VALORCONTRATO, 
  TO_CHAR(ORDENDECOMPRA.FECHA,'DD/MM/YYYY') FECHAINICIO,
  TRIM(TO_CHAR(ORDENDECOMPRA.FECHA,'DD "DE" MONTH','NLS_DATE_LANGUAGE = SPANISH')) || ' ' || TRIM(TO_CHAR(ORDENDECOMPRA.FECHA,' "DE" YYYY','NLS_DATE_LANGUAGE = SPANISH')) FECHAINICIOL,
  PCK_SYSMAN_UTL.FC_VALOR_LETRAS(TO_NUMBER(TO_CHAR(SYSDATE,'DD')),-1) || TO_CHAR(SYSDATE,' "("DD") dias del mes de" MONTH "de" YYYY','NLS_DATE_LANGUAGE = SPANISH') FECHAACTUAL,
  TO_CHAR(ORDENDECOMPRA.FECHAFINALIZACION,'DD/MM/YYYY') FECHAFIN ,
  TRIM(TO_CHAR(ORDENDECOMPRA.FECHAFINALIZACION,'DD "DE" MONTH','NLS_DATE_LANGUAGE = SPANISH')) || ' ' || TRIM(TO_CHAR(ORDENDECOMPRA.FECHAFINALIZACION,' "DE" YYYY','NLS_DATE_LANGUAGE = SPANISH')) FECHAFINL,
  TERCERO.NIT AS CEDULA,
  TERCERO.NOMBRE AS TERCERO,
  NVL(TERCERO.EXPEDIDACEDULA,'-') AS EXPCEDULA,
  NVL(PCK_SYSMAN_UTL.FC_PAR(ORDENDECOMPRA.COMPANIA, 'FIRMA DEL ASESOR JURIDICO',-1,SYSDATE),'-') FIRMA,
  NVL(PCK_SYSMAN_UTL.FC_PAR(ORDENDECOMPRA.COMPANIA, 'ASESOR JURIDICO',-1,SYSDATE),NVL(PCK_SYSMAN_UTL.FC_PAR(ORDENDECOMPRA.COMPANIA, 'ASESOR JURÍDICO',-1,SYSDATE),'-')) CARGO,
  PCK_CONEXION.FC_GETUSER ELABORO,
  DECODE(TERCERO.NATURALEZA,'N','el (la) señor (a)','la entidad') TEXTOENTIDAD,
  TIPOS_DOCUMENTOS.DESCRIPCION || ' No. ' || TERCERO.NIT || DECODE(TIPOS_DOCUMENTOS.DCTO_IDENTIDAD,'C',' expedida en ' || NVL(TERCERO.EXPEDIDACEDULA,'-'),'') TEXTOID,
  RUTA_IMAGEN LOGO
FROM ORDENDECOMPRA 
       INNER JOIN COMPANIA
              ON ORDENDECOMPRA.COMPANIA = COMPANIA.CODIGO
       INNER JOIN TIPOORDENDECOMPRA 
              ON  ORDENDECOMPRA.COMPANIA  = TIPOORDENDECOMPRA.COMPANIA 
             AND ORDENDECOMPRA.CLASEORDEN = TIPOORDENDECOMPRA.CODIGO 
       INNER JOIN TERCERO 
              ON  ORDENDECOMPRA.COMPANIA  = TERCERO.COMPANIA 
             AND ORDENDECOMPRA.TERCERO    = TERCERO.NIT 
             AND ORDENDECOMPRA.SUCURSAL   = TERCERO.SUCURSAL      
       INNER JOIN TIPOS_DOCUMENTOS
       			ON TERCERO.COMPANIA = TIPOS_DOCUMENTOS.COMPANIA
       			AND TERCERO.TIPOID = TIPOS_DOCUMENTOS.DCTO_IDENTIDAD	           
WHERE ORDENDECOMPRA.COMPANIA   = s$compania$s
  AND ORDENDECOMPRA.CLASEORDEN BETWEEN s$tipoContratoInicial$s AND s$tipoContratoFinal$s
  AND ORDENDECOMPRA.TERCERO    BETWEEN s$terceroInicial$s  AND s$terceroFinal$s
  AND ORDENDECOMPRA.FECHA      BETWEEN s$fechaInicial$s AND s$fechaFinal$s
 ORDER BY TERCERO.NIT, ORDENDECOMPRA.NUMERO]') CONSULTA, 9 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
COMMIT; 