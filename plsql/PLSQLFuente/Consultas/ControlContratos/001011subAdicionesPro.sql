SELECT TO_CHAR(FECHA,'DD/MM/YYYY') FECHA,
       DURACION,
       VALORTOTAL
FROM   ORDENDECOMPRA
WHERE  COMPANIA       = $P{PR_COMPANIA}
  AND  TIPOAFECTADO   = $P{PR_TIPO_CONTRATO}
  AND  NUMEROAFECTADO = $P{PR_NUMERO_CONTRATO}
  AND  CLASENOVEDAD IN ('O','D')
