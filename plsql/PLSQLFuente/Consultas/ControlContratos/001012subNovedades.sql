SELECT TO_CHAR(FECHA,'DD/MM/YYYY') FECHA,
       NOVEDAD NUMERO
FROM   NOVEDADCONTRATO
WHERE  COMPANIA      = $P{PR_COMPANIA}
  AND  CLASEORDEN    = $P{PR_TIPO_CONTRATO}
  AND  ORDENDECOMPRA = $P{PR_NUMERO_CONTRATO}
  AND  TIPOT         = 'ACS'
