MERGE INTO CONSULTAS FIN USING (SELECT '000389InformeDeContratos' INFORME ,TO_CLOB(q'[SELECT
      ORDENDECOMPRA.COMPANIA,
      TO_CHAR(ORDENDECOMPRA.FECHA,'DD/MM/YYYY') ORDENFECHA,
      ORDENDECOMPRA.NUMERO,
      ORDENDECOMPRA.REGISTROBPI,
      ORDENDECOMPRA.ESTUDIOSPREVIOS,
      V_ORDENDECOMPRA_DIS.COMPROBANTES DISPONIBILIDAD,
      V_ORDENDECOMPRA_DIS.FECHACDP FECHADEDISPONIBILIDAD,
      CASE ORDENDECOMPRA.TERMINOSDEREFERENCIA WHEN 0 THEN ' ' ELSE 'X' END  TERMINOSDEREFERENCIA,
      CASE ORDENDECOMPRA.LICITACIONOCONCURSO WHEN 0 THEN ' ' ELSE 'X' END LICITACIONOCONCURSO ,
      CASE TIPOORDENDECOMPRA.GENERACONTRATO WHEN 0 THEN ' ' ELSE 'X' END CONFORMALIDADESPLENAS,
      TO_CHAR(ORDENDECOMPRA.FECHAFIJACION,'DD/MM/YYYY') FECHAFIJACION,
      TO_CHAR(ORDENDECOMPRA.FECHADESFIJACION,'DD/MM/YYYY') FECHADESFIJACION,
      ORDENDECOMPRA.OBJETOCONTRATO,
      ORDENDECOMPRA.CLASE,
      ORDENDECOMPRA.VALORTOTAL ORDENVALORTOTAL,
      TERCERO.NOMBRE,
      TERCERO.NIT NIT_CEDULA,
      TERCERO.DIRECCION,
      TO_CHAR(ORDENDECOMPRA.FECHAFIRMA,'DD/MM/YYYY') FECHAFIRMA,
      CASE WHEN ORDENDECOMPRAPPTO.TIPOPPTO IN (SELECT CODIGO FROM TIPO_COMPROBPP where CLASE NOT IN ('DIS')) THEN TO_CHAR(ORDENDECOMPRAPPTO.FECHA,'DD/MM/YYYY') END FECHAREGISTRO,
      CASE WHEN ORDENDECOMPRAPPTO.TIPOPPTO IN (SELECT CODIGO FROM TIPO_COMPROBPP where CLASE NOT IN ('DIS')) THEN ORDENDECOMPRAPPTO.NUMEROPPTO END RESERVA,
      CASE WHEN ORDENDECOMPRAPPTO.TIPOPPTO IN (SELECT CODIGO FROM TIPO_COMPROBPP where CLASE NOT IN ('DIS')) THEN ORDENDECOMPRAPPTO.RUBRO END RUBRORESERVA,
      ORDENDECOMPRA.DURACION,
      TO_CHAR(NOVEDADCONTRATO.FECHA,'DD/MM/YYYY') FECHANOVEDAD,
      TO_CHAR(NOVEDADCONTRATO.FECHAVENCIMIENTO,'DD/MM/YYYY') FECHAVENCIMIENTO,
      NOVEDADCONTRATO.VALORTOTAL NOVEDADVALORTOTAL,
      ORDENDECOMPRA.VALORPAGOS,
      TO_CHAR(ORDENDECOMPRA.FECHARECIBO,'DD/MM/YYYY') FECHARECIBO,
      TO_CHAR(ORDENDECOMPRA.FECHALIQUIDACION,'DD/MM/YYYY') FECHALIQUIDACION,
      TO_CHAR(ORDENDECOMPRA.FECHAPAGOS,'DD/MM/YYYY') FECHAPAGOS,
      TO_CHAR(ORDENDECOMPRA.FECHAPOLIZAS,'DD/MM/YYYY') FECHAPOLIZAS,
      ORDENDECOMPRA.NOPROCESO
FROM ORDENDECOMPRA
  LEFT JOIN NOVEDADCONTRATO
      ON  ORDENDECOMPRA.COMPANIA    = NOVEDADCONTRATO.COMPANIA
      AND ORDENDECOMPRA.CLASEORDEN  = NOVEDADCONTRATO.CLASEORDEN
      AND ORDENDECOMPRA.NUMERO      = NOVEDADCONTRATO.ORDENDECOMPRA
  LEFT JOIN ORDENDECOMPRAPPTO
      ON  ORDENDECOMPRA.COMPANIA    = ORDENDECOMPRAPPTO.COMPANIA
      AND ORDENDECOMPRA.CLASEORDEN  = ORDENDECOMPRAPPTO.CLASEORDEN
      AND ORDENDECOMPRA.NUMERO      = ORDENDECOMPRAPPTO.NUMERO
  LEFT JOIN CLASETRANSACCIONC
      ON NOVEDADCONTRATO.TIPOT      = CLASETRANSACCIONC.TIPOT
      AND NOVEDADCONTRATO.CLASET    = CLASETRANSACCIONC.CLASET
  LEFT JOIN TERCERO
      ON  ORDENDECOMPRA.COMPANIA    = TERCERO.COMPANIA
      AND ORDENDECOMPRA.TERCERO     = TERCERO.NIT
      AND ORDENDECOMPRA.SUCURSAL    = TERCERO.SUCURSAL
  LEFT JOIN TIPOORDENDECOMPRA
      ON  ORDENDECOMPRA.COMPANIA    = TIPOORDENDECOMPRA.COMPAN]') || TO_CLOB(q'[IA
      AND ORDENDECOMPRA.CLASEORDEN  = TIPOORDENDECOMPRA.CODIGO
  LEFT JOIN V_ORDENDECOMPRA_DIS
      ON  ORDENDECOMPRA.COMPANIA    = V_ORDENDECOMPRA_DIS.COMPANIA
      AND ORDENDECOMPRA.CLASEORDEN  = V_ORDENDECOMPRA_DIS.CLASEORDEN
      AND ORDENDECOMPRA.NUMERO      = V_ORDENDECOMPRA_DIS.NUMERO
WHERE ORDENDECOMPRA.COMPANIA = s$compania$s
    AND ORDENDECOMPRA.FECHA BETWEEN s$fechaInicial$s  AND s$fechaFinal$s
    AND NVL(TIPOORDENDECOMPRA.CLASE,' ') NOT IN 'M'
    AND ORDENDECOMPRA.ESTADO NOT IN 'A'
GROUP BY
     ORDENDECOMPRA.COMPANIA,
      TO_CHAR(ORDENDECOMPRA.FECHA,'DD/MM/YYYY') ,
      ORDENDECOMPRA.NUMERO,
      ORDENDECOMPRA.REGISTROBPI,
      ORDENDECOMPRA.ESTUDIOSPREVIOS,
      V_ORDENDECOMPRA_DIS.COMPROBANTES ,
      V_ORDENDECOMPRA_DIS.FECHACDP ,
      CASE ORDENDECOMPRA.TERMINOSDEREFERENCIA WHEN 0 THEN ' ' ELSE 'X' END  ,
      CASE ORDENDECOMPRA.LICITACIONOCONCURSO WHEN 0 THEN ' ' ELSE 'X' END  ,
      CASE TIPOORDENDECOMPRA.GENERACONTRATO WHEN 0 THEN ' ' ELSE 'X' END ,
      TO_CHAR(ORDENDECOMPRA.FECHAFIJACION,'DD/MM/YYYY') ,
      TO_CHAR(ORDENDECOMPRA.FECHADESFIJACION,'DD/MM/YYYY') ,
      ORDENDECOMPRA.OBJETOCONTRATO,
      ORDENDECOMPRA.CLASE,
      ORDENDECOMPRA.VALORTOTAL ,
      TERCERO.NOMBRE,
      TERCERO.NIT ,
      TERCERO.DIRECCION,
      TO_CHAR(ORDENDECOMPRA.FECHAFIRMA,'DD/MM/YYYY') ,
      CASE WHEN ORDENDECOMPRAPPTO.TIPOPPTO IN (SELECT CODIGO FROM TIPO_COMPROBPP where CLASE NOT IN ('DIS')) THEN TO_CHAR(ORDENDECOMPRAPPTO.FECHA,'DD/MM/YYYY') END ,
      CASE WHEN ORDENDECOMPRAPPTO.TIPOPPTO IN (SELECT CODIGO FROM TIPO_COMPROBPP where CLASE NOT IN ('DIS')) THEN ORDENDECOMPRAPPTO.NUMEROPPTO END ,
      CASE WHEN ORDENDECOMPRAPPTO.TIPOPPTO IN (SELECT CODIGO FROM TIPO_COMPROBPP where CLASE NOT IN ('DIS')) THEN ORDENDECOMPRAPPTO.RUBRO END ,
      ORDENDECOMPRA.DURACION,
      TO_CHAR(NOVEDADCONTRATO.FECHA,'DD/MM/YYYY') ,
      TO_CHAR(NOVEDADCONTRATO.FECHAVENCIMIENTO,'DD/MM/YYYY') ,
      NOVEDADCONTRATO.VALORTOTAL ,
      ORDENDECOMPRA.VALORPAGOS,
      TO_CHAR(ORDENDECOMPRA.FECHARECIBO,'DD/MM/YYYY') ,
      TO_CHAR(ORDENDECOMPRA.FECHALIQUIDACION,'DD/MM/YYYY') ,
      TO_CHAR(ORDENDECOMPRA.FECHAPAGOS,'DD/MM/YYYY') ,
      TO_CHAR(ORDENDECOMPRA.FECHAPOLIZAS,'DD/MM/YYYY'),
      ORDENDECOMPRA.NOPROCESO]') CONSULTA, 9 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);