MERGE INTO CONSULTAS FIN USING (SELECT '800504ContratosAvancesLegalizaciones' INFORME ,TO_CLOB(q'[SELECT ORDENDECOMPRA.CLASEORDEN,
       ORDENDECOMPRA.NUMERO,
       ORDENDECOMPRA.TERCERO,
       ORDENDECOMPRA.SUCURSAL,
       TERCERO.NOMBRE NOMBRE,
       TO_CHAR(ORDENDECOMPRA.FECHA,'DD/MM/YYYY') FECHA,
       ORDENDECOMPRA.OBJETOCONTRATO,
       ORDENDECOMPRAPPTO.RUBRO RUBRO,
       TO_CHAR(ORDENDECOMPRA.FECHAFINALIZACION,'DD/MM/YYYY') FECHAFINALIZACION,
       ORDENDECOMPRA.VALORTOTAL,
       ORDENDECOMPRA.VALORFINAL
FROM  ORDENDECOMPRA
  LEFT JOIN ORDENDECOMPRAPPTO 
    ON ORDENDECOMPRAPPTO.COMPANIA = ORDENDECOMPRA.COMPANIA 
    AND ORDENDECOMPRAPPTO.CLASEORDEN = ORDENDECOMPRA.CLASE
    AND ORDENDECOMPRAPPTO.NUMERO = ORDENDECOMPRA.NUMERO
  LEFT JOIN TERCERO 
    ON ORDENDECOMPRA.COMPANIA = TERCERO.COMPANIA 
    AND ORDENDECOMPRA.TERCERO = TERCERO.NIT
    AND ORDENDECOMPRA.SUCURSAL = TERCERO.SUCURSAL
WHERE ORDENDECOMPRA.COMPANIA = s$compania$s
  s$condicion$s  
  s$condicionB$s 
  AND ORDENDECOMPRA.FECHA BETWEEN   s$fechaInicial$s   AND  s$fechaFinal$s
ORDER BY  ORDENDECOMPRA.CLASEORDEN,ORDENDECOMPRA.NUMERO]') CONSULTA, 9 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
commit;