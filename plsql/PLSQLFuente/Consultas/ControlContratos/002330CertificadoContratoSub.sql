MERGE INTO CONSULTAS FIN USING (SELECT '002330CertificadoContratoSub' INFORME ,TO_CLOB(q'[SELECT 

DISTINCT
  ORDENDECOMPRA.COMPANIA AS COMPANIA,  
  ORDENDECOMPRA.NUMERO as CONTRATO, 
  ORDENDECOMPRA.CLASEORDEN AS CLASEORDEN,
  ORDENDECOMPRA.OBJETOCONTRATO AS OBJETO,
  ORDENDECOMPRA.OBLIGACIONC AS ACTIVIDAD,
  ORDENDECOMPRA.PLAZODEENTREGA AS PLAZO
,
  ORDENDECOMPRA.VALORTOTAL AS VALORTOTAL, 
  TO_CHAR(ORDENDECOMPRA.FECHA,'DD/MM/YYYY') FECHAINICIO,
  TO_CHAR(ORDENDECOMPRA.FECHAFINALIZACION,'DD/MM/YYYY') FECHAFIN
FROM ORDENDECOMPRA 
       INNER JOIN TIPOORDENDECOMPRA 
              ON  ORDENDECOMPRA.COMPANIA  = TIPOORDENDECOMPRA.COMPANIA 
             AND ORDENDECOMPRA.CLASEORDEN = TIPOORDENDECOMPRA.CODIGO 
       INNER JOIN TERCERO 
              ON  ORDENDECOMPRA.COMPANIA  = TERCERO.COMPANIA 
             AND ORDENDECOMPRA.TERCERO    = TERCERO.NIT 
             AND ORDENDECOMPRA.SUCURSAL   = TERCERO.SUCURSAL 
       LEFT JOIN ORDENDECOMPRAPPTO
              ON ORDENDECOMPRA.COMPANIA    = ORDENDECOMPRAPPTO.COMPANIA
             AND ORDENDECOMPRA.CLASEORDEN  = ORDENDECOMPRAPPTO.CLASEORDEN
             AND ORDENDECOMPRA.NUMERO      = ORDENDECOMPRAPPTO.NUMERO
                 
WHERE ORDENDECOMPRA.COMPANIA   = $P{PR_COMPANIA}
  AND ORDENDECOMPRA.CLASEORDEN BETWEEN $P{PR_TCONTRATOI} AND $P{PR_TCONTRATOF}
  AND ORDENDECOMPRA.TERCERO    = $P{PR_CEDULA}
  AND ORDENDECOMPRA.FECHA      BETWEEN $P{PR_FECHAINICIO} AND $P{PR_FECHAFIN}
 ORDER BY ORDENDECOMPRA.NUMERO 
 
]') CONSULTA, 9 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);

COMMIT;
MERGE INTO CONSULTAS_SUB FIN USING (SELECT '002329CertificadoContrato' INFORME_PADRE, '002330CertificadoContratoSub' INFORME_HIJO, 'PR_STRSQL2' PARAMETRO ,NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (FIN.INFORME_PADRE = INI.INFORME_PADRE AND INI.INFORME_HIJO = FIN.INFORME_HIJO)  WHEN MATCHED THEN  UPDATE SET FIN.PARAMETRO = INI.PARAMETRO , FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME_PADRE,INFORME_HIJO, PARAMETRO,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME_PADRE,INI.INFORME_HIJO, INI.PARAMETRO,INI.CREATED_BY,SYSDATE);


commit;       
