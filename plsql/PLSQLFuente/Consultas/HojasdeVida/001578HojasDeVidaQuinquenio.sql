SELECT 
  (QU_TIPODOCUACTO || ' ' || QU_NUME || ' ' || NVL(TO_CHAR(QU_FECHRESO,'DD/MM/YYYY'),' ')) DOCUMENTO
 ,QU_FECHDESDE
 ,QU_FECHHASTA
 ,QU_VALOR
FROM NAT_QUINQUENIO
WHERE COMPANIA    = $P!{PR_COMPANIA} 
  AND DP_NUMEDOCU = $P!{PR_DP_NUMEDOCU} 
  AND SUCURSAL    = $P!{PR_SUCURSAL} 
