SELECT DISTINCT
   CLASE_AGENTE,
   NOMBRECLASEAGENTE NOMBRECLASE,
   NOMBREAGENTE
FROM (s$consultaBase$s)TRANSACCION
WHERE COMPANIA          = $P!{PR_COMPANIA} 
  AND CODIGO            = $P!{PR_TIPO_TRANSACCION} 
  AND CONSECUTIVO       = $P!{PR_CONSECUTIVO}
  AND CLASE_AGENTE IS NOT NULL
