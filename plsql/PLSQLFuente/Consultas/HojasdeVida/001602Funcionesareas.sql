SELECT CODIGOAREA,
       IDCARGO,
       IDFUNCION,
       ANO,
       DESCRIPCION_FUNCION_AREAS,
       FECHA_CREACION
  FROM NAT_FUNCIONES_AREAS
 WHERE COMPANIA   = $P{PR_COMPANIA}
   AND CODIGOAREA = $P{PR_CODIGOAREA}
   AND IDCARGO    = $P{PR_CODIGO}
   AND ANO        = $P{PR_ANO}
