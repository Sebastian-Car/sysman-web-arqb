MERGE INTO CONSULTAS FIN USING (SELECT '000355LisTerceros' INFORME ,TO_CLOB(q'[SELECT 	TERCERO.NIT,
          TIPOS_DOCUMENTOS.DESCRIPCION TIPOID,
        TERCERO.SUCURSAL TERCEROSUCURSAL, 
        TERCERO.NOMBRE TERCERONOMBRE, 
        TERCERO.DIRECCION, 
        TERCERO.TELEFONOS, 
        TERCERO.FAX, 
        CLASETERCERO.NOMBRE CLASE,  
        DEPARTAMENTO.NOMBRE DEPARTAMENTO,  
        CIUDAD.NOMBRE NOMBRECIUDAD,
        TERCERO.DIRECCIONEMAIL 
FROM TERCERO  
	INNER JOIN PAISES  
 		ON TERCERO.PAIS 		= PAISES.PAIS 
 	INNER JOIN DEPARTAMENTO  
    ON TERCERO.PAIS 		= DEPARTAMENTO.PAIS 
 		AND TERCERO.DEPARTAMENTO 	= DEPARTAMENTO.CODIGO  
 	INNER JOIN CIUDAD  
 		ON  TERCERO.PAIS 		= CIUDAD.PAIS 
 		AND TERCERO.DEPARTAMENTO 	= CIUDAD.DEPARTAMENTO 
 		AND TERCERO.CIUDAD 		= CIUDAD.CODIGO 
  INNER JOIN CLASETERCERO 
    ON TERCERO.CLASE = CLASETERCERO.CODIGO 
  LEFT JOIN TIPOS_DOCUMENTOS
      ON TIPOS_DOCUMENTOS.COMPANIA = TERCERO.COMPANIA
     AND TIPOS_DOCUMENTOS.DCTO_IDENTIDAD = TERCERO.TIPOID
  s$strOrderBy$s]') CONSULTA, -1 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);