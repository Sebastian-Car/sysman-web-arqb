MERGE INTO CONSULTAS FIN USING (SELECT '001487INFCONTABILIDAD' INFORME ,TO_CLOB(q'[SELECT FSF.NUMERO_FACTURA,
  FSF.TERCERO,
  TERCERO.NOMBRE NOMBRETERCERO,
  FSF.VALOR_TOTAL,
  FSF.TIPO_CPTE,
  FSF.NRO_CPTE,
  CCNT.FECHA,
  FSF.TIPOCPTE_PAGO,
  FSF.NROCPTE_PAGO,
  FSF.FECHA_PAGO,
  CASE
    WHEN FSF.ANULADA = '0'
    THEN 'NO'
    ELSE 'SI'
  END ANULADA,
  FSF.CPTE_ANULACION,
  FSF.NRO_ANULACION,
  FSF.FECHA_ANULACION
FROM COMPROBANTE_CNT CCNT
INNER JOIN SF_FACTURA FSF
ON CCNT.COMPANIA   = FSF.COMPANIA
AND CCNT.NUMERO    = FSF.NRO_CPTE
AND CCNT.TIPO      = FSF.TIPO_CPTE
INNER JOIN TERCERO
ON TERCERO.COMPANIA = FSF.COMPANIA
AND TERCERO.NIT = FSF.TERCERO
WHERE FSF.COMPANIA = 's$compania$s'
s$condicionFechas$s
AND FSF.NUMERO_FACTURA BETWEEN 's$facturaInicial$s' AND 's$facturaFinal$s'
AND FSF.INTERFAZADA NOT IN (0)]') CONSULTA, 69 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);