MERGE INTO CONSULTAS FIN USING (SELECT '800670InformeContraloria' INFORME ,TO_CLOB(q'[SELECT SUBSTR(DP.ELEMENTO,1,PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA => DP.COMPANIA, UN_NOMBRE => 'DIGITOS AGRUPACION INVENTARIO',UN_MODULO => PCK_DATOS.FC_MODULOALMACEN,UN_FECHA_PAR => SYSDATE)) AS GRUPO,
TO_CHAR(DP.SERIE) AS PLACA,
I.NOMBRELARGO AS NOMBRE,
DV.DESCRIPCION,
DV.SERIEDEVOLUTIVO AS SERIE_DEVOLUTIVO,
ED.DESCRIPCION AS ESTADO,
DP.NIIF_VLRELEMENTO AS VALOR_ADQUISICION,
TO_CHAR(DV.NIIF_FECHAADQUISICION,'DD/MM/YYYY') AS FECHA_ADQUISICION,
TO_CHAR(DV.NIIF_FECHAFUNCIONAMIENTO,'DD/MM/YYYY') AS FECHA_FUNCIONAMIENTO,
DV.NIIF_VIDA_UTIL AS VIDA_UTIL_NIIF,
CASE
    WHEN DV.NIIF_VIDA_UTIL - MONTHS_BETWEEN(SYSDATE, DV.NIIF_FECHAFUNCIONAMIENTO) < 0 THEN 0
    ELSE ROUND(DV.NIIF_VIDA_UTIL - MONTHS_BETWEEN(SYSDATE, DV.NIIF_FECHAFUNCIONAMIENTO),0)
END AS VIDA_UTIL_RESTANTE,
DP.NIIF_VLRELEMENTO AS VALOR_HISTORICO,
DV.SALVAMENTO,
DP.NIIF_VLRDEPRECIACION AS DEPRECIACION_PERIODO,
DP.NIIF_VLRACUMULADO AS DEPRECIACION_ACUMULADA,
(DP.NIIF_VLRACUMULADO - DP.NIIF_VLRDEPRECIACION) AS DEPRECIACION_ANTERIOR,
(DP.NIIF_VLRLIBROS - DV.DETERIORO) AS VALOR_POR_DEPRECIAR,
DP.NIIF_VLRLIBROS AS VALOR_EN_LIBROS,
CASE  WHEN MOVIMIENTO.DEPENDENCIA_DESTINO = PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA => DP.COMPANIA ,UN_NOMBRE => 'BODEGA ALMACEN',UN_MODULO => PCK_DATOS.FC_MODULOALMACEN,UN_FECHA_PAR => SYSDATE) THEN 'BODEGA ALMACEN'
        WHEN MOVIMIENTO.DEPENDENCIA_DESTINO = PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA => DP.COMPANIA ,UN_NOMBRE => 'RESPONSABILIDADES',UN_MODULO => PCK_DATOS.FC_MODULOALMACEN,UN_FECHA_PAR => SYSDATE) THEN 'RESPONSABILIDADES'
        WHEN MOVIMIENTO.DEPENDENCIA_DESTINO = PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA => DP.COMPANIA ,UN_NOMBRE => 'BODEGA INSERVIBLES',UN_MODULO => PCK_DATOS.FC_MODULOALMACEN,UN_FECHA_PAR => SYSDATE) THEN 'INSERVIBLES'
        WHEN DE.COMODATO <> 0 THEN 'COMODATO'
        ELSE 'SERVICIO INTERNO'
    END AS RELACION_DE_DEPRECIACION,
CASE  WHEN MOVIMIENTO.DEPENDENCIA_DESTINO = PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA => DP.COMPANIA ,UN_NOMBRE => 'BODEGA ALMACEN',UN_MODULO => PCK_DATOS.FC_MODULOALMACEN,UN_FECHA_PAR => SYSDATE) THEN 'BODEGA ALMACEN'
        WHEN MOVIMIENTO.DEPENDENCIA_DESTINO = PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA => DP.COMPANIA ,UN_NOMBRE => 'RESPONSABILIDADES',UN_MODULO => PCK_DATOS.FC_MODULOALMACEN,UN_FECHA_PAR => SYSDATE) THEN 'RESPONSABILIDADES'
        WHEN MOVIMIENTO.DEPENDENCIA_DESTINO = PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA => DP.COMPANIA ,UN_NOMBRE => 'BODEGA INSERVIBLES',UN_MODULO => PCK_DATOS.FC_MODULOALMACEN,UN_FECHA_PAR => SYSDATE) THEN 'INSERVIBLES'
        WHEN DE.COMODATO <> 0 THEN 'COMODATO'
        ELSE 'SERVICIO INTERNO'
    END AS RELACION_DE_DEPRECIACION,
CASE
   WHEN PCK_SYSMAN_UTL.FC_PAR(DP.COMPANIA, 'APLICAR DETERIORO ANTES DE DEPRECIACION', 10, SYSDATE) = 'NO' THEN
        (SELECT NVL(SUM(RP2.DETERIORO), 0)
         FROM REEXPRESARVIDAUTIL RP2
         WHERE RP2.COMPANIA = DV.COMPANIA
           AND RP2.ELEMENTO = DV.ELEMENTO
           AND RP2.SERIE = DV.SERIE
           AND TO_CHAR(TRUNC(RP2.FECHACAMBIO, 'MM'), 'YYYYMM') =
    ]') || TO_CLOB(q'[           TO_CHAR(ADD_MONTHS(DP.PERIODO, -1), 'YYYYMM'))
   WHEN PCK_SYSMAN_UTL.FC_PAR(DP.COMPANIA, 'APLICAR DETERIORO ANTES DE DEPRECIACION', 10, SYSDATE) = 'SI' THEN
        (SELECT NVL(SUM(RP2.DETERIORO), 0)
         FROM REEXPRESARVIDAUTIL RP2
         WHERE RP2.COMPANIA = DV.COMPANIA
           AND RP2.ELEMENTO = DV.ELEMENTO
           AND RP2.SERIE = DV.SERIE
           AND TO_CHAR(TRUNC(RP2.FECHACAMBIO, 'MM'), 'YYYYMM') =
               TO_CHAR(DP.PERIODO, 'YYYYMM'))
    ELSE 0
END AS DETERIORO_DEL_PERIODO,
CASE
   WHEN PCK_SYSMAN_UTL.FC_PAR(DP.COMPANIA, 'APLICAR DETERIORO ANTES DE DEPRECIACION', 10, SYSDATE) = 'NO' THEN
        (SELECT NVL(SUM(RP2.DETERIORO), 0)
         FROM REEXPRESARVIDAUTIL RP2
         WHERE RP2.COMPANIA = DV.COMPANIA
           AND RP2.ELEMENTO = DV.ELEMENTO
           AND RP2.SERIE = DV.SERIE
           AND TO_CHAR(TRUNC(RP2.FECHACAMBIO, 'MM'), 'YYYYMM') <
               TO_CHAR(ADD_MONTHS(DP.PERIODO, -1), 'YYYYMM'))
   WHEN PCK_SYSMAN_UTL.FC_PAR(DP.COMPANIA, 'APLICAR DETERIORO ANTES DE DEPRECIACION', 10, SYSDATE) = 'SI' THEN
        (SELECT NVL(SUM(RP2.DETERIORO), 0)
         FROM REEXPRESARVIDAUTIL RP2
         WHERE RP2.COMPANIA = DV.COMPANIA
           AND RP2.ELEMENTO = DV.ELEMENTO
           AND RP2.SERIE = DV.SERIE
           AND TO_CHAR(TRUNC(RP2.FECHACAMBIO, 'MM'), 'YYYYMM') <
               TO_CHAR(DP.PERIODO, 'YYYYMM'))
    ELSE 0
END AS DETERIORO_ACUMULADO,
'NO' AS REVELACIONES,
CASE WHEN MOVIMIENTO.DEPENDENCIA_DESTINO = PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA => DP.COMPANIA ,UN_NOMBRE => 'BODEGA ALMACEN',UN_MODULO => PCK_DATOS.FC_MODULOALMACEN,UN_FECHA_PAR => SYSDATE) THEN I.NIIF_CUENTAACTIVO
        WHEN MOVIMIENTO.DEPENDENCIA_DESTINO = PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA => DP.COMPANIA ,UN_NOMBRE => 'RESPONSABILIDADES',UN_MODULO => PCK_DATOS.FC_MODULOALMACEN,UN_FECHA_PAR => SYSDATE) THEN I.NIIF_CUENTAACTIVOR
        WHEN MOVIMIENTO.DEPENDENCIA_DESTINO  = PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA => DP.COMPANIA ,UN_NOMBRE => 'BODEGA INSERVIBLES',UN_MODULO => PCK_DATOS.FC_MODULOALMACEN,UN_FECHA_PAR => SYSDATE) THEN I.NIIF_CUENTAACTIVOI
        WHEN DE.COMODATO <> 0 THEN I.CUENTAACTIVOC
        ELSE I.NIIF_CUENTAACTIVOS
    END AS CUENTA_CONTABLE_ACTIVO,
NI.CUENTACREDITO AS CUENTA_CONTABLE_DEPRECIACION,
' ' AS CUENTA_CONTABLE_DETERIORO,
TERCERO.NOMBRE AS RESPONSABLE,
DE.NOMBRE AS DEPENDENCIA
FROM DEPRECIAR  DP
INNER JOIN DEVOLUTIVO  DV
 ON DP.COMPANIA = DV.COMPANIA
AND DP.ELEMENTO = DV.ELEMENTO
AND DP.SERIE = DV.SERIE
INNER JOIN INVENTARIO  I
 ON DP.COMPANIA = I.COMPANIA
AND SUBSTR(DP.ELEMENTO, 1,PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA => DP.COMPANIA, UN_NOMBRE => 'DIGITOS AGRUPACION INVENTARIO',UN_MODULO => PCK_DATOS.FC_MODULOALMACEN,UN_FECHA_PAR => SYSDATE)) = I.CODIGOELEMENTO
INNER JOIN( SELECT MAX(TO_DATE(TO_CHAR(DM.FECHA, 'DD/MM/YYYY') || ' ' || TO_CHAR(DM.HORA, 'HH24:MI:SS'),'DD/MM/YYYY HH24:MI:SS'))FECHA,
                        COMPANIA,
                        TIPOMOVIMIENTO,
                        MOVIMIENTO,
   ]') || TO_CLOB(q'[                     ELEMENTO,
                        SERIE
                 FROM D_MOVIMIENTO DM
                 WHERE DM.COMPANIA = s$compania$s
                 AND  DM.FECHA <= TO_DATE('s$ultimoDia$s','DD/MM/YYYY')
                 GROUP BY COMPANIA,
                 TIPOMOVIMIENTO,
                 MOVIMIENTO,
                 ELEMENTO,
                 SERIE) TMP_ULTMOVIMIENTO_PLACA_AUX
 ON DP.COMPANIA = TMP_ULTMOVIMIENTO_PLACA_AUX.COMPANIA
AND DP.ELEMENTO = TMP_ULTMOVIMIENTO_PLACA_AUX.ELEMENTO
AND DP.SERIE = TMP_ULTMOVIMIENTO_PLACA_AUX.SERIE
AND TO_DATE(DP.FECHAULTMOV,'DD/MM/YYYY HH24:MI:SS') = TMP_ULTMOVIMIENTO_PLACA_AUX.FECHA
INNER JOIN MOVIMIENTO
 ON TMP_ULTMOVIMIENTO_PLACA_AUX.MOVIMIENTO = MOVIMIENTO.NUMERO
AND TMP_ULTMOVIMIENTO_PLACA_AUX.TIPOMOVIMIENTO = MOVIMIENTO.TIPOMOVIMIENTO
AND TMP_ULTMOVIMIENTO_PLACA_AUX.COMPANIA = MOVIMIENTO.COMPANIA
LEFT JOIN DEPENDENCIA  DE
 ON MOVIMIENTO.COMPANIA = DE.COMPANIA
AND MOVIMIENTO.DEPENDENCIA_DESTINO = DE.CODIGO
LEFT JOIN RESPONSABLE
 ON MOVIMIENTO.COMPANIA = RESPONSABLE.COMPANIA
AND MOVIMIENTO.RESPONSABLE_DESTINO = RESPONSABLE.CEDULA
AND MOVIMIENTO.SUCURSAL_RESDESTINO = RESPONSABLE.SUCURSAL
LEFT JOIN TERCERO
ON MOVIMIENTO.COMPANIA = TERCERO.COMPANIA
AND RESPONSABLE.CEDULA = TERCERO.NIT
AND RESPONSABLE.SUCURSAL = TERCERO.SUCURSAL
LEFT JOIN ESTADODEVOLUTIVO  ED
 ON DV.ESTADO = ED.ESTADO
LEFT JOIN NIIF_INVENTARIOCONTA  NI
 ON DP.COMPANIA = NI.COMPANIA
AND SUBSTR(DP.ELEMENTO,1,s$agrupacion$s) = NI.CODIGOELEMENTO
AND DP.ANO = NI.ANO
AND DP.NIIF_TIPO_ACTIVO = NI.TIPOACTIVO
AND DP.BODEGA = NI.BODEGA
AND DP.CENTRO_COSTO = NI.CENTRO_COSTO
WHERE DP.COMPANIA = s$compania$s
AND DP.ELEMENTO BETWEEN 's$elementoInicial$s' AND 's$elementoFinal$s'
AND DP.PERIODO = TO_DATE('s$ultimoDia$s','DD/MM/YYYY')
AND (DV.FECHAANULADA IS NULL
OR DV.FECHAANULADA > TO_DATE('s$ultimoDia$s','DD/MM/YYYY') )
AND DV.FECHAADQUISICION <= TO_DATE('s$ultimoDia$s','DD/MM/YYYY')
ORDER BY DP.COMPANIA, DP.SERIE, DP.PERIODO]') CONSULTA, 10 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);