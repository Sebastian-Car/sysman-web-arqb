MERGE INTO CONSULTAS FIN USING (SELECT '002812DepreciacionAcumNoCalculada' INFORME ,TO_CLOB(q'[SELECT DA.ELEMENTO, I.NOMBRELARGO, DA.SERIE, D.VALOR VALOR_HISTORICO, D.NIIF_VIDA_UTIL VIDA_UTIL,  TO_CHAR(DA.PERIODO,'DD/MM/YYYY') PERIODO, DA.VALOR_ACUMULADO, DA.VALOR_LIBROS, DA.OBSERVACIONES,
TO_CHAR(DA.DATE_CREATED,'DD/MM/YYYY') FECHA
FROM DEPRECIACION_ACUMULADA DA
INNER JOIN INVENTARIO I
 ON I.COMPANIA = DA.COMPANIA
AND I.CODIGOELEMENTO = DA.ELEMENTO
INNER JOIN DEVOLUTIVO D
 ON D.COMPANIA = DA.COMPANIA
AND D.ELEMENTO = DA.ELEMENTO
AND D.SERIE = DA.SERIE
WHERE DA.COMPANIA = s$compania$s
AND DA.ELEMENTO = s$elemento$s
AND DA.SERIE = s$serie$s]') CONSULTA, 10 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
COMMIT;