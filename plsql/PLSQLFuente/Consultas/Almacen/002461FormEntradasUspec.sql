MERGE INTO CONSULTAS FIN USING (SELECT '002461FormEntradasUspec' INFORME ,TO_CLOB(q'[SELECT AJUSTE_DEC_MANUAL, 
    AJUSTECENTAVOS1_MOV*-1 AJUSTECENTAVOSM, 
    AJUSTECENTAVOS1_MOV AJUSTECENTAVOS1, 
    CANTIDAD,
    COMPANIA,  
    DESCRIPCION_MOV DESCRIPCION,
    ELEMENTO CODIGOELEMENTO,
    EXTRACT(DAY FROM FECHAMOVASOCIADO) || ' de ' || TRIM(TO_CHAR(FECHAMOVASOCIADO,'Month','nls_date_language=spanish')) || ' de ' || EXTRACT(YEAR FROM FECHAMOVASOCIADO) FECHAMOVASOCIADO,  
    TIPOMOVIMIENTO || MOVIMIENTO GRUPO,  
    MOVASOCIADO,
    MOVIMIENTO, 
    (CASE WHEN TRIM(IND_REG) IS NULL 
        THEN 'NONUM' 
    END) NOREGISTRADO, 
    (CASE TRIM(SERIE) WHEN '' 
        THEN NULL  
        ELSE SERIE  
    END) SERIE,
    (CASE TRIM(SERIEDEVOLUTIVO) WHEN '' 
        THEN NULL 
        ELSE SERIEDEVOLUTIVO 
    END) SERIEDEVOLUTIVO, 
    TIPOMOVIMIENTO, 
    TIPOMOVIMIENTO_NOM TIPOMOV,
    TOTALCONAJUSTE_MOV TOTALCONAJUSTE, 
    UNIDAD_NOM NOMBREUNIDAD, 
    VALORTOTAL_D VALORTOTAL, 
    VALORUNITARIO,
    ((CASE TIPOPESONA WHEN 'R' 
        THEN TERCERO_NOM || ', C.C.' || TERCERO  
        ELSE TERCERO_NOM || ', Tel:' || TERCERO_TEL || ' , Nit: ' ||  TERCERO 
     END) || 
     (CASE WHEN TRIM(TERCERO_DIR) IS NULL OR TRIM(TERCERO_DIR) = ' '  
        THEN ' '  
        ELSE ' Dirección: ' || TERCERO_DIR || ' ' 
     END) || 
     (CASE WHEN TRIM(TERCERO_FAX) IS NULL OR TRIM(TERCERO_FAX) = ' ' 
        THEN '' 
        ELSE '  Fax: ' || TERCERO_FAX 
     END)) INFPERSONA, 
    (NOMBRELARGO ||  ' ' || 
     (CASE TRIM(ESPECIFICACION) WHEN NULL 
        THEN ' '  
        ELSE ESPECIFICACION 
      END) || 
     (CASE WHEN TRIM(MARCA) IS NOT NULL OR TRIM(MARCA )NOT IN '' 
        THEN ', MARCA ' || MARCA || '' 
        ELSE '' 
      END) ||  
     (CASE WHEN TRIM(SERIEDEVOLUTIVO) IS NULL OR SERIEDEVOLUTIVO = ' ' 
        THEN ''  
        ELSE ', Serie: ' || SERIEDEVOLUTIVO 
      END) || 
     (CASE WHEN TRIM(ESCRITURA) IS NULL  
        THEN ' ' 
        ELSE 'Escritura: ' || ESCRITURA 
      END) ||  
     (CASE WHEN TRIM(NUMEROCATASTRAL) IS  NULL 
        THEN ' ' 
        ELSE 'No. Catastro: ' || NUMEROCATASTRAL 
      END) ||  
     (CASE WHEN TRIM(MATINMOBILIARIA) IS NULL 
        THEN ' ' 
        ELSE 'Matrícula: ' || MATINMOBILIARIA 
      END) ||  
     (CASE WHEN TRIM(COD_CHIP) IS NULL 
        THEN ' ' 
        ELSE 'Chip: ' || COD_CHIP 
      END)) DESCRELEMENTO,
    FACTURANO FACTURA,
    CREATED_BY ELABORO,
    TO_CHAR(FECHA_MOV, 'DD/MM/YYYY') AS FECHA_MOV     
FROM V_MOVIMIENTO_INFORME 
WHERE COMPANIA       = s$compania$s 
  AND TIPOMOVIMIENTO = 's$tipoMovimiento$s' 
  AND MOVIMIENTO BETWEEN s$movimientoInicial$s AND s$movimientoFinal$s ]') CONSULTA, 10 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'jcrojas' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);