MERGE INTO CONSULTAS FIN USING (SELECT '002057AlmacenContabilidadMSIIF3' INFORME ,TO_CLOB(q'[SELECT TO_CHAR(ROWNUM) CONSECUTIVO_O, '1' TIPO_AUXILIAR, '1' AUXILIAR_CONTABLE, 's$identificacionAuxiliar$s' IDENTIFICACION_AUXILIAR, (VALOR_DEBE + VALOR_HABER) VALOR_AUXILIAR
FROM

(SELECT CUENTADEBITO CODIGO_CONTABLE, SUM(DEPRECIAR.NIIF_VLRDEPRECIACION) AS VALOR_DEBE, 0 VALOR_HABER
    FROM DEPRECIAR LEFT JOIN NIIF_INVENTARIOCONTA NI
      ON DEPRECIAR.COMPANIA                          = NI.COMPANIA
     AND DEPRECIAR.ANO                               = NI.ANO 
     AND SUBSTR(DEPRECIAR.ELEMENTO,1, s$digitosAgrupacionInventario$s) = NI.CODIGOELEMENTO 
     AND DEPRECIAR.NIIF_TIPO_ACTIVO                  = NI.TIPOACTIVO   
     AND DEPRECIAR.BODEGA                            = NI.BODEGA
     AND (CASE WHEN s$manejaCentro$s = 'SI' THEN DEPRECIAR.CENTRO_COSTO ELSE '99999999999999999999' END ) = NI.CENTRO_COSTO                    
    LEFT JOIN (SELECT COMPANIA, ANO, CODIGO, NATURALEZA 
               FROM PLAN_CONTABLE 
               WHERE COMPANIA = s$compania$s
                 AND (MOVIMIENTO  NOT IN(0)
                   OR MAN_CEN_CTO NOT IN(0)
                   OR MAN_AUX_TER NOT IN(0)
                   OR MAN_AUX_GEN NOT IN(0)
                   OR MAN_AUX_REF NOT IN(0)
                   OR MAN_AUX_FUE NOT IN(0)
                      )
              ) PLAN_DEBITO  
     ON PLAN_DEBITO.COMPANIA = NI.COMPANIA
    AND PLAN_DEBITO.ANO      = NI.ANO 
    AND PLAN_DEBITO.CODIGO   = NI.CUENTADEBITO         
    LEFT JOIN (SELECT COMPANIA, ANO, CODIGO, NATURALEZA 
               FROM PLAN_CONTABLE 
               WHERE COMPANIA = s$compania$s
                 AND (MOVIMIENTO  NOT IN(0)
                   OR MAN_CEN_CTO NOT IN(0)
                   OR MAN_AUX_TER NOT IN(0)
                   OR MAN_AUX_GEN NOT IN(0)
                   OR MAN_AUX_REF NOT IN(0)
                   OR MAN_AUX_FUE NOT IN(0)
                     )
              ) PLAN_CREDITO  
     ON PLAN_CREDITO.COMPANIA = NI.COMPANIA
    AND PLAN_CREDITO.ANO      = NI.ANO 
    AND PLAN_CREDITO.CODIGO   = NI.CUENTACREDITO   
    WHERE DEPRECIAR.COMPANIA       = s$compania$s
      AND DEPRECIAR.ANO        = s$ano$s
      AND DEPRECIAR.MES        = s$mes$s 
    GROUP BY 
             CUENTADEBITO,
             0
    HAVING SUM(DEPRECIAR.NIIF_VLRDEPRECIACION )>0

UNION 

SELECT CUENTACREDITO CODIGO_CONTABLE,0 VALOR_DEBE, SUM(DEPRECIAR.NIIF_VLRDEPRECIACION) AS VALOR_HABER
    FROM DEPRECIAR LEFT JOIN NIIF_INVENTARIOCONTA NI
      ON DEPRECIAR.COMPANIA                          = NI.COMPANIA
     AND DEPRECIAR.ANO                               = NI.ANO 
     AND SUBSTR(DEPRECIAR.ELEMENTO,1, s$digitosAgrupacionInventario$s) = NI.CODIGOELEMENTO 
     AND DEPRECIAR.NIIF_TIPO_ACTIVO                  = NI.TIPOACTIVO   
     AND DEPRECIAR.BODEGA                            = NI.BODEGA
    AND (CASE WHEN s$manejaCentro$s = 'SI' THEN DEPRECIAR.CENTRO_COSTO ELSE '99999999999999999999' END ) = NI.CENTRO_COSTO                    
    LEFT JOIN (SELECT COMPANIA, ANO, CODIGO, NATURALEZA 
             ]') || TO_CLOB(q'[  FROM PLAN_CONTABLE 
               WHERE COMPANIA = s$compania$s
                 AND (MOVIMIENTO  NOT IN(0)
                   OR MAN_CEN_CTO NOT IN(0)
                   OR MAN_AUX_TER NOT IN(0)
                   OR MAN_AUX_GEN NOT IN(0)
                   OR MAN_AUX_REF NOT IN(0)
                   OR MAN_AUX_FUE NOT IN(0)
                      )
              ) PLAN_DEBITO  
     ON PLAN_DEBITO.COMPANIA = NI.COMPANIA
    AND PLAN_DEBITO.ANO      = NI.ANO 
    AND PLAN_DEBITO.CODIGO   = NI.CUENTADEBITO         
    LEFT JOIN (SELECT COMPANIA, ANO, CODIGO, NATURALEZA 
               FROM PLAN_CONTABLE 
               WHERE COMPANIA = s$compania$s
                 AND (MOVIMIENTO  NOT IN(0)
                   OR MAN_CEN_CTO NOT IN(0)
                   OR MAN_AUX_TER NOT IN(0)
                   OR MAN_AUX_GEN NOT IN(0)
                   OR MAN_AUX_REF NOT IN(0)
                   OR MAN_AUX_FUE NOT IN(0)
                     )
              ) PLAN_CREDITO  
     ON PLAN_CREDITO.COMPANIA = NI.COMPANIA
    AND PLAN_CREDITO.ANO      = NI.ANO 
    AND PLAN_CREDITO.CODIGO   = NI.CUENTACREDITO   
    WHERE DEPRECIAR.COMPANIA       = s$compania$s
      AND DEPRECIAR.ANO        = s$ano$s
      AND DEPRECIAR.MES        = s$mes$s 
    GROUP BY 0,
             CUENTACREDITO
             

    HAVING SUM(DEPRECIAR.NIIF_VLRDEPRECIACION )>0)]') CONSULTA, 10 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);