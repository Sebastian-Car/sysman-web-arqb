MERGE INTO CONSULTAS FIN USING (SELECT '002055AlmacenContabilidadTSIIF3' INFORME ,TO_CLOB(q'[SELECT TO_CHAR(ROWNUM) "Consecutivo O", '1'  "Tipo de Auxiliar", '1'  "Auxiliar Contable", 's$identificacionAuxiliar$s' "Identificación Auxiliar", (VALOR_DEBE + VALOR_HABER) "Valor Auxiliar"
FROM

(SELECT 
  CASE WHEN NIIF_CUENTACREDITO IS NOT NULL THEN  NIIF_CUENTACREDITO  ELSE  ALMACENCONTABILIDAD.CUENTACREDITO  END  CODIGO_CONTABLE,
 0 VALOR_DEBE,
  SUM(CASE WHEN  0  NOT IN(0) THEN  NIIF_VALOR_TOTAL  ELSE D_MOVIMIENTO.VALORTOTAL  END)    VALOR_HABER
FROM   D_MOVIMIENTO

INNER JOIN TIPOMOVIMIENTO 
  ON TIPOMOVIMIENTO.COMPANIA = D_MOVIMIENTO.COMPANIA
  AND TIPOMOVIMIENTO.CODIGO   = D_MOVIMIENTO.TIPOMOVIMIENTO
INNER JOIN ALMACENCONTABILIDAD 
  ON D_MOVIMIENTO.COMPANIA       = ALMACENCONTABILIDAD.COMPANIA
  AND D_MOVIMIENTO.TIPOMOVIMIENTO = ALMACENCONTABILIDAD.TIPOMOVIMIENTO
  AND SUBSTR(D_MOVIMIENTO.ELEMENTO,1,LENGTH(ALMACENCONTABILIDAD.CODIGOELEMENTO)) = ALMACENCONTABILIDAD.CODIGOELEMENTO
WHERE D_MOVIMIENTO.COMPANIA  = 's$compania$s' 
  AND TRUNC(D_MOVIMIENTO.FECHA)=TO_DATE('s$fechaAlmacenContabilidadTSiif$s','DD/MM/YYYY') 
  AND IND_REG   NOT IN(0)
  AND ALMACENCONTABILIDAD.ANO = TO_NUMBER(TO_CHAR(TO_DATE('s$fechaAlmacenContabilidadTSiif$s','DD/MM/YYYY'),'YYYY'))
  AND NOT (TIPOMOVIMIENTO.CONCEPTO IN ('T') AND TIPOMOVIMIENTO.CLASE IN('T'))
GROUP BY 
  CASE WHEN NIIF_CUENTACREDITO IS NOT NULL THEN  NIIF_CUENTACREDITO  ELSE  ALMACENCONTABILIDAD.CUENTACREDITO  END ,
  0

UNION

SELECT 
  CASE WHEN NIIF_CUENTADEBITO IS NOT NULL THEN  NIIF_CUENTADEBITO   ELSE  ALMACENCONTABILIDAD.CUENTADEBITO   END  CODIGO_CONTABLE,
  SUM(CASE WHEN  0  NOT IN(0) THEN  NIIF_VALOR_TOTAL  ELSE D_MOVIMIENTO.VALORTOTAL  END)    VALOR_DEBE,
  0 VALOR_HABER

FROM   D_MOVIMIENTO

INNER JOIN TIPOMOVIMIENTO 
  ON TIPOMOVIMIENTO.COMPANIA = D_MOVIMIENTO.COMPANIA
  AND TIPOMOVIMIENTO.CODIGO   = D_MOVIMIENTO.TIPOMOVIMIENTO
INNER JOIN ALMACENCONTABILIDAD 
  ON D_MOVIMIENTO.COMPANIA       = ALMACENCONTABILIDAD.COMPANIA
  AND D_MOVIMIENTO.TIPOMOVIMIENTO = ALMACENCONTABILIDAD.TIPOMOVIMIENTO
  AND SUBSTR(D_MOVIMIENTO.ELEMENTO,1,LENGTH(ALMACENCONTABILIDAD.CODIGOELEMENTO)) = ALMACENCONTABILIDAD.CODIGOELEMENTO
WHERE D_MOVIMIENTO.COMPANIA  = 's$compania$s' 
  AND TRUNC(D_MOVIMIENTO.FECHA)=TO_DATE('s$fechaAlmacenContabilidadTSiif$s','DD/MM/YYYY') 
  AND IND_REG   NOT IN(0)
  AND ALMACENCONTABILIDAD.ANO = TO_NUMBER(TO_CHAR(TO_DATE('s$fechaAlmacenContabilidadTSiif$s','DD/MM/YYYY'),'YYYY'))
  AND NOT (TIPOMOVIMIENTO.CONCEPTO IN ('T') AND TIPOMOVIMIENTO.CLASE IN('T'))
GROUP BY 
 CASE WHEN NIIF_CUENTADEBITO IS NOT NULL THEN NIIF_CUENTADEBITO ELSE  ALMACENCONTABILIDAD.CUENTADEBITO   END,
 0)]') CONSULTA, 10 APLICACION ,TO_CLOB(q'[(null)]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);