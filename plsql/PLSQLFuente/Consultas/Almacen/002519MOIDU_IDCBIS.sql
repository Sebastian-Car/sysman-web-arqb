MERGE INTO CONSULTAS FIN USING (SELECT '002519MOIDU_IDCBIS' INFORME ,TO_CLOB(q'[SELECT 
  VM.COMPANIA
, VM.CREATED_BY ELABORADO  
 , VM.TIPOMOVIMIENTO
 , VM.MOVIMIENTO NUMERO
 , VM.CODIGO
 , VM.ELEMENTO
 , VM.SERIEDEVOLUTIVO
 , VM.SERIE
 , VM.ESPECIFICACION
 , CASE WHEN VM.IND_REG  IN (0)  THEN 'X' END NOREGISTRADO
 , VM.MARCA
 , VM.TIPOMOVIMIENTO_NOM 
 , VM.TIPOPESONA
 , VM.NOMBRELARGO ||'-'|| VM.ESPECIFICACION NOMBRELARGO
 , VM.UNIDAD
 , VM.CANTIDAD
 , VM.VALORUNITARIO
 , VM.VALORTOTAL_D VALORTOTAL
 , VM.TERCERO
,TO_CHAR(VM.FECHA_MOV, 'DD') || ' de  '|| PCK_SYSMAN_UTL.FC_NOMBRE_MES(TO_CHAR(VM.FECHA_MOV, 'MM'))||' de ' || TO_CHAR(VM.FECHA_MOV, 'YYYY') FECHA
 , VM.TIPOMOVASOCIADO
 , VM.MOVASOCIADO
 , TO_CHAR(OC.FECHAINICIO,'YYYY') FECHAMOVASOCIADO
 , VM.FACTURANO
 , VM.REMISIONNO 
 , VM.TERCERO_NOM
 , VM.TERCERO_TEL TELEFONOS
 , VM.TERCERO_DIR DIRECCION
 , VM.TERCERO_FAX FAX
 , VM.TIPOMOVASOCIADO_NOM
 , VM.UNIDAD_NOM 
 , VM.AJUSTECENTAVOS1_MOV AJUSTECENTAVOS1
 , VM.RESPONSABLE_ORIGEN_NOM 
 , VM.TOTALCONAJUSTE_MOV TOTALCONAJUSTE 
 , CASE WHEN VM.CLASE  IN ('E')  THEN 'ENTRADA' ELSE 'SALIDA'  END  CLASEMOV
 , VM.CENTRODECOSTO
 , (VM.TIPOMOVIMIENTO || VM.MOVIMIENTO) GRUPO
 , (VM.VALORUNITARIO * VM.PORCDESCUENTO)/100 VALORTOTALDESC 
 , VM.AJUSTECENTAVOS1_MOV * (-1) AJUSTECENTAVOS  
 , VM.AJUSTECENTAVOS_MOV AJU 
 , VM.AJUSTECENTAVOS_MOV AJUSTEDETALLE 
 , VM.AJUSTE_DEC_MANUAL
 , VM.ESCRITURA
 , VM.NUMEROCATASTRAL
 , VM.MATINMOBILIARIA
 , VM.COD_CHIP 
 , VM.DESCRIPCION_MOV DESCRIPCION 
 , VM.REFERENCIA
 , M.FUENTEDERECURSO || ' - ' || FR.NOMBRE FUENTEDERECURSO
 , VM.FUENTEDERECURSO FUENTERECURSO_DET
FROM V_MOVIMIENTO_INFORME VM
INNER JOIN MOVIMIENTO M
ON M.COMPANIA = VM.COMPANIA
AND M.TIPOMOVIMIENTO = VM.TIPOMOVIMIENTO 
AND M.NUMERO = VM.MOVIMIENTO
INNER JOIN FUENTE_RECURSOS FR
ON FR.COMPANIA = M.COMPANIA
AND FR.ANO = TO_CHAR(M.FECHA,'YYYY')
AND FR.CODIGO = M.FUENTEDERECURSO
INNER JOIN ORDENDECOMPRA OC
ON OC.COMPANIA = VM.COMPANIA
AND OC.CLASEORDEN = VM.TIPOMOVASOCIADO
AND OC.NUMERO = VM.MOVASOCIADO
WHERE VM.COMPANIA       = s$compania$s 
  AND VM.TIPOMOVIMIENTO = 's$tipoMovimiento$s'
  AND VM.MOVIMIENTO BETWEEN s$movimientoInicial$s AND s$movimientoFinal$s]') CONSULTA, 10 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);