MERGE INTO CONSULTAS FIN USING (SELECT '002609ALMSINCHI' INFORME ,TO_CLOB(q'[SELECT 
    D_MOVIMIENTO.MOVIMIENTO,
    D_MOVIMIENTO.TIPOMOVIMIENTO, 
    D_MOVIMIENTO.COMPANIA,
    CENTRO_COSTO.CODIGO || ' ' || CENTRO_COSTO.NOMBRE NOMBRE_CCOSTO,
    CASE TIPOMOVIMIENTO.TIPOPESONA 
        WHEN 'R' THEN DTRESPONSABLE_D.NOMBRE || ', c.c. ' || RESPONSABLE.CEDULA
                 ELSE PROVEEDOR.NOMBRE || ', Tel: ' || PROVEEDOR.TELEFONOS || ' , Nit: ' || PROVEEDOR.NIT || CASE WHEN PROVEEDOR.DIRECCION IS NULL THEN ''
                        ELSE ', Dirección: ' || PROVEEDOR.DIRECCION || '' 
                        END || CASE WHEN PROVEEDOR.FAX IS NULL THEN '' ELSE '  ,Fax:' || PROVEEDOR.FAX END
    END DESC_TIPOPERSONA,
    PCK_ALMACEN_COM4.FC_NUMREQUISICIONES(D_MOVIMIENTO.COMPANIA,NVL(MOVIMIENTO.TIPOMOVASOCIADO,'A'),NVL(MOVIMIENTO.MOVASOCIADO,1),NVL(MOVIMIENTO.MOVASOCIADO,1)) AS NUMREQ,
    D_MOVIMIENTO.ELEMENTO, 
    D_MOVIMIENTO.SERIE, 
    CASE D_MOVIMIENTO.IND_REG 
      WHEN  -1 THEN ''  
      ELSE '*' 
    END NOREGISTRADO ,  
    TIPOMOVIMIENTO.TIPOPESONA, 
    CASE TIPOMOVIMIENTO.TIPOPESONA WHEN 'R'
        THEN 'RESPONSABLE'
        ELSE 'PROVEEDOR'
    END NOM_TIPOPERSONA,
    D_MOVIMIENTO.CANTIDAD, 
    D_MOVIMIENTO.VALORUNITARIO, 
    D_MOVIMIENTO.VALORTOTAL ,
    MOVIMIENTO.MOVASOCIADO,
    TO_CHAR(MOVIMIENTO.FECHA, 'DD/MM/YYYY')FECHA,
    TO_CHAR(MOVIMIENTO.FECHAMOVASOCIADO, 'DD/MM/YYYY')FECHAMOVASOCIADO,  
    MOVIMIENTO.FACTURANO, 
    MOVIMIENTO.REMISIONNO , 
    DOCASOCIADO.NOMBRE AS NOMBRE_DOCASOC,
    UNIDAD.NOMBRE AS NOMBREUNIDAD, 
    MOVIMIENTO.AJUSTECENTAVOS1,
    MOVIMIENTO.TOTALCONAJUSTE, 
    MOVIMIENTO.AUXILIAR,
    AUXILIAR.NOMBRE AS NOMBRE_AUXILIAR,
    MOVIMIENTO.REFERENCIA,
    REFERENCIA.NOMBRE NOMBRE_REFERENCIA,
    D_MOVIMIENTO.AJUSTECENTAVOS AS AJUSTEDETALLE, 
    MOVIMIENTO.AJUSTE_DEC_MANUAL, 
    MOVIMIENTO.DESCRIPCION,
    TIPOMOVIMIENTO.NOMBRE AS NOMBRE_TIPOMOV,
    CASE WHEN INVENTARIO.NOMBRELARGO IS NULL 
        THEN '' 
        ELSE    'Elemento: ' || INVENTARIO.NOMBRELARGO END ||
            CASE WHEN  D_MOVIMIENTO.ESPECIFICACION IS NULL 
                THEN '' 
                ELSE   ' Especificación: '|| D_MOVIMIENTO.ESPECIFICACION END ||
                    CASE WHEN  D_MOVIMIENTO.MARCA IS NULL   
                        THEN '' 
                        ELSE  ' Marca: '|| D_MOVIMIENTO.MARCA END ||
                            CASE WHEN  D_MOVIMIENTO.SERIE IS NULL 
                                THEN '' 
                                ELSE   ' Serie: '|| D_MOVIMIENTO.SERIEDEVOLUTIVO END  NOMBRELARGO,
    USUARIO.CODIGO CREADOR,
    CIUDAD.NOMBRE CIUDADUSUARIO
FROM D_MOVIMIENTO
    INNER JOIN INVENTARIO
        ON D_MOVIMIENTO.COMPANIA = INVENTARIO.COMPANIA 
        AND D_MOVIMIENTO.ELEMENTO  = INVENTARIO.CODIGOELEMENTO
    LEFT JOIN UNIDAD
        ON INVENTARIO.UNIDAD = UNIDAD.UNIDAD
    INNER JOIN MOVIMIENTO
        ON D_MOVIMIENTO.COMPANIA        = MOVIMIENTO.COMPANIA
        AND D_MOVIMIENTO.TIPOMOVIMIENTO = MOVIMIENTO.TIPOMOVIMIENTO
        AND D_MOVIMIENTO.MOVIMIENTO     = MOVIMIENTO.NUMERO
    INNER JOIN DEPENDENCIA
        ON MOVIMIENTO.COMP]') || TO_CLOB(q'[ANIA   = DEPENDENCIA.COMPANIA 
        AND MOVIMIENTO.DEPENDENCIA_DESTINO = DEPENDENCIA.CODIGO
    LEFT JOIN DOCASOCIADO
        ON MOVIMIENTO.TIPOMOVASOCIADO = DOCASOCIADO.CODIGO
    INNER JOIN RESPONSABLE
        ON MOVIMIENTO.COMPANIA         = RESPONSABLE.COMPANIA 
        AND MOVIMIENTO.RESPONSABLE_DESTINO           = RESPONSABLE.CEDULA
        AND MOVIMIENTO.SUCURSAL_RESDESTINO           = RESPONSABLE.SUCURSAL
    INNER JOIN TERCERO DTRESPONSABLE_D
        ON RESPONSABLE.COMPANIA = DTRESPONSABLE_D.COMPANIA
        AND RESPONSABLE.CEDULA = DTRESPONSABLE_D.NIT
        AND RESPONSABLE.SUCURSAL = DTRESPONSABLE_D.SUCURSAL
    INNER JOIN TERCERO PROVEEDOR
        ON MOVIMIENTO.COMPANIA = PROVEEDOR.COMPANIA
        AND MOVIMIENTO.TERCERO = PROVEEDOR.NIT
        AND MOVIMIENTO.SUCURSAL = PROVEEDOR.SUCURSAL
    INNER JOIN TIPOMOVIMIENTO
        ON MOVIMIENTO.COMPANIA        = TIPOMOVIMIENTO.COMPANIA
        AND MOVIMIENTO.TIPOMOVIMIENTO = TIPOMOVIMIENTO.CODIGO
    INNER JOIN AUXILIAR
        ON MOVIMIENTO.COMPANIA = AUXILIAR.COMPANIA 
        AND MOVIMIENTO.ANO  = AUXILIAR.ANO
        AND MOVIMIENTO.AUXILIAR  = AUXILIAR.CODIGO
    INNER JOIN CENTRO_COSTO
        ON D_MOVIMIENTO.CENTRODECOSTO = CENTRO_COSTO.CODIGO
        AND D_MOVIMIENTO.COMPANIA = CENTRO_COSTO.COMPANIA
    AND MOVIMIENTO.ANO  = CENTRO_COSTO.ANO
    INNER JOIN REFERENCIA
        ON MOVIMIENTO.REFERENCIA = REFERENCIA.CODIGO
        AND MOVIMIENTO.COMPANIA = REFERENCIA.COMPANIA
        AND MOVIMIENTO.ANO  = REFERENCIA.ANO
    INNER JOIN USUARIO 
        ON MOVIMIENTO.CREATED_BY = USUARIO.CODIGO
    INNER JOIN CIUDAD 
        ON  USUARIO.PAIS = CIUDAD.PAIS
        AND USUARIO.REGION = CIUDAD.DEPARTAMENTO
        AND USUARIO.CIUDAD = CIUDAD.CODIGO
WHERE D_MOVIMIENTO.COMPANIA   = s$compania$s
AND D_MOVIMIENTO.TIPOMOVIMIENTO = 's$tipoMovimiento$s'
AND D_MOVIMIENTO.MOVIMIENTO BETWEEN s$movimientoInicial$s AND s$movimientoFinal$s
ORDER BY D_MOVIMIENTO.COMPANIA,
  D_MOVIMIENTO.TIPOMOVIMIENTO,
  D_MOVIMIENTO.MOVIMIENTO,
  D_MOVIMIENTO.ELEMENTO ASC,
  D_MOVIMIENTO.SERIE ASC]') CONSULTA, 10 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'mrosero' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);