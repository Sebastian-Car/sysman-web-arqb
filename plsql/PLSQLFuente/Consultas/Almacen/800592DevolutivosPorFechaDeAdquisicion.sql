MERGE INTO CONSULTAS FIN USING (SELECT '800592DevolutivosPorFechaDeAdquisicion' INFORME ,TO_CLOB(q'[SELECT DEVOLUTIVO.SERIE PLACA,
 ' ' AS SITUACION,
 INVENTARIO.NOMBRELARGO ELEMENTO, 
 DEVOLUTIVO.DESCRIPCION DESCRIPCION,
 DEVOLUTIVO.SERIEDEVOLUTIVO SERIE,
 TERCERO.NOMBRE RESPONSABLE,
 ESTADODEVOLUTIVO.DESCRIPCION ESTADO,
 TO_NUMBER( NVL(DEVOLUTIVO.VALOR,0) )AS VALOR  
 
FROM ((((DEVOLUTIVO
LEFT JOIN INVENTARIO
ON (DEVOLUTIVO.COMPANIA  = INVENTARIO.COMPANIA)
AND (DEVOLUTIVO.ELEMENTO = INVENTARIO.CODIGOELEMENTO))
LEFT JOIN DEPENDENCIA
ON (DEVOLUTIVO.COMPANIA     = DEPENDENCIA.COMPANIA)
AND (DEVOLUTIVO.DEPENDENCIA = DEPENDENCIA.CODIGO))
LEFT JOIN TERCERO
ON (DEVOLUTIVO.COMPANIA     = TERCERO.COMPANIA)
AND (DEVOLUTIVO.RESPONSABLE = TERCERO.NIT)
AND DEVOLUTIVO.SUCURSAL_RESPONSABLE = TERCERO.SUCURSAL) )
LEFT JOIN ESTADODEVOLUTIVO
ON DEVOLUTIVO.ESTADO         = ESTADODEVOLUTIVO.ESTADO
  WHERE DEVOLUTIVO.COMPANIA=s$compania$s
AND DEVOLUTIVO.FECHAADQUISICION BETWEEN s$fechaInicial$s AND s$fechaFinal$s]') CONSULTA, 10 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);