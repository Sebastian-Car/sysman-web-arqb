MERGE INTO CONSULTAS FIN USING (SELECT '002587MOEHP_CRE' INFORME ,TO_CLOB(q'[SELECT VMI.COMPANIA,
  VMI.TIPOMOVIMIENTO,
  VMI.ELEMENTO CODIGOELEMENTO,
  VMI.SERIE,
  CASE WHEN VMI.IND_REG IN (-1) THEN '' ELSE 'X' END NOREGISTRADO,
  VMI.TIPOMOVIMIENTO_NOM NOMBRETIPOMOVIMIENTO, 
  VMI.UNIDAD,
  VMI.CANTIDAD,
  VMI.VALORUNITARIO,
  VMI.VALORTOTAL_D  VALORTOTAL,
  TO_CHAR(VMI.FECHA_MOV, 'DD/MM/YYYY') FECHA, 
  VMI.MOVASOCIADO,
  TO_CHAR(VMI.FECHAMOVASOCIADO, 'DD/MM/YYYY') FECHAMOVASOCIADO,
  VMI.DESCRIPCION_MOV DESCRIPCION,
  VMI.DEPENDENCIA_DESTINO_NOM NOMBREDEPENDENCIA, 
  VMI.TERCERO_NOM NOMBRETERCERO,
  NVL(VMI.TERCERO_TEL,' ') TELEFONOS,
  VMI.TERCERO,
  VMI.TERCERO_FAX FAX,
  VMI.TIPOMOVASOCIADO_NOM NOMBREDOCASOCIADO, 
  VMI.RESPONSABLE_DESTINO_NOM NOMBRERESPONSABLE,
  NVL(VMI.VALORDOCASOCIADO,0)   VALORDOCASOCIADO,
  VMI.TOTALCONAJUSTE_MOV TOTALCONAJUSTE,
  VMI.TIPOMOVIMIENTO || VMI.MOVIMIENTO  GRUPO,
  VMI.TIPOPESONA,
  VMI.AJUSTECENTAVOS1_MOV AJUSTECENTAVOS,
  VMI.MOVIMIENTO MOVIMIENTO,
  VMI.NOMBRELARGO,
  I.CODIGOCUBS
FROM V_MOVIMIENTO_INFORME VMI
INNER JOIN INVENTARIO I
 ON I.COMPANIA = VMI.COMPANIA
AND I.CODIGOELEMENTO = VMI.ELEMENTO
WHERE VMI.COMPANIA      = s$compania$s
AND VMI.TIPOMOVIMIENTO  = 's$tipoMovimiento$s'
AND VMI.MOVIMIENTO BETWEEN s$movimientoInicial$s AND s$movimientoFinal$s
ORDER BY 
  VMI.MOVIMIENTO,
  VMI.ELEMENTO]') CONSULTA, 10 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);