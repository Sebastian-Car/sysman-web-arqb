MERGE INTO CONSULTAS FIN USING (SELECT '800358InventarioInmueblesDNP' INFORME ,TO_CLOB(q'[SELECT TO_CHAR( DEVOLUTIVO.SERIE)                           No_IDENTIFICACION_BIEN,
       INVENTARIO.NOMBRELARGO                               NOMBRE_O_ALIAS,
       CASE WHEN DEPENDENCIA.CLASE_BODEGA IN(90)
            THEN 'OCUPADO POR UN TERCERO'
            ELSE  'USO PROPIO'
            END                                            ESTADO,
       DEVOLUTIVO.DESCRIPCION                              No_MATRICULA_INMOBILIARIA,
       DEVOLUTIVO.DESCRIPCION                              CEDULA_CATASTRAL,
	   ''                                                   DIRECCION,
       ''                                                   REPORTE_INFORMACION_MANUALES,
       ''                                                   USUARIOS_Y_CLAVES
FROM DEVOLUTIVO INNER JOIN INVENTARIO
 ON  DEVOLUTIVO.COMPANIA=INVENTARIO.COMPANIA
 AND DEVOLUTIVO.ELEMENTO=INVENTARIO.CODIGOELEMENTO
 INNER JOIN DEPENDENCIA
 ON  DEVOLUTIVO.COMPANIA=DEPENDENCIA.COMPANIA
 AND DEVOLUTIVO.DEPENDENCIA=DEPENDENCIA.CODIGO
 
 WHERE DEVOLUTIVO.COMPANIA=s$compania$s
 AND DEVOLUTIVO.TIPOELEMENTO IN('N')
 AND PLACAANULADA IN(0)
 ORDER BY ELEMENTO]') CONSULTA, 10 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);