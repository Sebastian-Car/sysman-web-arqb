SELECT  ADICIONES.ID_PREDIO, 
              ADICIONES.TIPO_MOVIMIENTO, 
              ADICIONES.NUMERO_MOV, 
              TIPOMOVIMIENTO.NOMBRE, 
              ADICIONES.FECHA, 
              ADICIONES.VALOR_CONSTRUCCION
  FROM    ADICIONES 
  INNER JOIN TIPOMOVIMIENTO ON (ADICIONES.TIPO_MOVIMIENTO = TIPOMOVIMIENTO.CODIGO)

s$condicionSub$s
