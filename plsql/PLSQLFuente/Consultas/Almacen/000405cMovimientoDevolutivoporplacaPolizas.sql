MERGE INTO CONSULTAS FIN USING (SELECT '000405cMovimientoDevolutivoporplacaPolizas' INFORME ,TO_CLOB(q'[SELECT 
  INVENTARIO.TIPO,
  QRYMOVDEVOLUTIVO1.TIPOMOVIMIENTO,
  QRYMOVDEVOLUTIVO1.MOVIMIENTO,
  QRYMOVDEVOLUTIVO1.ELEMENTO CODIGOELEMENTO,
  QRYMOVDEVOLUTIVO1.SERIE,
  TO_CHAR(QRYMOVDEVOLUTIVO1.FECHA,'DD/MM/YYYY') FECHA,
  QRYMOVDEVOLUTIVO1.HORA,
  QRYMOVDEVOLUTIVO1.IND_REG,
  QRYMOVDEVOLUTIVO1.RESPONSABLE,
  QRYMOVDEVOLUTIVO1.DEPENDENCIA_DESTINO DEPENDENCIA,
  PCK_ALMACEN.FC_VERIFICAR_NOMBRE_DEVOLUTIVO(1,-1,QRYMOVDEVOLUTIVO1.DEPENDENCIASALE,QRYMOVDEVOLUTIVO1.COMPANIA) DEPENDENCIASALIENTE,
  PCK_ALMACEN.FC_VERIFICAR_NOMBRE_DEVOLUTIVO(2,-1,QRYMOVDEVOLUTIVO1.RESPONSABLESALE,QRYMOVDEVOLUTIVO1.COMPANIA) RESPONSABLESALIENTE,
  PCK_ALMACEN.FC_VERIFICAR_NOMBRE_DEVOLUTIVO(1,-1, CASE WHEN QRYMOVDEVOLUTIVO1.CLASE  ='E' AND QRYMOVDEVOLUTIVO1.CONCEPTO='R' THEN PCK_SYSMAN_UTL.FC_PAR(QRYMOVDEVOLUTIVO1.COMPANIA,'BODEGA ALMACEN',3,SYSDATE) ELSE DEPENDENCIAENTRA END, QRYMOVDEVOLUTIVO1.COMPANIA) DEPENDENCIAENTRANTE,
  PCK_ALMACEN.FC_VERIFICAR_NOMBRE_DEVOLUTIVO(2,-1, CASE WHEN QRYMOVDEVOLUTIVO1.CLASE  ='E' AND QRYMOVDEVOLUTIVO1.CONCEPTO='R' THEN PCK_SYSMAN_UTL.FC_PAR(QRYMOVDEVOLUTIVO1.COMPANIA,'CEDULA RESPONSABLE ALMACEN',3,SYSDATE) ELSE QRYMOVDEVOLUTIVO1.RESPONSABLEENTRA END,QRYMOVDEVOLUTIVO1.COMPANIA) RESPONSABLEENTRANTE,
  DEPENDENCIAANT.NOMBRE DEPENDENCIAANT_NOMBRE,
  DEPENDENCIANUE.NOMBRE DEPENDENCIANUE_NOMBRE,
  DEVOLUTIVO.VALOR,
  ESTADODEVOLUTIVO.DESCRIPCION ESTADOD,
  DEVOLUTIVO.DESCRIPCION,
  DEVOLUTIVO.NUMEROORIGEN,
  DEVOLUTIVO.SERIEANTERIOR,
  INVENTARIO.NOMBRELARGO,
  D_POLIZAS_ACTIVOS.ASEGURADORA,
  MAX(D_POLIZAS_ACTIVOS.NUMERO_POLIZA) KEEP (DENSE_RANK LAST ORDER BY ROWNUM) NUMERO_POLIZA,
  TO_CHAR(MAX(D_POLIZAS_ACTIVOS.FECHAI) KEEP (DENSE_RANK LAST ORDER BY ROWNUM),'DD/MM/YYYY') FECHAI,
  TO_CHAR(MAX(D_POLIZAS_ACTIVOS.FECHAF) KEEP (DENSE_RANK LAST ORDER BY ROWNUM),'DD/MM/YYYY') FECHAF,
  ASEGURADORA.NOMBRE ASEGURADORA_NOMBRE,
  TO_CHAR(QRYMOVDEVOLUTIVO1.FECHA,'DD/MM/YYYY') FECHA
FROM (SELECT D_MOVIMIENTO.COMPANIA,
        D_MOVIMIENTO.TIPOMOVIMIENTO,
        D_MOVIMIENTO.MOVIMIENTO,
        D_MOVIMIENTO.ELEMENTO,
        D_MOVIMIENTO.SERIE,
        D_MOVIMIENTO.FECHA,
        TO_CHAR(D_MOVIMIENTO.HORA,'HH24:MI:SS') HORA,
        D_MOVIMIENTO.IND_REG,
        MOVIMIENTO.TERCERO RESPONSABLE,
        MOVIMIENTO.DEPENDENCIA_DESTINO,
        CASE WHEN TIPOMOVIMIENTO.CLASE = 'E' AND GENERAPLACA NOT IN (0) THEN DEPENDENCIA_ORIGEN ELSE CASE WHEN TIPOMOVIMIENTO.CLASE='S' THEN '000000000000' ELSE CASE WHEN DEPENDENCIA_ORIGEN IS NULL THEN DEPENDENCIA_DESTINO ELSE DEPENDENCIA_ORIGEN END END END DEPENDENCIASALE,
        CASE WHEN TIPOMOVIMIENTO.CLASE = 'E' AND GENERAPLACA NOT IN (0) THEN RESPONSABLE_ORIGEN ELSE CASE WHEN TIPOMOVIMIENTO.CLASE='S' THEN RESPONSABLE_ORIGEN ELSE CASE WHEN RESPONSABLE_ORIGEN IS NULL THEN RESPONSABLE_DESTINO ELSE RESPONSABLE_ORIGEN END END END RESPONSABLESALE,
        CASE WHEN TIPOMOVIMIENTO.CLASE = 'E' AND GENERAPLACA NOT IN (0) AND TIPOMOVIMIENTO.CONCEPTO='R' THEN '000000000000' ELSE DEPENDENC]') || TO_CLOB(q'[IA_DESTINO END DEPENDENCIAENTRA,
        CASE WHEN TIPOMOVIMIENTO.CLASE = 'E' AND GENERAPLACA NOT IN (0) AND TIPOMOVIMIENTO.CONCEPTO = 'R' THEN RESPONSABLE_ORIGEN ELSE RESPONSABLE_DESTINO END RESPONSABLEENTRA,
        TIPOMOVIMIENTO.CLASE,
        TIPOMOVIMIENTO.CONCEPTO
      FROM D_MOVIMIENTO
        LEFT JOIN MOVIMIENTO
           ON D_MOVIMIENTO.COMPANIA       = MOVIMIENTO.COMPANIA
          AND D_MOVIMIENTO.TIPOMOVIMIENTO = MOVIMIENTO.TIPOMOVIMIENTO
          AND D_MOVIMIENTO.MOVIMIENTO     = MOVIMIENTO.NUMERO
        LEFT JOIN TIPOMOVIMIENTO
           ON D_MOVIMIENTO.COMPANIA       = TIPOMOVIMIENTO.COMPANIA
          AND D_MOVIMIENTO.TIPOMOVIMIENTO = TIPOMOVIMIENTO.CODIGO
        WHERE D_MOVIMIENTO.COMPANIA = s$compania$s
          AND D_MOVIMIENTO.SERIE    = s$Placa$s

ORDER BY   D_MOVIMIENTO.FECHA
)QRYMOVDEVOLUTIVO1
  LEFT JOIN DEPENDENCIA DEPENDENCIAANT
     ON QRYMOVDEVOLUTIVO1.COMPANIA         = DEPENDENCIAANT.COMPANIA
    AND QRYMOVDEVOLUTIVO1.DEPENDENCIASALE  = DEPENDENCIAANT.CODIGO
  LEFT JOIN RESPONSABLE RESPONSABLEANT
     ON QRYMOVDEVOLUTIVO1.COMPANIA         = RESPONSABLEANT.COMPANIA
    AND QRYMOVDEVOLUTIVO1.RESPONSABLESALE  = RESPONSABLEANT.CEDULA
  LEFT JOIN DEPENDENCIA DEPENDENCIANUE
     ON QRYMOVDEVOLUTIVO1.COMPANIA         = DEPENDENCIANUE.COMPANIA
    AND QRYMOVDEVOLUTIVO1.DEPENDENCIAENTRA = DEPENDENCIANUE.CODIGO
  LEFT JOIN RESPONSABLE RESPONSABLENUE
     ON QRYMOVDEVOLUTIVO1.COMPANIA         = RESPONSABLENUE.COMPANIA
    AND QRYMOVDEVOLUTIVO1.RESPONSABLEENTRA = RESPONSABLENUE.CEDULA
  LEFT JOIN DEVOLUTIVO
     ON QRYMOVDEVOLUTIVO1.COMPANIA = DEVOLUTIVO.COMPANIA
    AND QRYMOVDEVOLUTIVO1.ELEMENTO = DEVOLUTIVO.ELEMENTO
    AND QRYMOVDEVOLUTIVO1.SERIE    = DEVOLUTIVO.SERIE
  LEFT JOIN INVENTARIO
     ON QRYMOVDEVOLUTIVO1.COMPANIA = INVENTARIO.COMPANIA
    AND QRYMOVDEVOLUTIVO1.ELEMENTO = INVENTARIO.CODIGOELEMENTO
  LEFT JOIN ESTADODEVOLUTIVO
     ON DEVOLUTIVO.ESTADO = ESTADODEVOLUTIVO.ESTADO
  LEFT JOIN D_POLIZAS_ACTIVOS
     ON DEVOLUTIVO.COMPANIA        = D_POLIZAS_ACTIVOS.COMPANIA
    AND DEVOLUTIVO.ELEMENTO        = D_POLIZAS_ACTIVOS.ELEMENTO
    AND DEVOLUTIVO.SERIE           = D_POLIZAS_ACTIVOS.SERIE
  LEFT JOIN ASEGURADORA
     ON D_POLIZAS_ACTIVOS.ASEGURADORA = ASEGURADORA.NITASEGURADORA
WHERE INVENTARIO.COMPANIA = s$compania$s
  AND INVENTARIO.TIPO    <>'C'
  AND QRYMOVDEVOLUTIVO1.RESPONSABLESALE=RESPONSABLEANT.CEDULA
GROUP BY 
  INVENTARIO.TIPO,
  QRYMOVDEVOLUTIVO1.TIPOMOVIMIENTO,
  QRYMOVDEVOLUTIVO1.MOVIMIENTO,
  QRYMOVDEVOLUTIVO1.ELEMENTO,
  QRYMOVDEVOLUTIVO1.SERIE,
  QRYMOVDEVOLUTIVO1.FECHA,
  QRYMOVDEVOLUTIVO1.HORA,
  QRYMOVDEVOLUTIVO1.IND_REG,
  QRYMOVDEVOLUTIVO1.RESPONSABLE,
  QRYMOVDEVOLUTIVO1.DEPENDENCIA_DESTINO,
  PCK_ALMACEN.FC_VERIFICAR_NOMBRE_DEVOLUTIVO(1,-1,QRYMOVDEVOLUTIVO1.DEPENDENCIASALE,QRYMOVDEVOLUTIVO1.COMPANIA),
  PCK_ALMACEN.FC_VERIFICAR_NOMBRE_DEVOLUTIVO(2,-1,QRYMOVDEVOLUTIVO1.RESPONSABLESALE,QRYMOVDEVOLUTIVO1.COMPANIA),
  PCK_ALMA]') || TO_CLOB(q'[CEN.FC_VERIFICAR_NOMBRE_DEVOLUTIVO(1,-1, CASE WHEN QRYMOVDEVOLUTIVO1.CLASE  ='E' AND QRYMOVDEVOLUTIVO1.CONCEPTO='R' THEN PCK_SYSMAN_UTL.FC_PAR(QRYMOVDEVOLUTIVO1.COMPANIA,'BODEGA ALMACEN',3,SYSDATE) ELSE DEPENDENCIAENTRA END, QRYMOVDEVOLUTIVO1.COMPANIA),
  PCK_ALMACEN.FC_VERIFICAR_NOMBRE_DEVOLUTIVO(2,-1, CASE WHEN QRYMOVDEVOLUTIVO1.CLASE  ='E' AND QRYMOVDEVOLUTIVO1.CONCEPTO='R' THEN PCK_SYSMAN_UTL.FC_PAR(QRYMOVDEVOLUTIVO1.COMPANIA,'CEDULA RESPONSABLE ALMACEN',3,SYSDATE) ELSE QRYMOVDEVOLUTIVO1.RESPONSABLEENTRA END,QRYMOVDEVOLUTIVO1.COMPANIA),
  DEPENDENCIAANT.NOMBRE,
  DEPENDENCIANUE.NOMBRE,
  DEVOLUTIVO.VALOR,
  ESTADODEVOLUTIVO.DESCRIPCION,
  DEVOLUTIVO.DESCRIPCION,
  DEVOLUTIVO.NUMEROORIGEN,
  DEVOLUTIVO.SERIEANTERIOR,
  INVENTARIO.NOMBRELARGO,
  D_POLIZAS_ACTIVOS.ASEGURADORA,
  ASEGURADORA.NOMBRE,
  QRYMOVDEVOLUTIVO1.FECHA
ORDER BY QRYMOVDEVOLUTIVO1.FECHA]') CONSULTA, 10 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);