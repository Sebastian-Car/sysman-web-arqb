SELECT FECHA_HORA_INICIO,
       FECHA_HORA_FIN 
FROM   HORARIO_SOLICITUD 
WHERE  COMPANIA           = $P{PR_COMPANIA}
  AND  TIPO_TRANSACCION   = $P{PR_TIPO_TRANSACCION}
  AND  CODIGO_TRANSACCION = $P{PR_CODIGO_TRANSACCION}
