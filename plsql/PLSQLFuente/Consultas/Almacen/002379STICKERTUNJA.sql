MERGE INTO CONSULTAS FIN USING (SELECT '002379STICKERTUNJA' INFORME ,TO_CLOB(q'[SELECT 	DEVOLUTIVO.ELEMENTO,  		
        	DEVOLUTIVO.SERIE,  		
       	PCK_CODIGODEBARRAS.FC_IMPRIMIRCODIGODEBARRAS(CHR(205)||CHR(102)||LPAD(DEVOLUTIVO.ELEMENTO,10,'0')||PCK_SYSMAN_UTL.FC_STRZERO(DEVOLUTIVO.SERIE,8)) AS CODIGODEBARRAS_BIN,  
        	LPAD(DEVOLUTIVO.ELEMENTO,10,'0')||PCK_SYSMAN_UTL.FC_STRZERO(DEVOLUTIVO.SERIE,8) BARRAS,
        	INVENTARIO.NOMBRELARGO AS NOMBREELEMENTO,  		
       	DEPENDENCIA.NOMBRE AS NOMBREDEPENDENCIA 
FROM DEVOLUTIVO 
	INNER JOIN INVENTARIO 
		ON  DEVOLUTIVO.COMPANIA = INVENTARIO.COMPANIA 
		AND DEVOLUTIVO.ELEMENTO = INVENTARIO.CODIGOELEMENTO 
	INNER JOIN DEPENDENCIA 
		ON  DEVOLUTIVO.COMPANIA    = DEPENDENCIA.COMPANIA 
		AND DEVOLUTIVO.DEPENDENCIA = DEPENDENCIA.CODIGO
	s$variableInner$s		
WHERE DEVOLUTIVO.COMPANIA     = s$compania$s 
  AND DEVOLUTIVO.PLACAANULADA = 0
  AND s$condicion$s
ORDER BY s$orden$s]') CONSULTA, 10 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'dcastiblanco' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);