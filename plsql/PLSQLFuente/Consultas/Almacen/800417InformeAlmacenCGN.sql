MERGE INTO CONSULTAS FIN USING (SELECT '800417InformeAlmacenCGN' INFORME ,TO_CLOB(q'[SELECT SALDOS.cuenta,
       SALDOS.nombrecuenta,
       inventario.nombrelargo  nomBRe_iNVENTARIO,
       SALDOS.TIPO,
       DEPENDENCIA.NOMBRE NombreDependencia,
       DEPRECIAR.elemento,
       saldos.grupo,
      TO_CHAR( DEPRECIAR.serie) PLACA,
      TO_CHAR( devolutivo.niif_fechaadquisicion,
'DD/MM/YYYY') FECHA_ADQUISICION,
       CASE WHEN  TIPO_COD='N'
        THEN DEVOLUTIVO.DESCRIPCION||'-'||BIENES_INMUEBLES.DIRECCION||'-'||BIENES_INMUEBLES.UBICACION||'-'||BIENES_INMUEBLES.CODIGO_PREDIAL||'-'||BIENES_INMUEBLES.MATINMOBILIARIA||'-'||BIENES_INMUEBLES.ESCRITURA||'-'||BIENES_INMUEBLES.AREA||'-'||BIENES_INMUEBLES.OBSERVACIONES 
        ELSE DEVOLUTIVO.DESCRIPCION  END NOMBRE_DESCRIPCION,        
       DEPRECIAR.niif_vlreleMenTo VALOR_HISTORICO,
       DEPRECIAR.NIIF_COSTOAJUSTADO,
       DEPRECIAR.NIIF_VLRDEPRECIACION DEPRECIACION_PERIODO,
       DEPRECIAR.NIIF_VLRACUMULADO DEPRECIACIONACUMULADA,
       DEPRECIAR.NIIF_VLRLIBROS VALOR_LIBROS,
       DEPRECIAR.NIIF_VLRACUMULADO-DEPRECIAR.NIIF_VLRDEPRECIACION DEPRECIACION_ANTERIOR,
       DEVOLUTIVO.NIIF_VIDA_UTIL MESES_VIDA_UTIL,
       CLASE_BODEGA.NOMBRE NOMBRE_BODEGA,
       SALDOS.SUMADESALDOANTERIOR SALDOANTERIOR, 
       SALDOS.SUMADEENTRADAS ENTRADAS, 
       SALDOS.SUMADESALIDAS SALIDAS, 
       SALDOS.SUMADEAJUSTESDEBITO AJUSTEDEBITO, 
       SALDOS.SUMADEAJUSTESCREDITO AJUSTECREDITO , 
      SALDOS.SUMADESALDOFINAL SALDOFINAL


  FROm DEPRECIAR inNeR JoIN deVoLuTIvo
    on DEPRECIAR.COmpaNia=dEvolutivO.COmPANIa
    And DEPRECIAR.eLEmEnTo=dEvolutivO.ELeMENTo
    AND DEPRECIAR.serie=dEvolutivO.sERie
  InNer JoIN InVENTaRiO   
    on DEPRECIAR.COmpaNia=InVENTaRiO.COmPANIa
    And DEPRECIAR.eLEmEnTo=InVENTaRiO.CodigoELeMENTo
LEFT JOIN (  
            WITH VISTA_CUENTAALMACEN AS (
              SELECT  V_BASE_CUENTAALMACEN.compania, 
                   V_BASE_CUENTAALMACEN.BODEGA_DESTINO1, 
                   V_BASE_CUENTAALMACEN.CLASEFINAL,
                   V_BASE_CUENTAALMACEN.ELEMENTO,
                   V_BASE_CUENTAALMACEN.GRUPO,
                   V_BASE_CUENTAALMACEN.SERIE,
                   V_BASE_CUENTAALMACEN.CUENTA, 
                   PLAN_CONTABLE.NOMBRE NOMBRECUENTA, 
                   V_BASE_CUENTAALMACEN.NOMBREBODEGA DEPEND,
                   V_BASE_CUENTAALMACEN.TIPO TIPO_COD,
                   DECODE(V_BASE_CUENTAALMACEN.TIPO,'C' ,'Consumo'
                                                   ,'D' ,'Devolutivos'
                                                   ,'E' ,'Recibidos en Comodato'
                                                   ,'EC','Entregados en Comodato'
                                                   ,'M' ,'Consumo Controlado'
                                                   ,'N' ,'ActivosFijos'
                                                   ,'S' ,'Servicios') TIPO,  
                   SUM(CASE WHEN (V_BASE_CUENTAALMACEN.FECHA < TO_DATE('01/'||s$mesInicial$s ||'/'||s$ano$s,'DD/MM/YYYY'))  
                        THEN NVL(V_BASE_CUENTA]') || TO_CLOB(q'[ALMACEN.VALOR_ENTRADAS,0)- NVL(V_BASE_CUENTAALMACEN.VALOR_SALIDAS,0) 
                                +NVL(V_BASE_CUENTAALMACEN.AJUSTESDEBITO,0)  + NVL(V_BASE_CUENTAALMACEN.AJUSTESCREDITO,0) ELSE 0 END ) AS SUMADESALDOANTERIOR, 
                   SUM(CASE WHEN (V_BASE_CUENTAALMACEN.FECHA >= TO_DATE('01/'||s$mesInicial$s ||'/'||s$ano$s,'DD/MM/YYYY') AND V_BASE_CUENTAALMACEN.FECHA <= LAST_DAY(TO_DATE('01/'||s$mesFinal$s||'/'||s$ano$s,'DD/MM/YYYY'))
 )
                             THEN NVL(V_BASE_CUENTAALMACEN.VALOR_ENTRADAS,0) ELSE 0 END ) AS SUMADEENTRADAS,  
                   SUM(CASE WHEN (V_BASE_CUENTAALMACEN.FECHA >= TO_DATE('01/'||s$mesInicial$s ||'/'||s$ano$s,'DD/MM/YYYY') AND V_BASE_CUENTAALMACEN.FECHA <= LAST_DAY(TO_DATE('01/'||s$mesFinal$s||'/'||s$ano$s,'DD/MM/YYYY'))
)
                            THEN NVL(V_BASE_CUENTAALMACEN.VALOR_SALIDAS,0) ELSE 0 END )  AS SUMADESALIDAS,       
                   SUM(CASE WHEN (V_BASE_CUENTAALMACEN.FECHA >= TO_DATE('01/'||s$mesInicial$s ||'/'||s$ano$s,'DD/MM/YYYY') AND V_BASE_CUENTAALMACEN.FECHA <= LAST_DAY(TO_DATE('01/'||s$mesFinal$s||'/'||s$ano$s,'DD/MM/YYYY'))
)
                              THEN NVL(V_BASE_CUENTAALMACEN.AJUSTESDEBITO,0) ELSE 0 END )  AS SUMADEAJUSTESDEBITO,  
                   SUM(CASE WHEN (V_BASE_CUENTAALMACEN.FECHA >= TO_DATE('01/'||s$mesInicial$s ||'/'||s$ano$s,'DD/MM/YYYY') AND V_BASE_CUENTAALMACEN.FECHA <= LAST_DAY(TO_DATE('01/'||s$mesFinal$s||'/'||s$ano$s,'DD/MM/YYYY'))
)
                            THEN NVL(V_BASE_CUENTAALMACEN.AJUSTESCREDITO,0) ELSE 0 END )  AS SUMADEAJUSTESCREDITO, 
                   SUM(NVL(V_BASE_CUENTAALMACEN.VALOR_ENTRADAS,0)-NVL(V_BASE_CUENTAALMACEN.VALOR_SALIDAS,0)+  
                       NVL(V_BASE_CUENTAALMACEN.AJUSTESDEBITO,0) +NVL(V_BASE_CUENTAALMACEN.AJUSTESCREDITO,0))  AS SUMADESALDOFINAL     
            FROM V_BASE_CUENTAALMACEN INNER JOIN BODEGA  
              ON V_BASE_CUENTAALMACEN.COMPANIA        = BODEGA.COMPANIA 
             AND V_BASE_CUENTAALMACEN.BODEGA_DESTINO1 = BODEGA.CODIGO  
             LEFT JOIN (SELECT COMPANIA, ANO, CODIGO, NOMBRE
                         FROM PLAN_CONTABLE
                         WHERE COMPANIA = s$compania$s
                           AND ANO      = s$ano$s ) PLAN_CONTABLE 
               ON PLAN_CONTABLE.COMPANIA  = V_BASE_CUENTAALMACEN.COMPANIA
              AND PLAN_CONTABLE.CODIGO    = V_BASE_CUENTAALMACEN.CUENTA
              AND PLAN_CONTABLE.ANO       =s$ano$s 
             WHERE V_BASE_CUENTAALMACEN.COMPANIA= s$compania$s
              AND TO_CHAR(V_BASE_CUENTAALMACEN.FECHA, 'DD/MM/YYYY') <= LAST_DAY(TO_DATE('01/'||s$mesFinal$s||'/'||s$ano$s,'DD/MM/YYYY'))
              AND V_BASE_CUENTAALMACEN.IND_REG NOT IN (0) 
              AND BODEGA.MOSTRAR NOT IN(0)
              AND CASE
                  WHEN 0 NOT IN (0)
                  THEN (TIPO||BODEGA_DESTINO1)
                  ELSE ' '
              END NOT IN ('M30')  
            GROUP BY  V_BASE_CUENTAALMACEN.compania, 
             ]') || TO_CLOB(q'[         V_BASE_CUENTAALMACEN.BODEGA_DESTINO1,  
                      V_BASE_CUENTAALMACEN.CLASEFINAL, 
                      V_BASE_CUENTAALMACEN.ELEMENTO,
                      V_BASE_CUENTAALMACEN.GRUPO,
                      V_BASE_CUENTAALMACEN.NOMBREBODEGA,
                      V_BASE_CUENTAALMACEN.SERIE,
                      V_BASE_CUENTAALMACEN.TIPO, 
                      V_BASE_CUENTAALMACEN.CUENTA,  
                      PLAN_CONTABLE.NOMBRE) 
            SELECT  compania,
                    BODEGA_DESTINO1 ,  
                    CLASEFINAL ,
                    ELEMENTO,
                    GRUPO,
                    SERIE,
                    CUENTA, 
                    NOMBRECUENTA, 
                    DEPEND, 
                    TIPO_COD, 
                    TIPO, 
                    SUMADESALDOANTERIOR, 
                    SUMADEENTRADAS, 
                    SUMADESALIDAS, 
                    SUMADEAJUSTESDEBITO, 
                    SUMADEAJUSTESCREDITO, 
                    SUMADESALDOANTERIOR + SUMADEENTRADAS - SUMADESALIDAS + SUMADEAJUSTESDEBITO + SUMADEAJUSTESCREDITO SUMADESALDOFINAL 
            FROM   VISTA_CUENTAALMACEN
            WHERE  SUMADESALDOANTERIOR + SUMADEENTRADAS - SUMADESALIDAS + SUMADEAJUSTESDEBITO + SUMADEAJUSTESCREDITO <>0
            AND TIPO_COD NOT IN('C')
                       ORDER BY CLASEFINAL, BODEGA_DESTINO1, TIPO_COD, CUENTA
                        ) SALDOS

       on DEPRECIAR.COmpaNia=SALDOS.COmPANIa
    And DEPRECIAR.eLEmEnTo=SALDOS.ELeMENTo
    AND DEPRECIAR.serie=SALDOS.sERie   
  LEFT JOIN   BIENES_INMUEBLES
  ON DEPRECIAR.COmpaNia=BIENES_INMUEBLES.COmPANIa
    And DEPRECIAR.eLEmEnTo=BIENES_INMUEBLES.ELeMENTo
    AND DEPRECIAR.serie=BIENES_INMUEBLES.sERie  
  LEFT JOIN CLASE_BODEGA
   ON DEPRECIAR.BODEGA=CLASE_BODEGA.CODIGO
  LEFT JOIN DEPENDENCIA
    ON DEPRECIAR.COMPANIA=DEPENDENCIA.COMPANIA
    AND DEPRECIAR.DEPENDENCIA=DEPENDENCIA.CODIGO
    where depreciar.compania=s$compania$s
    and depreciar.ano=s$ano$s
    and depreciar.mes=s$mesFinal$s]') CONSULTA, 35 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);