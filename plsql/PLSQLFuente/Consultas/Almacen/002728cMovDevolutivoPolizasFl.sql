MERGE INTO CONSULTAS FIN USING (SELECT '002728cMovDevolutivoPolizasFl' INFORME ,TO_CLOB(q'[SELECT COMPANIA,TIPOMOVIMIENTO,MOVIMIENTO,ELEMENTO,SERIE,FECHA,HORA,IND_REG,RESPON,DEPEN,VALORUNITARIO,
    NOMBRE,TIPOELEMENTO,ELEM,CLASE,EXP,ASEGURADORA,NUMERO_POLIZA,FECHAI,FECHAF,NOMBRE_ASEGURADORA,VLR_LIBROS
FROM(
SELECT ROW_NUMBER() OVER(PARTITION BY TO_CHAR(D_MOVIMIENTO.FECHA,'DD/MM/YYYY'),TO_CHAR(D_MOVIMIENTO.HORA,'HH24:MI:SS') ORDER BY DEPRECIAR.PERIODO DESC) RN,
  D_MOVIMIENTO.COMPANIA,
  D_MOVIMIENTO.TIPOMOVIMIENTO,
  D_MOVIMIENTO.MOVIMIENTO,
  D_MOVIMIENTO.ELEMENTO,
  D_MOVIMIENTO.SERIE,
  TO_CHAR(D_MOVIMIENTO.FECHA,'DD/MM/YYYY') FECHA,
  TO_CHAR(D_MOVIMIENTO.HORA,'HH24:MI:SS') HORA,
  D_MOVIMIENTO.IND_REG,
  MOVIMIENTO.RESPONSABLE_DESTINO RESPON,
  MOVIMIENTO.DEPENDENCIA_DESTINO DEPEN,
  D_MOVIMIENTO.VALORUNITARIO,
  TIPOMOVIMIENTO.NOMBRE,
  TIPOMOVIMIENTO.TIPOELEMENTO,
  D_MOVIMIENTO.ELEMENTO ELEM,
  TIPOMOVIMIENTO.CLASE,
  TO_CHAR(D_MOVIMIENTO.FECHA,'DD/MM/YYYY') EXP,
  D_POLIZAS_ACTIVOS.ASEGURADORA,
  MAX(D_POLIZAS_ACTIVOS.NUMERO_POLIZA) KEEP (DENSE_RANK LAST ORDER BY ROWNUM) NUMERO_POLIZA,
  TO_CHAR(MAX(D_POLIZAS_ACTIVOS.FECHAI) KEEP (DENSE_RANK LAST ORDER BY ROWNUM),'DD/MM/YYYY') FECHAI,
  TO_CHAR(MAX(D_POLIZAS_ACTIVOS.FECHAF) KEEP (DENSE_RANK LAST ORDER BY ROWNUM),'DD/MM/YYYY') FECHAF,
  ASEGURADORA.NOMBRE NOMBRE_ASEGURADORA,NVL(DEPRECIAR.VLRLIBROS,DEVOLUTIVO.VALOR) VLR_LIBROS
FROM DEVOLUTIVO
  INNER JOIN D_MOVIMIENTO
     ON DEVOLUTIVO.COMPANIA           = D_MOVIMIENTO.COMPANIA
    AND DEVOLUTIVO.SERIE              = D_MOVIMIENTO.SERIE
    AND DEVOLUTIVO.ELEMENTO           = D_MOVIMIENTO.ELEMENTO
  INNER JOIN MOVIMIENTO
     ON D_MOVIMIENTO.COMPANIA         = MOVIMIENTO.COMPANIA
    AND D_MOVIMIENTO.MOVIMIENTO       = MOVIMIENTO.NUMERO
    AND D_MOVIMIENTO.TIPOMOVIMIENTO   = MOVIMIENTO.TIPOMOVIMIENTO
  INNER JOIN TIPOMOVIMIENTO
     ON D_MOVIMIENTO.COMPANIA         = TIPOMOVIMIENTO.COMPANIA
    AND D_MOVIMIENTO.TIPOMOVIMIENTO   = TIPOMOVIMIENTO.CODIGO
  LEFT JOIN D_POLIZAS_ACTIVOS
     ON DEVOLUTIVO.COMPANIA           = D_POLIZAS_ACTIVOS.COMPANIA
    AND DEVOLUTIVO.SERIE              = D_POLIZAS_ACTIVOS.SERIE
    AND DEVOLUTIVO.ELEMENTO           = D_POLIZAS_ACTIVOS.ELEMENTO
  LEFT JOIN ASEGURADORA
     ON D_POLIZAS_ACTIVOS.ASEGURADORA = ASEGURADORA.NITASEGURADORA
LEFT JOIN (
SELECT DEPRECIAR.COMPANIA,
    DEPRECIAR.ELEMENTO,
    DEPRECIAR.SERIE,
    CASE WHEN PCK_SYSMAN_UTL.FC_PAR(DEPRECIAR.COMPANIA,'MANEJA NIIF EN ALMACEN',10,DEPRECIAR.PERIODO) = 'SI'
        THEN DEPRECIAR.NIIF_VLRLIBROS
        ELSE DEPRECIAR.VLRLIBROS
    END VLRLIBROS,
    DEPRECIAR.PERIODO
FROM DEPRECIAR
INNER JOIN (
    SELECT DEPRECIAR.COMPANIA,
        DEPRECIAR.ELEMENTO,
        DEPRECIAR.SERIE,
        MAX(DEPRECIAR.PERIODO) PERIODO,
        D_MOVIMIENTO.FECHA
    FROM DEPRECIAR
    INNER JOIN D_MOVIMIENTO
    ON DEPRECIAR.COMPANIA = D_MOVIMIENTO.COMPANIA
    AND DEPRECIAR.ELEMENTO = D_MOVIMIENTO.ELEMENTO
    AND DEPRECIAR.SERIE = D_MOVIMIENTO.SERIE
    WHERE DEPRECIAR.COMPANIA = s$compania$s
        AND DEPRECIAR.PERIODO <= D_MOVIMIENTO.FECHA
    ]') || TO_CLOB(q'[GROUP BY DEPRECIAR.COMPANIA,
        DEPRECIAR.ELEMENTO,
        DEPRECIAR.SERIE,D_MOVIMIENTO.FECHA) ULTIMO 
ON DEPRECIAR.COMPANIA = ULTIMO.COMPANIA
AND DEPRECIAR.ELEMENTO = ULTIMO.ELEMENTO
AND DEPRECIAR.SERIE = ULTIMO.SERIE
AND DEPRECIAR.PERIODO = ULTIMO.PERIODO
WHERE DEPRECIAR.COMPANIA = s$compania$s
    AND DEPRECIAR.PERIODO <= ULTIMO.FECHA) DEPRECIAR
ON DEPRECIAR.COMPANIA = D_MOVIMIENTO.COMPANIA
AND DEPRECIAR.ELEMENTO = D_MOVIMIENTO.ELEMENTO
AND DEPRECIAR.SERIE = D_MOVIMIENTO.SERIE
AND DEPRECIAR.PERIODO <= D_MOVIMIENTO.FECHA
WHERE D_MOVIMIENTO.COMPANIA = s$compania$s
  AND D_MOVIMIENTO.SERIE = s$Placa$s
  AND TIPOMOVIMIENTO.TIPOELEMENTO<>'C'
GROUP BY
  D_MOVIMIENTO.COMPANIA, D_MOVIMIENTO.TIPOMOVIMIENTO, D_MOVIMIENTO.MOVIMIENTO, D_MOVIMIENTO.ELEMENTO, D_MOVIMIENTO.SERIE, TO_CHAR(D_MOVIMIENTO.FECHA,'DD/MM/YYYY'),
  TO_CHAR(D_MOVIMIENTO.HORA,'HH24:MI:SS'),   D_MOVIMIENTO.IND_REG, MOVIMIENTO.RESPONSABLE_DESTINO, MOVIMIENTO.DEPENDENCIA_DESTINO, D_MOVIMIENTO.VALORUNITARIO,
  TIPOMOVIMIENTO.NOMBRE, TIPOMOVIMIENTO.TIPOELEMENTO, D_MOVIMIENTO.ELEMENTO, TIPOMOVIMIENTO.CLASE, D_POLIZAS_ACTIVOS.ASEGURADORA,
  ASEGURADORA.NOMBRE,NVL(DEPRECIAR.VLRLIBROS,DEVOLUTIVO.VALOR),DEPRECIAR.PERIODO
ORDER BY
  D_MOVIMIENTO.COMPANIA,
  TO_DATE(TO_CHAR(D_MOVIMIENTO.FECHA,'DD/MM/YYYY') || ' ' || TO_CHAR(D_MOVIMIENTO.HORA,'HH24:MI:SS'),'DD/MM/YYYY HH24:MI:SS')) WHERE RN = 1]') CONSULTA, 10 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);