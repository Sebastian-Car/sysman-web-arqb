MERGE INTO CONSULTAS FIN USING (SELECT '001830CDocAlmacen' INFORME ,TO_CLOB(q'[SELECT D_MOVIMIENTO.COMPANIA,
    D_MOVIMIENTO.TIPOMOVIMIENTO,
    TIPOMOVIMIENTO.NOMBRECORTO AS NOMBREMOVIMIENTO,
    MOVIMIENTO.NUMERO,
    D_MOVIMIENTO.ELEMENTO,
    TIPOMOVIMIENTO.CLASE,
    TO_CHAR(D_MOVIMIENTO.FECHA, 'DD/MM/YYYY') AS TFECHA,
    INVENTARIO.NOMBRELARGO,
    INVENTARIO.UNIDAD,
    CASE
      WHEN TIPOMOVIMIENTO.CLASE = 'E'
      THEN D_MOVIMIENTO.CANTIDAD
      ELSE 0
    END CENTRADAS,
    CASE
      WHEN TIPOMOVIMIENTO.CLASE = 'S' OR TIPOMOVIMIENTO.CLASE = 'D'
      THEN D_MOVIMIENTO.CANTIDAD
      ELSE 0
    END CSALIDAS,
    CASE
      WHEN TIPOMOVIMIENTO.CLASE = 'E'
      THEN D_MOVIMIENTO.VALORTOTAL
      ELSE 0
    END VENTRADAS,
    CASE
      WHEN TIPOMOVIMIENTO.CLASE = 'S' OR TIPOMOVIMIENTO.CLASE = 'D'
      THEN D_MOVIMIENTO.VALORTOTAL
      ELSE 0
    END VSALIDAS,
        CASE
      WHEN TIPOMOVIMIENTO.CLASE = 'T'
      THEN D_MOVIMIENTO.CANTIDAD
      ELSE 0
    END CTRASLADOS,
    CASE
      WHEN TIPOMOVIMIENTO.CLASE <> 'T'
      THEN D_MOVIMIENTO.CANTIDAD
      ELSE 0
    END COTROS,
    CASE
      WHEN TIPOMOVIMIENTO.CLASE = 'T'
      THEN D_MOVIMIENTO.VALORTOTAL
      ELSE 0
    END VTRASLADOS,
    CASE
      WHEN TIPOMOVIMIENTO.CLASE <> 'T'
      THEN D_MOVIMIENTO.VALORTOTAL
      ELSE 0
    END VOTROS,
    D_MOVIMIENTO.CENTRODECOSTO,
    D_MOVIMIENTO.FUENTEDERECURSO,
    EXTRACT(YEAR FROM D_MOVIMIENTO.FECHA)  AS ANO,
    EXTRACT(MONTH FROM D_MOVIMIENTO.FECHA) AS MES,
    EXTRACT(DAY FROM D_MOVIMIENTO.FECHA)   AS DIA,
    MOVIMIENTO.DESCRIPCION                 AS TTEXTO,
    D_MOVIMIENTO.TIPOMOVIMIENTO || UPPER(PCK_SYSMAN_UTL.FC_STRZERO(MOVIMIENTO,12)) AS NUMCOMP,
    CASE
      WHEN TIPOMOVIMIENTO.CLASE IN ('S','T','D') THEN NVL(TERCERO_DESTINO.NOMBRE, ' ' )
      ELSE NVL(TERCERO.NOMBRE, ' ' )
    END TERCERO,
    INVENTARIO.CUENTAACTIVO
   FROM D_MOVIMIENTO
  INNER JOIN MOVIMIENTO
     ON D_MOVIMIENTO.COMPANIA        = MOVIMIENTO.COMPANIA
    AND D_MOVIMIENTO.TIPOMOVIMIENTO  = MOVIMIENTO.TIPOMOVIMIENTO
    AND D_MOVIMIENTO.MOVIMIENTO      = MOVIMIENTO.NUMERO
  INNER JOIN TIPOMOVIMIENTO
     ON D_MOVIMIENTO.COMPANIA        = TIPOMOVIMIENTO.COMPANIA
    AND D_MOVIMIENTO.TIPOMOVIMIENTO  = TIPOMOVIMIENTO.CODIGO
  INNER JOIN INVENTARIO
     ON D_MOVIMIENTO.COMPANIA  = INVENTARIO.COMPANIA
    AND D_MOVIMIENTO.ELEMENTO  = INVENTARIO.CODIGOELEMENTO
  INNER JOIN TERCERO
     ON MOVIMIENTO.COMPANIA  = TERCERO.COMPANIA
    AND MOVIMIENTO.TERCERO   = TERCERO.NIT
    AND MOVIMIENTO.SUCURSAL  = TERCERO.SUCURSAL
  LEFT JOIN TERCERO TERCERO_DESTINO
     ON MOVIMIENTO.COMPANIA              = TERCERO_DESTINO.COMPANIA
    AND MOVIMIENTO.RESPONSABLE_DESTINO   = TERCERO_DESTINO.NIT
    AND MOVIMIENTO.SUCURSAL_RESDESTINO   = TERCERO_DESTINO.SUCURSAL
   LEFT JOIN RESPONSABLE
     ON TERCERO.COMPANIA  = RESPONSABLE.COMPANIA
    AND TERCERO.NIT   = RESPONSABLE.CEDULA
    AND TERCERO.SUCURSAL  = RESPONSABLE.SUCURSAL
  WHERE D_MOVIMIENTO.COMPANIA =  s$compania$s
    AND D_MOVIMIENTO.TIPOMOVIMIENTO BETWEEN 's$tipoInicial$s' AND 's$tipoFinal$s' 
    AND TRUNC(D_MOVIMIENTO.FECHA) BETWEEN s$fechaInicial$s AND s$fechaFinal$s
    AND D_MOVIMIENTO.IND_REG  NOT IN (0)
    AND D_MOVIMIENTO.TIPOMOVIMIENTO NOT IN ('SDF')
  ORDER BY D_MOVIMIENTO.TIPOMOVIMIENTO ASC,
           MOVIMIENTO.NUMERO ASC,
           D_MOVIMIENTO.ELEMENTO ASC]') CONSULTA, 10 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);

