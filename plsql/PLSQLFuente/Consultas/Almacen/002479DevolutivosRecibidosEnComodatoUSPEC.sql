MERGE INTO CONSULTAS FIN USING (SELECT '002479DevolutivosRecibidosEnComodatoUSPEC' INFORME ,TO_CLOB(q'[SELECT ROWNUM ITEM,
    DEVOLUTIVO.SERIE PLACA,
    DEVOLUTIVO.DESCRIPCION,
    DEVOLUTIVO.MARCA,
    DEVOLUTIVO.SERIEDEVOLUTIVO,
    DEVOLUTIVO.REFERENCIA,
    DEVOLUTIVO.ELEMENTO,
    TO_CHAR(DEVOLUTIVO.FECHAENTRADA,'DD') DIA_ENTRADA,
    TO_CHAR(DEVOLUTIVO.FECHAENTRADA,'MM') MES_ENTRADA,
    TO_CHAR(DEVOLUTIVO.FECHAENTRADA,'YYYY') ANO_ENTRADA,
    TERCERO.NOMBRE1||' '||TERCERO.NOMBRE2||' '||TERCERO.APELLIDO1||' '||TERCERO.APELLIDO2 NOMBRE_TER,
    DEVOLUTIVO.DEPENDENCIA CODIGO_DEPE,
    DEPENDENCIA.NOMBRE NOMBRE_DEPE,
    RESPONSABLE.NOMBRE1||' '||RESPONSABLE.NOMBRE2||' '||RESPONSABLE.APELLIDO1||' '||RESPONSABLE.APELLIDO2 NOMBRE_RES,
    DEVOLUTIVO.RESPONSABLE CEDULA_RES,
    CLASE_BODEGA.NOMBRE UBICACION,
    DEVOLUTIVO.ESTADO
  FROM INVENTARIO
  LEFT JOIN DEVOLUTIVO
  ON INVENTARIO.COMPANIA        = DEVOLUTIVO.COMPANIA
  AND INVENTARIO.CODIGOELEMENTO = DEVOLUTIVO.ELEMENTO
  INNER JOIN TIPOMOVIMIENTO
  ON DEVOLUTIVO.COMPANIA         = TIPOMOVIMIENTO.COMPANIA
  AND DEVOLUTIVO.TIPOMOVIMIENTOI = TIPOMOVIMIENTO.CODIGO
  INNER JOIN DEPENDENCIA
  ON DEVOLUTIVO.COMPANIA         = DEPENDENCIA.COMPANIA
  AND DEVOLUTIVO.DEPENDENCIA     = DEPENDENCIA.CODIGO
  INNER JOIN CLASE_BODEGA
  ON DEVOLUTIVO.CLASE_BODEGA         = CLASE_BODEGA.CODIGO
   INNER JOIN TERCERO RESPONSABLE
  ON DEVOLUTIVO.COMPANIA         = RESPONSABLE.COMPANIA
  AND DEVOLUTIVO.RESPONSABLE     = RESPONSABLE.NIT
   INNER JOIN MOVIMIENTO
  ON DEVOLUTIVO.COMPANIA         = MOVIMIENTO.COMPANIA
  AND DEVOLUTIVO.TIPOMOVIMIENTOI = MOVIMIENTO.TIPOMOVIMIENTO
  AND DEVOLUTIVO.MOVIMIENTOI     = MOVIMIENTO.NUMERO
  INNER JOIN TERCERO
  ON MOVIMIENTO.COMPANIA             = TERCERO.COMPANIA
  AND MOVIMIENTO.RESPONSABLE_ORIGEN  = TERCERO.NIT
 WHERE INVENTARIO.COMPANIA = s$compania$s
 AND INVENTARIO.CODIGOELEMENTO BETWEEN  s$cmbElementoDesde$s AND s$cmbElementoHasta$s
 AND TIPOMOVIMIENTO.CLASE_BODEGA_ORIGEN IN ('90')
 AND TIPOMOVIMIENTO.CLASE_BODEGA_DESTINO IN ('20')
 AND INVENTARIO.TIPO = 'E']') CONSULTA, 10 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);