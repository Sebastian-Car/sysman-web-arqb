MERGE INTO CONSULTAS FIN USING (SELECT '800654RevisarPersSinConfSUI' INFORME ,TO_CLOB(q'[SELECT
    CONCEPTOS.CLASE,
    HISTORICOS.ANO,
    PERSONAL.SUI_TIPO_NOM,
    PERSONAL.SUI_NIVEL,
    PERSONAL.SUI_NOMBRAMIENTO,
    PERSONAL.ID_DE_EMPLEADO,
    PERSONAL.APELLIDO1,
    PERSONAL.APELLIDO2,
    PERSONAL.NOMBRES
FROM HISTORICOS 
LEFT JOIN CONCEPTOS 
ON HISTORICOS.COMPANIA = CONCEPTOS.COMPANIA 
AND HISTORICOS.ID_DE_CONCEPTO = CONCEPTOS.ID_DE_CONCEPTO
LEFT JOIN PERSONAL 
ON HISTORICOS.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO
AND HISTORICOS.COMPANIA = PERSONAL.COMPANIA
WHERE HISTORICOS.COMPANIA = s$compania$s
AND  CONCEPTOS.CLASE = 3 
    AND HISTORICOS.ANO = s$anio1$s
    AND (
        PERSONAL.SUI_TIPO_NOM IS NULL 
        OR PERSONAL.SUI_NIVEL IS NULL 
        OR PERSONAL.SUI_NOMBRAMIENTO IS NULL
    )
GROUP BY
    CONCEPTOS.CLASE,
    HISTORICOS.ANO,
    PERSONAL.SUI_TIPO_NOM,
    PERSONAL.SUI_NIVEL,
    PERSONAL.SUI_NOMBRAMIENTO,
    PERSONAL.ID_DE_EMPLEADO,
    PERSONAL.APELLIDO1,
    PERSONAL.APELLIDO2,
    PERSONAL.NOMBRES
ORDER BY
    PERSONAL.APELLIDO1,
    PERSONAL.APELLIDO2]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);