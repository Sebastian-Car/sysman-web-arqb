MERGE INTO CONSULTAS FIN USING (SELECT '000106ListadoRteFteINFORMEPERIODO' INFORME ,TO_CLOB(q'[SELECT PERSONAL.NOMBRECOMPLETO,
  PERSONAL.NUMERO_DCTO,
  rc.COMPANIA,
  rc.ID_DE_PROCESO,
  rc.ANO,
  rc.MES,
  PCK_SYSMAN_UTL.FC_NOMBRE_MES(rc.MES) AS NOMBREMES,
  CASE WHEN PERSONAL.PROCESORETENCION = 1 AND PERIODO = 4 THEN rc.PERIODO ELSE 3 END PERIODO ,
  rc.ID_DE_EMPLEADO,
  rc.ID,
  CASE rc.DECLARANTES384 WHEN -1 THEN 'Si' ELSE 'No' END AS DECLARANTES384,
  rc.VALOR_UVT,
  PCK_SYSMAN_UTL.FC_ROUND(ANO.LIM_40_DRE*rc.VALOR_UVT,-3) AS ROUND_VALOR_UVT,
  PCK_SYSMAN_UTL.FC_ROUND(VALOR_UVT* s$p1$s ,-3) AS RND_VIV, 
  PCK_SYSMAN_UTL.FC_ROUND(VALOR_UVT* s$p2$s ,-3) AS RND_MED, 
  PCK_SYSMAN_UTL.FC_ROUND(VALOR_UVT* s$p3$s ,-3) AS RND_PER, 
  PCK_SYSMAN_UTL.FC_ROUND(VALOR_UVT* s$p4$s ,-3) AS RND_REN,
  SUM(PCK_SYSMAN_UTL.FC_ROUND(SUBTOTAL2*PORCENTAJE_APLICADO/100,-3)) AS RTE_APLICADO,   
  SUM(rc.PAGOS_BRUTOS) PAGOS_BRUTOS,
  SUM(rc.OTROS_PAGOS) OTROS_PAGOS,
  SUM(rc.VALOR_PRIMA_NAVIDAD) VALOR_PRIMA_NAVIDAD,
  SUM(rc.TOTAL_PAGOS) TOTAL_PAGOS,
  SUM(rc.NG_1_1_ALIMENTACION) NG_1_1_ALIMENTACION,
  SUM(rc.NG_1_2_TRANSPORTE) NG_1_2_TRANSPORTE,
  SUM(rc.NG_1_3_VIATICOS) NG_1_3_VIATICOS,
  SUM(rc.NG_1_4_OTROS) NG_1_4_OTROS,
  SUM(rc.DED_2_1_VIIVENDA) DED_2_1_VIIVENDA,
  SUM(rc.DED_2_2_MEDICINA) DED_2_2_MEDICINA,
  SUM(rc.DED_2_3_PERSONASACARGO) DED_2_3_PERSONASACARGO,
  SUM(rc.DED_2_4_PROMEDIOSALUD) DED_2_4_PROMEDIOSALUD,
  SUM(rc.DED_2_5_TOTAL_DEDUCIBLES) DED_2_5_TOTAL_DEDUCIBLES,
  SUM(rc.DED_3_1_PENSIONYFSP) DED_3_1_PENSIONYFSP,
  SUM(rc.DED_3_2_AP_VOL_PENSION) DED_3_2_AP_VOL_PENSION,
  SUM(rc.DED_3_3_AP_VOL_AFC) DED_3_3_AP_VOL_AFC,
  SUM(rc.DED_3_4_INDEMNIZACIONES) DED_3_4_INDEMNIZACIONES,
  SUM(rc.DED_3_5_INTCESANTIAS) DED_3_5_INTCESANTIAS,
  SUM(rc.DED_3_6_TOTAL_DEDUCCIONES) DED_3_6_TOTAL_DEDUCCIONES,
  SUM(rc.SUBTOTAL) SUBTOTAL,
  SUM(rc.SUBTOTAL2) SUBTOTAL2,
  SUM(rc.DED_4_25_EXENTO) DED_4_25_EXENTO,
  SUM(rc.BASE_GRAVABLE) BASE_GRAVABLE,
  rc.PORCENTAJE_APLICADO,
  SUM(rc.RETENCION_APLICADA) RETENCION_APLICADA,
  SUM(rc.MENOS_SS_384) MENOS_SS_384,
  SUM(rc.SUBTOTAL1_384) SUBTOTAL1_384,
  SUM(rc.PROMEDIO_UVT) PROMEDIO_UVT,
  SUM(rc.TOTAL_UVT) TOTAL_UVT,
  SUM(rc.RETENCION_MINIMIA384) RETENCION_MINIMIA384,
  SUM(rc.RETE383) RETE383,
  rc.ART_APLICADO,
  CASE rc.ART_APLICADO WHEN 385 THEN rc.RETENCION_APLICADA ELSE 0 END AS RETENCION_FINAL,
  NVL(SUM(rc.VALORPN1),0) AS VALORPN1,
  NVL(SUM(rc.PORCPN1),0) AS PORCPN1,
  NVL(SUM(rc.BASEPN1),0) AS BASEPN1,
  NVL(SUM(rc.RTEFTEPN1),0) AS RTEFTEPN1,
  rc.TIPORET,
  0 AS RFTOTAL,
  NVL(SUM(rc.VR_INDEM),0) AS VR_INDEM,
  NVL(SUM(rc.EXENTO_INDEM),0) AS EXENTO_INDEM,
  NVL(SUM(rc.BASE_INDEM),0) AS BASE_INDEM,
  NVL(SUM(rc.RFTE_INDEM),0) AS RFTE_INDEM,
  SUM(rc.BASE1_DEPURACION) BASE1_DEPURACION,
  SUM(rc.BASE2_MINIMA60) BASE2_MINIMA60,
  SUM(rc.BASE3_LIMINTE420) BASE3_LIMINTE420,
  SUM(rc.BASE4_SUMA_DEDYRENTAS) BASE4_SUMA_DEDYRENTAS,
  SUM(rc.BASE5_LIMITE40) BASE5_LIMITE40,
  SUM(rc.BASE6_DIFERENCIA) BASE6_DIFERENCIA,
  ANO.LIM_25_RE LIM_25_RE,
 ]') || TO_CLOB(q'[ ANO.LIM_40_DRE LIM_40_DRE,
  PCK_SYSMAN_UTL.FC_ROUND(ANO.LIM_25_RE*rc.VALOR_UVT,-3) AS ROUND_VALOR25_UVT,
  rc.DED_4_25_EXENTOACUMULADO ACUM25,
  rc.LIMITE_RENTAEXENTA_1340ANUAL ACUM40
FROM RETEFUENTE_CALCULOS rc
LEFT JOIN PERSONAL
ON rc.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO
AND rc.COMPANIA      = PERSONAL.COMPANIA
INNER JOIN ANO
ON rc.COMPANIA = ANO.COMPANIA
AND rc.ANO     = ANO.NUMERO
WHERE rc.COMPANIA    = s$compania$s
AND rc.ID_DE_PROCESO = s$proceso$s
AND rc.ANO           = s$ano1$s
AND rc.MES           = s$mes1$s

s$CONDTODOS$s
GROUP BY PERSONAL.NOMBRECOMPLETO,
  PERSONAL.NUMERO_DCTO,
  rc.COMPANIA,
  rc.ID_DE_PROCESO,
  rc.ANO,
  rc.MES,
  PCK_SYSMAN_UTL.FC_NOMBRE_MES(rc.MES),
  CASE WHEN PERSONAL.PROCESORETENCION = 1 AND PERIODO = 4 THEN rc.PERIODO ELSE 3 END ,
  rc.ID_DE_EMPLEADO,
  rc.ID,
  CASE rc.DECLARANTES384 WHEN -1 THEN 'Si' ELSE 'No' END,
  rc.VALOR_UVT,
  PCK_SYSMAN_UTL.FC_ROUND(420*rc.VALOR_UVT,-3) ,
  PCK_SYSMAN_UTL.FC_ROUND(VALOR_UVT* s$p1$s ,-3), 
  PCK_SYSMAN_UTL.FC_ROUND(VALOR_UVT* s$p2$s ,-3), 
  PCK_SYSMAN_UTL.FC_ROUND(VALOR_UVT* s$p3$s ,-3), 
  PCK_SYSMAN_UTL.FC_ROUND(VALOR_UVT* s$p4$s ,-3),
  PORCENTAJE_APLICADO,
  rc.ART_APLICADO,
  CASE rc.ART_APLICADO WHEN 385 THEN rc.RETENCION_APLICADA ELSE 0 END ,
  rc.TIPORET,
  ANO.LIM_25_RE,
  ANO.LIM_40_DRE,
  rc.DED_4_25_EXENTOACUMULADO,
  rc.LIMITE_RENTAEXENTA_1340ANUAL
ORDER BY NOMBRECOMPLETO,
  rc.COMPANIA,
  rc.ID_DE_PROCESO,
  rc.ANO,
  rc.MES
  --,rc.PERIODO]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);