MERGE INTO CONSULTAS FIN USING (SELECT '002555PlanillaPrimaDicSINCHI' INFORME ,TO_CLOB(q'[SELECT 
  HISTORICOS.ID_DE_PROCESO,
  HISTORICOS.ANO,
  UPPER(PCK_SYSMAN_UTL.FC_NOMBRE_MES(HISTORICOS.MES)) MES,
  HISTORICOS.PERIODO,
  HISTORICOS.ID_DE_EMPLEADO,
  (PERSONAL.APELLIDO1 || ' ' || PERSONAL.APELLIDO2 || ' ' || PERSONAL.NOMBRES) NOMCOMPLETO, 
  PERSONAL.NUMERO_DCTO,
  PERSONAL.FECHA_DE_INGRESO,
  PERSONAL.ID_CENTRO_DE_COSTO,
  DEPENDENCIA.CODIGO DEPENDENCIA,
  DEPENDENCIA.NOMBRE NOMBRE_DEPENDENCIA,
  PERSONAL.ID_DE_CARGO,
  ENCARGOS.FECHAFINAL, 
  CENTRO_COSTO.NOMBRE NOMBRE_CENTRO_DE_COSTO,
  (SELECT PERIODOS.FECHAFINAL FROM PERIODOS WHERE   PERIODOS.COMPANIA = s$compania$s AND PERIODOS.MES  = 6 AND PERIODOS.PERIODO = 3 AND PERIODOS.ID_DE_PROCESO = s$proceso$s AND PERIODOS.ANO = s$anio$s) AS PERJUN,
  SUM(CASE WHEN 
            HISTORICOS.ID_DE_CONCEPTO = 160
        THEN 
            VALOR
        ELSE
            0
        END)  PRIMAJUN,
    SUM(CASE WHEN 
            HISTORICOS.ID_DE_CONCEPTO = 159
        THEN 
            VALOR
        ELSE
            0
        END)  PRIMAEXTJUN,
        
         SUM(CASE WHEN 
            HISTORICOS.ID_DE_CONCEPTO = 160
        THEN 
            VALOR
        ELSE
            0
        END) +
    SUM(CASE WHEN 
            HISTORICOS.ID_DE_CONCEPTO = 159
        THEN 
            VALOR
        ELSE
            0
        END) TOTALDEVENGO,
    
   CASE WHEN PERSONAL.CARGO_ENCARGO IS NOT NULL AND ENCARGOS.FECHAFINAL > 
   (SELECT PERIODOS.FECHAFINAL FROM PERIODOS WHERE   PERIODOS.COMPANIA = s$compania$s AND PERIODOS.MES  = 6 AND PERIODOS.PERIODO = 3 AND PERIODOS.ID_DE_PROCESO = s$proceso$s AND PERIODOS.ANO = s$anio$s)
		THEN ( SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 945 THEN VALOR ELSE 0 END))
        ELSE SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 1 THEN VALOR ELSE 0 END)
        END SUELDOJUN,  
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 950 THEN VALOR ELSE 0 END) GASTOSREP,  
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 946 THEN VALOR ELSE 0 END) RECARGOSJUN,   
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 947 THEN VALOR ELSE 0 END) EXTRASJUN,   
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 948 THEN VALOR ELSE 0 END) TRANSPJUN,  
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 949 THEN VALOR ELSE 0 END) ALIMENJUN,  
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 950 THEN VALOR ELSE 0 END) PLOCALJUN,  
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 951 THEN VALOR ELSE 0 END) DIASPJUN,  
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 952 THEN VALOR ELSE 0 END) DIASCOMJUN,  
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 140 THEN VALOR ELSE 0 END) DESCUENTOS,  
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 144 THEN VALOR ELSE 0 END) NETO,  
  CARGOS.NOMBRE_DEL_CARGO,
  PERSONAL.ID_DE_TIPO,  
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 67  THEN VALOR ELSE 0 END) DIASPROP,  
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 946 THEN VALOR ELSE 0 END) BON   

FROM HISTORICOS
	INNER JOIN PERSONAL
		ON HISTORICOS.COMPANIA          = PERSONAL.COMPANIA
		AND HISTORICOS.ID_DE_EMPLEADO   = PERSONAL.ID_DE_EMPLEADO
	INNER JOI]') || TO_CLOB(q'[N CENTRO_COSTO
		ON PERSONAL.COMPANIA            = CENTRO_COSTO.COMPANIA
		AND PERSONAL.ID_CENTRO_DE_COSTO = CENTRO_COSTO.CODIGO
		AND PERSONAL.ANO  				= CENTRO_COSTO.ANO
	INNER JOIN DEPENDENCIA
		ON PERSONAL.COMPANIA   			= DEPENDENCIA.COMPANIA
		AND PERSONAL.DEPENDENCIA 		= DEPENDENCIA.CODIGO
	INNER JOIN CARGOS
		ON PERSONAL.COMPANIA         	= CARGOS.COMPANIA
		AND  PERSONAL.ID_DE_CARGO       = CARGOS.ID_DE_CARGO
   LEFT JOIN ENCARGOS 
        ON PERSONAL.COMPANIA        = ENCARGOS.COMPANIA
        AND PERSONAL.ANO            = ENCARGOS.ANO
        AND PERSONAL.ID_DE_EMPLEADO = ENCARGOS.ID_DE_EMPLEADO
        AND TO_CHAR(ENCARGOS.FECHAFINAL,'MM') = 6
WHERE PERSONAL.COMPANIA         = s$compania$s  
AND HISTORICOS.ID_DE_PROCESO    = s$proceso$s  
AND HISTORICOS.ANO              = s$anio$s 
AND HISTORICOS.MES              = s$mes$s  
AND HISTORICOS.PERIODO          = s$periodo$s  
AND PERSONAL.ID_DE_TIPO NOT IN (2)
AND HISTORICOS.ID_DE_EMPLEADO <> 0
GROUP BY HISTORICOS.ID_DE_PROCESO,
  HISTORICOS.ANO,
  HISTORICOS.MES,
  HISTORICOS.PERIODO,
  HISTORICOS.ID_DE_EMPLEADO,
  (PERSONAL.APELLIDO1 || ' ' || PERSONAL.APELLIDO2 || ' ' || PERSONAL.NOMBRES),
  PERSONAL.NUMERO_DCTO,
  PERSONAL.FECHA_DE_INGRESO,
  PERSONAL.ID_CENTRO_DE_COSTO,
  DEPENDENCIA.CODIGO,
  DEPENDENCIA.NOMBRE,
  PERSONAL.ID_DE_CARGO,
  CENTRO_COSTO.NOMBRE, 
  CARGOS.NOMBRE_DEL_CARGO,
  ENCARGOS.FECHAFINAL,
  PERSONAL.CARGO_ENCARGO,
  PERSONAL.ID_DE_TIPO  
HAVING  SUM(CASE WHEN 
            HISTORICOS.ID_DE_CONCEPTO = 160
        THEN 
            VALOR
        ELSE
            0
        END) > 0]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);