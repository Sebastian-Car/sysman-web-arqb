MERGE INTO CONSULTAS FIN USING (SELECT '002903PlanillaPrimaDic' INFORME ,TO_CLOB(q'[SELECT HISTORICOS.ID_DE_PROCESO,
  HISTORICOS.ANO,
  HISTORICOS.MES,
  HISTORICOS.PERIODO,
  PERSONAL.ID_CENTRO_DE_COSTO,
  CENTRO_COSTO.NOMBRE NOMBRE_CENTRO_DE_COSTO,
  HISTORICOS.ID_DE_EMPLEADO,
  (PERSONAL.APELLIDO1 || ' ' || PERSONAL.APELLIDO2 || ' ' || PERSONAL.NOMBRES) NOMCOMPLETO,
  TO_CHAR(PERSONAL.FECHA_DE_INGRESO,'DD/MM/YYYY') FECHA_DE_INGRESO,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 930 THEN HISTORICOS.VALOR ELSE 0 END) SUELDOPRIMADIC,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 935 THEN HISTORICOS.VALOR ELSE 0 END) GASTOSREP,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 933 THEN HISTORICOS.VALOR ELSE 0 END) TRANSPRIMADIC,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 934 THEN HISTORICOS.VALOR ELSE 0 END) ALIMENPRIMADIC,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 940 THEN HISTORICOS.VALOR ELSE 0 END) VPT,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 931 THEN HISTORICOS.VALOR ELSE 0 END) PRIMASEMESTRAL,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 932 THEN HISTORICOS.VALOR ELSE 0 END) PRIMAVAC,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 939 THEN HISTORICOS.VALOR ELSE 0 END) BON,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 936 THEN HISTORICOS.VALOR ELSE 0 END) DIASPRIMADIC,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 937 THEN HISTORICOS.VALOR ELSE 0 END) DIASCOMPRIMADIC,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 938 THEN HISTORICOS.VALOR ELSE 0 END) DIASLICPRIMADIC,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 947 THEN HISTORICOS.VALOR ELSE 0 END) EXTRASPSEM,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 942 THEN HISTORICOS.VALOR ELSE 0 END) EXTRAS,
  (SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 159 THEN HISTORICOS.VALOR ELSE 0 END)+
   SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 160 THEN HISTORICOS.VALOR ELSE 0 END)) PRIMAEXTRASEMESTRAL,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 158 THEN HISTORICOS.VALOR ELSE 0 END) PRIMADIC,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 140 THEN HISTORICOS.VALOR ELSE 0 END) DESCUENTOS,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 144 THEN HISTORICOS.VALOR ELSE 0 END) NETO
FROM HISTORICOS
INNER JOIN PERSONAL
ON HISTORICOS.COMPANIA = PERSONAL.COMPANIA
AND HISTORICOS.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO
INNER JOIN CENTRO_COSTO
ON PERSONAL.COMPANIA = CENTRO_COSTO.COMPANIA
AND PERSONAL.ID_CENTRO_DE_COSTO = CENTRO_COSTO.CODIGO
AND PERSONAL.ANO = CENTRO_COSTO.ANO
WHERE HISTORICOS.COMPANIA = s$compania$s
AND HISTORICOS.ID_DE_PROCESO = s$proceso$s
AND HISTORICOS.ANO = s$anio$s
AND HISTORICOS.MES = s$mes$s
AND HISTORICOS.PERIODO = s$periodo$s
AND HISTORICOS.ID_DE_EMPLEADO <> 0
AND HISTORICOS.ID_DE_CONCEPTO IN(158,930,931,932,933,934,935,936,937,938,939,940,942,140,144,159,150,97,163,170,160,605,614,947)
GROUP BY HISTORICOS.ID_DE_PROCESO,
  HISTORICOS.ANO,
  HISTORICOS.MES,
  HISTORICOS.PERIODO,
  PERSONAL.ID_CENTRO_DE_COSTO,
  CENTRO_COSTO.NOMBRE,
  HISTORICOS.ID_DE_EMPLEADO,
  (PERSONAL.APELLIDO1 || ' ' || PERSONAL.APELLIDO2 || ' ' || PERSONAL.NOMBRES),
  TO_CHAR(PERSONAL.FECHA_DE_INGRESO,'DD/MM/YYYY')
HAVING (S]') || TO_CLOB(q'[UM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('158') THEN HISTORICOS.VALOR ELSE 0 END) <> 0)
   AND (SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('144') THEN HISTORICOS.VALOR ELSE 0 END) <> 0)]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);