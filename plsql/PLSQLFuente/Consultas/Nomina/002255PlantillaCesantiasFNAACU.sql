MERGE INTO CONSULTAS FIN USING (SELECT '002255PlantillaCesantiasFNAACU' INFORME ,TO_CLOB(q'[SELECT
    HISTORICOS.ID_DE_EMPLEADO,
    PERSONAL.NOMBRECOMPLETO            AS NOMBRES,
    PERSONAL.NUMERO_DCTO               AS CEDULA,
    PERSONAL.CODIGO_ESTABLECIMIENTO    AS ID_FUENTE,
    ESTABLECIMIENTOS_DOCENTES.NOMBRE   AS FUENTE,
    MES.NOMBRE                         AS NOMBRE_MES,
    PERIODOS.NOM_PERIODO,
    HISTORICOS.ANO,
    VFC.NOMBRE_FONDO_CESANTIAS,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 1 THEN HISTORICOS.VALOR ELSE 0 END) AS C001,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 2 THEN HISTORICOS.VALOR ELSE 0 END) AS C002,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 79 THEN HISTORICOS.VALOR ELSE 0 END) AS C079,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 82 THEN HISTORICOS.VALOR ELSE 0 END) AS C082,
    CASE WHEN (SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 79 THEN HISTORICOS.VALOR ELSE 0 END)) <> 0 THEN
            SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 82 THEN HISTORICOS.VALOR ELSE 0 END)ELSE 0 END AS AUXA,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 80 THEN HISTORICOS.VALOR ELSE 0 END) AS C080,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 81 THEN HISTORICOS.VALOR ELSE 0 END) AS C081,
    CASE WHEN (SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 80 THEN HISTORICOS.VALOR ELSE 0 END)) <> 0 THEN
            SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 81 THEN HISTORICOS.VALOR ELSE 0 END) ELSE 0 END AS AUXT,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 991 THEN HISTORICOS.VALOR ELSE 0 END) AS C991,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 990 THEN HISTORICOS.VALOR ELSE 0 END) AS C990,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 987 THEN HISTORICOS.VALOR ELSE 0 END) AS C987,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 989 THEN HISTORICOS.VALOR ELSE 0 END) AS C989,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 992 THEN HISTORICOS.VALOR ELSE 0 END) AS C992,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 483 THEN HISTORICOS.VALOR ELSE 0 END) AS C483,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 988 THEN HISTORICOS.VALOR ELSE 0 END) AS C988,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 9 THEN HISTORICOS.VALOR ELSE 0 END) AS C009,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 995 THEN HISTORICOS.VALOR ELSE 0 END) AS C995,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 483 THEN HISTORICOS.VALOR ELSE 0 END) - 
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 995 THEN HISTORICOS.VALOR ELSE 0 END) AS MENSUAL,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 986 THEN HISTORICOS.VALOR ELSE 0 END) AS C986,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 993 THEN HISTORICOS.VALOR ELSE 0 END) AS C993,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 900 THEN HISTORICOS.VALOR ELSE 0 END) AS ACUM_ASB,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 901 THEN HISTORICOS.VALOR ELSE 0 END) AS ACUM_AJUS,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 903 THEN HISTORICOS.VALOR ELSE 0 END) AS ACUM_AUXT,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 904 THEN HISTORICOS.VALOR ELSE 0 END) AS ACUM_AUXA,
    SUM(CASE ]') || TO_CLOB(q'[WHEN HISTORICOS.ID_DE_CONCEPTO = 905 THEN HISTORICOS.VALOR ELSE 0 END) AS ACUM_DOC_NAV,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 906 THEN HISTORICOS.VALOR ELSE 0 END) AS ACUM_DOC_SER,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 907 THEN HISTORICOS.VALOR ELSE 0 END) AS ACUM_DOC_VAC,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 946 THEN HISTORICOS.VALOR ELSE 0 END) AS ACUM_DOC_BON,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 910 THEN HISTORICOS.VALOR ELSE 0 END) AS ACUM_DIAS,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 911 THEN HISTORICOS.VALOR ELSE 0 END) AS ACUM_ANT,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 912 THEN HISTORICOS.VALOR ELSE 0 END) AS ACUM_LIC
FROM
    HISTORICOS LEFT
    JOIN PERSONAL ON PERSONAL.COMPANIA = HISTORICOS.COMPANIA
                     AND HISTORICOS.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO
    LEFT JOIN ESTABLECIMIENTOS_DOCENTES ON PERSONAL.COMPANIA = ESTABLECIMIENTOS_DOCENTES.COMPANIA
                                           AND PERSONAL.CODIGO_ESTABLECIMIENTO = ESTABLECIMIENTOS_DOCENTES.CODIGO
    INNER JOIN MES ON MES.COMPANIA = HISTORICOS.COMPANIA
                      AND MES.ANO = HISTORICOS.ANO
                      AND MES.NUMERO = HISTORICOS.MES
    INNER JOIN PERIODOS ON PERIODOS.COMPANIA = HISTORICOS.COMPANIA
                           AND PERIODOS.ANO = HISTORICOS.ANO
                           AND PERIODOS.MES = HISTORICOS.MES
                           AND PERIODOS.PERIODO = HISTORICOS.PERIODO
                           AND PERIODOS.ID_DE_PROCESO = HISTORICOS.ID_DE_PROCESO
    LEFT JOIN V_FONDO_DE_CESANTIAS VFC
    ON VFC.COMPANIA = HISTORICOS.COMPANIA
    AND VFC.FONDO_CESANTIAS = 's$fondo$s'
WHERE
    HISTORICOS.ID_DE_CONCEPTO IN (1,2,79,82,80,81,991,990,987,989,992,483,988,9,995,986,993,900,901,903,904,905,906,907,908,910,911,912,946)
    AND HISTORICOS.COMPANIA = s$compania$s
    AND HISTORICOS.ANO = s$anio$s
    AND HISTORICOS.MES = s$mes$s
    AND HISTORICOS.PERIODO = s$periodo$s
    AND PERSONAL.FONDO_CESANTIAS = 's$fondo$s'
GROUP BY
    HISTORICOS.ID_DE_EMPLEADO,
    PERSONAL.NUMERO_DCTO,
    PERSONAL.CODIGO_ESTABLECIMIENTO,
    PERSONAL.NOMBRECOMPLETO,
    ESTABLECIMIENTOS_DOCENTES.NOMBRE,
    MES.NOMBRE,
    PERIODOS.NOM_PERIODO,
    HISTORICOS.ANO,
    VFC.NOMBRE_FONDO_CESANTIAS
ORDER BY
    ESTABLECIMIENTOS_DOCENTES.NOMBRE ASC,
    PERSONAL.NOMBRECOMPLETO ASC]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);