MERGE INTO CONSULTAS FIN USING (SELECT '002571ResumenDtoSaludTotal' INFORME ,TO_CLOB(q'[SELECT  
  PERSONAL_HISTORICO.NOMBRE_FONDOSALUD,  
  V_FONDO_DE_SALUD.NIT NITFONDO,    
  SUM(CASE WHEN V_ACUMULADOS.ID_DE_CONCEPTO=134 THEN V_ACUMULADOS.VALOR ELSE 0 END)  IBL,  
  SUM(CASE WHEN V_ACUMULADOS.ID_DE_CONCEPTO=112 THEN V_ACUMULADOS.VALOR ELSE 0 END)   IBC,  
  SUM(CASE WHEN V_ACUMULADOS.ID_DE_CONCEPTO=113 THEN V_ACUMULADOS.VALOR ELSE 0 END)   SALUDEMPLEADO,  
  SUM(CASE WHEN V_ACUMULADOS.ID_DE_CONCEPTO=116 THEN V_ACUMULADOS.VALOR ELSE 0 END)   SALUDPATRONO,  
  SUM(CASE WHEN V_ACUMULADOS.ID_DE_CONCEPTO=114 THEN V_ACUMULADOS.VALOR ELSE 0 END)   GARANTIAEMPLEADO,  
  SUM(CASE WHEN V_ACUMULADOS.ID_DE_CONCEPTO=119 THEN V_ACUMULADOS.VALOR ELSE 0 END)   GARANTIAPATRONO, 
  SUM(CASE WHEN V_ACUMULADOS.ID_DE_CONCEPTO=123 THEN V_ACUMULADOS.VALOR ELSE 0 END)   UPC,  
  SUM(CASE WHEN V_ACUMULADOS.ID_DE_CONCEPTO=486 THEN V_ACUMULADOS.VALOR ELSE 0 END)   UPCPATRONO,
  SUM(CASE WHEN V_ACUMULADOS.ID_DE_CONCEPTO=369  
      THEN  
        CASE WHEN V_ACUMULADOS.VALOR<0 THEN V_ACUMULADOS.VALOR*-1 ELSE V_ACUMULADOS.VALOR END  
      ELSE 0 END + 
      CASE WHEN V_ACUMULADOS.ID_DE_CONCEPTO=399 
      THEN  
        CASE WHEN V_ACUMULADOS.VALOR <0 THEN V_ACUMULADOS.VALOR*-1 ELSE V_ACUMULADOS.VALOR END  
      ELSE 0  END + 
      CASE WHEN V_ACUMULADOS.ID_DE_CONCEPTO=398 
      THEN  
        CASE WHEN V_ACUMULADOS.VALOR <0 THEN V_ACUMULADOS.VALOR*-1 ELSE V_ACUMULADOS.VALOR END 
      ELSE 0 END)  DESCUENTOS, 
  V_FONDO_DE_SALUD.CODAIFS 
FROM V_ACUMULADOS  
  INNER JOIN PERSONAL_HISTORICO  
    ON  V_ACUMULADOS.COMPANIA = PERSONAL_HISTORICO.COMPANIA 
   AND  V_ACUMULADOS.ID_DE_PROCESO = PERSONAL_HISTORICO.ID_DE_PROCESO 
   AND  V_ACUMULADOS.ANO = PERSONAL_HISTORICO.ANO 
   AND  V_ACUMULADOS.MES = PERSONAL_HISTORICO.MES 
   AND  V_ACUMULADOS.PERIODO = PERSONAL_HISTORICO.PERIODO 
   AND  V_ACUMULADOS.ID_DE_EMPLEADO = PERSONAL_HISTORICO.ID_DE_EMPLEADO 
  INNER JOIN V_FONDO_DE_SALUD 
     ON PERSONAL_HISTORICO.COMPANIA = V_FONDO_DE_SALUD.COMPANIA 
    AND PERSONAL_HISTORICO.FONDO_SALUD = V_FONDO_DE_SALUD.FONDO_SALUD     
WHERE V_ACUMULADOS.COMPANIA = s$compania$s 
  AND V_ACUMULADOS.ID_DE_PROCESO = s$idProceso$s 
  AND V_ACUMULADOS.ANO = s$ano$s 
  AND V_ACUMULADOS.MES = s$mes$s  
  AND V_ACUMULADOS.PERIODO = s$periodo$s  
GROUP BY PERSONAL_HISTORICO.NOMBRE_FONDOSALUD,  
         V_FONDO_DE_SALUD.NIT, 
         V_FONDO_DE_SALUD.CODAIFS]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);