MERGE INTO CONSULTAS FIN USING (SELECT '002488PlanillaDescuentosRV_1' INFORME ,TO_CLOB(q'[WITH HIST AS (
	SELECT 	*
	FROM 	HISTORICOS
	WHERE 	HISTORICOS.ID_DE_CONCEPTO IN (9,130,131,132,125,129,600,601,602,603,604,605,606,607,608,609,610,611,628,632,634,799,140)
			AND HISTORICOS.COMPANIA = s$compania$s
			AND HISTORICOS.ID_DE_PROCESO = s$proceso$s
			AND HISTORICOS.ANO = s$ano$s
			AND HISTORICOS.MES = s$mes$s
			AND HISTORICOS.PERIODO = s$periodo$s
)
SELECT    HIST.ID_DE_EMPLEADO AS CODIGO,
          CENTRO_COSTO.CODIGO AS ID_CENTRO_DE_COSTO,
          CENTRO_COSTO.NOMBRE AS NOMBRE_CENTRO_DE_COSTO,
          TRIM(PERSONAL.APELLIDO1 || ' ' || PERSONAL.APELLIDO2 || ' ' || PERSONAL.NOMBRES) AS NOM_EMPLEADO, 
          HIST.ID_DE_PROCESO AS PROCESO,
          HIST.ANO AS ANO,
          HIST.MES AS MES,
          PERSONAL.ID_DE_CARGO AS CARGO,
          PERSONAL.NUMERO_DCTO AS CEDULA,
          CARGOS.NOMBRE_DEL_CARGO AS NOMBRECARGO,
          PERSONAL.DEPENDENCIA AS DEPENDENCIA,
          DEPENDENCIA.NOMBRE AS NOMBREDEPENDENCIA,
          HIST.PERIODO AS PERIODO,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,9,HIST.VALOR,0)) AS DIASLAB,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,130,HIST.VALOR,0)) AS DTSALUD,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,131,HIST.VALOR,0)) AS DTPENSION,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,132,HIST.VALOR,0)) AS DTFSP,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,125,HIST.VALOR,0)) AS DTRETEFUENTE,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,129,HIST.VALOR,0)) AS DTSINDICATO,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,600,HIST.VALOR,0)) AS DTCOMFABOY,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,601,HIST.VALOR,0)) AS DTAGRARIO2,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,602,HIST.VALOR,0)) AS DTFODEMPA,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,603,HIST.VALOR,0)) AS DTDAVIVIENDA1,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,604,HIST.VALOR,0)) AS DTDAVIVIENDA2,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,605,HIST.VALOR,0)) AS DTOBELISCO,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,606,HIST.VALOR,0)) AS DTBOLIVAR,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,607,HIST.VALOR,0)) AS DTRECORDAR,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,608,HIST.VALOR,0)) AS DTAGRARIO,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,609,HIST.VALOR,0)) AS DTCONFIAR,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,610,HIST.VALOR,0)) AS DTOCCIDENTE,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,611,HIST.VALOR,0)) AS DTAGRARIO3,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,628,HIST.VALOR,0)) AS DTCOOPSERP,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,632,HIST.VALOR,0)) AS DTCOOPSERP2,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,634,HIST.VALOR,0)) AS DTCOOPSERP1,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,799,HIST.VALOR,0)) AS DTEMBARGOS,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,9,0,140,1,-1) * HIST.VALOR) AS OTRASDEDUC,
          SUM(DECODE(HIST.ID_DE_CONCEPTO,140,HIST.VALOR,0)) AS TOTALDEDUC]') || TO_CLOB(q'[
 FROM 
		HIST
		INNER JOIN PERSONAL
			ON HIST.COMPANIA = PERSONAL.COMPANIA
			AND HIST.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO
		INNER JOIN CONCEPTOS
			ON HIST.COMPANIA = CONCEPTOS.COMPANIA
			AND HIST.ID_DE_CONCEPTO = CONCEPTOS.ID_DE_CONCEPTO
		INNER JOIN PERIODOS
			ON HIST.COMPANIA = PERIODOS.COMPANIA
			AND HIST.ID_DE_PROCESO = PERIODOS.ID_DE_PROCESO
			AND HIST.ANO = PERIODOS.ANO
			AND HIST.MES = PERIODOS.MES
			AND HIST.PERIODO = PERIODOS.PERIODO
		INNER JOIN CARGOS
			ON PERSONAL.COMPANIA = CARGOS.COMPANIA
			AND PERSONAL.ID_DE_CARGO = CARGOS.ID_DE_CARGO
		INNER JOIN CENTRO_COSTO 
          ON PERSONAL.ID_CENTRO_DE_COSTO = CENTRO_COSTO.CODIGO
          AND PERSONAL.COMPANIA = CENTRO_COSTO.COMPANIA
          AND PERSONAL.ANO = CENTRO_COSTO.ANO
		INNER JOIN DEPENDENCIA
			ON PERSONAL.COMPANIA = DEPENDENCIA.COMPANIA
			AND PERSONAL.DEPENDENCIA = DEPENDENCIA.CODIGO
WHERE     PERSONAL.ESTADO_ACTUAL<>0 
GROUP BY  HIST.ID_DE_EMPLEADO,
          CENTRO_COSTO.CODIGO,
          CENTRO_COSTO.NOMBRE ,
          HIST.ID_DE_PROCESO ,
          HIST.ANO,
          HIST.MES ,
          PERSONAL.ID_DE_CARGO ,
          CARGOS.NOMBRE_DEL_CARGO ,
          PERSONAL.DEPENDENCIA ,
          DEPENDENCIA.NOMBRE ,
          PERSONAL.NUMERO_DCTO,
          HIST.PERIODO,
          TRIM(PERSONAL.APELLIDO1 || ' ' || PERSONAL.APELLIDO2 || ' ' || PERSONAL.NOMBRES)]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);