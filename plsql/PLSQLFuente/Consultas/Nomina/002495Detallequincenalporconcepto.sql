MERGE INTO CONSULTAS FIN USING (SELECT '002495Detallequincenalporconcepto' INFORME ,TO_CLOB(q'[WITH    ACUMCONCEPTO430 AS
        (
        SELECT  HISTORICOS.COMPANIA,
                HISTORICOS.ID_DE_EMPLEADO,
                HISTORICOS.PERIODO,
                HISTORICOS.MES,
                HISTORICOS.ANO,
                HISTORICOS.ID_DE_PROCESO,
                SUM(HISTORICOS.VALOR) AS SUMACONCEPTO
        FROM    HISTORICOS LEFT JOIN PERIODOS 
                ON HISTORICOS.PERIODO = PERIODOS.PERIODO
                AND HISTORICOS.MES = PERIODOS.MES
                AND HISTORICOS.ANO = PERIODOS.ANO 
                AND HISTORICOS.COMPANIA = PERIODOS.COMPANIA
        WHERE   PERIODOS.ACUMULADO NOT IN (0) 
                AND HISTORICOS.ID_DE_CONCEPTO = '439' 
                AND HISTORICOS.VALOR > 0 
        GROUP BY HISTORICOS.COMPANIA, HISTORICOS.ID_DE_EMPLEADO, HISTORICOS.PERIODO, HISTORICOS.MES, HISTORICOS.ANO, 
                HISTORICOS.ID_DE_PROCESO
        ) 
SELECT  PERIODOS.NOM_PERIODO,
        MES.NOMBRE NOMBRE_MES,
        HISTORICOS.ANO,
        HISTORICOS.ID_DE_CONCEPTO, 
        V_FONDOS.COD_FONDO FONDO_SINDICATO, 
        V_FONDOS.NOMBRE_FONDO NOMBRE_FONDO_SINDICATO, 
        HISTORICOS.ID_DE_CONCEPTO || ' - ' || CONCEPTOS.NOMBRE_CONCEPTO NOMBRE_CONCEPTO, 
        PERSONAL.ID_DE_EMPLEADO, 
        PERSONAL.APELLIDO1 || ' ' || PERSONAL.APELLIDO2 || ' ' || PERSONAL.NOMBRES AS NOMBRECOMPLETO,
        ROUND(NVL(HISTORICOS.VALOR - 0.4, 0) / ACUMCONCEPTO430.SUMACONCEPTO, 0) SVALOR,
        CASE WHEN ACUMCONCEPTO430.SUMACONCEPTO >= 2 THEN ROUND(NVL(HISTORICOS.VALOR - 0.4, 0) / ACUMCONCEPTO430.SUMACONCEPTO, 0) ELSE 0 END SVALO1,
        CASE WHEN ACUMCONCEPTO430.SUMACONCEPTO >= 3 THEN ROUND(NVL(HISTORICOS.VALOR - 0.4, 0) / ACUMCONCEPTO430.SUMACONCEPTO, 0) ELSE 0 END SVALO2,
        CASE WHEN ACUMCONCEPTO430.SUMACONCEPTO >= 4 THEN ROUND(NVL(HISTORICOS.VALOR - 0.4, 0) / ACUMCONCEPTO430.SUMACONCEPTO, 0) ELSE 0 END SVALO3,
        CASE WHEN ACUMCONCEPTO430.SUMACONCEPTO >= 5 THEN ROUND(NVL(HISTORICOS.VALOR - 0.4, 0) / ACUMCONCEPTO430.SUMACONCEPTO, 0) ELSE 0 END SVALO4,
        CASE WHEN ACUMCONCEPTO430.SUMACONCEPTO >= 6 THEN ROUND(NVL(HISTORICOS.VALOR - 0.4, 0) / ACUMCONCEPTO430.SUMACONCEPTO, 0) ELSE 0 END SVALO5,
        CASE WHEN ACUMCONCEPTO430.SUMACONCEPTO > 6 THEN ROUND(NVL(HISTORICOS.VALOR - 0.4, 0) / ACUMCONCEPTO430.SUMACONCEPTO, 0) ELSE 0 END SVALO6,
        PERIODOS.FECHAFINAL, 
        PERSONAL.NUMERO_DCTO, 
        PERSONAL.ID_DE_TIPO, 
        TIPOS_DE_EMPLEADO.NOMBRE_TIPO, 
        HISTORICOS.ID_DE_PROCESO, 
        CONCEPTOS.CLASE, 
        PERSONAL.ID_CENTRO_DE_COSTO, 
        HISTORICOS.VALOR AS TOTSAL
FROM    HISTORICOS 
        LEFT JOIN PERSONAL 
        ON HISTORICOS.COMPANIA = PERSONAL.COMPANIA 
            AND HISTORICOS.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO 
        LEFT JOIN PERIODOS]') || TO_CLOB(q'[ 
        ON HISTORICOS.COMPANIA = PERIODOS.COMPANIA 
            AND HISTORICOS.ID_DE_PROCESO = PERIODOS.ID_DE_PROCESO 
            AND HISTORICOS.ANO = PERIODOS.ANO 
            AND HISTORICOS.MES = PERIODOS.MES 
            AND HISTORICOS.PERIODO = PERIODOS.PERIODO 
        LEFT JOIN CONCEPTOS 
        ON HISTORICOS.COMPANIA = CONCEPTOS.COMPANIA 
            AND HISTORICOS.ID_DE_CONCEPTO = CONCEPTOS.ID_DE_CONCEPTO 
        LEFT JOIN TIPOS_DE_EMPLEADO 
        ON PERSONAL.COMPANIA = TIPOS_DE_EMPLEADO.COMPANIA 
            AND PERSONAL.ID_DE_TIPO = TIPOS_DE_EMPLEADO.ID_DE_TIPO 
        LEFT JOIN V_FONDOS 
        ON PERSONAL.COMPANIA = V_FONDOS.COMPANIA 
            AND PERSONAL.FONDO_SINDICATO = V_FONDOS.COD_FONDO
        INNER JOIN ACUMCONCEPTO430
        ON HISTORICOS.COMPANIA = ACUMCONCEPTO430.COMPANIA 
            AND HISTORICOS.ID_DE_PROCESO = ACUMCONCEPTO430.ID_DE_PROCESO 
            AND HISTORICOS.ANO = ACUMCONCEPTO430.ANO 
            AND HISTORICOS.MES = ACUMCONCEPTO430.MES 
            AND HISTORICOS.PERIODO = ACUMCONCEPTO430.PERIODO 
            AND HISTORICOS.ID_DE_EMPLEADO = ACUMCONCEPTO430.ID_DE_EMPLEADO
        LEFT JOIN MES 
        ON HISTORICOS.COMPANIA = MES.COMPANIA 
            AND HISTORICOS.ANO = MES.ANO
            AND HISTORICOS.MES = MES.NUMERO
WHERE   (HISTORICOS.ID_DE_CONCEPTO = '129' OR (CONCEPTOS.PERMITE_DUPLICAR != 0 AND NVL(PCK_SYSMAN_UTL.FC_PAR(HISTORICOS.COMPANIA,'DESCUENTO ANTICIPADO AUTOMATICO POR VACACIONES',6,SYSDATE),'NO') = 'SI'))
        AND HISTORICOS.ID_DE_PROCESO = 's$procesoNomina$s'
        AND CONCEPTOS.CLASE = 5 
        AND HISTORICOS.COMPANIA = s$compania$s
        AND HISTORICOS.ANO = 's$anioNomina$s'
        AND HISTORICOS.MES = 's$mesNomina$s'
        AND HISTORICOS.PERIODO = 's$periodoNomina$s'
        AND PERIODOS.ACUMULADO NOT IN (0)
ORDER BY HISTORICOS.ID_DE_CONCEPTO, 
        V_FONDOS.COD_FONDO, 
        PERSONAL.ID_DE_EMPLEADO]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);