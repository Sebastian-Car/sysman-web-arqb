MERGE INTO CONSULTAS FIN USING (SELECT '002121LISTADOFAVIDIIDI' INFORME ,TO_CLOB(q'[SELECT 
	'0.00' AS CAMBIO1,
	'0.00' AS CAMBIO2,
	HISTORICOS.ANO, 
	HISTORICOS.MES,
	HISTORICOS.ID_DE_PROCESO,
	 SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO='441' THEN HISTORICOS.VALOR ELSE 0 END)  AS ADMON, 
	 SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO='405' THEN HISTORICOS.VALOR ELSE 0 END)  AS DTO, 
	 SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO='972' THEN HISTORICOS.VALOR ELSE 0 END)  AS BASEREAL,  
	 SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO='441' THEN HISTORICOS.VALOR ELSE 0 END)  
		+ SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO='440' THEN HISTORICOS.VALOR ELSE 0 END)  AS TOTAL, 
	ROUND( SUM( CASE WHEN HISTORICOS.ID_DE_CONCEPTO='972' THEN HISTORICOS.VALOR ELSE 0 END)  * 0.12,0) AS CESANTIAS,
	ROUND(SUM (CASE WHEN HISTORICOS.ID_DE_CONCEPTO='972' THEN HISTORICOS.VALOR ELSE 0 END)  * 0.12*0.02,0) AS COMISION_CESANTIAS
FROM
	HISTORICOS
	INNER JOIN CONCEPTOS 
		ON CONCEPTOS.ID_DE_CONCEPTO = HISTORICOS.ID_DE_CONCEPTO
		AND CONCEPTOS.COMPANIA = HISTORICOS.COMPANIA
	INNER JOIN PERSONAL
		ON PERSONAL.ID_DE_EMPLEADO = HISTORICOS.ID_DE_EMPLEADO
		AND PERSONAL.COMPANIA = HISTORICOS.COMPANIA
WHERE 
	HISTORICOS.COMPANIA = s$compania$s
	AND HISTORICOS.ANO=s$anioNomina$s
	AND HISTORICOS.MES=s$mesNomina$s
	AND HISTORICOS.ID_DE_CONCEPTO IN ('440','441','158','405','97','151','972','902','504','901','954') 
	AND PERSONAL.FECHA_DE_INGRESO<SYSDATE 
	AND HISTORICOS.ID_DE_PROCESO=s$procesoNomina$s 
	AND HISTORICOS.PERIODO=3
GROUP BY 
	HISTORICOS.ANO, 
	HISTORICOS.MES, 
	HISTORICOS.ID_DE_PROCESO]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);