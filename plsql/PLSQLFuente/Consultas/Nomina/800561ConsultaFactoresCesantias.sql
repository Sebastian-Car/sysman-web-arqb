MERGE INTO CONSULTAS FIN USING (SELECT '800561ConsultaFactoresCesantias' INFORME ,TO_CLOB(q'[SELECT * 
FROM 
(
    SELECT CONCEPTOS.ID_DE_CONCEPTO,
        CONCEPTOS.CLASE,
        CONCEPTOS.FACTOR_CESANTIASCONSOLIDADAS,
        CENTRO_COSTO.CODIGO,
        CENTRO_COSTO.NOMBRE,
        PERSONAL.NOMBRECOMPLETO,
        PERSONAL.GRUPOCONTABLE,
        SUM(HISTORICOS.VALOR) TOTAL
    FROM HISTORICOS
    INNER JOIN CONCEPTOS 
    ON HISTORICOS.COMPANIA = CONCEPTOS.COMPANIA 
    AND HISTORICOS.ID_DE_CONCEPTO = CONCEPTOS.ID_DE_CONCEPTO
    INNER JOIN PERIODOS 
    ON HISTORICOS.COMPANIA = PERIODOS.COMPANIA
    AND HISTORICOS.ID_DE_PROCESO = PERIODOS.ID_DE_PROCESO
    AND HISTORICOS.ANO = PERIODOS.ANO
    AND HISTORICOS.MES = PERIODOS.MES
    AND HISTORICOS.PERIODO = PERIODOS.PERIODO
    INNER JOIN PERSONAL 
    ON HISTORICOS.COMPANIA = PERSONAL.COMPANIA
    AND HISTORICOS.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO
    INNER JOIN CENTRO_COSTO
    ON PERSONAL.COMPANIA = CENTRO_COSTO.COMPANIA
    AND PERSONAL.ANO = CENTRO_COSTO.ANO
    AND PERSONAL.ID_CENTRO_DE_COSTO = CENTRO_COSTO.CODIGO
    WHERE HISTORICOS.COMPANIA = s$compania$s
        AND HISTORICOS.ANO = s$anio$s
        AND HISTORICOS.MES = s$mes$s
        AND PERIODOS.ACUMULADO NOT IN(0)
  AND CONCEPTOS.FACTOR_CESANTIASCONSOLIDADAS NOT IN(0)
    GROUP BY CONCEPTOS.ID_DE_CONCEPTO,
        CONCEPTOS.CLASE,
        CONCEPTOS.FACTOR_CESANTIASCONSOLIDADAS,
        CENTRO_COSTO.CODIGO,
        CENTRO_COSTO.NOMBRE,
        PERSONAL.NOMBRECOMPLETO,
        PERSONAL.GRUPOCONTABLE
) PIVHIST
PIVOT (SUM(TOTAL) FOR ID_DE_CONCEPTO IN (s$pivot$s))
ORDER BY 1]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);