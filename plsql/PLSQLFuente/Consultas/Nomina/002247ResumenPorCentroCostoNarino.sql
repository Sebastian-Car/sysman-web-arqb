MERGE INTO CONSULTAS FIN USING (SELECT '002247ResumenPorCentroCostoNarino' INFORME ,TO_CLOB(q'[SELECT DISTINCT
 CENTRO_COSTO.CODIGO, 
 CENTRO_COSTO.NOMBRE, 
 HISTORICOS.ID_DE_CONCEPTO, 
 CONCEPTOS.NOMBRE_CONCEPTO, 
 CONCEPTOS.CLASE, 
SUM(HISTORICOS.VALOR) AS SUMA_DE_VALOR, 
  CASE WHEN CONCEPTOS.CLASE = 3 THEN 'VALOR DEVENGOS' 
    ELSE
      CASE WHEN CONCEPTOS.CLASE = 5 THEN 'VALOR DESCUENTOS'
       ELSE 
        CASE WHEN CONCEPTOS.CLASE = 4 THEN 'VALOR TOTAL DEVENGOS'
         ELSE 
           CASE WHEN CONCEPTOS.CLASE = 6 THEN 'VALOR TOTAL DESCUENTOS'
              ELSE 
                CASE WHEN CONCEPTOS.CLASE = 7 THEN 'VALOR NETO A PAGAR'
                 ELSE
                    CASE WHEN CONCEPTOS.CLASE = 99 THEN 'BASES'
                     ELSE 'CONCEPTOS CONTABLES'
                     END
                END
              END
            END
          END
        END  AS CLASECONCEPTO, 
 PERSONAL.ID_CENTRO_DE_COSTO
FROM HISTORICOS
LEFT JOIN CONCEPTOS
    ON HISTORICOS.ID_DE_CONCEPTO = CONCEPTOS.ID_DE_CONCEPTO 
    AND HISTORICOS.COMPANIA = CONCEPTOS.COMPANIA
LEFT JOIN PERIODOS
    ON HISTORICOS.COMPANIA = PERIODOS.COMPANIA
    AND HISTORICOS.MES = PERIODOS.MES 
    AND HISTORICOS.ANO = PERIODOS.ANO 
    AND HISTORICOS.PERIODO = PERIODOS.PERIODO   
    AND HISTORICOS.ID_DE_PROCESO = PERIODOS.ID_DE_PROCESO  
LEFT JOIN PERSONAL 
    ON HISTORICOS.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO
    AND HISTORICOS.COMPANIA = PERSONAL.COMPANIA
LEFT JOIN CENTRO_COSTO 
    ON  PERSONAL.COMPANIA = CENTRO_COSTO.COMPANIA 
    AND PERSONAL.ID_CENTRO_DE_COSTO = CENTRO_COSTO.CODIGO
WHERE PERSONAL.COMPANIA = s$compania$s
    AND(LPAD(HISTORICOS.ID_DE_PROCESO,2,0) || 
       LPAD(HISTORICOS.ANO,4,0) || 
       LPAD(HISTORICOS.MES,2,0) || 
       LPAD(HISTORICOS.PERIODO,2,0))
       BETWEEN LPAD(s$proceso$s,2,0)|| 
               LPAD(s$ano1$s,4,0)||
               LPAD (s$mes1$s,2,0) ||
               LPAD (s$periodo1$s,2,0) 
        AND    LPAD(s$proceso$s,2,0)||
               LPAD(s$anio2$s,4,0)|| 
               LPAD(s$mes2$s,2,0)||
               LPAD(s$periodo2$s,2,0)
            s$centroP$s 
GROUP BY CENTRO_COSTO.CODIGO,
    CENTRO_COSTO.NOMBRE, 
    HISTORICOS.ID_DE_CONCEPTO,
    CONCEPTOS.NOMBRE_CONCEPTO,
    CONCEPTOS.CLASE, 
    CASE WHEN CONCEPTOS.CLASE = 3 THEN 'VALOR DEVENGOS' 
  ELSE
   CASE WHEN CONCEPTOS.CLASE = 5 THEN 'VALOR DESCUENTOS'
     ELSE 
       CASE WHEN CONCEPTOS.CLASE = 4 THEN 'VALOR TOTAL DEVENGOS'
         ELSE 
           CASE WHEN CONCEPTOS.CLASE = 6 THEN 'VALOR TOTAL DESCUENTOS'
              ELSE 
                CASE WHEN CONCEPTOS.CLASE = 7 THEN 'VALOR NETO A PAGAR'
                 ELSE
                    CASE WHEN CONCEPTOS.CLASE = 99 THEN 'BASES'
                     ELSE 'CONCEPTOS CONTABLES'
                     END
                END
              END
            END
          END
        END ,
    PERSONAL.ID_CENTRO_DE_COSTO
HAVING CONCEPTOS.CLASE = 3 
OR CONCEPTOS.CLASE = 4 
OR CONCEPTOS.CLASE = 5 
OR CONCEPTOS.CLASE = 6 
OR CONCEPTOS.CLASE = 7 
OR CONCEPTOS.CLASE = 8 
OR CONCEPTOS.CLASE = 99
ORDER B]') || TO_CLOB(q'[Y HISTORICOS.ID_DE_CONCEPTO]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);