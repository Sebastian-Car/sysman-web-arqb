MERGE INTO CONSULTAS FIN USING (SELECT '800650ConsulDist_ID_CC_Y_AUX_Acum' INFORME ,TO_CLOB(q'[SELECT
    DISTRIBUCION.COMPANIA,
    DISTRIBUCION.ANO,
    DISTRIBUCION.MES,
    DISTRIBUCION.ID_CENTRO_DE_COSTO,
    CENTRO_COSTO.NOMBRE AS NOMBRE_CENTRO_DE_COSTO,
    DISTRIBUCION.ID_AUXILIAR,
    AUXILIAR.NOMBRE,
    DISTRIBUCION.ID_DE_CONCEPTO,
    CONCEPTOS.NOMBRE_CONCEPTO,
    SUM(DISTRIBUCION.VALOR) AS SUMADEVALOR,    
    CASE WHEN PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA =>s$compania$s,UN_NOMBRE =>'MANEJA CUENTAS CONFIGURADAS PARA INTERFAZAR',UN_MODULO=>PCK_DATOS.FC_MODULONOMINA,UN_FECHA_PAR=>SYSDATE,UN_IND_MAYUS=>0) ='SI' THEN 
    	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'A' THEN 
    		CONCEPTOS.CTA_CRD_ADMINISTRACION
        ELSE
        	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'V' THEN 
        		CONCEPTOS.CTA_CRD_VENTAS
            ELSE
            	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'P' THEN 
            		CONCEPTOS.CTA_CRD_PRODUCCION
                ELSE
                	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'O' THEN 
                		CONCEPTOS.CTA_CRD_OTRO
                    ELSE
                    	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'S' THEN 
                    		CONCEPTOS.CTA_CRD_SUPERNUM
                        ELSE
                        	' '
                        END)
                	END)
            	END)
        	END)
        END) 
    ELSE CONCEPTO_CENTROCOSTO.CTA_CREDITO 
    END CTA_CREDITO,
    CASE WHEN PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA =>s$compania$s,UN_NOMBRE =>'MANEJA CUENTAS CONFIGURADAS PARA INTERFAZAR',UN_MODULO=>PCK_DATOS.FC_MODULONOMINA,UN_FECHA_PAR=>SYSDATE,UN_IND_MAYUS=>0) ='SI' THEN 
    	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'A' THEN 
    		CONCEPTOS.CTA_DBT_ADMINISTRACION
        ELSE
        	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'V' THEN 
        		CONCEPTOS.CTA_DBT_VENTAS
            ELSE
            	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'P' THEN 
            		CONCEPTOS.CTA_DBT_PRODUCCION
                ELSE
                	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'O' THEN 
                		CONCEPTOS.CTA_DBT_OTRO
                    ELSE
                    	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'S' THEN 
                    		CONCEPTOS.CTA_DBT_SUPERNUM
                        ELSE
                        	' '
                        END)
                	END)
            	END)
        	END)
        END) 
    ELSE CONCEPTO_CENTROCOSTO.CTA_DEBITO 
    END CTA_DEBITO    
FROM
    DISTRIBUCION
    INNER JOIN PERSONAL
    ON DISTRIBUCION.COMPANIA = PERSONAL.COMPANIA
  AND DISTRIBUCION.ID_DE_EMPLEADO  = PERSONAL.ID_DE_EMPLEADO
    LEFT JOIN CENTRO_COSTO 
    ON PERSONAL.COMPANIA = CENTRO_COSTO.COMPANIA
   AND PERSONAL.ANO = CENTRO_COSTO.ANO 
   AND PERSONAL.ID_CENTRO_DE_COSTO = CENTRO_COSTO.CODIGO 
    INNER JOIN AUXILIAR 
    ON DISTRIBUCION.COMPANIA = AUXILIAR.COMPANIA
  AND DISTRIBUCION.ANO = AUXILIAR.ANO
  AND DISTRIBUCION.ID_AUXILIAR = AUXILIAR.CODIGO
INNER JOIN CONCEPTOS 
ON DISTRIBUCION.COMPANIA = CONCEPTOS.COMPANIA
AND DISTRIBUCION.ID_DE_CONCEPTO = CONCEPTOS.ID_DE_CONCEPTO
INNER JOIN CONCEPTO_CENTROCOSTO 
ON DISTR]') || TO_CLOB(q'[IBUCION.COMPANIA = CONCEPTO_CENTROCOSTO.COMPANIA
AND DISTRIBUCION.ANO = CONCEPTO_CENTROCOSTO.ANO
AND DISTRIBUCION.ID_DE_CONCEPTO = CONCEPTO_CENTROCOSTO.ID_DE_CONCEPTO
AND DISTRIBUCION.ID_CENTRO_DE_COSTO = CONCEPTO_CENTROCOSTO.ID_CENTRO_DE_COSTO
WHERE  DISTRIBUCION.COMPANIA = s$compania$s
AND DISTRIBUCION.ANO = s$anio$s 
AND DISTRIBUCION.MES = s$mes$s  
GROUP BY
    DISTRIBUCION.COMPANIA,
    DISTRIBUCION.ANO,
    DISTRIBUCION.MES,
    DISTRIBUCION.ID_CENTRO_DE_COSTO,
    CENTRO_COSTO.NOMBRE,
    DISTRIBUCION.ID_AUXILIAR,
    AUXILIAR.NOMBRE,
    DISTRIBUCION.ID_DE_CONCEPTO,
    CONCEPTOS.NOMBRE_CONCEPTO,
    CASE WHEN PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA =>s$compania$s,UN_NOMBRE =>'MANEJA CUENTAS CONFIGURADAS PARA INTERFAZAR',UN_MODULO=>PCK_DATOS.FC_MODULONOMINA,UN_FECHA_PAR=>SYSDATE,UN_IND_MAYUS=>0) ='SI' THEN 
    	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'A' THEN 
    		CONCEPTOS.CTA_CRD_ADMINISTRACION
        ELSE
        	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'V' THEN 
        		CONCEPTOS.CTA_CRD_VENTAS
            ELSE
            	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'P' THEN 
            		CONCEPTOS.CTA_CRD_PRODUCCION
                ELSE
                	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'O' THEN 
                		CONCEPTOS.CTA_CRD_OTRO
                    ELSE
                    	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'S' THEN 
                    		CONCEPTOS.CTA_CRD_SUPERNUM
                        ELSE
                        	' '
                        END)
                	END)
            	END)
        	END)
        END) 
    ELSE CONCEPTO_CENTROCOSTO.CTA_CREDITO 
    END,
    CASE WHEN PCK_SYSMAN_UTL.FC_PAR(UN_COMPANIA =>s$compania$s,UN_NOMBRE =>'MANEJA CUENTAS CONFIGURADAS PARA INTERFAZAR',UN_MODULO=>PCK_DATOS.FC_MODULONOMINA,UN_FECHA_PAR=>SYSDATE,UN_IND_MAYUS=>0) ='SI' THEN 
    	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'A' THEN 
    		CONCEPTOS.CTA_DBT_ADMINISTRACION
        ELSE
        	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'V' THEN 
        		CONCEPTOS.CTA_DBT_VENTAS
            ELSE
            	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'P' THEN 
            		CONCEPTOS.CTA_DBT_PRODUCCION
                ELSE
                	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'O' THEN 
                		CONCEPTOS.CTA_DBT_OTRO
                    ELSE
                    	(CASE WHEN PERSONAL.GRUPOCONTABLE = 'S' THEN 
                    		CONCEPTOS.CTA_DBT_SUPERNUM
                        ELSE
                        	' '
                        END)
                	END)
            	END)
        	END)
        END) 
    ELSE CONCEPTO_CENTROCOSTO.CTA_DEBITO 
    END
HAVING Sum(DISTRIBUCION.VALOR) <> 0]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);