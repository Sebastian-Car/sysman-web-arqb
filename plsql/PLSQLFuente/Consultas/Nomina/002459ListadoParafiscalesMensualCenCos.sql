MERGE INTO CONSULTAS FIN USING (SELECT '002459ListadoParafiscalesMensualCenCos' INFORME ,TO_CLOB(q'[SELECT CAJA.NOMBRE_CAJA,
       PCK_NOMINA_COM1.FC_PARAMETRO(UN_COMPANIA => s$compania$s, UN_OPCION => 47) AS PORC_CAJA,
       PCK_NOMINA_COM1.FC_PARAMETRO(UN_COMPANIA => s$compania$s, UN_OPCION => 45) AS PORC_ICBF,
       PCK_NOMINA_COM1.FC_PARAMETRO(UN_COMPANIA => s$compania$s, UN_OPCION => 46) AS PORC_SENA ,
       PCK_NOMINA_COM1.FC_PARAMETRO(UN_COMPANIA => s$compania$s, UN_OPCION => 48) AS PORC_ESAP ,
       PCK_NOMINA_COM1.FC_PARAMETRO(UN_COMPANIA => s$compania$s, UN_OPCION => 49) AS PORC_INST ,
       MIN(CASE CC.ID_DE_CONCEPTO WHEN 101 THEN CC.TERCERO END) AS NIT_CAJA,
       MIN(CASE CC.ID_DE_CONCEPTO WHEN 102 THEN CC.TERCERO END) AS NIT_ICBF,
       MIN(CASE CC.ID_DE_CONCEPTO WHEN 103 THEN CC.TERCERO END) AS NIT_SENA,
       MIN(CASE CC.ID_DE_CONCEPTO WHEN 104 THEN CC.TERCERO END) AS NIT_ESAP,
       MIN(CASE CC.ID_DE_CONCEPTO WHEN 105 THEN CC.TERCERO END) AS NIT_INST,
       SUM(CASE HIS.ID_DE_CONCEPTO WHEN 101 THEN HIS.VALOR ELSE 0 END) APORTE,
       SUM(CASE HIS.ID_DE_CONCEPTO WHEN 102 THEN HIS.VALOR ELSE 0 END) APORTE_ICBF,
       SUM(CASE HIS.ID_DE_CONCEPTO WHEN 103 THEN HIS.VALOR ELSE 0 END) APORTE_SENA,
       SUM(CASE HIS.ID_DE_CONCEPTO WHEN 104 THEN HIS.VALOR ELSE 0 END) APORTE_ESAP,
       SUM(CASE HIS.ID_DE_CONCEPTO WHEN 105 THEN HIS.VALOR ELSE 0 END) APORTE_INSTITUTOS_TECNICOS,
       SUM(CASE HIS.ID_DE_CONCEPTO WHEN 001 THEN HIS.VALOR ELSE 0 END) SALARIO_BASE,
       SUM(CASE HIS.ID_DE_CONCEPTO WHEN 108 THEN HIS.VALOR ELSE 0 END) BASE_PARAFISCAL,
       (SUM(CASE HIS.ID_DE_CONCEPTO WHEN 101 THEN HIS.VALOR ELSE 0 END)+ 
        SUM(CASE HIS.ID_DE_CONCEPTO WHEN 102 THEN HIS.VALOR ELSE 0 END)+
        SUM(CASE HIS.ID_DE_CONCEPTO WHEN 103 THEN HIS.VALOR ELSE 0 END)+
        SUM(CASE HIS.ID_DE_CONCEPTO WHEN 104 THEN HIS.VALOR ELSE 0 END)+ 
        SUM(CASE HIS.ID_DE_CONCEPTO WHEN 105 THEN HIS.VALOR ELSE 0 END)) AS SUMA,
        PHIS.ID_CENTRO_DE_COSTO,
        PHIS.NOMBRE_CENTRO_DE_COSTO
FROM HISTORICOS HIS
INNER JOIN CONCEPTOS CC 
  ON HIS.COMPANIA         = CC.COMPANIA
  AND HIS.ID_DE_CONCEPTO  = CC.ID_DE_CONCEPTO
INNER JOIN PERIODOS 
  ON HIS.COMPANIA       = PERIODOS.COMPANIA
  AND HIS.ID_DE_PROCESO = PERIODOS.ID_DE_PROCESO
  AND HIS.ANO           = PERIODOS.ANO
  AND HIS.MES           = PERIODOS.MES
  AND HIS.PERIODO       = PERIODOS.PERIODO
INNER JOIN PERSONAL PE 
  ON  HIS.COMPANIA        = PE.COMPANIA 
  AND HIS.ID_DE_EMPLEADO  = PE.ID_DE_EMPLEADO
INNER JOIN V_CAJA_COMPENSACION CAJA 
  ON  PE.COMPANIA            = CAJA.COMPANIA
  AND PE.CAJA_COMPENSACION  = CAJA.CAJA_COMPENSACION
LEFT JOIN PERSONAL_HISTORICO PHIS
ON HIS.COMPANIA        = PHIS.COMPANIA
AND HIS.ID_DE_PROCESO  = PHIS.ID_DE_PROCESO
AND HIS.MES            = PHIS.MES
AND HIS.ANO            = PHIS.ANO
AND HIS.PERIODO        = PHIS.PERIODO
AND HIS.ID_DE_EMPLEADO = PHIS.ID_DE_EMPLEADO  
WHERE HIS.COMPANIA = s$compania$s
  AND HIS.ID_DE_PROCESO = s$idProceso$s
  AND HIS.ANO = s$ano$s
  AND HIS.MES = s$mes$s
  s$filtro$s
  AND PERIODOS.ACUMULADO NOT IN (0)
  AND ]') || TO_CLOB(q'[HIS.ID_DE_CONCEPTO IN (101,102,103,104,105,001,107,108)
GROUP BY CAJA.NOMBRE_CAJA,
         PCK_NOMINA_COM1.FC_PARAMETRO(UN_COMPANIA => s$compania$s,UN_OPCION => 47) ,
         PCK_NOMINA_COM1.FC_PARAMETRO(UN_COMPANIA => s$compania$s,UN_OPCION => 45) ,
         PCK_NOMINA_COM1.FC_PARAMETRO(UN_COMPANIA => s$compania$s,UN_OPCION => 46) ,
         PCK_NOMINA_COM1.FC_PARAMETRO(UN_COMPANIA => s$compania$s,UN_OPCION => 48) ,
         PCK_NOMINA_COM1.FC_PARAMETRO(UN_COMPANIA => s$compania$s,UN_OPCION => 49),
         PHIS.ID_CENTRO_DE_COSTO,
         PHIS.NOMBRE_CENTRO_DE_COSTO]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);