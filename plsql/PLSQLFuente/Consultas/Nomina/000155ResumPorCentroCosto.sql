MERGE INTO CONSULTAS FIN USING (SELECT '000155ResumPorCentroCosto' INFORME ,TO_CLOB(q'[SELECT DISTINCT
    CASE WHEN '899.999.428-8' = '890704536-7' OR '899.999.428-8' = '832000283-6' 
    THEN 
        CASE WHEN PERSONAL_HISTORICO.GRUPOCONTABLE = 'A' 
        THEN 'ADMINISTRACION' 
        WHEN PERSONAL_HISTORICO.GRUPOCONTABLE = 'V' 
        THEN 'POLITICA'
        WHEN PERSONAL_HISTORICO.GRUPOCONTABLE = 'P' 
        THEN 'GESTION' 
        ELSE 'EJECUCION'
        END 
    ELSE
        CASE WHEN PERSONAL_HISTORICO.GRUPOCONTABLE = 'A' 
        THEN  'ADMINISTRACION' 
        WHEN PERSONAL_HISTORICO.GRUPOCONTABLE = 'V' 
        THEN 'OPERACION/VENTAS' 
        WHEN PERSONAL_HISTORICO.GRUPOCONTABLE = 'P' 
        THEN 'PRODUCCION/PENSIONADO' 
        ELSE 'OTRO'
        END
    END GRUPOCONTABLE, 
    PERSONAL_HISTORICO.ID_CENTRO_DE_COSTO CENTRO_COSTO,
    PERSONAL_HISTORICO.NOMBRE_CENTRO_DE_COSTO,
    CONCEPTOS.CLASE, 
    SUM(HISTORICOS.VALOR) SUMA_DE_VALOR, 
    CASE WHEN CONCEPTOS.CLASE = 3 
    THEN 'VALOR DEVENGOS' 
    WHEN CONCEPTOS.CLASE = 5 
    THEN 'VALOR DESCUENTOS' 
    ELSE  'CONCEPTOS CONTABLES'
    END CLASECONCEPTO,
    HISTORICOS.ID_DE_CONCEPTO, 
    CONCEPTOS.NOMBRE_CONCEPTO NOMBRE_CONCEPTO
FROM PERSONAL_HISTORICO 
    LEFT JOIN HISTORICOS 
        ON PERSONAL_HISTORICO.ID_DE_EMPLEADO = HISTORICOS.ID_DE_EMPLEADO
        AND PERSONAL_HISTORICO.COMPANIA = HISTORICOS.COMPANIA
        AND PERSONAL_HISTORICO.ANO = HISTORICOS.ANO
        AND PERSONAL_HISTORICO.MES = HISTORICOS.MES
        AND PERSONAL_HISTORICO.PERIODO = HISTORICOS.PERIODO
        AND PERSONAL_HISTORICO.ID_DE_PROCESO = HISTORICOS.ID_DE_PROCESO
    LEFT JOIN CENTRO_COSTO 
        ON PERSONAL_HISTORICO.ID_CENTRO_DE_COSTO = CENTRO_COSTO.CODIGO 
        AND PERSONAL_HISTORICO.COMPANIA = CENTRO_COSTO.COMPANIA
        AND PERSONAL_HISTORICO.ANO = CENTRO_COSTO.ANO
    LEFT JOIN CONCEPTOS 
        ON HISTORICOS.ID_DE_CONCEPTO = CONCEPTOS.ID_DE_CONCEPTO 
        AND HISTORICOS.COMPANIA = CONCEPTOS.COMPANIA 
    LEFT JOIN PERIODOS 
        ON HISTORICOS.PERIODO = PERIODOS.PERIODO
        AND HISTORICOS.MES = PERIODOS.MES
        AND HISTORICOS.ANO = PERIODOS.ANO 
        AND HISTORICOS.ID_DE_PROCESO = PERIODOS.ID_DE_PROCESO
        AND HISTORICOS.COMPANIA = PERIODOS.COMPANIA
WHERE (LPAD(HISTORICOS.ID_DE_PROCESO,2,'0')
    || LPAD(HISTORICOS.ANO,4,'0') 
    || LPAD(HISTORICOS.MES,2,'0') 
    || LPAD(HISTORICOS.PERIODO, 2,'0'))
    BETWEEN  s$codigoInicial$s AND s$codigoFinal$s
    AND HISTORICOS.COMPANIA = 's$compania$s' 
    AND CONCEPTOS.CLASE IN (8, 3,5)
   s$filtro$s
GROUP BY 
    CASE WHEN '899.999.428-8' = '890704536-7' OR '899.999.428-8' = '832000283-6' 
    THEN 
        CASE WHEN PERSONAL_HISTORICO.GRUPOCONTABLE = 'A' 
        THEN 'ADMINISTRACION' 
        WHEN PERSONAL_HISTORICO.GRUPOCONTABLE = 'V' 
        THEN 'POLITICA' 
        WHEN PERSONAL_HISTORICO.GRUPOCONTABLE = 'P' 
        THEN 'GESTION' 
        ELSE 'EJECUCION'
       END
    END, 
    CASE WHEN PERSONAL_HISTORICO.GRU]') || TO_CLOB(q'[POCONTABLE = 'A' 
    THEN  'ADMINISTRACION' 
    WHEN PERSONAL_HISTORICO.GRUPOCONTABLE = 'V' 
    THEN 'OPERACION/VENTAS' 
    WHEN PERSONAL_HISTORICO.GRUPOCONTABLE = 'P' 
    THEN  'PRODUCCION/PENSIONADO' 
    ELSE 'OTRO'
    END, 
    GRUPOCONTABLE,
    PERSONAL_HISTORICO.ID_CENTRO_DE_COSTO, 
    PERSONAL_HISTORICO.NOMBRE_CENTRO_DE_COSTO, 
    CONCEPTOS.CLASE,
    CASE WHEN  CONCEPTOS.CLASE = 3 
    THEN 'VALOR DEVENGOS'
    WHEN CONCEPTOS.CLASE = 5 
    THEN 'VALOR DESCUENTOS' 
    ELSE  'CONCEPTOS CONTABLES'    
    END,
    HISTORICOS.ID_DE_CONCEPTO, 
    CONCEPTOS.NOMBRE_CONCEPTO]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);