MERGE INTO CONSULTAS FIN USING (SELECT '002470PlanillaRiesgosxCDC' INFORME ,TO_CLOB(q'[SELECT  PERSONAL_HISTORICO.FONDO_RIESGOS AS FONDO_RIESGOS,
	    PERSONAL_HISTORICO.NOMBRE_FONDORIESGOS AS NOMBRE_FONDO_RIESGOS, 
        PERSONAL_HISTORICO.NUMERO_DCTO AS CEDULA, 
        PERSONAL_HISTORICO.NOMBRECOMPLETO AS NOM_EMPLEADO,
        PERSONAL_HISTORICO.ID_CENTRO_DE_COSTO AS ID_CENTRO_DE_COSTO, 
        PERSONAL_HISTORICO.NOMBRE_CENTRO_DE_COSTO  NOMBRE_CENTRO_DE_COSTO,
        MIN(PERSONAL_HISTORICO.SALARIO_BASE_IBC) KEEP (DENSE_RANK FIRST ORDER BY PERSONAL_HISTORICO.NUMERO_DCTO)SALARIO_BASE_IBC,
        UPPER(PCK_SYSMAN_UTL.FC_NOMBRE_MES(HISTORICOS.MES))  NOMBREMES,
        HISTORICOS.ID_DE_PROCESO AS PROCESO, 
        HISTORICOS.ANO AS ANO, 
        HISTORICOS.MES AS MES,
        PERSONAL_HISTORICO.ID_DE_CARGO AS CARGO,
        HISTORICOS.PERIODO AS PERIODO, 
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = '109' THEN HISTORICOS.VALOR ELSE 0 END) AS C109,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = '112' THEN HISTORICOS.VALOR ELSE 0 END) AS C112,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = '209' THEN HISTORICOS.VALOR ELSE 0 END) AS C209,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = '212' THEN HISTORICOS.VALOR ELSE 0 END) AS C212,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = '397' THEN HISTORICOS.VALOR ELSE 0 END) AS C397,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = '111' THEN HISTORICOS.VALOR ELSE 0 END) AS C111,
        HISTORICOS.COMPANIA, 
        PERSONAL_HISTORICO. FACTOR_RIESGO AS PORRIESGO, 
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = '111' THEN HISTORICOS.VALOR ELSE 0 END)-SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = '397' THEN HISTORICOS.VALOR ELSE 0 END) AS TOTAL
FROM    HISTORICOS
	    LEFT JOIN PERIODOS 
            ON HISTORICOS.COMPANIA = PERIODOS.COMPANIA 
            AND HISTORICOS.ID_DE_PROCESO = PERIODOS.ID_DE_PROCESO
            AND HISTORICOS.ANO = PERIODOS.ANO 
            AND HISTORICOS.MES = PERIODOS.MES
            AND HISTORICOS.PERIODO = PERIODOS.PERIODO
	    LEFT JOIN PERSONAL_HISTORICO 
            ON HISTORICOS.COMPANIA = PERSONAL_HISTORICO.COMPANIA
            AND HISTORICOS.ID_DE_PROCESO = PERSONAL_HISTORICO.ID_DE_PROCESO
            AND HISTORICOS.ANO = PERSONAL_HISTORICO.ANO
            AND HISTORICOS.MES = PERSONAL_HISTORICO.MES
            AND HISTORICOS.PERIODO = PERSONAL_HISTORICO.PERIODO
            AND HISTORICOS.ID_DE_EMPLEADO = PERSONAL_HISTORICO.ID_DE_EMPLEADO
WHERE   PERIODOS.ACUMULADO <> 0 
        AND HISTORICOS.ID_DE_PROCESO = s$procesoNomina$s
        AND HISTORICOS.ANO = s$anioNomina$s
        AND HISTORICOS.MES = s$mesNomina$s 
        AND HISTORICOS.COMPANIA =s$compania$s 
        AND HISTORICOS.PERIODO =s$periodoNomina$s
        AND HISTORICOS.ID_DE_CONCEPTO IN (112,209,111,397,212,109)
GROUP BY PERSONAL_HISTORICO.FONDO_RIESGOS,
	    PERSONAL_HISTORICO.NOMBRE_FONDORIESGOS, 
        PERSONAL_HISTORICO.NUMERO_DCTO, 
        PERSONAL_HISTORICO.NOMBRECOMPLETO,
        PERSONAL_HISTORICO.ID_CENTRO_DE_COSTO, 
        PERSONAL_HISTORICO.NOMBRE_CENTRO_DE_COSTO,
        HISTORICOS.ID_DE_PROCESO, 
        HISTORICOS.ANO, 
        HISTORICOS.MES,
        PERSONAL_HISTORICO.ID_DE_CARGO,
        HISTORICOS.PERIODO, 
        HISTORICOS.COMPANIA, 
        PERSONAL_HISTORICO. FACTOR_RIESGO
ORDER BY PERSONAL_HISTORICO.FONDO_RIESGOS, PERSONAL_HISTORICO.ID_CENTRO_DE_COSTO, LPAD(PERSONAL_HISTORICO.NUMERO_DCTO,30,'0')]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
COMMIT;