MERGE INTO CONSULTAS FIN USING (SELECT '002250VolantesUnoGobNarino' INFORME ,TO_CLOB(q'[SELECT
	V_VOLANTES.COMPANIA,
	V_VOLANTES.ID_CENTRO_DE_COSTO,
	V_VOLANTES.NOMBRE_CENTRO_DE_COSTO,
	V_VOLANTES.NUMERO_DCTO,
	V_VOLANTES.NOMBRECOMPLETO,
	V_VOLANTES.ID_DE_EMPLEADO,
	V_VOLANTES.SALARIO_BASE,
	V_VOLANTES.CLASE,
	V_VOLANTES.CLASE_NOMBRE,
	V_VOLANTES.CUENTA,
	V_VOLANTES.ID_DE_PROCESO,
	V_VOLANTES.ANO,
	V_VOLANTES.MES,
	V_VOLANTES.PERIODO,
	V_VOLANTES.ID_DE_CONCEPTO,
	V_VOLANTES.VALOR,
	TO_CHAR(V_VOLANTES.FECHA, 'DD/MM/YYYY') FECHA,
	V_VOLANTES.CANTIDAD,
	V_VOLANTES.DEVENGOS,
	V_VOLANTES.DESCUENTOS,
	V_VOLANTES.NETOAPAGAR,
	V_VOLANTES.DEPENDENCIA,
	V_VOLANTES.NOMBRE_DEPENDENCIA,
	V_VOLANTES.BANCO,
	V_VOLANTES.NOMBRE_BANCO,
	V_VOLANTES.NOMBRE_DE_CARGO,
	V_VOLANTES.ID_DE_CARGO,
	V_VOLANTES.NOMBRE_FONDOSALUD,
	V_VOLANTES.NOMBRE_FONDOPENSION,
	V_VOLANTES.NOMBRE_FONDORIESGOS,
	V_VOLANTES.NOMBRE_FONDOPENSION_VOL,
	V_VOLANTES.NOMBRE_FONDOCESANTIAS,
	V_VOLANTES.NOMBRE_CAJACOMPENSACION,
	V_VOLANTES.NOMBRE_MEDICINAPREPAGADA,
	V_VOLANTES.CUENTA,
	NVL(V_VOLANTES.DEDUCIBLE, 0) DEDUCIBLE ,
	V_VOLANTES.NOMBRE_CONCEPTO ,
	V_VOLANTES.NOMBRE_DE_CARGO,
	Personal.CEDULA_TUTOR,
	CASE
		WHEN personal.Tipo_Pensionado = 'P' THEN 'El mismos'
		WHEN personal.Tipo_Pensionado = 'H' THEN 'Hijo(a)'
		WHEN personal.Tipo_Pensionado = 'T' THEN 'Tutor'
		WHEN personal.Tipo_Pensionado = 'E' THEN 'Esposo(a)'
		WHEN personal.Tipo_Pensionado = 'M' THEN 'Padres'
		WHEN personal.Tipo_Pensionado = 'V' THEN 'Hijo Discapacitado'
		ELSE ''
	END AS TIPO_PENSIONADO,
	'Edad Pens/Sust.: ' || TRUNC(PCK_SYSMAN_UTL.FC_EDAD(personal.Fechancto, sysdate, 3)/ 360)  AS Sustituto1,
	CASE
		WHEN NVL(Personal_1.numero_dcto, 'NVLVAL')!= 'NVLVAL' THEN 'CC. ' || Trim(Personal_1.numero_dcto) || ' ' || personal.apellido1causante || ' ' || personal.apellido2causante || ' ' || personal.nombre1causante || ' ' || personal.nombre2causante
		ELSE ' ' || personal.apellido1causante || ' ' || personal.apellido2causante || ' ' || personal.nombre1causante || ' ' || personal.nombre2causante
	END || '  CC.' || personal.Numero_Dctocausante AS NOMBSUSTITUTO,
	CASE
		WHEN PERSONAL.NOMBRES_TUTOR <> ' ' THEN 'CURADOR: C.C.' || personal.CEDULA_TUTOR || ' ' || PERSONAL.APELLIDOS_TUTOR || ' ' || PERSONAL.NOMBRES_TUTOR
		ELSE ' '
	END AS CURADOR
FROM
	V_VOLANTES
INNER JOIN PERSONAL ON
	V_VOLANTES.COMPANIA = PERSONAL.COMPANIA
	AND V_VOLANTES.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO
LEFT JOIN Personal Personal_1 ON
	Personal.SustitutoDe = Personal_1.ID_de_Empleado
	AND Personal.Compania = Personal_1.Compania
WHERE V_VOLANTES.COMPANIA    = s$compania$s
AND V_VOLANTES.ID_DE_PROCESO = s$proceso$s
AND V_VOLANTES.ANO           = s$ano$s
AND V_VOLANTES.MES           = s$mes$s
AND V_VOLANTES.PERIODO       = s$periodo$s
AND V_VOLANTES.ID_DE_EMPLEADO BETWEEN s$empleadoIni$s AND s$empleadoFin$s
AND V_VOLANTES.ID_CENTRO_DE_COSTO BETWEEN 's$centroCostoIni$s' AND 's$centroCostoFin$s'
ORDER BY V_VOLANTES.ID_DE_EMPLEADO,
  V_VOLANTES.ID_DE_CONCEPTO]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'gfigueredo' CREATED_BY, 'gfigueredo' MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);