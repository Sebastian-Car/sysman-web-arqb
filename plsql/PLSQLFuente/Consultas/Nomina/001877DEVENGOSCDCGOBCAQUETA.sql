MERGE INTO CONSULTAS FIN USING (SELECT '001877DEVENGOSCDCGOBCAQUETA' INFORME ,TO_CLOB(q'[SELECT  
MAX(HISTORICOS.COMPANIA) KEEP (DENSE_RANK LAST ORDER BY ROWNUM) COMPANIA,
        MAX(HISTORICOS.ID_DE_PROCESO) KEEP (DENSE_RANK LAST ORDER BY ROWNUM) ID_DE_PROCESO,
        MAX(HISTORICOS.ANO) KEEP (DENSE_RANK LAST ORDER BY ROWNUM) ANO,
        MAX(HISTORICOS.MES) KEEP (DENSE_RANK LAST ORDER BY ROWNUM) MES,
        MAX(HISTORICOS.PERIODO) KEEP (DENSE_RANK LAST ORDER BY ROWNUM) PERIODO,
        ESTABLECIMIENTOS_DOCENTES.CODIGO AS ESTABLECIMIENTO, 
        ESTABLECIMIENTOS_DOCENTES.NOMBRE AS NOMBRE_ESTABLECIMIENTO, 
        MAX(PERSONAL.NUMERO_DCTO) AS CEDULA, 
        PERSONAL.NOMBRECOMPLETO NOM_EMPLEADO,
        PERSONAL.DEPENDENCIA DEPENDENCIA,
        DEPENDENCIA.NOMBRE NOMBREDEPENDENCIA,
        CENTRO_COSTO.CODIGO ID_CENTRO_DE_COSTO,
        CENTRO_COSTO.NOMBRE NOMBRE_CENTRO_DE_COSTO,
        MAX(PERSONAL.CUENTA) CUENTA, 
        MAX(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN('002') THEN NVL(CONCEPTOS.CTA_CRE_PPTAL,'0') ELSE '0' END) CUENTAPPTAL,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('001') THEN HISTORICOS.VALOR ELSE 0 END) C001,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('002') THEN HISTORICOS.VALOR ELSE 0 END) C002,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('009') THEN HISTORICOS.VALOR ELSE 0 END) C009,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('026') THEN HISTORICOS.VALOR ELSE 0 END) C026,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('062') THEN HISTORICOS.VALOR ELSE 0 END) C062,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('070') THEN HISTORICOS.VALOR ELSE 0 END) C070,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('079') THEN HISTORICOS.VALOR ELSE 0 END) C079,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('080') THEN HISTORICOS.VALOR ELSE 0 END) C080,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('097') THEN HISTORICOS.VALOR ELSE 0 END) C097,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('123') THEN HISTORICOS.VALOR ELSE 0 END) C123,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('125') THEN HISTORICOS.VALOR ELSE 0 END) C125,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('129') THEN HISTORICOS.VALOR ELSE 0 END) C129,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('130') THEN HISTORICOS.VALOR ELSE 0 END) C130,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('131') THEN HISTORICOS.VALOR ELSE 0 END) C131,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('132') THEN HISTORICOS.VALOR ELSE 0 END) C132,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('140') THEN HISTORICOS.VALOR ELSE 0 END) C140,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('144') THEN HISTORICOS.VALOR ELSE 0 END) C144,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('150') THEN HISTORICOS.VALOR ELSE 0 END) C150,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('151') THEN HISTORICOS.VALOR ELSE 0 END) C151,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('155') THEN HISTORICOS.VALOR ELSE 0 END) C155,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO I]') || TO_CLOB(q'[N ('156') THEN HISTORICOS.VALOR ELSE 0 END) C156,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('157') THEN HISTORICOS.VALOR ELSE 0 END) C157,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('158') THEN HISTORICOS.VALOR ELSE 0 END) C158,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('160') THEN HISTORICOS.VALOR ELSE 0 END) C160,
       SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('170','190') THEN HISTORICOS.VALOR ELSE 0 END) C170,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('174') THEN HISTORICOS.VALOR ELSE 0 END) C174,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('175') THEN HISTORICOS.VALOR ELSE 0 END) C175,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('177') THEN HISTORICOS.VALOR ELSE 0 END) C177,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('362') THEN HISTORICOS.VALOR ELSE 0 END) C362,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('379') THEN HISTORICOS.VALOR ELSE 0 END) C379,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('449') THEN HISTORICOS.VALOR ELSE 0 END) C449,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('600') THEN HISTORICOS.VALOR ELSE 0 END) C600,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('609') THEN HISTORICOS.VALOR ELSE 0 END) C609,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('616') THEN HISTORICOS.VALOR ELSE 0 END) C616,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('610') THEN HISTORICOS.VALOR ELSE 0 END) C610,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('611') THEN HISTORICOS.VALOR ELSE 0 END) C611,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('612') THEN HISTORICOS.VALOR ELSE 0 END) C612,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('697') THEN HISTORICOS.VALOR ELSE 0 END) C697,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('698') THEN HISTORICOS.VALOR ELSE 0 END) C698,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('799') THEN HISTORICOS.VALOR ELSE 0 END) C799,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('129') THEN HISTORICOS.VALOR ELSE 0 END + 
        CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('635') THEN HISTORICOS.VALOR ELSE 0 END) SINDICATO,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('699') THEN HISTORICOS.VALOR ELSE 0 END -  
            CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('637') THEN HISTORICOS.VALOR ELSE 0 END - 
            CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('635') THEN HISTORICOS.VALOR ELSE 0 END +
            CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('124') THEN HISTORICOS.VALOR ELSE 0 END +
            CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('127') THEN HISTORICOS.VALOR ELSE 0 END +
            CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('120') THEN HISTORICOS.VALOR ELSE 0 END +
            CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('123') THEN HISTORICOS.VALOR ELSE 0 END +
            CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('224') THEN HISTORICOS.VALOR ELSE 0 END)TOTALDSTOS,
        SUM(CASE WHEN CONCEPTOS.CLASE IN (3) THEN HISTORICOS.VALOR  ELSE 0 END) DEVENGOS,
        SUM(CA]') || TO_CLOB(q'[SE WHEN CONCEPTOS.CLASE IN (5) THEN HISTORICOS.VALOR  ELSE 0 END) AS DESCUENTOS,  
        SUM(CASE WHEN CONCEPTOS.CLASE IN (3) THEN HISTORICOS.VALOR  ELSE 0 END - 
            CASE WHEN CONCEPTOS.CLASE IN (5) THEN HISTORICOS.VALOR  ELSE 0 END)NETO,
        SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('637') THEN HISTORICOS.VALOR ELSE 0 END +
            CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('635') THEN HISTORICOS.VALOR ELSE 0 END) ESTAMPILLAS
FROM HISTORICOS 
  INNER JOIN CONCEPTOS 
    ON HISTORICOS.COMPANIA        = CONCEPTOS.COMPANIA  
    AND HISTORICOS.ID_DE_CONCEPTO = CONCEPTOS.ID_DE_CONCEPTO 
  INNER JOIN PERIODOS
    ON HISTORICOS.COMPANIA        = PERIODOS.COMPANIA
    AND HISTORICOS.ID_DE_PROCESO  = PERIODOS.ID_DE_PROCESO
    AND HISTORICOS.ANO            = PERIODOS.ANO
    AND HISTORICOS.MES            = PERIODOS.MES
    AND HISTORICOS.PERIODO        = PERIODOS.PERIODO 
  INNER JOIN PERSONAL 
    ON HISTORICOS.COMPANIA        = PERSONAL.COMPANIA  
    AND HISTORICOS.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO 
  LEFT JOIN ESTABLECIMIENTOS_DOCENTES 
    ON PERSONAL.COMPANIA                = ESTABLECIMIENTOS_DOCENTES.COMPANIA 
    AND PERSONAL.CODIGO_ESTABLECIMIENTO = ESTABLECIMIENTOS_DOCENTES.CODIGO
  INNER JOIN DEPENDENCIA
    ON PERSONAL.COMPANIA     = DEPENDENCIA.COMPANIA
    AND PERSONAL.DEPENDENCIA = DEPENDENCIA.CODIGO
  INNER JOIN CENTRO_COSTO
  ON PERSONAL.COMPANIA            = CENTRO_COSTO.COMPANIA 
  AND PERSONAL.ID_CENTRO_DE_COSTO = CENTRO_COSTO.CODIGO 
                     AND HISTORICOS.ANO                              = CENTRO_COSTO.ANO 
WHERE HISTORICOS.COMPANIA = s$compania$s
  AND HISTORICOS.ID_DE_PROCESO = s$proceso$s 
  AND HISTORICOS.ANO = s$ano$s
  AND HISTORICOS.MES = s$mes$s
  AND HISTORICOS.PERIODO = s$periodo$s 
  AND PERIODOS.ACUMULADO NOT IN (0) 
  AND ESTADO_ACTUAL IN (1, 3, 4, 5, 6, 7, 8)
  AND (ESTADO_ACTUAL <> 3 OR TO_CHAR(FECHA_DE_RETIRO, 'YYYYMM') >= s$ano$s || LPAD(s$mes$s, 2, '0'))
GROUP BY  ESTABLECIMIENTOS_DOCENTES.CODIGO ,
          ESTABLECIMIENTOS_DOCENTES.NOMBRE ,
          PERSONAL.DEPENDENCIA,
          PERSONAL.NOMBRECOMPLETO,
          DEPENDENCIA.NOMBRE,
          CENTRO_COSTO.CODIGO,
          CENTRO_COSTO.NOMBRE,
          PERSONAL.ID_DE_EMPLEADO
ORDER BY ESTABLECIMIENTOS_DOCENTES.CODIGO]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);