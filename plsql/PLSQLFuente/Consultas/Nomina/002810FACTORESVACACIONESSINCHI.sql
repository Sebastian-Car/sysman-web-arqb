MERGE INTO CONSULTAS FIN USING (SELECT '002810FACTORESVACACIONESSINCHI' INFORME ,TO_CLOB(q'[SELECT COMPANIA, 
      ID_DE_PROCESO, 
      ANO, 
      MES, 
      ID_DE_EMPLEADO, 
      NOMBRECOMPLETO, 
      NOMBRE_DEL_CARGO, 
      NUMERO_DCTO, 
      FECHA_DE_INGRESO, 
      FECHA_INICIO, 
      FECHA_FINAL, 
      INICIO_DISFRUTE, 
      FINAL_DISFRUTE, 
      NOMBRE, 
      NOMBRECIUDAD, 
      ANTIGUEDAD, 
      FACTOR2, 
      FACTOR3, 
      FACTOR7, 
      FACTOR8, 
      FACTOR9, 
      FACTOR10, 
      FACTOR11, 
      FACTOR12,
      FACTOR13, 
      NUMPER, 
      DIASVAC, 
      DIASPRIMA, 
      DIASCAL, 
      DIASDINERO, 
      BONRECRE, 
      PRIMVAC, 
      DIASHAB, 
      PANTIGUEDAD, 
      VACDINERO, 
      EXTRASPV, 
      SALVAC, 
      TOTALFACTOR, 
      TOTALPAGO, 
      FECHA, 
      DIASRECRE, 
      ACTO,
      FECHA_ACTO 
FROM V_FACTORESVACACIONES_SINCHI
WHERE COMPANIA       = s$compania$s
  AND ID_DE_PROCESO  = s$procesoNomina$s
  AND ANO            = s$anioNomina$s
  AND MES            = s$mesNomina$s
  AND ID_DE_EMPLEADO = s$empleado$s]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);