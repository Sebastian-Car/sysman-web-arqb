MERGE INTO CONSULTAS FIN USING (SELECT '000169ListadoParafiscalesDependencias' INFORME ,TO_CLOB(q'[WITH BASE AS ( 
SELECT HISTORICOS.COMPANIA, 	 
       HISTORICOS.ID_DE_PROCESO,  
       HISTORICOS.ANO, 
       HISTORICOS.MES,  
       SUM( HISTORICOS.VALOR ) AS FACTOR,  
       PERSONAL.DEPENDENCIA 
FROM PERSONAL INNER JOIN HISTORICOS  
 ON HISTORICOS.COMPANIA   		= PERSONAL.COMPANIA 
AND HISTORICOS.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO 
WHERE  HISTORICOS.COMPANIA        =   's$compania$s' 
   AND HISTORICOS.ID_DE_PROCESO   =   s$procesoNomina$s 
   AND HISTORICOS.ANO             =   s$anioNomina$s 
   AND HISTORICOS.MES             =   s$mesNomina$s 
   AND HISTORICOS.ID_DE_CONCEPTO  IN  (002,073,186,171,070,160,174,175,008,047,048,049,050,051,052,053,054,055,056,057,058,059,060,079,248,153,155,244,243,158)  
GROUP BY HISTORICOS.COMPANIA, HISTORICOS.ID_DE_PROCESO,  
        HISTORICOS.ANO,  
        HISTORICOS.MES,  
        PERSONAL.DEPENDENCIA 
) 
SELECT HISTORICOS.ID_DE_PROCESO,  
  HISTORICOS.ANO,  
  PCK_SYSMAN_UTL.FC_NOMBRE_MES(HISTORICOS.MES) AS MES,  
  BASE.FACTOR,  
    SUM(( CASE WHEN HISTORICOS.ID_DE_CONCEPTO =101 THEN  HISTORICOS.VALOR  ELSE 0 END )) AS COMPENSACION, 
    SUM(( CASE WHEN HISTORICOS.ID_DE_CONCEPTO =102 THEN  HISTORICOS.VALOR  ELSE 0 END )) AS BIENESTAR,  
    SUM(( CASE WHEN HISTORICOS.ID_DE_CONCEPTO =103 THEN  HISTORICOS.VALOR  ELSE 0 END )) AS SENA,  
    SUM(( CASE WHEN HISTORICOS.ID_DE_CONCEPTO =104 THEN  HISTORICOS.VALOR  ELSE 0 END )) AS ESAP,  
    SUM(( CASE WHEN HISTORICOS.ID_DE_CONCEPTO =105 THEN  HISTORICOS.VALOR  ELSE 0 END )) AS INSTITUTOS,  
  PERSONAL.DEPENDENCIA CODIGO , 
  DEPENDENCIA.NOMBRE NOMBRE, 
  PCK_SYSMAN_UTL.FC_ROUND((BASE.FACTOR*9/100)- 
	 (SUM(( CASE WHEN HISTORICOS.ID_DE_CONCEPTO =101 THEN  HISTORICOS.VALOR  ELSE 0 END )) + 
	  SUM(( CASE WHEN HISTORICOS.ID_DE_CONCEPTO =102 THEN  HISTORICOS.VALOR  ELSE 0 END )) + 
		SUM(( CASE WHEN HISTORICOS.ID_DE_CONCEPTO =103 THEN  HISTORICOS.VALOR  ELSE 0 END )) + 
		SUM(( CASE WHEN HISTORICOS.ID_DE_CONCEPTO =105 THEN  HISTORICOS.VALOR  ELSE 0 END )) +  
		SUM(( CASE WHEN HISTORICOS.ID_DE_CONCEPTO =104 THEN  HISTORICOS.VALOR  ELSE 0 END ))),0)  AS AJUSTECALCULO 
FROM HISTORICOS INNER JOIN PERSONAL 
 ON  HISTORICOS.COMPANIA         = PERSONAL.COMPANIA 
 AND HISTORICOS.ID_DE_EMPLEADO   = PERSONAL.ID_DE_EMPLEADO 
INNER JOIN DEPENDENCIA 
  ON DEPENDENCIA.COMPANIA   = PERSONAL.COMPANIA 
 AND DEPENDENCIA.CODIGO     = PERSONAL.DEPENDENCIA 
INNER JOIN BASE 
 ON  BASE.COMPANIA      = HISTORICOS.COMPANIA 
 AND BASE.ID_DE_PROCESO = HISTORICOS.ID_DE_PROCESO  
 AND BASE.ANO           = HISTORICOS.ANO  
 AND BASE.MES           = HISTORICOS.MES  
WHERE HISTORICOS.COMPANIA        =   's$compania$s' 
   AND HISTORICOS.ID_DE_PROCESO   =   s$procesoNomina$s 
   AND HISTORICOS.ANO             =   s$anioNomina$s 
   AND HISTORICOS.MES             =   s$mesNomina$s 
  AND HISTORICOS.ID_DE_CONCEPTO   IN (101 ,102,103,104,105)  
GROUP BY  
HISTORICOS.ID_DE_PROCESO,  
HISTORICOS.ANO,  
HISTORICOS.MES, 
BASE.FACT]') || TO_CLOB(q'[OR,  
PERSONAL.DEPENDENCIA,  
DEPENDENCIA.NOMBRE]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, 'ECABRERA' MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
