MERGE INTO CONSULTAS FIN USING (SELECT '002487PlanillaDevengosRV_1' INFORME ,TO_CLOB(q'[WITH HIST AS (
	SELECT 	*
	FROM 	HISTORICOS
	WHERE 	HISTORICOS.ID_DE_CONCEPTO IN (1,2,80,79,379,150,151,160,158,70,140,144,97,9,360)
			AND HISTORICOS.COMPANIA = s$compania$s
			AND HISTORICOS.ID_DE_PROCESO = s$proceso$s
			AND HISTORICOS.ANO = s$ano$s
			AND HISTORICOS.MES = s$mes$s
			AND HISTORICOS.PERIODO = s$periodo$s
)
SELECT 	HIST.ID_DE_EMPLEADO AS "CODIGOEM",
		CENTRO_COSTO.CODIGO AS "ID_CENTRO_DE_COSTO",
		CENTRO_COSTO.NOMBRE AS "NOMBRE_CENTRO_DE_COSTO",
		HIST.ID_DE_PROCESO AS "PROCESO",
		HIST.ANO AS "ANO",
		HIST.MES AS "MES",
		PERSONAL.ID_DE_CARGO AS "CARGO",
		PERSONAL.NUMERO_DCTO AS "CEDULA",
		CARGOS.NOMBRE_DEL_CARGO AS "NOMBRECARGO",
		PERSONAL.DEPENDENCIA AS "DEPENDENCIA",
		DEPENDENCIA.NOMBRE AS "NOMBREDEPENDENCIA",
		HIST.PERIODO AS "PERIODO",
		MAX(PERSONAL.APELLIDO1 || ' ' || PERSONAL.APELLIDO2 || ' ' || PERSONAL.NOMBRES) AS NOM_EMPLEADO, 
		SUM(DECODE(HIST.ID_DE_CONCEPTO,1,HIST.VALOR,0)) AS "ASIGMENSUAL",
		SUM(DECODE(HIST.ID_DE_CONCEPTO,2,HIST.VALOR,0)) AS "SUELDO",
		SUM(DECODE(HIST.ID_DE_CONCEPTO,80,HIST.VALOR,0)) AS "AUXTRANS",
		SUM(DECODE(HIST.ID_DE_CONCEPTO,79,HIST.VALOR,0)) AS "SUBALIMEN",
		SUM(DECODE(HIST.ID_DE_CONCEPTO,379,HIST.VALOR,360,-HIST.VALOR,0)) AS "INCAPACIDAD",
		SUM(DECODE(HIST.ID_DE_CONCEPTO,150,HIST.VALOR,0)) AS "BONIFBASAP",
		SUM(DECODE(HIST.ID_DE_CONCEPTO,151,HIST.VALOR,0)) AS "BONIFRECREACION",
		SUM(DECODE(HIST.ID_DE_CONCEPTO,160,HIST.VALOR,0)) AS "PRIMASEMESTRAL",
		SUM(DECODE(HIST.ID_DE_CONCEPTO,158,HIST.VALOR,0)) AS "PRIMANAVIDAD",
		SUM(DECODE(HIST.ID_DE_CONCEPTO,70,HIST.VALOR,0)) AS "HORASEXTRA",
		SUM(DECODE(HIST.ID_DE_CONCEPTO,97,1,1,0,140,0,144,0,97,0,9,0,360,1,-1) * HIST.VALOR ) AS "OTROSINGRESOS",
		SUM(DECODE(HIST.ID_DE_CONCEPTO,140,HIST.VALOR,0)) AS "TOTALDEDUC",
		SUM(DECODE(HIST.ID_DE_CONCEPTO,144,HIST.VALOR,0)) AS  "NETOPAGAR",
		SUM(DECODE(HIST.ID_DE_CONCEPTO,97,HIST.VALOR,0)) AS "TOTALREMUNE",
		SUM(DECODE(HIST.ID_DE_CONCEPTO,9,HIST.VALOR,0)) AS "DIASLAB"
FROM 
		HIST
		INNER JOIN PERSONAL
			ON HIST.COMPANIA = PERSONAL.COMPANIA
			AND HIST.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO
		INNER JOIN CONCEPTOS
			ON HIST.COMPANIA = CONCEPTOS.COMPANIA
			AND HIST.ID_DE_CONCEPTO = CONCEPTOS.ID_DE_CONCEPTO
		INNER JOIN PERIODOS
			ON HIST.COMPANIA = PERIODOS.COMPANIA
			AND HIST.ID_DE_PROCESO = PERIODOS.ID_DE_PROCESO
			AND HIST.ANO = PERIODOS.ANO
			AND HIST.MES = PERIODOS.MES
			AND HIST.PERIODO = PERIODOS.PERIODO
		INNER JOIN CARGOS
			ON PERSONAL.COMPANIA = CARGOS.COMPANIA
			AND PERSONAL.ID_DE_CARGO = CARGOS.ID_DE_CARGO
		INNER JOIN CENTRO_COSTO 
          	ON PERSONAL.ID_CENTRO_DE_COSTO = CENTRO_COSTO.CODIGO
          	AND PERSONAL.COMPANIA = CENTRO_COSTO.COMPANIA
          	AND PERSONAL.ANO = CENTRO_COSTO.ANO
		INNER JOIN DEPENDENCIA
			ON PERSONAL.COMPANIA = DEPENDENCIA.COMPANIA
			AND PERSONAL.DEPENDENCIA = DEPENDENCIA.CODIGO
WHERE	PERSONAL.ESTADO_ACTUAL<>0 
GROUP BY 	HIST.ID_DE_EMPLEADO,
			CENTRO_COSTO.CODIGO,
			CENTRO_COSTO.NOMBRE ,
			HIST.ID_DE_PROCESO ,
			HIST.ANO,
			HIST.MES ,
			PERSONAL.ID_DE_CARGO ,
			CARGOS.NOMBRE_DEL_CARGO ,
			PERSONAL.DEPENDENCIA ,
			DEPENDENCIA.NOMBRE ,
			PERSONAL.NUMERO_DCTO,
			HIST.PERIODO ]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
