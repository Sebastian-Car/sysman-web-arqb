MERGE INTO CONSULTAS FIN USING (SELECT '002645DistribucionfinalHistoricos' INFORME ,TO_CLOB(q'[SELECT
    HISTORICOS.ID_DE_EMPLEADO,
    HISTORICOS.ID_DE_CONCEPTO,
    HISTORICOS.VALOR AS VALORI,
    CONCEPTOS.NOMBRE_CONCEPTO, 
    PERSONAL.NOMBRECOMPLETO AS NOMBRE
FROM HISTORICOS 
LEFT JOIN PERSONAL 
ON HISTORICOS.COMPANIA = PERSONAL.COMPANIA 
AND HISTORICOS.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO 
LEFT JOIN CONCEPTOS 
ON HISTORICOS.COMPANIA = CONCEPTOS.COMPANIA 
AND HISTORICOS.ID_DE_CONCEPTO = CONCEPTOS.ID_DE_CONCEPTO
WHERE HISTORICOS.COMPANIA= s$compania$s
AND HISTORICOS.ID_DE_PROCESO = s$proceso$s
AND HISTORICOS.ANO= s$anio$s
AND HISTORICOS.MES= s$mes$s
AND HISTORICOS.PERIODO= s$periodo$s
AND HISTORICOS.ID_DE_CONCEPTO BETWEEN 's$conceptoInicial$s' AND 's$conceptoFinal$s' 
AND HISTORICOS.ID_DE_EMPLEADO BETWEEN 's$emplInicial$s' AND 's$emplFinal$s' 
AND (CONCEPTOS.CLASE = 3
    OR CONCEPTOS.CLASE = 8)]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);