MERGE INTO CONSULTAS FIN USING (SELECT '002442Electronica_NominaBase' INFORME ,TO_CLOB(q'[SELECT
    NUMERO,
    TIPO_NOMINA,
    ESTADO,
    FECHA,
    NUMERO_DOCUMENTO,
    CUNE
FROM
    ESTADO_NOM
WHERE
    TIPO_NOMINA = s$tipo_Nomina$s
 
ORDER BY
    NUMERO ASC]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'lvega' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);