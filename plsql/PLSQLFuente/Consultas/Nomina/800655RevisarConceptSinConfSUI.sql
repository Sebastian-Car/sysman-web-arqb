MERGE INTO CONSULTAS FIN USING (SELECT '800655RevisarConceptSinConfSUI' INFORME ,TO_CLOB(q'[SELECT
    CONCEPTOS.CLASE,
    HISTORICOS.ANO,
    HISTORICOS.ID_DE_CONCEPTO,
    CONCEPTOS.NOMBRE_CONCEPTO,
    SUM(CONCEPTOS.FACTOR_SUELDOS + CONCEPTOS.FACTOR_SALARIAL + CONCEPTOS.FACTOR_PRESTACIONAL + CONCEPTOS.FACTOR_EXTRALEGALES + CONCEPTOS.FACTOR_LEGALES) AS TOT, 
    CONCEPTOS.FACTOR_OTROSPAGOS
FROM HISTORICOS 
LEFT JOIN CONCEPTOS 
ON HISTORICOS.COMPANIA = CONCEPTOS.COMPANIA 
AND HISTORICOS.ID_DE_CONCEPTO = CONCEPTOS.ID_DE_CONCEPTO
WHERE HISTORICOS.COMPANIA = s$compania$s
AND CONCEPTOS.CLASE = 3
AND HISTORICOS.ANO = s$anio1$s
GROUP BY CONCEPTOS.CLASE, 
HISTORICOS.ANO, 
HISTORICOS.ID_DE_CONCEPTO, 
CONCEPTOS.NOMBRE_CONCEPTO, 
CONCEPTOS.FACTOR_OTROSPAGOS
HAVING SUM(CONCEPTOS.FACTOR_SUELDOS+CONCEPTOS.FACTOR_SALARIAL+CONCEPTOS.FACTOR_PRESTACIONAL+CONCEPTOS.FACTOR_EXTRALEGALES+CONCEPTOS.FACTOR_LEGALES) = 0 
AND CONCEPTOS.FACTOR_OTROSPAGOS = 0
ORDER BY HISTORICOS.ANO,
HISTORICOS.ID_DE_CONCEPTO]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);