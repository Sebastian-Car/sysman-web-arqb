MERGE INTO CONSULTAS FIN USING (SELECT '000185ResumenPorDependencias' INFORME ,TO_CLOB(q'[SELECT CASE 's$nitCompania$s'  
       WHEN '890704536-7'  
       THEN CASE PERSONAL.GRUPOCONTABLE WHEN 'A' THEN 'ADMINISTRACION' 
                                        WHEN 'V' THEN 'POLITICA' 
                                        WHEN 'P' THEN 'GESTION' 
                                        ELSE  'EJECUCION' END  
       ELSE CASE PERSONAL.GRUPOCONTABLE WHEN 'A' THEN 'ADMINISTRACION' 
                                        WHEN 'V' THEN 'OPERACION/VENTAS' 
                                        WHEN 'P' THEN 'PRODUCCION/PENSIONADO' 
                                        ELSE  'OTRO' END  
       END AS GRUPOCONTABLE, 
       V_ACUMULADOS.ID_CENTRO_DE_COSTO AS ID_CENTRO_DE_COSTO,  
       MAX(V_ACUMULADOS.NOMBRE_CENTRO_DE_COSTO) KEEP (DENSE_RANK LAST ORDER BY ROWNUM) AS NOMBRE_CENTRO_DE_COSTO,  
       V_ACUMULADOS.DEPENDENCIA AS DEPENDENCIA1, 
       MAX(V_ACUMULADOS.NOMBRE_DEPENDENCIA) KEEP (DENSE_RANK LAST ORDER BY ROWNUM) AS NOMBRE1, 
       MAX(V_ACUMULADOS.CLASE) KEEP (DENSE_RANK LAST ORDER BY ROWNUM) AS CLASE, 
       V_ACUMULADOS.ID_DE_CONCEPTO AS ID_DE_CONCEPTO,  
       -1 AS PRIMERODEACUMULADO,  
       MAX(CASE V_ACUMULADOS.CLASE WHEN 3 THEN 'VALOR DEVENGOS' 
                                   WHEN 4 THEN 'VALOR TOTAL DEVENGOS' 
                                   WHEN 5 THEN 'VALOR DESCUENTOS' 
                                   WHEN 6 THEN 'VALOR TOTAL  DESCUENTOS' 
                                   WHEN 7 THEN 'VALOR NETO A PAGAR' 
                                  ELSE 'CONCEPTOS CONTABLES' END) KEEP (DENSE_RANK LAST ORDER BY ROWNUM) AS ClaseConcepto, 
       MAX(V_ACUMULADOS.NOMBRE_CONCEPTO) KEEP (DENSE_RANK LAST ORDER BY ROWNUM) AS NOMBRE_CONCEPTO, 
       SUM(V_ACUMULADOS.VALOR) AS SUMA_DE_VALOR 
FROM PERSONAL LEFT JOIN  
   V_ACUMULADOS 
    ON PERSONAL.COMPANIA    = V_ACUMULADOS.COMPANIA AND 
    PERSONAL.ID_DE_EMPLEADO = V_ACUMULADOS.ID_DE_EMPLEADO 
WHERE V_ACUMULADOS.COMPANIA = s$compania$s 
AND LPAD(V_ACUMULADOS.ID_DE_PROCESO,2,'0') || V_ACUMULADOS.ano || LPAD(V_ACUMULADOS.mes,2,'0') || LPAD(V_ACUMULADOS.periodo,2,'0')  
    BETWEEN LPAD(s$proceso$s,2,'0') || s$ano1$s || LPAD(s$mes1$s,2,'0') || LPAD(s$periodo1$s,2,'0')  
        AND LPAD(s$proceso$s,2,'0') || s$ano2$s || LPAD(s$mes2$s,2,'0') || LPAD(s$periodo2$s,2,'0')  
s$CONDDEPENDENCIA$s 
GROUP BY CASE 's$nitCompania$s'  
         WHEN '890704536-7'  
         THEN CASE PERSONAL.GRUPOCONTABLE WHEN 'A' THEN 'ADMINISTRACION' 
                                        WHEN 'V' THEN 'POLITICA' 
                                        WHEN 'P' THEN 'GESTION' 
                                        ELSE  'EJECUCION' END    
         ELSE CASE PERSONAL.GRUPOCONTABLE WHEN 'A' THEN 'ADMINISTRACION' 
                                        WHEN 'V' THEN 'OPERACION/VENTAS' 
                                        WHEN 'P' THEN 'PRODUCCION/PENSIONADO' 
                                        ELSE  'OTRO' END END, 
V_ACUMULADOS.ID_CENTRO_DE_COSTO, 
V_ACUMULADOS.DEPENDEN]') || TO_CLOB(q'[CIA,  
V_ACUMULADOS.ID_DE_CONCEPTO 
HAVING MAX(V_ACUMULADOS.CLASE) KEEP (DENSE_RANK LAST ORDER BY ROWNUM) IN (3,4,5,6,7,8)]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);