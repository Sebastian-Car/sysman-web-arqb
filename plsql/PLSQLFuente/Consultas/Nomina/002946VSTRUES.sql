MERGE INTO CONSULTAS FIN USING (SELECT '002946VSTRUES' INFORME ,TO_CLOB(q'[SELECT DISTINCT COMPANIA,
    ID_DE_PROCESO,
    ANO,
    MES,
    PERIODO,
    ID_DE_EMPLEADO,
    NOMBRE_CENTRO_DE_COSTO,
    NUMERO_DCTO,
    NOMBRE_DE_CARGO,
    NOMBRE_CONCEPTO,
    DEVENGOS,
    DESCUENTOS,
    ID_DE_CONCEPTO,
    NOMBRECOMPLETO,
    CANTIDAD,
    NVL(DEDUCIBLE,0) DEDUCIBLE,
    SALARIO_BASE,
    NETOAPAGAR,
    NOMBRE_FONDOSALUD,
    NOMBRE_FONDOPENSION,
    NOMBRE_FONDORIESGOS,
    NOMBRE_FONDOPENSION_VOL,
    NOMBRE_FONDOCESANTIAS,
    NOMBRE_MEDICINAPREPAGADA,
    NOMBRE_CAJACOMPENSACION,
    CLASE
FROM V_VOLANTES
WHERE COMPANIA = s$compania$s
AND ID_DE_PROCESO = s$proceso$s
AND ANO = s$ano$s
AND MES = s$mes$s
AND PERIODO = s$periodo$s
AND ID_DE_EMPLEADO BETWEEN s$empleadoIni$s AND s$empleadoFin$s
AND ID_CENTRO_DE_COSTO BETWEEN 's$centroCostoIni$s' AND 's$centroCostoFin$s']') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);