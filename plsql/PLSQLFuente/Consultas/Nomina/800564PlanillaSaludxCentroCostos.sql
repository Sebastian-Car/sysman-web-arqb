MERGE INTO CONSULTAS FIN USING (SELECT '800564PlanillaSaludxCentroCostos' INFORME ,TO_CLOB(q'[SELECT V_ACUMULADOS.FONDO_SALUD AS FONDO_SALUD, 
	V_FONDO_DE_SALUD.NOMBRE_FONDO_SALUD,
	V_FONDO_DE_SALUD.CODAIFS, 
    V_FONDO_DE_SALUD.NIT,
	V_ACUMULADOS.ID_CENTRO_DE_COSTO AS ID_CENTRO_COSTOS,
	MAX(V_ACUMULADOS.NOMBRE_CENTRO_DE_COSTO) KEEP (DENSE_RANK LAST ORDER BY ROWNUM)  AS NOMBRE_CENTRO_COSTOS,
	MAX(V_ACUMULADOS.NUMERO_DCTO) KEEP (DENSE_RANK LAST ORDER BY ROWNUM)  AS CEDULA, 
	MAX( V_ACUMULADOS.NOMBRECOMPLETO) KEEP (DENSE_RANK LAST ORDER BY ROWNUM)  AS NOM_EMPLEADO,
	MIN(V_ACUMULADOS.SALARIO_BASE_IBC) KEEP (DENSE_RANK FIRST ORDER BY ROWNUM)  AS SALARIO_BASE_IBC, 
	SUM(DECODE(V_ACUMULADOS.ID_DE_CONCEPTO,112,V_ACUMULADOS.VALOR,0)) AS INGRESO_BASE_COTIZACION, 
	SUM(DECODE(V_ACUMULADOS.ID_DE_CONCEPTO,113,V_ACUMULADOS.VALOR,0)) AS APORTES_SALUD_EMPLEADO, 
	SUM(DECODE(V_ACUMULADOS.ID_DE_CONCEPTO,116,V_ACUMULADOS.VALOR,0)) AS APORTES_SALUD_PATRONO, 
	SUM(DECODE(V_ACUMULADOS.ID_DE_CONCEPTO,113,V_ACUMULADOS.VALOR,116,V_ACUMULADOS.VALOR,0)) AS TOTAL_APORTES_SALUD, 
	SUM(DECODE(V_ACUMULADOS.ID_DE_CONCEPTO,114,V_ACUMULADOS.VALOR,0)) AS APORTE_GARANTIA_EMPLEADO, 
	SUM(DECODE(V_ACUMULADOS.ID_DE_CONCEPTO,119,V_ACUMULADOS.VALOR,0)) AS APORTE_GARANTIA_PATRONO, 
	SUM(DECODE(V_ACUMULADOS.ID_DE_CONCEPTO,114,V_ACUMULADOS.VALOR,119,V_ACUMULADOS.VALOR,0)) AS TOTAL_APORTES_GARANTIA, 
	SUM(( CASE WHEN V_ACUMULADOS.ID_DE_CONCEPTO IN (114,119,123,486,113,116) THEN  V_ACUMULADOS.VALOR  ELSE 0 END )) AS GRAN_TOTAL_SALUD_Y_GARANTIA, 
	SUM(DECODE(V_ACUMULADOS.ID_DE_CONCEPTO,123,V_ACUMULADOS.VALOR,486,V_ACUMULADOS.VALOR,0)) AS UPC_ADICIONAL
FROM 
      V_ACUMULADOS INNER JOIN V_FONDO_DE_SALUD ON 
	  V_ACUMULADOS.COMPANIA    = V_FONDO_DE_SALUD.COMPANIA 
  AND V_ACUMULADOS.FONDO_SALUD = V_FONDO_DE_SALUD.FONDO_SALUD
WHERE V_ACUMULADOS.COMPANIA = s$compania$s
      AND V_ACUMULADOS.ID_DE_PROCESO= s$procesoNomina$s
      AND V_ACUMULADOS.ANO  = s$anioNomina$s
      AND V_ACUMULADOS.MES  = s$mesNomina$s
      AND V_ACUMULADOS.PERIODO  = s$periodoNomina$s
      AND V_ACUMULADOS.ID_DE_CONCEPTO IN (111,112,113,114,115,116,117,118,119,123,486)
GROUP BY V_ACUMULADOS.FONDO_SALUD , 
         V_ACUMULADOS.ID_CENTRO_DE_COSTO,
         V_ACUMULADOS.NUMERO_DCTO , 
		 V_FONDO_DE_SALUD.NOMBRE_FONDO_SALUD, 
		 V_ACUMULADOS.SALARIO_BASE_IBC , 
		 V_ACUMULADOS.ID_DE_PROCESO, 
		 V_ACUMULADOS.ANO,
		 V_ACUMULADOS.MES, 
         V_ACUMULADOS.ID_DE_CARGO, 
		 V_ACUMULADOS.DEPENDENCIA, 
		 V_ACUMULADOS.PERIODO, 
		 V_ACUMULADOS.NOMBRECOMPLETO, 
         V_FONDO_DE_SALUD.CODAIFS, 
		 V_FONDO_DE_SALUD.NIT 
ORDER BY V_FONDO_DE_SALUD.CODAIFS, V_ACUMULADOS.ID_CENTRO_DE_COSTO, LPAD(V_ACUMULADOS.NUMERO_DCTO,30,'0')]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
COMMIT;