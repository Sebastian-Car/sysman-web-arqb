MERGE INTO CONSULTAS FIN USING (SELECT '000210InformeEmbargos' INFORME ,TO_CLOB(q'[SELECT 
    EMBARGOS.ANO, 
    UPPER(PCK_SYSMAN_UTL.FC_NOMBRE_MES(EMBARGOS.MES))  NOMBREMES, 
    EMBARGOS.PERIODO, 
    TIPO_EMBARGO.NOMBRE  EMB, 
    EMBARGOS.ID_DE_EMPLEADO, 
    PERSONAL.NOMBRECOMPLETO, 
    PERSONAL.NUMERO_DCTO, 
    EMBARGOS.FECHA_EMBARGO, 
    EMBARGOS.OFICIO, 
    EMBARGOS.MONTO_EMBARGO, 
    EMBARGOS.PORCENTAJE, 
    MAX(CASE WHEN EMBARGOS.VR_CUOTA = 0 THEN  HISTORICOS.VALOR ELSE EMBARGOS.VR_CUOTA END)AS VR_CUOTA,
    EMBARGOS.SALDO, 
    EMBARGOS.CUOTAS, 
    CLASE_DEMANDANTE.DEMANDANTE, 
    EMBARGOS.FECHA_RADICACION, 
    EMBARGOS.NUM_RADICACION, 
    EMBARGOS.DEMANDANTE || ' ' || EMBARGOS.DEMANDANTE_NOMBRES DEMANDANTE_NOMBRES, 
    EMBARGOS.CEDULA, 
    EMBARGOS.CONCEPTO_DEPOSITO, 
    EMBARGOS.CUENTA, 
    EMBARGOS.BANCO, 
    EMBARGOS.NUMERO_PROCESO 
FROM 
    EMBARGOS 
      LEFT JOIN CLASE_DEMANDANTE 
        ON  EMBARGOS.COMPANIA = CLASE_DEMANDANTE.COMPANIA
        AND EMBARGOS.TIPO_DEMANDANTE = CLASE_DEMANDANTE.ID_DEMANDANTE
      INNER JOIN TIPO_EMBARGO 
        ON  EMBARGOS.COMPANIA = TIPO_EMBARGO.COMPANIA
        AND EMBARGOS.ID_EMBARGO = TIPO_EMBARGO.CODIGO
      INNER JOIN PERSONAL 
        ON  EMBARGOS.COMPANIA = PERSONAL.COMPANIA
        AND EMBARGOS.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO
	  INNER JOIN HISTORICOS
        ON HISTORICOS.ID_DE_PROCESO=EMBARGOS.ID_DE_PROCESO
        AND HISTORICOS.ID_DE_EMPLEADO=EMBARGOS.ID_DE_EMPLEADO
        AND HISTORICOS.ID_DE_CONCEPTO=EMBARGOS.ID_DE_CONCEPTO
        AND HISTORICOS.COMPANIA = EMBARGOS.COMPANIA
WHERE 
       EMBARGOS.COMPANIA = s$compania$s
  AND  EMBARGOS.ID_DE_PROCESO = s$idProceso$s
AND  LPAD(EMBARGOS.ANO, 4, '0') ||  LPAD(EMBARGOS.MES, 2, '0') || LPAD(EMBARGOS.PERIODO, 2, '0') BETWEEN LPAD(s$ano$s, 4, '0') || LPAD(s$mes$s, 2, '0') || LPAD(s$periodo$s, 2, '0') AND  LPAD(s$ano$s, 4, '0') || LPAD(s$mes$s, 2, '0') || LPAD(s$periodo$s, 2, '0')
GROUP BY EMBARGOS.ID_DE_EMPLEADO, 
         EMBARGOS.ANO, 
         UPPER(PCK_SYSMAN_UTL.FC_NOMBRE_MES(EMBARGOS.MES)), 
         EMBARGOS.PERIODO, 
         TIPO_EMBARGO.NOMBRE, 
         PERSONAL.NOMBRECOMPLETO, 
         PERSONAL.NUMERO_DCTO, 
         EMBARGOS.FECHA_EMBARGO, 
         EMBARGOS.OFICIO, 
         EMBARGOS.MONTO_EMBARGO, 
         EMBARGOS.PORCENTAJE, 
         EMBARGOS.VR_CUOTA, 
         EMBARGOS.SALDO, 
         EMBARGOS.CUOTAS, 
         CLASE_DEMANDANTE.DEMANDANTE, 
         EMBARGOS.FECHA_RADICACION, 
         EMBARGOS.NUM_RADICACION, 
         EMBARGOS.DEMANDANTE || ' ' || EMBARGOS.DEMANDANTE_NOMBRES, 
         EMBARGOS.CEDULA, 
         EMBARGOS.CONCEPTO_DEPOSITO, 
         EMBARGOS.CUENTA, 
         EMBARGOS.BANCO, 
         EMBARGOS.NUMERO_PROCESO]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);