MERGE INTO CONSULTAS FIN USING (SELECT '002789FACTORESLIQFINALSTR' INFORME ,TO_CLOB(q'[SELECT PERSONAL_HISTORICO.COMPANIA,
    PERSONAL_HISTORICO.ID_DE_PROCESO,
    PERSONAL_HISTORICO.ANO,
    PERSONAL_HISTORICO.MES,
    PERSONAL_HISTORICO.PERIODO,
    PERSONAL_HISTORICO.ID_DE_EMPLEADO,
    PERSONAL_HISTORICO.NOMBRECOMPLETO,
    PERSONAL_HISTORICO.NOMBRE_DE_CARGO,
    CAUSARETIRO.NOMBRE CAUSA_RETIRO,
    (CASE PERSONAL_HISTORICO.REGIMEN WHEN 1 THEN 'Retroactivas' WHEN 2 THEN 'Ley 50/1990' ELSE 'Salario Integral' END) REGIMEN,
    PERSONAL_HISTORICO.NOMBRE_FORMANOMBRAMIENTO NOMBREFORMA,
    PERSONAL_HISTORICO.NUMERO_DCTO,
    TO_CHAR(PERSONAL_HISTORICO.FECHATERCONTRATO,'YYYY') ANIOTERCONT,
    TO_CHAR(PERSONAL_HISTORICO.FECHA_DE_INGRESO,'YYYY') ANIOINGRESO,
    TO_CHAR(PERSONAL_HISTORICO.FECHATERCONTRATO,'MM') MESTERCONT,
    TO_CHAR(PERSONAL_HISTORICO.FECHA_DE_INGRESO,'MM') MESINGRESO,
    TO_CHAR(PERSONAL_HISTORICO.FECHATERCONTRATO,'DD') DIATERCONT,
    TO_CHAR(PERSONAL_HISTORICO.FECHA_DE_INGRESO,'DD') DIAINGRESO,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 1 THEN HISTORICOS.VALOR ELSE 0 END) SBM,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 904 THEN HISTORICOS.VALOR ELSE 0 END) CN904,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 903 THEN HISTORICOS.VALOR ELSE 0 END) CN903,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 902 THEN HISTORICOS.VALOR ELSE 0 END) EXTRAS,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 905 THEN HISTORICOS.VALOR ELSE 0 END) DOCEAVOPN,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 907 THEN HISTORICOS.VALOR ELSE 0 END) DOCEAVOPV,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 906 THEN HISTORICOS.VALOR ELSE 0 END) DOCEAVOPSJ,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 909 THEN HISTORICOS.VALOR ELSE 0 END) DOCEAVASBASP,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 969 THEN HISTORICOS.VALOR ELSE 0 END) SALARIO_PROM,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 911 THEN HISTORICOS.VALOR ELSE 0 END) ANTCES,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 177 THEN HISTORICOS.VALOR ELSE 0 END) CES,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 169 THEN HISTORICOS.VALOR ELSE 0 END) INTCES,
    SUBSTR(PCK_SYSMAN_UTL.FC_EDAD(PERSONAL_HISTORICO.FECHA_DE_INGRESO,PERSONAL_HISTORICO.FECHATERCONTRATO-0+1,1),1,2) TSERV1,
    SUBSTR(PCK_SYSMAN_UTL.FC_EDAD(PERSONAL_HISTORICO.FECHA_DE_INGRESO,PERSONAL_HISTORICO.FECHATERCONTRATO-0+1,1),4,2) TSERV2,
    SUBSTR(PCK_SYSMAN_UTL.FC_EDAD(PERSONAL_HISTORICO.FECHA_DE_INGRESO,PERSONAL_HISTORICO.FECHATERCONTRATO-0+1,1),7,2) TSERV3,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 912 THEN HISTORICOS.VALOR ELSE 0 END) LIC,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 910 THEN HISTORICOS.VALOR ELSE 0 END) DIASC,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 973 THEN HISTORICOS.VALOR ELSE 0 END) DIASDURCONT,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 952 THEN HISTORICOS.VALOR ELSE 0 END) CN952,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 112 THEN HISTORICOS.VALOR ELSE 0 END) CN112,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 130 THEN HISTORICOS.VALOR ELSE 0 END) SALUD,
    SUM(CASE]') || TO_CLOB(q'[ WHEN HISTORICOS.ID_DE_CONCEPTO = 131 THEN HISTORICOS.VALOR ELSE 0 END) PENSION,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 132 THEN HISTORICOS.VALOR ELSE 0 END) FSP,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 120 THEN HISTORICOS.VALOR ELSE 0 END) FSPAD,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 125 THEN HISTORICOS.VALOR ELSE 0 END) RTEFTE,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 600 THEN HISTORICOS.VALOR ELSE 0 END) CN600,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 601 THEN HISTORICOS.VALOR ELSE 0 END) CN601,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 602 THEN HISTORICOS.VALOR ELSE 0 END) CN602,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 603 THEN HISTORICOS.VALOR ELSE 0 END) CN603,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 604 THEN HISTORICOS.VALOR ELSE 0 END) CN604,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 605 THEN HISTORICOS.VALOR ELSE 0 END) CN605,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 606 THEN HISTORICOS.VALOR ELSE 0 END) CN606,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 607 THEN HISTORICOS.VALOR ELSE 0 END) CN607,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 140 THEN HISTORICOS.VALOR ELSE 0 END) CN140,
    SUM((CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 175 THEN HISTORICOS.VALOR ELSE 0 END) +
        (CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 176 THEN HISTORICOS.VALOR ELSE 0 END)) VACDINERO,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 96 THEN HISTORICOS.VALOR ELSE 0 END) CN96,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 160 THEN HISTORICOS.VALOR ELSE 0 END) PRIMAJUN,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 67 THEN HISTORICOS.VALOR ELSE 0 END) CN67,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 93 THEN HISTORICOS.VALOR ELSE 0 END) CN93,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 826 THEN HISTORICOS.VALOR ELSE 0 END) CN826,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 964 THEN HISTORICOS.VALOR ELSE 0 END) CN964,
    SUM((CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 2 THEN HISTORICOS.VALOR ELSE 0 END) +
        (CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 7 THEN HISTORICOS.VALOR ELSE 0 END)) SUELDO,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 9 THEN HISTORICOS.VALOR ELSE 0 END) CN9,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 79 THEN HISTORICOS.VALOR ELSE 0 END) AUXALIMENT,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 158 THEN HISTORICOS.VALOR ELSE 0 END) PRIMANAV,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 155 THEN HISTORICOS.VALOR ELSE 0 END) PRIMAVAC,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 159 THEN HISTORICOS.VALOR ELSE 0 END) PRIMAEXSEMEST,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 163 THEN HISTORICOS.VALOR ELSE 0 END) PRIMAEXNAV,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 150 THEN HISTORICOS.VALOR ELSE 0 END) BASP,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 80 THEN HISTORICOS.VALOR ELSE 0 END) AUXTRANSP,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 97 THEN HISTORICOS.VALOR ELSE 0 END) DEVENGOS,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 140 THEN HISTORICOS.VALOR ELSE 0 END]') || TO_CLOB(q'[) DESCUENTOS,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = 144 THEN HISTORICOS.VALOR ELSE 0 END) NETO,
    COMPANIA.NITCOMPANIA
FROM PERSONAL_HISTORICO
LEFT JOIN CAUSARETIRO
ON PERSONAL_HISTORICO.COMPANIA = CAUSARETIRO.COMPANIA
AND PERSONAL_HISTORICO.CAUSA_RETIRO = CAUSARETIRO.CODIGO  
INNER JOIN HISTORICOS 
ON PERSONAL_HISTORICO.COMPANIA = HISTORICOS.COMPANIA
AND PERSONAL_HISTORICO.ID_DE_PROCESO = HISTORICOS.ID_DE_PROCESO
AND PERSONAL_HISTORICO.ANO = HISTORICOS.ANO
AND PERSONAL_HISTORICO.MES = HISTORICOS.MES
AND PERSONAL_HISTORICO.PERIODO = HISTORICOS.PERIODO               
AND PERSONAL_HISTORICO.ID_DE_EMPLEADO = HISTORICOS.ID_DE_EMPLEADO
INNER JOIN COMPANIA
ON PERSONAL_HISTORICO.COMPANIA = COMPANIA.CODIGO
WHERE PERSONAL_HISTORICO.COMPANIA = s$compania$s
    AND PERSONAL_HISTORICO.ID_DE_PROCESO = s$idProceso$s
    AND PERSONAL_HISTORICO.ANO = s$ano$s
    AND PERSONAL_HISTORICO.MES = s$mes$s
    AND PERSONAL_HISTORICO.PERIODO = s$periodo$s
    AND PERSONAL_HISTORICO.ID_DE_EMPLEADO = s$idEmpleado$s
GROUP BY PERSONAL_HISTORICO.COMPANIA,
    PERSONAL_HISTORICO.ID_DE_PROCESO,
    PERSONAL_HISTORICO.ANO,
    PERSONAL_HISTORICO.MES,
    PERSONAL_HISTORICO.PERIODO,
    PERSONAL_HISTORICO.ID_DE_EMPLEADO,
    PERSONAL_HISTORICO.NOMBRECOMPLETO,
    PERSONAL_HISTORICO.NOMBRE_DE_CARGO,
    CAUSARETIRO.NOMBRE,
    (CASE PERSONAL_HISTORICO.REGIMEN WHEN 1 THEN 'Retroactivas' WHEN 2 THEN 'Ley 50/1990' ELSE 'Salario Integral' END),
    PERSONAL_HISTORICO.NOMBRE_FORMANOMBRAMIENTO,
    PERSONAL_HISTORICO.NUMERO_DCTO,
    PERSONAL_HISTORICO.FECHATERCONTRATO,
    PERSONAL_HISTORICO.FECHA_DE_INGRESO,
    COMPANIA.NITCOMPANIA]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);