MERGE INTO CONSULTAS FIN USING (SELECT '002242INFORMACIONPENSIONADOS2017' INFORME ,TO_CLOB(q'[SELECT DISTINCT
    HISTORICOS.COMPANIA,
    HISTORICOS.ANO,
    HISTORICOS.MES,
    HISTORICOS.PERIODO,
    HISTORICOS.ID_DE_EMPLEADO,
    PERSONAL.APELLIDO1,
    PERSONAL.APELLIDO2,
    PERSONAL.NOMBRES,
    PERSONAL.NUMERO_DCTO,
    TO_CHAR(PERSONAL.FECHANCTO,'DD/MM/YYYY') AS FECHANCTO ,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO = '2' THEN HISTORICOS.VALOR ELSE 0 END) AS MESADA,
    SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO <> '2' THEN HISTORICOS.VALOR ELSE 0 END ) AS MESADAS_ADICIONALES,
	CARGOS.NOMBRE_DEL_CARGO,
    PERSONAL.TIPOVINCULACION,
    PERSONAL.TIPOPENSIONADO,
	PERSONAL.TIPO_PENSIONADO AS CLASE_DE_PENSIONADO,
    UPPER(P1.NOM_PERIODO) AS NOM_P1,
    UPPER(P2.NOM_PERIODO) AS NOM_P2,
    UPPER(M1.NOMBRE) AS NOM_MES1,
    UPPER(M2.NOMBRE) AS NOM_MES2,
    P1.ANO ANO_I,
    P2.ANO ANO_F
FROM
    HISTORICOS LEFT
    JOIN CONCEPTOS ON HISTORICOS.ID_DE_CONCEPTO = CONCEPTOS.ID_DE_CONCEPTO
                      AND HISTORICOS.COMPANIA = CONCEPTOS.COMPANIA
    LEFT JOIN PERIODOS ON HISTORICOS.COMPANIA = PERIODOS.COMPANIA
                          AND HISTORICOS.ID_DE_PROCESO = PERIODOS.ID_DE_PROCESO
                          AND HISTORICOS.ANO = PERIODOS.ANO
                          AND HISTORICOS.MES = PERIODOS.MES
                          AND HISTORICOS.PERIODO = PERIODOS.PERIODO
    LEFT JOIN PERSONAL ON HISTORICOS.ID_DE_EMPLEADO = PERSONAL.ID_DE_EMPLEADO
                          AND HISTORICOS.COMPANIA = PERSONAL.COMPANIA
    LEFT JOIN COMPANIA ON HISTORICOS.COMPANIA = COMPANIA.CODIGO
    LEFT JOIN CARGOS ON PERSONAL.ID_DE_CARGO = CARGOS.ID_DE_CARGO
                        AND PERSONAL.COMPANIA = CARGOS.COMPANIA
    LEFT JOIN PERIODOS P1 ON P1.COMPANIA = HISTORICOS.COMPANIA
                        AND P1.ANO = s$anoI$s
                        AND P1.MES = s$mesI$s
                        AND P1.PERIODO = s$periodoI$s
    LEFT JOIN PERIODOS P2 ON P2.COMPANIA = HISTORICOS.COMPANIA
                        AND P2.ANO = s$anoF$s
                        AND P2.MES = s$mesF$s
                        AND P2.PERIODO = s$periodoF$s
    LEFT JOIN MES M1 ON M1.COMPANIA = HISTORICOS.COMPANIA
                        AND M1.ANO = s$anoI$s
                        AND M1.NUMERO = s$mesI$s
    LEFT JOIN MES M2 ON M2.COMPANIA = HISTORICOS.COMPANIA
                        AND M2.ANO = s$anoF$s
                        AND M2.NUMERO = s$mesF$s
WHERE
    CONCEPTOS.CLASE = 3
    AND HISTORICOS.ID_DE_CONCEPTO IN (
        2
    )
    AND HISTORICOS.ID_DE_PROCESO = s$proceso$s
    AND HISTORICOS.COMPANIA = s$compania$s
    AND LPAD(HISTORICOS.ANO, 4, 0) * 100 + LPAD(HISTORICOS.MES, 2, 0) + LPAD(HISTORICOS.PERIODO, 2, 0) BETWEEN s$anoI$s * 100 + s$mesI$s + s$periodoI$s  AND s$anoF$s *100 + s$mesF$s + s$periodoF$s
group by HISTORICOS.COMPANIA, 
HISTORICOS.ANO, 
HISTORICOS.MES, 
HISTORICOS.PERIODO, 
HISTORICOS.ID_DE_EMPLEADO, 
PERSONAL.APELLIDO1, 
PERSONAL.APELLIDO2, 
PERSONAL.NOMBRES, 
PERSONAL.NUMERO_DCTO, 
PERSONAL.FECHANCTO, 
CARGOS.NOMBRE_DEL_CARGO, 
PERSONA]') || TO_CLOB(q'[L.TIPOVINCULACION, 
PERSONAL.TIPOPENSIONADO, 
PERSONAL.TIPO_PENSIONADO , 
P1.NOM_PERIODO, 
P2.NOM_PERIODO,
M1.NOMBRE,
M2.NOMBRE,
P1.ANO,
P2.ANO]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'gfigueredo' CREATED_BY, 'gfigueredo' MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);