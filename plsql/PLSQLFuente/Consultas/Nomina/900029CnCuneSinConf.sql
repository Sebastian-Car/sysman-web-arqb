MERGE INTO CONSULTAS FIN USING (SELECT '900029CnCuneSinConf' INFORME ,TO_CLOB(q'[SELECT H.ANO, H.MES, SUM(H.VALOR) AS SUMADEVALOR,
CASE C.CLASE WHEN 1 THEN 'Informativo' WHEN 3 THEN 'RemuneraciÃ³n' WHEN 4 THEN 'Total Devengado' WHEN 5 THEN 'Descuento' WHEN 6 THEN 'Total Deducido'
WHEN 7 THEN 'Neto a Pagar' WHEN 2 THEN 'Otros Informativos' WHEN 8 THEN 'Contable' WHEN 9 THEN 'Anticipados' WHEN 99 THEN 'Bases' ELSE NULL END CLASE ,  
H.ID_DE_CONCEPTO, C.NOMBRE_CONCEPTO
FROM HISTORICOS H 
INNER JOIN CONCEPTOS C
    ON  H.COMPANIA = C.COMPANIA  
    AND H.ID_DE_CONCEPTO = C.ID_DE_CONCEPTO
INNER JOIN COMPANIA CP 
    ON H.COMPANIA = CP.CODIGO
WHERE H.COMPANIA = s$compania$s
AND CP.GENERA_CUNE NOT IN (0)
AND H.ANO = s$anio$s
AND H.MES = s$mes$s
AND C.CLASE BETWEEN 3 AND 7
AND C.CONCEPTO_CUNE IS NULL
GROUP BY H.ANO, H.MES, 
CASE C.CLASE WHEN 1 THEN 'Informativo' WHEN 3 THEN 'RemuneraciÃ³n' WHEN 4 THEN 'Total Devengado' WHEN 5 THEN 'Descuento' WHEN 6 THEN 'Total Deducido'
WHEN 7 THEN 'Neto a Pagar' WHEN 2 THEN 'Otros Informativos' WHEN 8 THEN 'Contable' WHEN 9 THEN 'Anticipados' WHEN 99 THEN 'Bases' ELSE NULL END ,  
H.ID_DE_CONCEPTO, C.NOMBRE_CONCEPTO,C.CLASE
HAVING SUM(H.VALOR) <>0
ORDER BY C.CLASE]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);
