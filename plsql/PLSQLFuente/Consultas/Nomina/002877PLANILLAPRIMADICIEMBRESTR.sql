MERGE INTO CONSULTAS FIN USING (SELECT '002877PLANILLAPRIMADICIEMBRESTR' INFORME ,TO_CLOB(q'[SELECT PERSONAL.ID_DE_EMPLEADO,
  PERSONAL.NOMBRECOMPLETO AS NOMCOMPLETO,
  PERSONAL.FECHA_DE_INGRESO,
  PERSONAL.NUMERO_DCTO AS NRO_DOCUMENTO,
  CENTRO_COSTO.CODIGO AS ID_CENTRO_DE_COSTO,
  CENTRO_COSTO.NOMBRE AS NOMBRE_CENTRO_DE_COSTO,
  HISTORICOS.ID_DE_PROCESO,
  HISTORICOS.ANO,
  HISTORICOS.MES,
  HISTORICOS.PERIODO,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('158') THEN HISTORICOS.VALOR ELSE 0 END) AS PRIMADIC,
  (CASE WHEN PERSONAL.ID_DE_TIPO = '05' THEN
     SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('930') THEN HISTORICOS.VALOR ELSE 0 END) + 
     SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('945') THEN HISTORICOS.VALOR ELSE 0 END)
   ELSE 
     SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('930') THEN HISTORICOS.VALOR ELSE 0 END)
   END) AS SUELDOPRIMADIC,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('159') THEN HISTORICOS.VALOR ELSE 0 END) + SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('160') THEN HISTORICOS.VALOR ELSE 0 END) AS PRIMAEXTRASEMESTRAL,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('931') THEN HISTORICOS.VALOR ELSE 0 END) AS PRIMASEMESTRAL,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('932') THEN HISTORICOS.VALOR ELSE 0 END) AS PRIMAVAC,
  (CASE WHEN PERSONAL.ID_DE_TIPO = '05' THEN
     SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('933') THEN HISTORICOS.VALOR ELSE 0 END) + 
     SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('948') THEN HISTORICOS.VALOR ELSE 0 END)
   ELSE 
     SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('933') THEN HISTORICOS.VALOR ELSE 0 END)
   END) AS TRANSPRIMADIC,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('934') THEN HISTORICOS.VALOR ELSE 0 END) AS ALIMENPRIMADIC,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('935') THEN HISTORICOS.VALOR ELSE 0 END) AS GASTOSREP,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('943') THEN HISTORICOS.VALOR ELSE 0 END) AS INCRANTIG,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('939') THEN HISTORICOS.VALOR ELSE 0 END) AS BON,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('940') THEN HISTORICOS.VALOR ELSE 0 END) AS VPT,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('936') THEN HISTORICOS.VALOR ELSE 0 END) AS DIASPRIMADIC,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('937') THEN HISTORICOS.VALOR ELSE 0 END) AS DIASCOMPRIMADIC,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('938') THEN HISTORICOS.VALOR ELSE 0 END) AS DIASLICPRIMADIC,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('942') THEN HISTORICOS.VALOR ELSE 0 END) AS EXTRAS,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('144') THEN HISTORICOS.VALOR ELSE 0 END) AS NETO,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('150') THEN HISTORICOS.VALOR ELSE 0 END) AS BASP,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('170') THEN HISTORICOS.VALOR ELSE 0 END) AS PANTIG,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('125') THEN HISTORICOS.VALOR ELSE 0 END) AS RETEFUENTE,
SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('160') THEN HISTORICOS.VALOR ELSE 0 END) AS PRIMASEMESTRALNAVIDAD,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('6]') || TO_CLOB(q'[99') THEN HISTORICOS.VALOR ELSE 0 END) + SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('799') THEN HISTORICOS.VALOR ELSE 0 END) AS EMBARGOS,
  SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('125') THEN HISTORICOS.VALOR ELSE 0 END) + SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('699') THEN HISTORICOS.VALOR ELSE 0 END) + SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('799') THEN HISTORICOS.VALOR ELSE 0 END) AS DESCUENTOS
FROM HISTORICOS
INNER JOIN PERSONAL
   ON HISTORICOS.COMPANIA        = PERSONAL.COMPANIA
  AND HISTORICOS.ID_DE_EMPLEADO  = PERSONAL.ID_DE_EMPLEADO
INNER JOIN CARGOS
   ON PERSONAL.COMPANIA    = CARGOS.COMPANIA
  AND PERSONAL.ID_DE_CARGO = CARGOS.ID_DE_CARGO
INNER JOIN CENTRO_COSTO
   ON PERSONAL.COMPANIA           = CENTRO_COSTO.COMPANIA
  AND PERSONAL.ANO                = CENTRO_COSTO.ANO
  AND PERSONAL.ID_CENTRO_DE_COSTO = CENTRO_COSTO.CODIGO
WHERE HISTORICOS.COMPANIA    = s$compania$s
AND HISTORICOS.ID_DE_PROCESO = s$proceso$s
AND HISTORICOS.ANO           = s$anio$s
AND HISTORICOS.MES           = s$mes$s
AND HISTORICOS.PERIODO       = s$periodo$s
AND HISTORICOS.ID_DE_CONCEPTO IN ('158','930','931','932','933','934','935','936','937','938','939','940','942','943','140','144','159','150','140','097','163','170','160','125','699','799','160','948','945')
GROUP BY  PERSONAL.ID_DE_EMPLEADO,
  PERSONAL.NUMERO_DCTO,
  PERSONAL.NOMBRECOMPLETO,
  PERSONAL.FECHA_DE_INGRESO,
  PERSONAL.FECHATERCONTRATO,
  CENTRO_COSTO.NOMBRE,
  CARGOS.NOMBRE_DEL_CARGO,
  HISTORICOS.ID_DE_PROCESO,
  HISTORICOS.ANO,
  HISTORICOS.MES,
  HISTORICOS.PERIODO,
  CENTRO_COSTO.CODIGO,
  PERSONAL.ID_DE_TIPO
HAVING (SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('158','160') THEN HISTORICOS.VALOR ELSE 0 END) <> 0)
   AND (SUM(CASE WHEN HISTORICOS.ID_DE_CONCEPTO IN ('144') THEN HISTORICOS.VALOR ELSE 0 END) > 0)]') CONSULTA, 6 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);