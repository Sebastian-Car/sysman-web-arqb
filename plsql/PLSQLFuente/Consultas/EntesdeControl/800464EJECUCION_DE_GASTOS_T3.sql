MERGE INTO CONSULTAS FIN USING (SELECT '800464EJECUCION_DE_GASTOS_T3' INFORME ,TO_CLOB(q'[SELECT CODIGO,
  s$nombre$s 
  VIGENCIA,
  SECCION_PPTAL_CUIPO,
  --NVL(SECTOR_CUIPO,0) SECTOR_CUIPO,
  CPC,
  FUENTE_FINANCIACION,
  NVL(PROGRAMATICO,0)PROGRAMATICO,
  NVL(BPIN,0) BPIN ,
  SITUACION_FONDOS,
  NVL(POLITICA_PUBLICA,0) POLITICA_PUBLICA,
  TERCERO,
  COMPROMISOS,
  OBLIGACIONES,
  PAGOS
FROM
  (SELECT CUIPO_CODIGO_CCEPT.COMPANIA,
    CUIPO_CODIGO_CCEPT.ANO,
    CUIPO_CODIGO_CCEPT.CODIGO,
    CUIPO_CODIGO_CCEPT.NOMBRE,
    P.VIGENCIA,
    CUIPO_SECCION_PPTAL.CODIGO_EQUIVALENTE SECCION_PPTAL_CUIPO,
    DETALLE_COMPROBANTE_PPTAL.SECTOR,
    NVL(DETALLE_COMPROBANTE_PPTAL.CODIGO_CPC,'0.0') CPC,
    CUIPO_FUENTES.CODIGO_EQUIVALENTE FUENTE_FINANCIACION,
    CASE
      WHEN P.APLICA_DEST_ESPECIFICA NOT IN (0)
      THEN P.TIPO_NORMA
      ELSE '0.0'
    END TIPO_NORMA,
    CASE
      WHEN P.APLICA_DEST_ESPECIFICA NOT IN (0)
      THEN P.NUMERO_NORMA
      ELSE '0.0'
    END NUMERO_NORMA,
    CASE
      WHEN P.APLICA_DEST_ESPECIFICA NOT IN (0)
      THEN TO_CHAR(P.FECHA_NORMA,'DD/MM/YYYY')
      ELSE '01/01/1900'
    END FECHA_NORMA,
    CUIPO_PROGRAMA.CODIGO_EQUIVALENTE PROGRAMATICO,
    DETALLE_COMPROBANTE_PPTAL.CODIGO_BPIN BPIN,
    CASE
      WHEN P.APLICA_DEST_ESPECIFICA IN (0)
      THEN '1.0'
      ELSE '2.0'
    END SITUACION_FONDOS,
    NVL(P.POLITCA_PUBLICA_CUIPO,'0') POLITICA_PUBLICA,
    CASE
      WHEN P.TRANSFERENCIA NOT IN (0)
      THEN TERCERO.NIT_CEDULA
      ELSE '1'
    END TERCERO,
    SUM(
    CASE
      WHEN TIPO_COMPROBPP.CLASE                   IN ('RES','ADR','DMR')
      THEN (DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO - DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)
      ELSE 0
    END) AS COMPROMISOS,
    SUM(
    CASE
      WHEN TIPO_COMPROBPP.CLASE                   IN ('REO','ARO','DRO')
      THEN (DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO - DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)
      ELSE 0
    END) AS OBLIGACIONES,
    SUM(
    CASE
      WHEN TIPO_COMPROBPP.CLASE                   IN ('EGR','ADG','DEG')
      THEN (DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO - DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)
      ELSE 0
    END) AS PAGOS
  FROM CUIPO_CODIGO_CCEPT
  INNER JOIN PLAN_PRESUPUESTAL P
  ON CUIPO_CODIGO_CCEPT.COMPANIA = P.COMPANIA
  AND CUIPO_CODIGO_CCEPT.ANO     = P.ANO
  AND CUIPO_CODIGO_CCEPT.CODIGO  = P.CODIGO
  INNER JOIN DETALLE_COMPROBANTE_PPTAL
  ON CUIPO_CODIGO_CCEPT.COMPANIA        = DETALLE_COMPROBANTE_PPTAL.COMPANIA
  AND CUIPO_CODIGO_CCEPT.ANO            = DETALLE_COMPROBANTE_PPTAL.ANO
  AND CUIPO_CODIGO_CCEPT.CODIGO         = DETALLE_COMPROBANTE_PPTAL.CUENTA
  AND DETALLE_COMPROBANTE_PPTAL.FUENTE_RECURSO = 
  CASE
      WHEN P.MAN_AUX_FUE NOT IN(0)
      THEN DETALLE_COMPROBANTE_PPTAL.FUENTE_CUIPO
      ELSE '99999999999999999999'
    END
  AND DETALLE_COMPROBANTE_PPTAL.REFERENCIA     =
    CASE
      WHEN P.MAN_AUX_REF NOT IN(0)
      THEN DETALLE_COMPROBANTE_PPTAL.REFERENCIA
      ELSE '99999999999999999999'
    END
  AND DETALLE_COMPROBANTE_PPTAL.CENTRO_COSTO = 
  CASE
      WHEN P.MAN_CEN_CTO NOT IN]') || TO_CLOB(q'[(0)
      THEN DETALLE_COMPROBANTE_PPTAL.CENTRO_COSTO
      ELSE '99999999999999999999'
    END
  AND DETALLE_COMPROBANTE_PPTAL.AUXILIAR = 
  CASE
      WHEN P.MAN_AUX_GEN NOT IN(0)
      THEN DETALLE_COMPROBANTE_PPTAL.AUXILIAR
      ELSE '99999999999999999999'
    END
  INNER JOIN TIPO_COMPROBPP
  ON DETALLE_COMPROBANTE_PPTAL.COMPANIA   = TIPO_COMPROBPP.COMPANIA
  AND DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE = TIPO_COMPROBPP.CODIGO
  INNER JOIN TERCERO
  ON DETALLE_COMPROBANTE_PPTAL.COMPANIA  = TERCERO.COMPANIA
  AND DETALLE_COMPROBANTE_PPTAL.TERCERO  = TERCERO.NIT
  AND DETALLE_COMPROBANTE_PPTAL.SUCURSAL = TERCERO.SUCURSAL
  INNER JOIN CUIPO_SECCION_PPTAL
  ON CUIPO_SECCION_PPTAL.COMPANIA=DETALLE_COMPROBANTE_PPTAL.COMPANIA
  AND CUIPO_SECCION_PPTAL.CODIGO = DETALLE_COMPROBANTE_PPTAL.CODIGOUNIDADEJE
  LEFT JOIN CUIPO_PROGRAMA
  ON CUIPO_PROGRAMA.COMPANIA=DETALLE_COMPROBANTE_PPTAL.COMPANIA
  AND CUIPO_PROGRAMA.CODIGO = DETALLE_COMPROBANTE_PPTAL.PROGRAMA
  INNER JOIN CUIPO_FUENTES
  ON CUIPO_FUENTES.COMPANIA = DETALLE_COMPROBANTE_PPTAL.COMPANIA
  AND CUIPO_FUENTES.ANO     = DETALLE_COMPROBANTE_PPTAL.ANO
  AND CUIPO_FUENTES.CODIGO  =
    CASE
      WHEN DETALLE_COMPROBANTE_PPTAL.FUENTE_CUIPO IS NULL
      THEN P.CODIGOFUENTE
      ELSE DETALLE_COMPROBANTE_PPTAL.FUENTE_CUIPO
    END
  WHERE P.COMPANIA                   = s$compania$s
  AND P.ANO                          = s$anio$s
  AND DETALLE_COMPROBANTE_PPTAL.MES <= s$mesfinal$s
  AND P.NATURALEZA                   ='D'
  AND P.REGALIAS                    IN(0)
  GROUP BY CUENTA,
    CUIPO_CODIGO_CCEPT.COMPANIA,
    CUIPO_CODIGO_CCEPT.ANO,
    CUIPO_CODIGO_CCEPT.CODIGO,
    CUIPO_CODIGO_CCEPT.NOMBRE,
    P.VIGENCIA,
    CUIPO_PROGRAMA.CODIGO_EQUIVALENTE,
    DETALLE_COMPROBANTE_PPTAL.CODIGO_BPIN,
    CUIPO_SECCION_PPTAL.CODIGO_EQUIVALENTE,
    DETALLE_COMPROBANTE_PPTAL.SECTOR,
    NVL(DETALLE_COMPROBANTE_PPTAL.CODIGO_CPC,'0.0'),
    DETALLE_COMPROBANTE_PPTAL.CODIGO_CPC,
    '0.0',
    CUIPO_FUENTES.CODIGO_EQUIVALENTE,
    DETALLE_COMPROBANTE_PPTAL.FUENTE_CUIPO,
    '0.0',
    CASE
      WHEN P.APLICA_DEST_ESPECIFICA NOT IN (0)
      THEN P.TIPO_NORMA
      ELSE '0.0'
    END,
    P.APLICA_DEST_ESPECIFICA,
    0,
    P.TIPO_NORMA,
    '0.0',
    CASE
      WHEN P.APLICA_DEST_ESPECIFICA NOT IN (0)
      THEN P.NUMERO_NORMA
      ELSE '0.0'
    END,
    P.APLICA_DEST_ESPECIFICA,
    0,
    P.NUMERO_NORMA,
    '0.0',
    CASE
      WHEN P.APLICA_DEST_ESPECIFICA NOT IN (0)
      THEN TO_CHAR(P.FECHA_NORMA,'DD/MM/YYYY')
      ELSE '01/01/1900'
    END,
    P.APLICA_DEST_ESPECIFICA,
    0,
    TO_CHAR(P.FECHA_NORMA,'DD/MM/YYYY'),
    P.FECHA_NORMA,
    'DD/MM/YYYY',
    '01/01/1900',
    '',
    '',
    CASE
      WHEN P.APLICA_DEST_ESPECIFICA IN (0)
      THEN '1.0'
      ELSE '2.0'
    END,
    P.APLICA_DEST_ESPECIFICA,
    0,
    '1.0',
    '2.0',
    NVL(P.POLITCA_PUBLICA_CUIPO,'0'),
    P.POLITCA_PUBLICA_CUIPO,
    '0',
    CASE
      WHEN P.TRANSFERENCIA NOT IN (0)
      THEN TERCERO.NIT_CEDULA
      E]') || TO_CLOB(q'[LSE '1'
    END ,
    1,
    CASE
      WHEN P.TRANSFERENCIA NOT IN (0)
      THEN TERCERO.NIT_CEDULA
      ELSE '0'
    END
  ) CUIPO
WHERE (COMPROMISOS + OBLIGACIONES + PAGOS) NOT IN(0)
ORDER BY CODIGO]') CONSULTA, 99 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);