MERGE INTO CONSULTAS FIN USING (SELECT '800291Formato1007' INFORME ,TO_CLOB(q'[SELECT CONFIGURACION_EXOGENA.CONCEPTO                          CONCEPTO,
       CASE WHEN TERCERO.TIPOID='C' AND  TERCERO.IND_PERSONANIT <> 0 THEN 31 ELSE TIPOS_DOCUMENTOS.CODIGODIAN END                             TIPO_DE_IDENTIFICACION,
       DETALLE_COMPROBANTE_CNT.TERCERO                       NUMERO_IDENTIFICACION,
       CASE WHEN TERCERO.TIPOID='C'
          THEN TERCERO.APELLIDO1
         END                                                  PRIMER_APELLIDO_DEL_INFORMADO,
       CASE WHEN TERCERO.TIPOID='C'
               THEN TERCERO.APELLIDO2
         END                                                 SEGUNDO_APELLIDO_DEL_INFORMADO,
       CASE WHEN TERCERO.TIPOID='C'
               THEN TERCERO.NOMBRE1
         END                                                  PRIMER_NOMBRE_DEL_INFORMADO,
       CASE WHEN TERCERO.TIPOID='C'
               THEN TERCERO.NOMBRE2
         END                                                  OTROS_NOMBRES_DEL_INFORMADO,
       CASE WHEN TERCERO.TIPOID='N'
               THEN TERCERO.NOMBRE
         END                                                  RAZON_SOCIAL_INFORMADO,
        PAISES.CODIGODIAN                                     PAIS_DE_RESIDENCIA,
      SUM(DETALLE_COMPROBANTE_CNT.VALOR_CREDITO)              INGRESOS_BRUTOS,
      SUM(DETALLE_COMPROBANTE_CNT.VALOR_DEBITO)               DEVOLUCIONES 

  FROM DETALLE_COMPROBANTE_CNT  INNER JOIN CONFIGURACION_EXOGENA
        ON  DETALLE_COMPROBANTE_CNT.COMPANIA=CONFIGURACION_EXOGENA.COMPANIA
        AND DETALLE_COMPROBANTE_CNT.ANO=CONFIGURACION_EXOGENA.ANO
        AND DETALLE_COMPROBANTE_CNT.CUENTA=CONFIGURACION_EXOGENA.CUENTA
     LEFT JOIN TERCERO
           ON  DETALLE_COMPROBANTE_CNT.COMPANIA=TERCERO.COMPANIA
           AND DETALLE_COMPROBANTE_CNT.TERCERO=TERCERO.NIT
           AND DETALLE_COMPROBANTE_CNT.SUCURSAL=TERCERO.SUCURSAL
      LEFT JOIN TIPOS_DOCUMENTOS
           ON   TERCERO.COMPANIA=TIPOS_DOCUMENTOS.COMPANIA
           AND  TERCERO.TIPOID  =TIPOS_DOCUMENTOS.DCTO_IDENTIDAD
     LEFT JOIN PAISES
            ON TERCERO.PAIS=PAISES.PAIS 
    INNER JOIN  TIPO_COMPROBANTE 
            ON  DETALLE_COMPROBANTE_CNT.COMPANIA=TIPO_COMPROBANTE.COMPANIA
            AND DETALLE_COMPROBANTE_CNT.TIPO_CPTE=TIPO_COMPROBANTE.CODIGO
    INNER JOIN PLAN_CONTABLE 
         ON  DETALLE_COMPROBANTE_CNT.COMPANIA=PLAN_CONTABLE.COMPANIA
        AND DETALLE_COMPROBANTE_CNT.ANO=PLAN_CONTABLE.ANO
        AND DETALLE_COMPROBANTE_CNT.CUENTA=PLAN_CONTABLE.CODIGO
WHERE DETALLE_COMPROBANTE_CNT.COMPANIA=s$compania$s
      AND DETALLE_COMPROBANTE_CNT.ANO=s$ano$s
      AND DETALLE_COMPROBANTE_CNT.MES BETWEEN s$mesInicial$s AND s$mesFinal$s
      AND CONFIGURACION_EXOGENA.FORMATO=s$formato$s
      AND CONFIGURACION_EXOGENA.CONCEPTO IS NOT NULL
      AND PLAN_CONTABLE.CLASECUENTA IN('N')
     AND TIPO_COMPROBANTE.CLASE_CONTABLE NOT IN('Z')
GROUP BY  CONFIGURACION_EXOGENA.CONCEPTO,                          
       CASE WHEN TERCERO.TIPOID='C' AND  TERCERO.IND_PERSONANIT <> 0 THEN 31 ELSE TIPOS_DOCUMENTOS.CODIGODIAN END,                           
       DETALLE_COMPROBANTE_CNT.TERCERO,                     
       CASE WHEN TERCER]') || TO_CLOB(q'[O.TIPOID='C'
         THEN TERCERO.APELLIDO1
         END,                                                  
       CASE WHEN TERCERO.TIPOID='C'
               THEN TERCERO.APELLIDO2
         END,                                                 
       CASE WHEN TERCERO.TIPOID='C'
               THEN TERCERO.NOMBRE1
         END,                                                  
       CASE WHEN TERCERO.TIPOID='C'
               THEN TERCERO.NOMBRE2
         END,                                                 
       CASE WHEN TERCERO.TIPOID='N'
               THEN TERCERO.NOMBRE
         END,
       PAISES.CODIGODIAN]') CONSULTA, 99 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);