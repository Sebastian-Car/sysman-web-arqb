MERGE INTO CONSULTAS FIN USING (SELECT '800469EJECUCION_DE_GASTOS_T4' INFORME ,TO_CLOB(q'[SELECT CODIGO,
  s$nombre$s 
  VIGENCIA,
  SECCION_PPTAL_CUIPO,
  SECTOR,
  NVL(CPC,0) CPC,
  FUENTE_FINANCIACION,
  NVL(SUBSTR(SITUACION_FONDOS,0,(LENGTH(SITUACION_FONDOS) - 2)),0) SITUACION_FONDOS,
  NVL(POLITICA_PUBLICA,0) POLITICA_PUBLICA,
  NVL(TERCERO,0) TERCERO,
  COMPROMISOS,
  OBLIGACIONES,
  PAGOS
FROM
  (SELECT DETALLE_COMPROBANTE_PPTAL.COMPANIA,
    DETALLE_COMPROBANTE_PPTAL.ANO,
    CODIGO_CCEPT.CODIGO,
    CODIGO_CCEPT.NOMBRE,
    (SELECT EQUIVALENTE_CUIPO FROM TIPOVIGENCIA WHERE CODIGO = P.TIPOVIGENCIA AND EQUIVALENTE_CUIPO IS NOT NULL) VIGENCIA,
    NVL(DETALLE_COMPROBANTE_PPTAL.CODIGOUNIDADEJE, '0') SECCION_PPTAL_CUIPO,
    NVL(DETALLE_COMPROBANTE_PPTAL.SECTOR,0) SECTOR,
    NVL(COD_CPC_CUIPO.CODIGO,'0') CPC,
    FUENTE_CUIPO.CODIGO FUENTE_FINANCIACION,
    CASE
      WHEN P.APLICA_DEST_ESPECIFICA NOT IN (0)
      THEN P.TIPO_NORMA
      ELSE '0'
    END TIPO_NORMA,
    CASE
      WHEN P.APLICA_DEST_ESPECIFICA NOT IN (0)
      THEN P.NUMERO_NORMA
      ELSE '0.0'
    END NUMERO_NORMA,
    CASE
      WHEN P.APLICA_DEST_ESPECIFICA NOT IN (0)
      THEN TO_CHAR(P.FECHA_NORMA,'DD/MM/YYYY')
      ELSE '01/01/1900'
    END FECHA_NORMA,
    CASE
      WHEN DETALLE_COMPROBANTE_PPTAL.SITUACION_FONDOS IN (0)
      THEN 'C'
      ELSE 'S'
    END SITUACION_FONDOS,
    NVL(P.POLITCA_PUBLICA_CUIPO,'0') POLITICA_PUBLICA,
    CASE
      WHEN P.TRANSFERENCIA NOT IN (0)
      THEN TERCERO.NIT_CEDULA
      ELSE '1'
    END TERCERO,
    SUM(
    CASE
      WHEN TIPO_COMPROBPP.CLASE                   IN ('RES','ADR','DMR')
      THEN (DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO - DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)
      ELSE 0
    END) AS COMPROMISOS,
    SUM(
    CASE
      WHEN TIPO_COMPROBPP.CLASE                   IN ('REO','ARO','DRO')
      THEN (DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO - DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)
      ELSE 0
    END) AS OBLIGACIONES,
    SUM(
    CASE
      WHEN TIPO_COMPROBPP.CLASE                   IN ('EGR','ADG','DEG')
      THEN (DETALLE_COMPROBANTE_PPTAL.VALOR_DEBITO - DETALLE_COMPROBANTE_PPTAL.VALOR_CREDITO)
      ELSE 0
    END) AS PAGOS
  FROM DETALLE_COMPROBANTE_PPTAL  INNER JOIN PLAN_PRESUPUESTAL P
    ON DETALLE_COMPROBANTE_PPTAL.COMPANIA = P.COMPANIA
	     AND DETALLE_COMPROBANTE_PPTAL .ANO = P.ANO
        AND DETALLE_COMPROBANTE_PPTAL .CUENTA = P.CODIGO
      INNER JOIN TIPOCLASIFICADOR CODIGO_CCEPT
           ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = CODIGO_CCEPT.COMPANIA
           AND DETALLE_COMPROBANTE_PPTAL.ANO = CODIGO_CCEPT.ANO
           AND '006'=CODIGO_CCEPT.CLASECLASIFICADOR
           AND DETALLE_COMPROBANTE_PPTAL.CODIGO_CCPET=CODIGO_CCEPT.CODIGO              
      LEFT JOIN TIPOCLASIFICADOR FUENTE_CUIPO
           ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = FUENTE_CUIPO.COMPANIA
           AND DETALLE_COMPROBANTE_PPTAL.ANO = FUENTE_CUIPO.ANO
           AND '009'=FUENTE_CUIPO.CLASECLASIFICADOR
           AND DETALLE_COMPROBANTE_PPTAL.FUENTE_CUIPO = FUENTE_CUIPO.CODIGO
      LEFT JOIN TIP]') || TO_CLOB(q'[OCLASIFICADOR COD_CPC_CUIPO
           ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = COD_CPC_CUIPO.COMPANIA
           AND DETALLE_COMPROBANTE_PPTAL.ANO = COD_CPC_CUIPO.ANO
           AND '007'=COD_CPC_CUIPO.CLASECLASIFICADOR
           AND DETALLE_COMPROBANTE_PPTAL.CODIGO_CPC = COD_CPC_CUIPO.CODIGO    
      LEFT JOIN TIPOCLASIFICADOR SECCION_PPTAL_CUIPO
           ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = SECCION_PPTAL_CUIPO.COMPANIA
           AND DETALLE_COMPROBANTE_PPTAL.ANO = SECCION_PPTAL_CUIPO.ANO
           AND '008'= SECCION_PPTAL_CUIPO.CLASECLASIFICADOR
           AND DETALLE_COMPROBANTE_PPTAL.CODIGOUNIDADEJE  =  SECCION_PPTAL_CUIPO.CODIGO
     LEFT JOIN TIPOCLASIFICADOR SECTOR_CUIPO
           ON  DETALLE_COMPROBANTE_PPTAL.COMPANIA = SECTOR_CUIPO.COMPANIA
           AND DETALLE_COMPROBANTE_PPTAL.ANO = SECTOR_CUIPO.ANO
           AND '001'= SECTOR_CUIPO.CLASECLASIFICADOR
           AND DETALLE_COMPROBANTE_PPTAL.SECTOR   =  SECTOR_CUIPO.CODIGO
  INNER JOIN TIPO_COMPROBPP
  ON DETALLE_COMPROBANTE_PPTAL.COMPANIA   = TIPO_COMPROBPP.COMPANIA
  AND DETALLE_COMPROBANTE_PPTAL.TIPO_CPTE = TIPO_COMPROBPP.CODIGO
  INNER JOIN TERCERO
  ON DETALLE_COMPROBANTE_PPTAL.COMPANIA  = TERCERO.COMPANIA
  AND DETALLE_COMPROBANTE_PPTAL.TERCERO  = TERCERO.NIT
  AND DETALLE_COMPROBANTE_PPTAL.SUCURSAL = TERCERO.SUCURSAL
  WHERE P.COMPANIA                   = s$compania$s
  AND P.ANO                          = s$anio$s
  AND DETALLE_COMPROBANTE_PPTAL.MES <= s$mesfinal$s
  AND P.NATURALEZA                   ='D'
  AND P.REGALIAS                    IN(0)
  GROUP BY DETALLE_COMPROBANTE_PPTAL.COMPANIA,
    DETALLE_COMPROBANTE_PPTAL.ANO,
    CODIGO_CCEPT.CODIGO,
    CODIGO_CCEPT.NOMBRE,
    P.TIPOVIGENCIA,
    NVL(DETALLE_COMPROBANTE_PPTAL.CODIGOUNIDADEJE, '0'),
    NVL(COD_CPC_CUIPO.CODIGO,'0'),
    '0',
    FUENTE_CUIPO.CODIGO,
    '0.0',
    CASE
      WHEN P.APLICA_DEST_ESPECIFICA NOT IN (0)
      THEN P.TIPO_NORMA
      ELSE '0.0'
    END,
    P.APLICA_DEST_ESPECIFICA,
    0,
    P.TIPO_NORMA,
    '0',
    CASE
      WHEN P.APLICA_DEST_ESPECIFICA NOT IN (0)
      THEN P.NUMERO_NORMA
      ELSE '0.0'
    END,
    P.APLICA_DEST_ESPECIFICA,
    0,
    P.NUMERO_NORMA,
    '0.0',
    CASE
      WHEN P.APLICA_DEST_ESPECIFICA NOT IN (0)
      THEN TO_CHAR(P.FECHA_NORMA,'DD/MM/YYYY')
      ELSE '01/01/1900'
    END,
    P.APLICA_DEST_ESPECIFICA,
    0,
    TO_CHAR(P.FECHA_NORMA,'DD/MM/YYYY'),
    P.FECHA_NORMA,
    'DD/MM/YYYY',
    '01/01/1900',
    '',
    '',
    CASE
      WHEN DETALLE_COMPROBANTE_PPTAL.SITUACION_FONDOS IN (0)
      THEN 'C'
      ELSE 'S'
    END,
    P.APLICA_DEST_ESPECIFICA,
    0,
    '1.0',
    '2.0',
    NVL(P.POLITCA_PUBLICA_CUIPO,'0'),
    P.POLITCA_PUBLICA_CUIPO,
    '0',
    CASE
      WHEN P.TRANSFERENCIA NOT IN (0)
      THEN TERCERO.NIT_CEDULA
      ELSE '1'
    END ,
    1,
    CASE
      WHEN P.TRANSFERENCIA NOT IN (0)
      THEN TERCERO.NIT_CEDULA
      ELSE '0'
    END,
    NVL(DETALLE_COMPROBANTE_PPTAL.SECTOR,0)
  ) CUIPO
WHERE (COMPROMIS]') || TO_CLOB(q'[OS + OBLIGACIONES + PAGOS) NOT IN(0)
ORDER BY CODIGO]') CONSULTA, 99 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);