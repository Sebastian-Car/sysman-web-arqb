MERGE INTO CONSULTAS FIN USING (SELECT '800122V_RESUMENPPTO_P' INFORME ,TO_CLOB(q'[SELECT COMPANIA,
  ANO,
  ID,
  CODIGO,
  NOMBRE,
  NATURALEZA,
  CENTRO_COSTO,
  TERCERO,
  SUCURSAL,
  AUXILIAR,
  REFERENCIA,
  FUENTE_RECURSO,
  MOVIMIENTO,
  CONSITUACIONFONDOS,
  DESTINO,
  SUM(APROPIADO)APROPIADO,
  SUM(MODIFPAC)MODIFPAC,
  SUM(EJECUCIONCNT)EJECUCIONCNT,
  SUM(TRASLADO)TRASLADO,
  SUM(EJECUCIONPPT)EJECUCIONPPT,
  SUM(APLAZAMIENTO)APLAZAMIENTO,
  SUM(ADICION)ADICION,
  SUM(REDUCCION)REDUCCION,
  SUM(DISPONIBILIDAD)DISPONIBILIDAD,
  SUM(DISPONIBILIDAD_ADICION)DISPONIBILIDAD_ADICION,
  SUM(DISPONIBILIDAD_DISMINUCION)DISPONIBILIDAD_DISMINUCION,
  SUM(REG_CONTRACT)REG_CONTRACT,
  SUM(REG_NO_CONTRACT)REG_NO_CONTRACT,
  SUM(REG_REVERSION)REG_REVERSION,
  SUM(VIGENCIAANTERIOR)VIGENCIAANTERIOR,
  SUM(VIGENCIAFUTURA)VIGENCIAFUTURA,
  SUM(PAC_APROPIADO)PAC_APROPIADO,
  SUM(PAC_PROGRAMADO)PAC_PROGRAMADO,
  SUM(MODIF_REG_CONT)MODIF_REG_CONT,
  SUM(MODIF_REG_CONT_ADICION)MODIF_REG_CONT_ADICION,
  SUM(MODIF_REG_CONT_DISMINUCION)MODIF_REG_CONT_DISMINUCION,
  SUM(MODIF_REG_NOCONT)MODIF_REG_NOCONT,
  SUM(MODIF_REG_NOCONT_ADICION)MODIF_REG_NOCONT_ADICION,
  SUM(MODIF_REG_NOCONT_DISMINUCION)MODIF_REG_NOCONT_DISMINUCION,
  SUM(REINTEGRO)REINTEGRO,
  SUM(REO)REO,
  SUM(MODIFREO)MODIFREO,
  SUM(MODIFREO_ADICION)MODIFREO_ADICION,
  SUM(MODIFREO_DISMINUCION)MODIFREO_DISMINUCION,
  SUM(TOTALMODIFINGRESOS)TOTALMODIFINGRESOS,
  SUM(INGRESOSCAUSADOS)INGRESOSCAUSADOS,
  SUM(MODIFICACIONICA)MODIFICACIONICA,
  SUM(PACEJECUTADO)PACEJECUTADO,
  SUM(INGRESOS_EFECTIVO)INGRESOS_EFECTIVO,
  SUM(INGRESOS_PAPELES)INGRESOS_PAPELES,
  SUM(MODIF_INGRESOS_EFECTIVO)MODIF_INGRESOS_EFECTIVO,
  SUM(MODIF_INGRESOS_PAPELES)MODIF_INGRESOS_PAPELES,
  SUM(TRASLADO_DEBITO) TRASLADO_DEBITO,
  SUM(APLAZAM_CREDITO) APLAZAM_CREDITO,
  SUM(TRASLADO_CREDITO) TRASLADO_CREDITO,
  SUM(APLAZAM_DEBITO) APLAZAM_DEBITO,
  SUM(CREDITO)CREDITO,
  SUM(ADICIONES)ADICIONES,
  SUM(CONTRACREDITO)CONTRACREDITO,
  SUM(REDUCCIONES)REDUCCIONES,
  SUM(COMPROMISOSACUM)COMPROMISOSACUM,
  SUM(OBLIGACIONESACUM)OBLIGACIONESACUM ,
  SUM(TOTALREO)TOTALREO ,
  SUM(TOTALINGRESOS)TOTALINGRESOS ,
  SUM(APRDEFINITIVA)APRDEFINITIVA ,
  SUM(APROPIACIONVIGENTE)APROPIACIONVIGENTE,
  SUM(TOTALREGNOCONTRACT)TOTALREGNOCONTRACT ,
  SUM(TOTALREGCONT)TOTALREGCONT ,
  SUM(PACTESORERIA)PACTESORERIA,
  SUM(PAC_COMPROMETIDO)PAC_COMPROMETIDO,
  SUM(PACTOTAL)PACTOTAL,
  SUM(TOTALICA)TOTALICA
FROM V_RESUMENPPTO_BASE
WHERE COMPANIA     = s$compania$s
AND ANO            = s$anio$s
AND MES           <= s$mes$s
AND CODIGO         = 's$rubro$s'
AND FUENTE_RECURSO = 's$fuente$s'
AND REFERENCIA = 's$referencia$s'
AND CENTRO_COSTO = 's$centroC$s'
AND AUXILIAR = 's$auxiliar$s'
GROUP BY COMPANIA,
  ANO,
  ID,
  CODIGO,
  NOMBRE,
  NATURALEZA,
  CENTRO_COSTO,
  TERCERO,
  SUCURSAL,
  AUXILIAR,
  REFERENCIA,
  FUENTE_RECURSO,
  MOVIMIENTO,
  CONSITUACIONFONDOS,
  DESTINO]') CONSULTA, 99 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, NULL CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);