SELECT SUM(CUENTAS_CORRIENTES),  
       SUM(CUENTAS_AHORROS), 
       SUM(ENCARGO_FIDUCIARIO), 
       SUM(INFIS), 
       SUM(OTROS_DEPOSITOS), 
       SUM(OTROS_ENTIDAD), 
       SUM(RENDIMIENTOS), 
       SUM(CDT), 
       SUM(TES), 
       SUM(CARTERAS_COLECTIVAS), 
       SUM(OTRAS_INVERSIONES)
FROM (s$consultabase$s)
