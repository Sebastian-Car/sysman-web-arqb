MERGE INTO CONSULTAS FIN USING (SELECT '800606F03CDTMovInvPropPlantEquipo' INFORME ,TO_CLOB(q'[SELECT CODIGO "Código Contable",
    NOMBRE "Denominación",
    SUM(SALDOANT) "Saldo Anterior",
    SUM(SALDOALM) "Saldo Almacén",
    SUM(SALDOFINAL) "Saldo Final Contabilidad"
FROM(
SELECT COMPANIA,
    CODIGO,
    NOMBRE,
    0 SALDOANT,
    0 SALDOALM,
    SUM(CASE WHEN 0 = s$mesFinal$s THEN SALDO0 ELSE 0 END
        + CASE WHEN 1 = s$mesFinal$s THEN SALDO1 ELSE 0 END
        + CASE WHEN 2 = s$mesFinal$s THEN SALDO2 ELSE 0 END
        + CASE WHEN 3 = s$mesFinal$s THEN SALDO3 ELSE 0 END
        + CASE WHEN 4 = s$mesFinal$s THEN SALDO4 ELSE 0 END
        + CASE WHEN 5 = s$mesFinal$s THEN SALDO5 ELSE 0 END
        + CASE WHEN 6 = s$mesFinal$s THEN SALDO6 ELSE 0 END
        + CASE WHEN 7 = s$mesFinal$s THEN SALDO7 ELSE 0 END
        + CASE WHEN 8 = s$mesFinal$s THEN SALDO8 ELSE 0 END
        + CASE WHEN 9 = s$mesFinal$s THEN SALDO9 ELSE 0 END
        + CASE WHEN 10 = s$mesFinal$s THEN SALDO10 ELSE 0 END
        + CASE WHEN 11 = s$mesFinal$s THEN SALDO11 ELSE 0 END
        + CASE WHEN 12 = s$mesFinal$s THEN SALDO12 ELSE 0 END
        + CASE WHEN 13 = s$mesFinal$s THEN SALDO13 ELSE 0 END
    )SALDOFINAL
FROM PLAN_CONTABLE
WHERE COMPANIA = s$compania$s
    AND ANO = s$ano$s 
    AND INFORME_SIA NOT IN(0)
GROUP BY COMPANIA,
    CODIGO,
    NOMBRE
UNION ALL 
SELECT V_BASE_CUENTAALMACEN.COMPANIA,
    V_BASE_CUENTAALMACEN.CUENTA CODIGO, 
    PLAN_CONTABLE.NOMBRE, 
    SUM(CASE WHEN (V_BASE_CUENTAALMACEN.FECHA < TO_DATE('01/'||s$mesInicial$s||'/'||s$ano$s ,'DD/MM/YYYY'))  
            THEN NVL(V_BASE_CUENTAALMACEN.VALOR_ENTRADAS,0)-NVL(V_BASE_CUENTAALMACEN.VALOR_SALIDAS,0) 
                +NVL(V_BASE_CUENTAALMACEN.AJUSTESDEBITO,0)+NVL(V_BASE_CUENTAALMACEN.AJUSTESCREDITO,0) 
            ELSE 0 
        END 
    )SALDOANT,
    SUM(NVL(V_BASE_CUENTAALMACEN.VALOR_ENTRADAS,0)-NVL(V_BASE_CUENTAALMACEN.VALOR_SALIDAS,0)  
       +NVL(V_BASE_CUENTAALMACEN.AJUSTESDEBITO,0)+NVL(V_BASE_CUENTAALMACEN.AJUSTESCREDITO,0)) SALDOALM,
    0 SALDOFINAL
FROM V_BASE_CUENTAALMACEN  
LEFT JOIN (SELECT COMPANIA, ANO, CODIGO, NOMBRE, INFORME_SIA
             FROM PLAN_CONTABLE
             WHERE COMPANIA = s$compania$s
               AND ANO = s$ano$s) PLAN_CONTABLE 
   ON PLAN_CONTABLE.COMPANIA = V_BASE_CUENTAALMACEN.COMPANIA
  AND PLAN_CONTABLE.CODIGO = V_BASE_CUENTAALMACEN.CUENTA
  AND PLAN_CONTABLE.ANO = s$ano$s
WHERE V_BASE_CUENTAALMACEN.COMPANIA = s$compania$s
    AND PLAN_CONTABLE.CODIGO IS NOT NULL
    AND PLAN_CONTABLE.INFORME_SIA NOT IN(0)
GROUP BY V_BASE_CUENTAALMACEN.COMPANIA,
    V_BASE_CUENTAALMACEN.CUENTA, 
    PLAN_CONTABLE.NOMBRE)
WHERE (SALDOANT+SALDOALM+SALDOFINAL) > 0
GROUP BY CODIGO,NOMBRE
ORDER BY CODIGO]') CONSULTA, 99 APLICACION ,TO_CLOB(q'[]') CONSULTA_OPCIONAL, 'JCROJAS' CREATED_BY, NULL MODIFIED_BY  FROM DUAL ) INI ON (INI.INFORME = FIN.INFORME )  WHEN MATCHED THEN  UPDATE SET FIN.CONSULTA =  INI.CONSULTA, FIN.APLICACION =  INI.APLICACION, FIN.CONSULTA_OPCIONAL =  INI.CONSULTA_OPCIONAL, FIN.MODIFIED_BY = INI.MODIFIED_BY, FIN.DATE_MODIFIED = SYSDATE  WHEN NOT MATCHED THEN  INSERT (INFORME,CONSULTA, APLICACION,CONSULTA_OPCIONAL,CREATED_BY,DATE_CREATED)  VALUES (INI.INFORME,INI.CONSULTA, INI.APLICACION,INI.CONSULTA_OPCIONAL,INI.CREATED_BY,SYSDATE);