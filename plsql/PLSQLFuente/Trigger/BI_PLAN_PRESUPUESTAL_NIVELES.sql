create or replace TRIGGER "BI_PLAN_PRESUPUESTAL_NIVELES"          BEFORE
/*
      NAME              : BI_PLAN_PRESUPUESTAL
      AUTHORS           : LJDIAZ
      AUTHOR MIGRACION  : 
      DATE MIGRADOR     : 
      TIME              : 
      SOURCE MODULE     : 
      MODIFIER          : 
      DATE MODIFIED     : 
      TIME              :
      DESCRIPTION       : SE ANALIZA EL CAMPO NIVEL DEL 1 AL 6, PARA QUE ESTE CAMPO AL SER INSERTADO, SEA EVALUADO QUE SEA NUMEREO
                          Y NO TRAIGAMAS MAS CARATERES DE DIFERENTE TIPO CONCATENADO A EL.
*/  
  INSERT ON PLAN_PRESUPUESTAL FOR EACH ROW 
  BEGIN 
    -- SE REEMPLAZA EN EL CASO QUE LA INSERCION  EN SUS NIVELES CONTENGA UN SALTO DE LINEA, 
    -- SE ELIMINA EL SALTO DE LINEA Y SE INSERTA EL VALOR LIMPIO.
    -- SE PRESENTO CON EL TICKET 7718271 EN EL NIVEL 6, POR PREVENCION SE APLICA A LOS DEMAS NIVELES.
    SELECT replace(:NEW.nivel6,chr(10),'') INTO :NEW.nivel6 FROM DUAL; 
    SELECT replace(:NEW.nivel5,chr(10),'') INTO :NEW.nivel5 FROM DUAL;
    SELECT replace(:NEW.nivel4,chr(10),'') INTO :NEW.nivel4 FROM DUAL;
    SELECT replace(:NEW.nivel3,chr(10),'') INTO :NEW.nivel3 FROM DUAL;
    SELECT replace(:NEW.nivel2,chr(10),'') INTO :NEW.nivel2 FROM DUAL;
    SELECT replace(:NEW.nivel1,chr(10),'') INTO :NEW.nivel1 FROM DUAL;
END;